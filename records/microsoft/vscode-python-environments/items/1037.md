---
number: 1037
url: "https://github.com/microsoft/vscode-python-environments/issues/1037"
engine: copilot
owner: unassigned
labels: bug, needs PR, confirmation-pending
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.9
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-08-31T09:09:52.083Z"
itemUpdatedAt: "2026-02-18T04:14:48Z"
itemCreatedAt: "2024-07-15T20:00:02Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: 0b8b8569f248a905e3912fb73b5e1f9cbf11f50e9c730e4dc011cc01cfa13136
policyHash: 9481f2fe075c8690
---

# Issue #1037 — `git clean` in a python project triggers invalid python notification

[https://github.com/microsoft/vscode-python-environments/issues/1037](https://github.com/microsoft/vscode-python-environments/issues/1037)

## Summary

The stale-environment path remains visible in current source: externally deleting a selected venv triggers discovery refresh but can fail to publish the corresponding selection-removal event. This team-authored issue is already accepted with `needs PR`, so it should remain open for implementation.

## Change summary

Deleting a project-local environment through `git clean` leaves a stale selection that causes an invalid-environment notification.

## Best solution

Update the venv refresh path to retain the previous project-to-environment mapping, clear deleted selections, and emit an environment-change event with the correct old and fallback/new environment before the core Python extension resolves the stale path.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** area-environment
- **Confidence:** 0.9
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** The stale mapping and missing removal event are localized and source-confirmed, with an existing venv-manager unit-test surface suitable for regression coverage.
- **Likely files:** `src/managers/builtin/venvManager.ts`, `src/test/managers/builtin/venvManager.createRemove.unit.test.ts`
- **Validation:** npm run compile-tests && npm run unittest -- --grep "external venv deletion"
- **Fix prompt:**

  > Fix the stale selected-environment behavior reported in https://github.com/microsoft/vscode-python-environments/issues/1037. In src/managers/builtin/venvManager.ts, VenvManager.loadEnvMap() clears fsPathToEnv before it reads previousEnv and returns early when a persisted venv deleted outside the extension cannot be resolved, so no onDidChangeEnvironment removal event reaches the active-selection cache. Preserve the prior project mappings across refresh, rebuild the map, and emit the correct old-to-new event when an environment disappears, using the applicable fallback environment or undefined. Do not change explicit remove() behavior or emit duplicate events for unchanged selections. Add a regression test in src/test/managers/builtin/venvManager.createRemove.unit.test.ts that starts with a mapped project venv, makes refresh discovery/resolution report it missing, calls watcherRefresh(), and asserts that the stale mapping is removed and one environment-change event carries the deleted environment as old. Validate with `npm run compile-tests && npm run unittest -- --grep "external venv deletion"`.

## Evidence

- **accepted work:** The issue is team-authored and currently carries `bug` and `needs PR`, marking it as accepted work that must remain open.
- **deletion watcher:** src/managers/builtin/main.ts registers activation-file and workspace deletion listeners that call VenvManager.watcherRefresh(), covering externally removed project venvs such as those deleted by git clean.
- **source defect:** src/managers/builtin/venvManager.ts:584-627 clears fsPathToEnv before reading previousEnv; when the persisted environment cannot be resolved, loadEnvMap logs the failure and returns without firing onDidChangeEnvironment. The active-selection integration can therefore retain the deleted environment.
- **event propagation:** src/features/pythonApi.ts:115-136 refreshes the central active-selection cache only when a manager emits onDidChangeEnvironment, so an environment-list removal event alone does not clear the stale selected environment.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
