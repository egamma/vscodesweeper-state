---
number: 328319
url: "https://github.com/microsoft/vscode/issues/328319"
engine: copilot
owner: pwang347
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.4
severity: correctness
breadth: niche
priority: none
visionFit: aligned
autoFixable: false
reviewedAt: "2026-08-07T08:29:32.738Z"
itemUpdatedAt: "2026-08-03T11:11:11Z"
itemCreatedAt: "2026-07-30T23:31:58Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 8cc100056667ac33a5efb403da47c80e0444276d6c29a19aa5b617701154f5e2
policyHash: 70e952cc2fb28823
---

# Issue #328319 — Agents window doesn't load global skills when no workspace is selected

[https://github.com/microsoft/vscode/issues/328319](https://github.com/microsoft/vscode/issues/328319)

## Summary

Plausible Agents window bug (global ~/.copilot/skills not shown until a workspace is selected), but source tracing didn't turn up an obvious code-level cause — the skill-discovery defaults (chat.agentSkillsLocations schema default) and tilde-path resolution in PromptFilesLocator.findAgentSkills() don't appear to depend on workspace folders, so more detail is needed to pin down which code path (browser AI Customization panel vs. local/remote agent-host session discovery) is actually failing.

## Change summary

In the Agents window, global skills from ~/.copilot/skills aren't listed when no workspace/folder is selected, but do appear once a workspace is chosen.

## Best solution

Needs a confirmed repro with harness type (local vs. remote Copilot CLI/Claude) and whether this is the AI Customization overview/management editor count (workbench.contrib.chat.../aiCustomization/aiCustomizationOverviewView.ts, which calls IPromptsService.findAgentSkills()) or the actual per-session skill list surfaced via the agent host (SessionCustomizationDiscovery in src/vs/platform/agentHost/node/copilot/sessionCustomizationDiscovery.ts, which always scans the user's home `.copilot/skills` regardless of working directory). Once reproduced, the fix likely belongs in whichever of those two skill-discovery paths is scoped incorrectly to the active session/workspace instead of always including the user-level skill folders.

## Vision fit

- **Fit:** aligned
- **Reason:** Global agent skills discovery is part of the Agents window / agent-host harness parity work that is currently a stated priority.
- **Evidence:** Aligns with current agent-host and harness-parity workstream priorities (tools/context parity across configurations).

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.4
- **Proposed comment:**

  > Thanks for the report — this is a good catch if global skills should always be available in the Agents window regardless of workspace. Looking at the code, both the browser-side skill discovery (`PromptFilesLocator.findAgentSkills()`) and the agent-host's own discovery (`SessionCustomizationDiscovery`) scan `~/.copilot/skills` independent of the workspace, so it's not obvious from source alone why they'd be empty with no folder open — this may be a first-load/refresh timing issue in the AI Customization overview view rather than the discovery logic itself.
  > 
  > Could you confirm on the latest build: are you using the Local or a Remote agent harness, and does the skill count in the Agents window ever update on its own (e.g. after opening the Customizations editor) without selecting a workspace? That'll help pin down which code path to fix. This will auto-close in 7 days if there's no reply, so feel free to add details whenever you get a chance.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Could not confirm the exact failing code path from source alone (candidate paths look workspace-independent); needs a live repro/harness confirmation before a fix can be scoped.

## Evidence

- **browser-side skill discovery:** src/vs/workbench/contrib/chat/common/promptSyntax/utils/promptFilesLocator.ts `findAgentSkills()` resolves tilde paths like `~/.copilot/skills` via `userHome` directly (not workspace roots), and the `chat.agentSkillsLocations` setting has a schema default enabling all DEFAULT_SKILL_SOURCE_FOLDERS (chat.shared.contribution.ts ~line 1928), so this path shouldn't depend on an open workspace.
- **agent-host node-side discovery:** src/vs/platform/agentHost/node/copilot/sessionCustomizationDiscovery.ts always scans `searchRoots.user` (`~/.copilot/skills`, `~/.agents/skills`) against `_userHome` independent of `_workingDirectories`, so this path also shouldn't gate global skills on workspace selection.
- **overview view wiring:** src/vs/sessions/contrib/aiCustomizationTreeView/browser/aiCustomizationOverviewView.ts `loadCounts()` calls `promptsService.findAgentSkills()` and refreshes on `workspaceContextService.onDidChangeWorkspaceFolders` and `workspaceService.activeProjectRoot`, which could plausibly delay/skip a refresh before a workspace is picked, but I could not confirm this without running the app.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
