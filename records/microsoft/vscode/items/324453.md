---
number: 324453
url: "https://github.com/microsoft/vscode/issues/324453"
engine: copilot
owner: anthonykim1
labels: ""
proposedLabel: none
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-25T18:32:50.090Z"
itemUpdatedAt: "2026-07-06T09:17:59Z"
itemCreatedAt: "2026-07-06T09:07:06Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 9ac33835e5436c541248684103ae9784ec86f557338317ae32f911f342460b5d
policyHash: ef2efec6827348f6
---

# Issue #324453 — shellIntegration.executeCommand().read() stream yields no data for commands containing emoji on Windows/PowerShell

[https://github.com/microsoft/vscode/issues/324453](https://github.com/microsoft/vscode/issues/324453)

## Summary

Plausible bug report — emoji in the command line could cause a mismatch between the pending execution and the shell-integration-detected command line, blocking the data stream — but the report lacks essential reproduction details to confirm.

## Change summary

The `executeCommand().read()` async iterable never yields data when the executed command contains emoji characters on Windows/PowerShell.

## Best solution

If confirmed, the fix likely lives in the command-line matching logic in `src/vs/workbench/api/common/extHostTerminalShellIntegration.ts` (around line 281), where the pending execution's command string is compared to what shell integration detected via `promptInputModel`. A Unicode-normalized or fuzzy comparison may be needed. But first, the reporter needs to confirm whether the command actually runs in the terminal, share developer-console logs, and ideally test on the latest Insiders build.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** terminal-shell-integration
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the report — this is a plausible scenario where ConPTY's handling of emoji characters could cause the shell integration command-line matching to fail, preventing the `read()` stream from receiving data.
  > 
  > To investigate further, could you provide:
  > 
  > 1. **Complete extension code** — a minimal reproducible extension (the snippet is missing closing brackets and setup context).
  > 2. **Developer console output** — open Help > Toggle Developer Tools and check the Console tab for any errors when reproducing.
  > 3. **Terminal trace logs** — set `"terminal.integrated.shellIntegration.traceLogging": true` in settings, reproduce the issue, and share the relevant output from the terminal output channel.
  > 4. **Does the command actually execute?** — when you call `executeCommand("echo '❌'")`, does the terminal show the command running and its output, or does nothing happen at all?
  > 5. **Non-emoji comparison** — does `executeCommand("echo 'hello'")` with `.read()` work correctly on the same setup?
  > 6. **VS Code Insiders** — can you reproduce on the latest [Insiders build](https://code.visualstudio.com/insiders/)?
  > 
  > Note: `info-needed` issues auto-close after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction unclear — the exact failure point (ConPTY emoji mangling, shell integration detection, or command-line matching) is unconfirmed; needs reporter info first.

## Evidence

- **matching logic:** In src/vs/workbench/api/common/extHostTerminalShellIntegration.ts line 281, `startShellExecution` matches pending executions via exact string equality (`execution.value.commandLine.value === commandLine.value`). If ConPTY/PowerShell alters emoji encoding in the echoed command, this match fails and the original execution's stream never receives data.
- **promptInputModel extraction:** In src/vs/platform/terminal/common/capabilities/commandDetection/promptInputModel.ts line 297, the command is extracted from the terminal buffer via `translateToString`. On Windows with ConPTY, wide/emoji characters may be represented differently, causing the extracted string to differ from the original.
- **incomplete report:** The issue provides no developer console output, no terminal logs, no complete extension code, and no information about what is observed (does the command run? does the terminal show output? does shell integration detect the command at all?).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
