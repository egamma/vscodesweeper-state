---
number: 282237
url: "https://github.com/microsoft/vscode/issues/282237"
engine: copilot
owner: chrmarti
labels: info-needed
proposedLabel: "*question"
triageAction: propose-close
confidence: 0.88
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-21T09:39:29.309Z"
itemUpdatedAt: "2025-12-10T13:59:54Z"
itemCreatedAt: "2025-12-09T15:11:18Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: e4be665db256f52d28c23afb7a9d5f00b3bd92cd02307f850060914340b5cf9f
policyHash: ebd3763732abe97d
---

# Issue #282237 — Cannot Fetch Extensions or Create Conda Environments – net::ERR_PROXY_CONNECTION_FAILED (Only on My User Profile)

[https://github.com/microsoft/vscode/issues/282237](https://github.com/microsoft/vscode/issues/282237)

## Summary

The Network Proxy Test output in Comment 2 reveals the root cause: the user's profile has HTTP_PROXY/HTTPS_PROXY environment variables set to `http://proxy.company.com:8080`, which cannot be resolved (`getaddrinfo ENOTFOUND`). This is a system/profile configuration issue, not a VS Code bug — the user needs to remove or fix those environment variables.

## Change summary

User cannot fetch extensions or sync settings because VS Code honors proxy environment variables that point to an unresolvable proxy hostname.

## Best solution

Close as a support question. The user initially reported no proxy environment variables, but the diagnostic extension proved `HTTP_PROXY` and `HTTPS_PROXY` are set in their profile. They need to remove or correct those variables (likely injected by company group policy or login scripts for their Windows profile). This is outside VS Code's control.

## Proposed action

- **Triage action:** propose-close
- **Item type:** question
- **Reproduction:** not_applicable
- **Rating:** not applicable
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *question
- **Area labels:** proxy
- **Confidence:** 0.88
- **Proposed comment:**

  > Thanks for running the Network Proxy Test — it reveals the cause. Your Windows profile has proxy environment variables set:
  > 
  > ```
  > HTTPS_PROXY=http://proxy.company.com:8080
  > HTTP_PROXY=http://proxy.company.com:8080
  > ```
  > 
  > But that proxy hostname can't be resolved (`getaddrinfo ENOTFOUND proxy.company.com`), so all network requests through VS Code fail.
  > 
  > This is a system configuration issue outside VS Code's control. To fix it:
  > 1. Check your Windows environment variables (System Properties → Environment Variables) for `HTTP_PROXY` and `HTTPS_PROXY` and remove or correct them.
  > 2. If they're set by company policy or a login script, work with your IT team to fix the proxy configuration for your profile.
  > 3. As a workaround, you can override in VS Code by setting `"http.proxy": ""` in your user settings, though clearing the system variables is the proper fix.
  > 
  > Since this is a configuration/support question rather than a VS Code issue, I'm closing this — but feel free to open a new issue if you believe VS Code should handle this scenario differently.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Environment configuration issue on the user's machine — not a VS Code defect.

## Evidence

- **proxy env vars found:** Comment 2's Network Proxy Test output shows `HTTPS_PROXY=http://proxy.company.com:8080` and `HTTP_PROXY=http://proxy.company.com:8080` — contradicting the user's initial claim of 'no environment proxy variables'.
- **DNS failure on proxy:** The proxy connection fails with `getaddrinfo ENOTFOUND proxy.company.com` — the proxy hostname itself cannot be resolved, so VS Code correctly fails when trying to route through it.
- **profile-specific:** The user confirms the issue is unique to their Windows user profile, consistent with per-profile environment variables injected by company policy or login scripts.
- **maintainer diagnosis:** chrmarti (MEMBER) asked for the Network Proxy Test in Comment 1, and the results in Comment 2 clearly identify the external cause.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
