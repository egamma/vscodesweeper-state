---
number: 333352
url: "https://github.com/microsoft/vscode/issues/333352"
engine: copilot
owner: deepak1556
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: crash
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-30T13:43:11.580Z"
itemUpdatedAt: "2026-08-29T21:05:09Z"
itemCreatedAt: "2026-08-29T20:01:20Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: 63207ccdb29b1127601769f57a64f49c67939f5edea14528fc944e55080ae8e9
policyHash: 3d6943c3a15c721c
---

# Issue #333352 — Random Crash. It's happened three times already.

[https://github.com/microsoft/vscode/issues/333352](https://github.com/microsoft/vscode/issues/333352)

## Summary

The pasted coredump is for a `python3` process (crashing inside glib/gtk/gobject and a third-party `libDelphiFMX.so`/`libfmux` GUI stack) that merely happened to run under the VS Code Flatpak's cgroup — there's no VS Code/Electron/Node frame anywhere in the trace, so it's unclear this is actually a VS Code crash at all.

## Change summary

Reporter says VS Code has randomly crashed three times, pasting a systemd-coredump report they believe is from VS Code.

## Best solution

Ask the reporter to confirm whether VS Code's own window/process actually crashed (vs. a Python/DelphiFMX program they ran, e.g. in the integrated terminal or as a debugged process), and if so, to get an actual VS Code main/renderer crash report (About VS Code version, `--verbose` logs, or Help > Report Issue attaches proper crash info) rather than an unrelated python3 coredump.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** crash
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report. Looking at the coredump you pasted, the crashing process is actually `python3` (`Executable: /usr/bin/python3.13`), and the whole stack trace is in `glib`/`gtk`/`gobject` and a third-party `libDelphiFMX.so`/`libfmux` GUI library — there's no VS Code, Electron, or Node frame in it at all. It looks like this may be a separate Python/DelphiFMX program crashing (perhaps launched from VS Code's integrated terminal), rather than VS Code itself.
  > 
  > Could you confirm: did the VS Code window/UI actually crash or become unresponsive, or was this a Python script you were running? If VS Code itself crashed, a report generated via Help > Report Issue (or the main/renderer process logs) would help a lot more than this coredump.
  > 
  > This issue will auto-close in 7 days if there's no reply with more detail.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** The evidence shows the crash isn't even in VS Code's own code, so there's no defect to trace or fix without confirmation from the reporter.

## Evidence

- **coredump target:** The report's `Command Line: python3 -` and `Executable: /usr/bin/python3.13` show the crashing process is Python, not VS Code (`code`/electron/node).
- **stack trace:** All frames are in libglib-2.0, libgtk-3, libgobject-2.0, a third-party `libfmux-1.76.so`, and `delphifmx/Linux64/libDelphiFMX.so` — none reference VS Code, Electron, V8, or Node.
- **cgroup only:** The only VS Code linkage is the systemd `Control Group` path (the Flatpak sandbox scope), which just reflects that python3 was launched from within VS Code's Flatpak sandbox (e.g. an integrated terminal), not that VS Code itself crashed.
- **reporter context:** Reporter is on an outdated pinned VS Code Flatpak version per linked flathub PR discussion, but that doesn't change that the pasted crash is not a VS Code process crash.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
