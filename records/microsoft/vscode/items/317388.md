---
number: 317388
url: "https://github.com/microsoft/vscode/issues/317388"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: "*duplicate"
triageAction: propose-close
confidence: 0.9
severity: correctness
breadth: common
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-22T06:06:07.821Z"
itemUpdatedAt: "2026-07-14T15:06:52Z"
itemCreatedAt: "2026-05-19T21:12:17Z"
upvotes: 3
reactionsTotal: 3
commentCount: 8
relatedOpenIssues: 0
milestone: none
contentHash: 0b02feb4cb97b708617f7c9ff6a84dfdec6db2f48233b4d095378f42950887a5
policyHash: ebd3763732abe97d
---

# Issue #317388 — git keeps opening other worktrees immediately after i do "Close Other Repositories"

[https://github.com/microsoft/vscode/issues/317388](https://github.com/microsoft/vscode/issues/317388)

## Summary

Verified duplicate: the root cause — Copilot extension calling `gitAPI.openRepository()` with `openIfClosed=true`, bypassing the user's closed-repository list — is precisely tracked by the assigned canonical issue #316412.

## Change summary

Closing worktrees via 'Close Other Repositories' is immediately undone because the Copilot extension force-reopens them through the git API.

## Best solution

Consolidate on https://github.com/microsoft/vscode/issues/316412 which identifies the exact code path (public git API's `openRepository` passing `openIfClosed=true`) and is assigned to @lszomoru. The fix likely involves either making the public API respect the closed-repository list or having the Copilot extension use `getRepository` instead of `openRepository`.

## Proposed action

- **Triage action:** propose-close
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** common
- **Priority:** none
- **Proposed label:** *duplicate
- **Area labels:** git
- **Confidence:** 0.9
- **Proposed comment:**

  > Thanks for the detailed report and logs — this clearly shows the reopening cycle. The root cause is tracked in https://github.com/microsoft/vscode/issues/316412: the built-in Copilot extension calls the public git API's `openRepository()`, which bypasses the closed-repository list (it passes `openIfClosed=true` internally). That issue is assigned and covers exactly this scenario, so consolidating the discussion there.
  > 
  > /duplicate of #316412

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Duplicate — the fix is owned by the canonical issue which is already assigned.

## Evidence

- **canonical issue:** https://github.com/microsoft/vscode/issues/316412 is open and assigned to lszomoru, precisely tracking the same root cause: the Copilot extension calls `gitAPI.openRepository()` which passes `openIfClosed=true` to `Model.openRepository()`, bypassing the user's closed-repository list.
- **source trace:** In `extensions/git/src/api/api1.ts` line 484, the public `openRepository` API calls `this.#model.openRepository(root.fsPath, true, true)` with `openIfClosed=true`. In `extensions/git/src/model.ts` line 666, the closed-repository check is skipped when `openIfClosed` is true. Copilot's `gitServiceImpl.ts` line 182 calls `gitAPI.openRepository(uri)` to force-open repos.
- **user confirmation:** Multiple commenters (comments 1, 3, 8) confirm that disabling AI features resolves the problem, corroborating the Copilot extension as the trigger.
- **related issue:** https://github.com/microsoft/vscode/issues/313526 also tracks the same symptom from the worktree auto-discovery side; https://github.com/microsoft/vscode/issues/326522 tracks it specifically for Claude Code sessions.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
