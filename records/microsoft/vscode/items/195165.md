---
number: 195165
url: "https://github.com/microsoft/vscode/issues/195165"
engine: copilot
owner: osortega
labels: bug, search, confirmed
proposedLabel: none
triageAction: keep-open
confidence: 0.78
severity: correctness
breadth: common
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-07-24T12:47:54.535Z"
itemUpdatedAt: "2024-12-11T22:40:39Z"
itemCreatedAt: "2023-10-09T14:54:46Z"
upvotes: 0
reactionsTotal: 0
commentCount: 3
relatedOpenIssues: 0
milestone: On Deck
contentHash: c3ae6475031e3a819980632d9fb9011ab9b0ffc9e69fc854e23c09b7789d915d
policyHash: 0c32deb9982e8caf
---

# Issue #195165 — Quick search is showing results without full match

[https://github.com/microsoft/vscode/issues/195165](https://github.com/microsoft/vscode/issues/195165)

## Summary

Still open and valid — the race condition in Quick Search's async result handling appears unresolved on current main, and the issue carries the 'On Deck' milestone which requires maintainer judgment.

## Change summary

Quick Search shows partial-match results from a previous (shorter) query when typing fast, due to a race condition in async result handling.

## Best solution

Add sequencing or cancellation logic in `_getPicks` (textSearchQuickAccess.ts) so that async results from an earlier query are discarded when a newer query has already started — similar to how `PickerQuickAccessProvider` manages its `disposables` store per-query, but ensuring the `asyncResults` promise chain also respects supersession.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 4/6 Traced
- **Severity:** correctness
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** search
- **Confidence:** 0.78
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small localized race-condition fix in one file; the async results path needs a generation counter or cancellation check to discard stale results.
- **Likely files:** `src/vs/workbench/contrib/search/browser/quickTextSearch/textSearchQuickAccess.ts`
- **Validation:** Write a test that simulates two rapid successive calls to `_getPicks` with different patterns (e.g. 'cu' then 'cub') and asserts that only results matching the final pattern appear in the returned picks.
- **Fix prompt:**

  > Race condition in src/vs/workbench/contrib/search/browser/quickTextSearch/textSearchQuickAccess.ts: when the user types fast, `_getPicks` is called for each keystroke. The method at line 348 creates a `conditionalTokenCts` and launches `doSearch`, returning a `FastAndSlowPicks` whose `additionalPicks` is `allMatches.asyncResults.then(...)`. If a new query supersedes the old one, the base class disposes the old `disposables` store (cancelling the token), but the search model's results may already be populated with partial matches from the earlier query. The fix should ensure that when `conditionalTokenCts` is cancelled, the `asyncResults` promise chain returns an empty array rather than stale results — or use a generation counter to discard results from superseded queries. Do not restructure the overall search flow. Validate with a unit test that fires two rapid `_getPicks` calls and asserts only the second query's results are returned.

## Evidence

- **race condition path:** In src/vs/workbench/contrib/search/browser/quickTextSearch/textSearchQuickAccess.ts, `_getPicks` returns a `FastAndSlowPicks` with an `asyncResults` promise that resolves later — if a new query fires before it resolves, stale results from the old query can appear in the picker.
- **no fix found:** git log since the issue was filed shows no commit addressing this race condition; the async result chain at line 384 has no guard against superseded queries.
- **milestone:** Issue carries 'On Deck' milestone — a scheduling decision that requires maintainer judgment.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
