---
number: 325800
url: "https://github.com/microsoft/vscode/issues/325800"
engine: copilot
owner: deepak1556
labels: ""
proposedLabel: none
triageAction: needs-info
confidence: 0.75
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-17T10:24:04.947Z"
itemUpdatedAt: "2026-07-14T14:33:14Z"
itemCreatedAt: "2026-07-14T14:08:49Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 68a31bdf66e17274197bdd586a458810b2affdcb1e4fef3339c386ef7b922750
policyHash: ebd3763732abe97d
---

# Issue #325800 — Image preview fails with "WebGL2 is not supported" — GPU/ANGLE context init fails on Linux despite healthy host OpenGL

[https://github.com/microsoft/vscode/issues/325800](https://github.com/microsoft/vscode/issues/325800)

## Summary

The ANGLE/EGL context initialization failure is in Electron's bundled Chromium GPU process, not in VS Code's own code. The reporter hasn't tested with the .deb build to rule out the Snap runtime, and the specific hardware/driver/Snap combination makes this niche. Requesting more info before routing.

## Change summary

Image preview shows "WebGL2 is not supported" on Linux because Chromium's GPU process fails to create an ANGLE OpenGL context, despite a healthy host OpenGL stack.

## Best solution

Determine whether this is Snap-specific (test with the .deb build) and whether it regressed with the Electron upgrade in 1.128.0. If Snap-specific, the fix likely belongs in the Snap packaging or Electron's GPU sandbox handling. If it reproduces with .deb, it's an Electron/Chromium upstream regression to report there. VS Code's media-preview extension uses no WebGL directly — it renders images in a standard webview <img> tag.

## Proposed action

- **Triage action:** needs-info
- **Item type:** upstream
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.75
- **Proposed comment:**

  > Thanks for the thorough report — the diagnostic work is really helpful.
  > 
  > The ANGLE/EGL context initialization failure (`Could not create a backing OpenGL context`) happens inside Chromium's GPU process, which is bundled with Electron — VS Code's image preview itself doesn't use WebGL; it renders images in a standard webview.
  > 
  > To help us route this correctly, could you check:
  > 
  > 1. **Does this reproduce with the `.deb` build?** Install from https://code.visualstudio.com/download (the `.deb` package) and test the same image. This will tell us whether the Snap runtime is involved.
  > 2. **Does it reproduce on VS Code Insiders?** (`snap install code-insiders --classic` or the `.deb` Insiders build)
  > 3. What does `chrome://gpu` show when opened in a Chromium or Chrome browser on the same machine?
  > 
  > If it's Snap-specific, the fix likely belongs in the Snap packaging or Electron's GPU sandbox. If it reproduces with `.deb` too, this is an Electron/Chromium upstream regression we'd need to report there.
  > 
  > Note: issues labeled `info-needed` are auto-closed after 7 days without a reply, so please follow up when you can.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Upstream Chromium/Electron GPU initialization issue — not fixable in VS Code's own code.

## Evidence

- **upstream root cause:** The error 'ANGLE Display::initialize error 12289: Could not create a backing OpenGL context' and 'Exiting GPU process due to errors during initialization' originate in Chromium's GPU process (via Electron), not in any VS Code source code.
- **no WebGL in media-preview:** extensions/media-preview/ contains no WebGL references — the 'WebGL2 is not supported' message comes from the Chromium rendering layer, not from VS Code's image preview code.
- **prior similar issue closed:** https://github.com/microsoft/vscode/issues/207250 reported the same ANGLE context error on Ubuntu 24.04 and was closed as info-needed/not-planned.
- **snap not ruled out:** The reporter uses the Snap build and explicitly notes 'It is not yet confirmed whether the Microsoft .deb/apt build of the same version shows the same failure.'

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
