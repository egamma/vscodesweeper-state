---
number: 329436
url: "https://github.com/microsoft/vscode/issues/329436"
engine: copilot
owner: unassigned
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.45
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-11T09:44:34.565Z"
itemUpdatedAt: "2026-08-10T16:03:20Z"
itemCreatedAt: "2026-08-06T15:35:41Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 8df760c1ca64641a3c236b57fe5a691ea6c21eb5ae4554bcafffc83e4934f90e
policyHash: f13d7795d7aa28b9
---

# Issue #329436 — Custom OpenAI-compatible model streaming response usage metadata shows as "undefined" in Copilot chat log

[https://github.com/microsoft/vscode/issues/329436](https://github.com/microsoft/vscode/issues/329436)

## Summary

The symptom is real — `requestLoggerImpl.ts` literally prints `JSON.stringify(entry.usage)` as the string "undefined" when usage is missing — but I can't confirm from source alone why a custom OpenAI-compatible streaming server's usage chunk fails to reach that field; this depends on the exact SSE shape the reporter's server sends, so I'm asking for a capture before treating it as a confirmed parsing bug.

## Change summary

Usage token counts show as the literal string "undefined" in the Copilot chat request log when streaming is enabled for a custom (BYOK) OpenAI-compatible model, even though the same model reports usage correctly with streaming disabled.

## Best solution

Once a raw SSE capture (or a minimal reproducible custom-endpoint config) is available, trace whether `extensions/copilot/src/platform/networking/node/stream.ts`'s `processSSEInner` (the `json.usage` check around line 365) actually receives the final usage chunk for this provider — e.g. check if `expectedNumChoices > 1` is unexpectedly true, or if the provider embeds `usage` in a shape/order the parser doesn't expect — and fix the extraction there; if it's server nonconformance, this is `*not-reproducible` instead.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the detailed report — the "undefined" text you're seeing does trace back to the log simply stringifying a missing usage value, so the underlying issue is that the extension isn't capturing usage from the stream for your setup.
  > 
  > To help pin down the actual parsing gap, could you share:
  > - A raw capture of the last few SSE lines your server sends before `[DONE]` (a local proxy or your own server logs would show whether the `usage` field is present and how it's shaped)
  > - Whether you have `n` (multiple completions) configured, or any non-default request parameters
  > - Whether this reproduces with more than one custom OpenAI-compatible backend, or just your particular server
  > 
  > This will help confirm whether it's a gap in our SSE usage extraction or something specific to the server's response format. Note that `info-needed` issues auto-close after 7 days without a reply, so please follow up when you get a chance.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** The display mechanism is confirmed in source, but the root cause of the missing usage value depends on the exact SSE payload from the reporter's specific server — that can't be confirmed or validated without a capture, so this isn't yet a safe, localized auto-fix candidate.

## Evidence

- **log rendering:** extensions/copilot/src/extension/prompt/vscode-node/requestLoggerImpl.ts:666 does `usage : ${JSON.stringify(entry.usage)}` — when `entry.usage` is `undefined`, template interpolation prints the literal string "undefined", matching the reported log output exactly.
- **usage request enabled:** extensions/copilot/src/extension/byok/node/openAIEndpoint.ts:397 sets `stream_options.include_usage = true` for non-Responses/Messages (plain chat-completions) BYOK requests, so usage should be requested from the provider during streaming.
- **usage extraction path:** extensions/copilot/src/platform/networking/node/stream.ts:274-299,365-369 extracts `json.usage` from any SSE chunk and attaches it to the finished completion, but skips usage entirely when `expectedNumChoices > 1` (line 274) — worth checking whether that's inadvertently true for this reporter's config, or whether their server's usage chunk shape differs from what's parsed.
- **related but distinct issue:** https://github.com/microsoft/vscode/issues/317187 covers usage always showing 0 for models added via the `LanguageModelChatProvider` extension API, and explicitly states the built-in Custom Endpoint (BYOK) path reports usage correctly — a different code path than this report, so not a duplicate.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
