---
number: 329436
url: "https://github.com/microsoft/vscode/issues/329436"
engine: copilot
owner: unassigned
labels: new release
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.45
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-07T06:17:35.872Z"
itemUpdatedAt: "2026-08-06T15:38:00Z"
itemCreatedAt: "2026-08-06T15:35:41Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 21d5f8b2f3294fa03b2d7c03c8aaa09a624ad70e113e69cadaa8c00a78182047
policyHash: 70e952cc2fb28823
---

# Issue #329436 — Custom OpenAI-compatible model streaming response usage metadata shows as "undefined" in Copilot chat log

[https://github.com/microsoft/vscode/issues/329436](https://github.com/microsoft/vscode/issues/329436)

## Summary

Fresh, well-diagnosed report (0 comments, filed hours ago) about BYOK/custom OpenAI-compatible streaming not surfacing usage; I traced the likely code paths (SSEProcessor.processSSEInner in stream.ts, and stream_options.include_usage in openAIEndpoint.ts) and they appear designed to handle this, so I can't confirm a defect from source alone — needs more detail from the reporter (exact custom endpoint/provider, raw stream chunks) before an engineer can localize it.

## Change summary

Usage metadata (token counts) shows as "undefined" in the Copilot chat log when streaming is enabled for a custom OpenAI-compatible model, even though the API sends usage correctly in the final SSE chunk; non-streaming works fine, and the missing usage causes the context-window icon to disappear.

## Best solution

An engineer should reproduce against a real custom OpenAI-compatible provider with streaming + stream_options.include_usage, and trace extensions/copilot/src/extension/byok/node/openAIEndpoint.ts together with the SSE usage handling in extensions/copilot/src/platform/networking/node/stream.ts (processSSEInner) to see whether the final usage chunk is actually being parsed/attached for this specific model configuration; likely belongs under the BYOK / custom-model area.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** copilot, model-byok
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the detailed report and diagnosis — this is helpful. To get this in front of the right engineer, could you share a couple more details: which custom OpenAI-compatible endpoint/provider you're using (self-hosted, a specific gateway, etc.), and if possible a raw excerpt of the streamed SSE response showing the final `usage` chunk? That'll help pin down whether the gap is in how the extension requests `stream_options.include_usage` or in how it parses the final chunk for this endpoint shape. Note: this issue will auto-close in 7 days if there's no follow-up, so please reply when you get a chance.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Traced the likely usage-handling code paths but could not confirm a concrete defect without executing against a real custom OpenAI-compatible streaming endpoint; more reporter detail is needed before a fix can be localized with confidence.

## Evidence

- **stream usage handling:** extensions/copilot/src/platform/networking/node/stream.ts processSSEInner (lines ~353-370) parses `json.usage` per SSE line and yields it, and processSSE (lines ~264-300) combines it onto the final completion when expectedNumChoices === 1 — this appears designed to support exactly this case, so the defect (if any) is likely more specific (e.g. a particular BYOK code path or usage field shape) than a total absence of handling.
- **BYOK stream_options:** extensions/copilot/src/extension/byok/node/openAIEndpoint.ts line 397 sets `stream_options: { include_usage: true }` only when `!useResponsesApi && !useMessagesApi && body.stream`, which is relevant to whether a custom OpenAI-compatible provider actually receives the request needed to emit a final usage chunk.
- **related but distinct:** https://github.com/microsoft/vscode/issues/317187 tracks token usage always showing 0 for models added via the LanguageModelChatProvider extension API — a different provider mechanism than this BYOK custom-model streaming report, so it does not clearly own the same root cause.
- **no comments/confirmation yet:** Issue was filed same day with 0 comments and 0 reactions; no maintainer or other user has confirmed the repro.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
