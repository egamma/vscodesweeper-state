---
number: 327759
url: "https://github.com/microsoft/vscode/issues/327759"
engine: copilot
owner: anthonykim1
labels: bug, chat-terminal, agent-host
proposedLabel: none
triageAction: keep-open
confidence: 0.72
severity: visual
breadth: common
priority: none
visionFit: aligned
autoFixable: true
reviewedAt: "2026-08-07T08:38:44.058Z"
itemUpdatedAt: "2026-08-04T04:46:11Z"
itemCreatedAt: "2026-07-28T02:28:21Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: aaf56dcc8b4355fb71d67b9ab9182e2bf46883a64e97fc5f72222de3c1cbca53
policyHash: 70e952cc2fb28823
---

# Issue #327759 — Streaming terminal output text gets cut off

[https://github.com/microsoft/vscode/issues/327759](https://github.com/microsoft/vscode/issues/327759)

## Summary

Team-authored issue with no cited fix commit, so it stays open regardless of the reporter's 'works fine now' — and the thread has since pivoted to a real, source-confirmed follow-up: the chat terminal's vertical scrollbar never appears even though vertical scrolling works.

## Change summary

Streaming terminal output in the chat/agent terminal panel gets clipped, and the vertical scrollbar never appears (only horizontal shows on hover) even though the content is scrollable.

## Best solution

In ChatTerminalToolProgressPart._createScrollableContainer (src/vs/workbench/contrib/chat/browser/widget/chatContentParts/toolInvocationParts/chatTerminalToolProgressPart.ts), wire the vertical scrollbar into the same hover/focus toggle used for horizontal so it becomes visible when relevant, matching the existing horizontal show/hide pattern.

## Vision fit

- **Fit:** aligned
- **Reason:** This is part of the agent host terminal integration inside the Agents/agent-host chat surface, which the current priorities call out as an active investment area (agent host terminal integration and parity work).
- **Evidence:** Current priorities: 'Agent host & harnesses ... with parity work on tools, terminal integration, permissions, and context.'

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 4/6 Traced
- **Severity:** visual
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** chat-terminal, agent-host
- **Confidence:** 0.72
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small, localized fix in one method (_createScrollableContainer) mirroring the existing horizontal show/hide pattern; the defect is directly traced in source and testable via the scrollable container's options.
- **Likely files:** `src/vs/workbench/contrib/chat/browser/widget/chatContentParts/toolInvocationParts/chatTerminalToolProgressPart.ts`, `src/vs/workbench/contrib/chat/browser/widget/chatContentParts/media/chatTerminalToolProgressPart.css`
- **Validation:** Add/extend a test in src/vs/workbench/contrib/chat/test/browser/widget/chatContentParts/chatTerminalToolProgressPart.test.ts that renders terminal output taller than the container's max-height, simulates a MOUSE_ENTER (or FOCUS_IN) on the domNode, and asserts the scrollable element's vertical scrollbar visibility option becomes ScrollbarVisibility.Auto (and returns to Hidden on MOUSE_LEAVE/FOCUS_OUT) — the test should fail on current main (vertical stays Hidden) and pass after the fix.
- **Fix prompt:**

  > In src/vs/workbench/contrib/chat/browser/widget/chatContentParts/toolInvocationParts/chatTerminalToolProgressPart.ts, the method _createScrollableContainer (around lines 1481-1504) constructs a DomScrollableElement with `vertical: ScrollbarVisibility.Hidden` and `horizontal: ScrollbarVisibility.Hidden`, then registers MOUSE_ENTER/MOUSE_LEAVE/FOCUS_IN/FOCUS_OUT listeners that toggle ONLY `horizontal` between `ScrollbarVisibility.Auto` and `ScrollbarVisibility.Hidden` (comment says 'Show horizontal scrollbar on hover/focus, hide otherwise to prevent flickering during streaming'). Vertical is never toggled, so even when the terminal output content is taller than the `.chat-terminal-output-container`'s `max-height: 300px` (see chatTerminalToolProgressPart.css line 210) and can genuinely be scrolled with the mouse wheel or keyboard, no vertical scrollbar ever renders to indicate this — reported and screenshotted by the issue author in https://github.com/microsoft/vscode/issues/327759. Fix by including `vertical` alongside `horizontal` in each of the four updateOptions() calls in _createScrollableContainer, so the vertical scrollbar shows Auto on hover/focus and Hidden otherwise, exactly mirroring the existing horizontal behavior. Do not change the streaming/scroll-lock logic further down in the file (the onScroll handler and _isProgrammaticScroll tracking), and do not alter the CSS rule at chatTerminalToolProgressPart.css line 289 that hides the underlying xterm-native scrollbar — that rule is unrelated to the DomScrollableElement's own scrollbar and should remain. Validate with a unit test in chatTerminalToolProgressPart.test.ts that renders output taller than the container, fires MOUSE_ENTER, and asserts the vertical scrollbar becomes visible/Auto, then fires MOUSE_LEAVE and asserts it returns to Hidden.

## Evidence

- **author confirms original resolved:** jruales (MEMBER) commented 'It works fine now' regarding the original clipped-text report, but did not close the issue and no fix commit references #327759.
- **source trace of new sub-issue:** src/vs/workbench/contrib/chat/browser/widget/chatContentParts/toolInvocationParts/chatTerminalToolProgressPart.ts lines 1481-1504: DomScrollableElement is created with `vertical: ScrollbarVisibility.Hidden` and only horizontal is toggled to `Auto`/`Hidden` on MOUSE_ENTER/LEAVE/FOCUS_IN/FOCUS_OUT — vertical is never toggled, so the vertical scrollbar can never render, matching the reporter's second screenshot exactly.
- **CSS corroboration:** chatTerminalToolProgressPart.css line 289 also hides `.xterm-scrollbar` entirely, and only `.scrollbar.horizontal` gets special pointer-events handling (lines 294-300), reinforcing that vertical affordance was not designed in.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
