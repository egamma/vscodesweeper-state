---
number: 319278
url: "https://github.com/microsoft/vscode/issues/319278"
engine: copilot
owner: joshspicer
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.65
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-25T22:37:24.859Z"
itemUpdatedAt: "2026-06-01T09:15:17Z"
itemCreatedAt: "2026-06-01T09:08:13Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 43f72831ab82b3877e0357ae9e4e51010c8f24cc4f4357c2ce2dc0e3c6ecb835
policyHash: 26158a1d7dfd26ac
---

# Issue #319278 — Chat: Custom session titles are lost after extension update / index rebuild

[https://github.com/microsoft/vscode/issues/319278](https://github.com/microsoft/vscode/issues/319278)

## Summary

The reported root cause doesn't match the current code architecture: `customTitle` IS tracked in the operation-log schema (`chatSessionOperationLog.ts` line 191) and `setChatSessionTitle()` calls `saveState()` which writes the mutation to the `.jsonl` file. The reporter's description of the storage format ("first-line JSON") suggests an outdated understanding. More reproduction details against the current build are needed.

## Change summary

Custom chat session titles reportedly revert to the default after an extension update or index rebuild.

## Best solution

Ask the reporter to reproduce on the latest Insiders build and provide the actual `.jsonl` file contents (before and after rename, and after the alleged revert). On current `main`, `customTitle` is tracked in the operation log schema and should survive writes — the bug may be fixed or the reporter's root-cause analysis may be off.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** chat
- **Confidence:** 0.65
- **Proposed comment:**

  > Thanks for the detailed report and the local investigation. On current `main`, the `customTitle` field is tracked in the session operation log (`chatSessionOperationLog.ts`) and `setChatSessionTitle()` explicitly calls `saveState()`, which writes the mutation to the `.jsonl` file — so the title should survive a round-trip through the file.
  > 
  > The storage format description in your root cause ("first-line JSON") doesn't quite match the current architecture (it's an append-only operation log with diff entries), so it's possible the behavior you saw was on an older build or that something else is going on.
  > 
  > Could you try reproducing on the latest VS Code Insiders and share:
  > 1. Your exact VS Code version and Copilot Chat extension version
  > 2. The contents of the `.jsonl` file after renaming (before and after the revert)
  > 3. What specifically triggers the "index rebuild" — is it an extension update, a reload, or something else?
  > 
  > This will help us determine whether the issue is still present. Note: issues labeled `info-needed` auto-close after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Cannot confirm the defect on current main — the customTitle field is tracked in the operation log schema and should be persisted; needs reproduction info.

## Evidence

- **customTitle in operation log:** src/vs/workbench/contrib/chat/common/model/chatSessionOperationLog.ts line 191: `customTitle: Adapt.v(m => m.hasCustomTitle ? m.title : undefined)` — the field IS tracked in the `.jsonl` append-only schema and will be diffed/written on each `saveState()` call.
- **setChatSessionTitle triggers file write:** src/vs/workbench/contrib/chat/common/chatService/chatServiceImpl.ts lines 372–384: `setChatSessionTitle()` updates the model, updates the index via `setSessionTitle()`, and calls `saveState()` which triggers `storeSessions()` → `writeSession()` → the operation log's `write()` method to diff and append the `customTitle` change.
- **storage format mismatch:** The reporter describes the `.jsonl` as having a 'first-line JSON' with a `customTitle` field, but the actual storage format is an append-only operation log with diff entries — not a simple first-line-is-metadata format. This suggests an outdated understanding of the storage mechanism.
- **toJSON includes customTitle:** src/vs/workbench/contrib/chat/common/model/chatModel.ts line 3022: `toJSON()` includes `customTitle: this._customTitle`, so full serialization also preserves the title.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
