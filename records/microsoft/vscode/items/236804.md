---
number: 236804
url: "https://github.com/microsoft/vscode/issues/236804"
engine: copilot
owner: lszomoru
labels: bug, scm
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-21T17:54:45.644Z"
itemUpdatedAt: "2024-12-22T10:34:44Z"
itemCreatedAt: "2024-12-22T06:29:52Z"
upvotes: 1
reactionsTotal: 1
commentCount: 2
relatedOpenIssues: 0
milestone: Backlog
contentHash: 498b16c6be2fdd8a0652fe4fdb90276d8798204bf82193630f418aaf2196b2a2
policyHash: ebd3763732abe97d
---

# Issue #236804 — SCM Graph - does not detect changes in the cloud when working for a remote source (SSH)

[https://github.com/microsoft/vscode/issues/236804](https://github.com/microsoft/vscode/issues/236804)

## Summary

The report lacks key configuration details — most likely the user expects the SCM Graph refresh to fetch from remotes, but the graph shows local git ref state and requires `git.autofetch` or an explicit fetch/sync to reflect remote changes. Without knowing the user's `git.autofetch` setting and whether the issue is SSH-specific, there isn't enough to reproduce or classify definitively.

## Change summary

SCM Graph does not show new remote commits when working over SSH until an explicit sync is performed.

## Best solution

Ask the reporter whether `git.autofetch` is enabled in their SSH workspace and whether enabling it resolves the issue. If the graph refresh button is expected to trigger a fetch, that would be a feature request. The current design (graph reflects local refs, updated by git operations) appears intentional based on `extensions/git/src/historyProvider.ts` filtering on write operations only.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** scm
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the report. A few questions to help us understand what's happening:
  > 
  > 1. Do you have the `git.autofetch` setting enabled in your SSH workspace? This controls whether VS Code periodically runs `git fetch` to pick up remote changes — without it, the SCM Graph only reflects what's in your local `.git` directory.
  > 2. When you click "refresh" on the graph, are you expecting it to also fetch from the remote? Currently, refresh re-reads local refs but does not run `git fetch`.
  > 3. If you run `git fetch` manually in the terminal on the SSH side, does the graph then update?
  > 
  > Please let us know — this will help determine whether there's a bug specific to the SSH remote scenario or whether enabling `git.autofetch` would resolve this.
  > 
  > _This issue will be closed automatically in 7 days if no further information is provided._

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Unclear reproduction — may be as-designed behavior depending on user's git.autofetch setting; needs more info before any fix could be scoped.

## Evidence

- **history provider design:** extensions/git/src/historyProvider.ts line 78 filters on `!e.operation.readOnly` — the graph updates only after write operations (fetch, push, pull, etc.), not on a timer or manual refresh of the view.
- **autofetch mechanism:** extensions/git/src/autofetch.ts implements periodic `git fetch` controlled by `git.autofetch` setting (default: false). If disabled, the graph won't reflect remote changes until a manual fetch/sync.
- **missing config info:** The reporter does not mention their `git.autofetch` setting, which is the primary mechanism for keeping the graph current with remote state.
- **local vs remote comparison:** The reporter compares a local window (which reflects local operations directly) to an SSH window (which requires a fetch to see remote changes) — this difference is expected behavior without autofetch.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
