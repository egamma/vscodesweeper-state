---
number: 210828
url: "https://github.com/microsoft/vscode/issues/210828"
engine: copilot
owner: ulugbekna
labels: bug, rename
proposedLabel: none
triageAction: keep-open
confidence: 0.88
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-07-26T07:39:45.255Z"
itemUpdatedAt: "2024-12-06T10:22:49Z"
itemCreatedAt: "2024-04-20T13:39:00Z"
upvotes: 0
reactionsTotal: 0
commentCount: 3
relatedOpenIssues: 0
milestone: Backlog
contentHash: e51a6fdb5e30bcfdd92a997288d8961ff7e968625df738659b631f71d93a9cb1
policyHash: 26158a1d7dfd26ac
---

# Issue #210828 — rename symbol preview not working

[https://github.com/microsoft/vscode/issues/210828](https://github.com/microsoft/vscode/issues/210828)

## Summary

Still reproduces on main: the rename widget label shows literal "undefined" when the preview keybinding is overridden, because `lookupKeybinding(preview)?.getLabel()` returns `undefined` and it's interpolated directly into the localized string without a fallback.

## Change summary

Rename widget displays "undefined to Preview" when the preview keybinding is overridden by a custom shortcut.

## Best solution

Guard the label construction in `renameWidget.ts` line 312 — when `lookupKeybinding(preview)` resolves to `undefined`, either omit the preview portion of the label entirely or display a placeholder like an empty string. The fix is small and localized to the `beforeRender` method.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** rename
- **Confidence:** 0.88
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small localized guard in renameWidget.ts beforeRender(); source-reproducible with a clear validation path.
- **Likely files:** `src/vs/editor/contrib/rename/browser/renameWidget.ts`
- **Validation:** Add a unit test that constructs a RenameWidget where the preview keybinding lookup returns undefined, triggers beforeRender, and asserts the label does not contain the literal string 'undefined'.
- **Fix prompt:**

  > In src/vs/editor/contrib/rename/browser/renameWidget.ts, the `beforeRender()` method at line 312 builds a label via `nls.localize(...)` with two keybinding placeholders. When `this._keybindingService.lookupKeybinding(preview)` returns undefined (because the user overrode the keybinding), `?.getLabel()` evaluates to `undefined` which is interpolated as the literal string "undefined to Preview". Fix: when the preview keybinding label is undefined/falsy, omit the ", {1} to Preview" portion from the displayed label (e.g. conditionally use a different localized string that only shows the accept keybinding, or pass an empty string instead of undefined). Apply the same guard to the accept keybinding for robustness. Do NOT restructure the widget or change unrelated code. Validate with a unit test in src/vs/editor/contrib/rename/test/browser/ that mocks lookupKeybinding to return undefined for the preview command, calls beforeRender, and asserts the label element's innerText does not contain 'undefined'.

## Evidence

- **source trace:** src/vs/editor/contrib/rename/browser/renameWidget.ts line 312: `this._keybindingService.lookupKeybinding(preview)?.getLabel()` returns undefined when the keybinding is overridden, and that value is interpolated directly into the nls.localize template.
- **no fix on main:** git log on renameWidget.ts shows no commit addressing the undefined label; the unguarded interpolation is still present on current main.
- **contributor triage:** jrieken (CONTRIBUTOR) explicitly asked to keep this open to fix the undefined display (comment 2).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
