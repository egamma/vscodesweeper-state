---
number: 237781
url: "https://github.com/microsoft/vscode/issues/237781"
engine: copilot
owner: ulugbekna
labels: bug, error-telemetry
proposedLabel: "*not-reproducible"
triageAction: propose-close
confidence: 0.75
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-26T08:01:30.232Z"
itemUpdatedAt: "2025-12-10T22:14:00Z"
itemCreatedAt: "2025-01-13T08:55:00Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 1
milestone: Backlog
contentHash: 33a4d8bbff0dd216d3e60dd4c3fa2d93e19ce1d64463d5bb5cf413beaee248db
policyHash: 26158a1d7dfd26ac
---

# Issue #237781 — this.c.delete is not a function

[https://github.com/microsoft/vscode/issues/237781](https://github.com/microsoft/vscode/issues/237781)

## Summary

The error occurred during the history infrastructure refactoring (Nov 2024–Feb 2025). On current main, `HistoryNavigator._history` is typed as `IHistory<T>` which requires `delete`, and all callers in the keybindings editor path pass a proper `Set`. The code path is now type-safe and the error is no longer reproducible.

## Change summary

TypeError thrown in HistoryNavigator.add because `this._history.delete` was not a function, triggered from the keybindings editor search widget.

## Best solution

Close as not reproducible — the history refactoring in commits 8cf1060c7b9 and 5340ddd4ad0 (late 2024) introduced a transient state where callers could pass an object missing `delete`; the current `IHistory<T>` interface and TypeScript enforcement prevent this on main.

## Proposed action

- **Triage action:** propose-close
- **Item type:** bug
- **Reproduction:** not_reproduced
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *not-reproducible
- **Area labels:** none
- **Confidence:** 0.75
- **Proposed comment:**

  > Thanks for the telemetry report. This `TypeError: this.c.delete is not a function` occurred in the `HistoryNavigator.add` path during the history infrastructure refactoring (Nov–Dec 2024). On current main, the `IHistory<T>` interface enforces that all history objects must have a `delete` method, and the keybindings editor search widget passes a proper `Set`. The code path is now type-safe and this error should no longer occur on recent builds. Closing as not reproducible — please reopen if this is still seen on a current Insiders build.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Error no longer reproducible on current main — the type system now prevents the invalid state.

## Evidence

- **IHistory interface:** src/vs/base/common/history.ts line 11–19 defines `IHistory<T>` requiring `delete(t: T): boolean`; TypeScript enforces this for all callers.
- **keybindingsEditor caller:** src/vs/workbench/contrib/preferences/browser/keybindingsEditor.ts line 382 passes `new Set<string>(...)` which implements `delete`.
- **refactoring timeline:** Commit 8cf1060c7b9 (2024-11-15) changed `HistoryInputBox` to accept `IHistory<string>` via `options.history`; commit 5340ddd4ad0 (2024-12-04) fixed further issues; the error telemetry is from 2025-01-13, likely from a build in that transitional period.
- **no fix commit for this issue:** No commit references #237781, but the code path is now safe due to the type system and the completed refactoring.
- **label-validation:** Engine proposed areaLabels that do not exist in the repo — dropped: preferences-editor (real labels only).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
