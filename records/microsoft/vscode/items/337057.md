---
number: 337057
url: "https://github.com/microsoft/vscode/issues/337057"
engine: copilot
owner: unassigned
labels: ""
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.85
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
agentReadiness: implement
reviewedAt: "2026-09-21T14:59:41.682Z"
itemUpdatedAt: "2026-09-21T14:50:46Z"
itemCreatedAt: "2026-09-21T14:50:46Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 083b7494ba440b7d596a2d148b7f3e5aeb2f3b707dc1f011d885bd8455966831
policyHash: d13126680f5ee38a
---

# Issue #337057 — Remove unused Optional server connection token type

[https://github.com/microsoft/vscode/issues/337057](https://github.com/microsoft/vscode/issues/337057)

## Summary

Confirmed on main: `ServerConnectionTokenType.Optional` in serverConnectionToken.ts is dead — only `None` and `Mandatory` are ever constructed or compared against — a small, safe cleanup.

## Change summary

Remove the unused `Optional` server connection token enum member and its stale removal comment.

## Best solution

Delete the `Optional` member (and its `// TODO: Remove this soon` comment) from `ServerConnectionTokenType` in src/vs/server/node/serverConnectionToken.ts; no other code references it.

## Proposed action

- **Triage action:** keep-open
- **Item type:** debt
- **Reproduction:** source_reproducible
- **Evidence:** 5/6 Source-confirmed
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.85
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Agent readiness:** implement
- **Rationale:** Confirmed dead enum member from source; single bounded edit in one file with no other call sites, and existing unit tests provide the validation check.
- **Likely files:** `src/vs/server/node/serverConnectionToken.ts`
- **Validation:** Remove the `Optional` enum member and run `src/vs/server/test/node/serverConnectionToken.test.ts` — the existing suite (which only exercises None/Mandatory paths) must still pass unchanged, and a typecheck/build of src/vs/server must succeed with no remaining references to `ServerConnectionTokenType.Optional`.
- **Behavior:**

  > `ServerConnectionTokenType` should only expose `None` and `Mandatory` — the only two token states VS Code's server ever produces or checks. No parsing, validation, or connection behavior changes; this is purely removing a dead enum value and its stale comment.
- **Trace:**

  > In src/vs/server/node/serverConnectionToken.ts, `ServerConnectionTokenType` (lines 17-21) declares `Optional` between `None` and `Mandatory`, tagged with `// TODO: Remove this soon`. `parseServerConnectionToken` only ever returns `NoneServerConnectionToken` or `MandatoryServerConnectionToken`, and every consumer (remoteExtensionHostAgentServer.ts, webClientServer.ts, serverServices.ts, remoteAgentEnvironmentImpl.ts) compares only `.None`/`.Mandatory`. Boundary: do not touch the `None`/`Mandatory` values or any parsing/validation logic — only remove the unused `Optional` member and its comment. Related to https://github.com/microsoft/vscode/issues/326491 (context for why the TODO existed).

## Evidence

- **enum definition:** src/vs/server/node/serverConnectionToken.ts:17-21 defines `None, Optional (with TODO comment), Mandatory`.
- **only two constructed types:** NoneServerConnectionToken (type=None) and MandatoryServerConnectionToken (type=Mandatory) are the only two classes implementing ServerConnectionToken; `ServerConnectionToken` type union is `NoneServerConnectionToken | MandatoryServerConnectionToken`.
- **no other references:** rg for `ServerConnectionTokenType` across the repo shows every comparison site (remoteExtensionHostAgentServer.ts, webClientServer.ts, serverServices.ts, remoteAgentEnvironmentImpl.ts, tests) only checks `.None` or `.Mandatory`; `Optional` appears nowhere else.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
