---
number: 328736
url: "https://github.com/microsoft/vscode/issues/328736"
engine: copilot
owner: joshspicer
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: common
priority: none
visionFit: aligned
autoFixable: false
reviewedAt: "2026-08-07T08:10:11.037Z"
itemUpdatedAt: "2026-08-03T15:35:54Z"
itemCreatedAt: "2026-08-03T15:23:57Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 8e2c80f18808314ecac8775fc1c7a9917161ff3b8f0bf915a1c9fa83fc28b129
policyHash: 70e952cc2fb28823
---

# Issue #328736 — Copilot chat sessions created in agent ("Copilot") mode disappear from the Sessions panel on Remote-SSH

[https://github.com/microsoft/vscode/issues/328736](https://github.com/microsoft/vscode/issues/328736)

## Summary

Detailed, plausible report of agent-mode Copilot chats vanishing from the Sessions panel over Remote-SSH, but tracing the actual `listSessions` gate on current main shows a different mechanism than the one the reporter blames — asking for an Agent Host debug-log export to pin down the real cause before routing further.

## Change summary

Copilot 'agent' mode chats disappear from the Sessions panel on Remote-SSH shortly after creation, while Copilot CLI-mode chats stay listed.

## Best solution

Have the reporter attach an Export Agent Host Debug Logs bundle (or confirm on the latest Insiders) so a maintainer can check whether `_readStoredSessionMetadata`'s `workingDirectory`/`_META_CWD` value is failing to persist for agent-mode sessions over the remote connection — that is the actual filter in `CopilotAgent.listSessions`, not the presence of `events.jsonl` the reporter suspected.

## Vision fit

- **Fit:** aligned
- **Reason:** Agent host session reliability and parity across local/remote configurations is part of the current agent-host workstream priorities.
- **Evidence:** aligns with current agent-host rollout and remote-parity priorities

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** common
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** agent-host, remote
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the very detailed investigation — this is genuinely useful. Looking at the current `CopilotAgent.listSessions()` implementation on `main`, the actual gate that decides whether a session is listed is whether the session's `session.db` has a persisted working-directory (`_META_CWD`) value, not the presence of an `events.jsonl` file — that file is only used in the legacy Copilot CLI adoption path. So the specific mechanism you diagnosed doesn't quite match current code, but the symptom (agent-mode sessions vanishing from the panel over Remote-SSH) still sounds real and could point to the working-directory metadata not getting persisted correctly over the remote connection.
  > 
  > Could you attach an Agent Host debug log bundle from a session that disappeared (Command Palette → "Export Agent Host Debug Logs") and confirm the exact VS Code/Copilot Chat build you're on? That will help pin down whether the working-directory write is failing or being read from the wrong host. Note this issue will auto-close in 7 days if there's no reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Root cause is unconfirmed — the reporter's theorized mechanism (events.jsonl) doesn't match the actual listSessions gate found in source, so more diagnostic info is needed before a fix can be scoped or validated.

## Evidence

- **actual filter on main:** src/vs/platform/agentHost/node/copilot/copilotAgent.ts:1801-1859 `listSessions()` filters sessions based on `_readStoredSessionMetadata(session).workingDirectory` (backed by the `_META_CWD` value in the session's `session.db`), not on the presence of an `events.jsonl` file as the reporter's root-cause theory describes.
- **events.jsonl usage:** `events.jsonl` only appears (copilotAgent.ts:2292, 2388) in the legacy extension-host Copilot CLI adoption path (`_isExtensionHostCliSession`), used to adopt un-migrated CLI sessions — it is not part of the normal agent-mode session listing gate on current main.
- **related symptom on Remote-SSH:** https://github.com/microsoft/vscode/issues/293595 reports a very similar symptom (agent sessions disappearing from the Sessions panel specifically on Remote-SSH) for the Claude agent provider, suggesting a possible shared remote-metadata-persistence issue across agent-host providers, though the code path differs (Claude provider vs. Copilot provider).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
