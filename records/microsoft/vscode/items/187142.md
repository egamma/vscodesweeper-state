---
number: 187142
url: "https://github.com/microsoft/vscode/issues/187142"
engine: copilot
owner: alexdima
labels: debt
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.94
severity: none
breadth: common
priority: none
visionFit: not_applicable
autoFixable: true
agentReadiness: implement
reviewedAt: "2026-09-19T14:40:40.512Z"
itemUpdatedAt: "2023-07-06T13:32:31Z"
itemCreatedAt: "2023-07-06T13:32:31Z"
upvotes: 1
reactionsTotal: 1
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 9be794ffd5da74def963abe8a920e2d090223820af4254f33f2ce0e33b3a5e09
policyHash: 45300d6f6cd721ff
---

# Issue #187142 — Deprecate IRelaxedExtensionDescription.id

[https://github.com/microsoft/vscode/issues/187142](https://github.com/microsoft/vscode/issues/187142)

## Summary

This team-authored cleanup remains necessary on current main: `IRelaxedExtensionDescription.id` is still populated and consumed despite the required case-insensitive `identifier` representation. No issue-referencing completion commit was found, so the protected item should remain open.

## Change summary

Remove the legacy case-sensitive extension-description `id` property and document the purpose and comparison semantics of `identifier`.

## Best solution

Complete the extension identity migration by eliminating the redundant legacy property, retaining UUID-based renamed-extension lookup, and documenting that `ExtensionIdentifier` provides case-insensitive identity.

## Proposed action

- **Triage action:** keep-open
- **Item type:** debt
- **Reproduction:** not_applicable
- **Evidence:** not applicable
- **Severity:** none
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.94
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Agent readiness:** implement
- **Rationale:** The remaining debt is source-confirmed, bounded to extension-description identity construction and lookup, and has a concrete type-check and registry-test validation path.
- **Likely files:** `src/vs/platform/extensions/common/extensions.ts`, `src/vs/platform/extensionManagement/common/extensionsScannerService.ts`, `src/vs/workbench/services/extensions/common/extensions.ts`, `src/vs/workbench/services/extensions/common/abstractExtensionService.ts`, `src/vs/workbench/services/extensions/test/common/extensionDescriptionRegistry.test.ts`
- **Validation:** Run `npm run typecheck-client` and the extension description registry unit tests via `scripts/test.sh --grep "ExtensionDescriptionRegistry"`; verify extension lookup remains case-insensitive and UUID-based renamed-extension matching still passes without `IRelaxedExtensionDescription.id`.
- **Behavior:**

  > Extension descriptions have one canonical publisher/name identity representation whose comparisons are case-insensitive. Renamed extensions must continue to be discoverable through their UUID, and normal extension registration must continue rejecting duplicate identifiers regardless of casing. Acceptance requires the client type check and extension-description registry tests to pass without the legacy `id` property.
- **Trace:**

  > `IRelaxedExtensionDescription` in `src/vs/platform/extensions/common/extensions.ts` currently carries both optional raw `id` and required `identifier`, while `ExtensionIdentifier` already owns case-insensitive equality and key generation. The scanner and workbench conversion functions still populate both representations. `AbstractExtensionService._canAddExtension` passes the raw `id` as the UUID argument to `getExtensionDescriptionByIdOrUUID`, although extension descriptions separately carry `uuid`; the registry implementation confirms its second argument is specifically a UUID. Preserve UUID-based renamed-extension matching and all manifest identity behavior; do not broaden this cleanup into gallery-extension or public extension API identifier types that independently use `id`.

## Evidence

- **legacy declaration:** `src/vs/platform/extensions/common/extensions.ts:566-579` still declares `IRelaxedExtensionDescription.id?: string` alongside the required `identifier: ExtensionIdentifier`, with no deprecation annotation or property documentation.
- **case-insensitive identity:** `src/vs/platform/extensions/common/extensions.ts:430-477` implements `ExtensionIdentifier` with a lowercase comparison key and case-insensitive equality, confirming why callers should use `identifier` rather than a raw string.
- **production and consumption:** `src/vs/platform/extensionManagement/common/extensionsScannerService.ts:1055-1071` and `src/vs/workbench/services/extensions/common/extensions.ts:577-592` still populate the legacy `id`; `src/vs/workbench/services/extensions/common/abstractExtensionService.ts:368-369` still consumes it while checking extension identity.
- **history:** Repository history contains no commit matching issue 187142 or `IRelaxedExtensionDescription`; therefore there is no explicit issue-referencing implementation that could satisfy the factual-close requirement for this MEMBER-authored issue.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
