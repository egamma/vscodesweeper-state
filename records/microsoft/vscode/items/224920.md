---
number: 224920
url: "https://github.com/microsoft/vscode/issues/224920"
engine: codex
owner: alexdima
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.76
reviewedAt: "2026-06-26T19:35:13.290Z"
contentHash: 25c74c0fb8699f2ba637b8407ec81706925f6d8015defb618349298344025d6b
policyHash: 17b7468985688190
---

# Issue #224920 — [Web] Backspace doesn't remove the character right before the cursor but removes the character preceding it 

[https://github.com/microsoft/vscode/issues/224920](https://github.com/microsoft/vscode/issues/224920)

## Summary

Keep this in the Android editor input area, but ask for a deterministic repro before treating it as actionable. The report has videos and environment details, but no clear trigger or repeatable text/input sequence, and I did not find a fixing commit or canonical duplicate in local history.

## Change summary

Backspace in VS Code Web on Android sometimes deletes the wrong earlier character or inserts a newline after extended typing.

## Best solution

Get a minimal reproducible Android/Chrome sequence, ideally with the exact text before/after and keyboard/composition behavior, then route any fix through the editor textarea Android composition path such as `src/vs/editor/browser/controller/editContext/textArea/textAreaEditContextInput.ts` or `textAreaEditContextState.ts`.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Proposed label:** info-needed
- **Area labels:** editor-input, android
- **Confidence:** 0.76
- **Proposed comment:**

  > Thanks for the report. The recordings show the bad deletion, but we still need a small deterministic repro before this is actionable: the exact text before pressing Backspace, cursor position, keyboard/input method, and the shortest sequence that makes Backspace start deleting the wrong character. Marking as `info-needed`; issues with this label auto-close after 7 days without the requested details.

## Evidence

- **report completeness:** The issue body says the reporter is not sure what triggers the bug and only that it has a high chance after typing a lot, so maintainers do not have deterministic steps to reproduce.
- **current routing:** The issue is already labeled `bug`, `editor-input`, and `android`, matching the implicated VS Code Web editor input path.
- **code path:** Current main still contains Android-specific textarea composition handling in `src/vs/editor/browser/controller/editContext/textArea/textAreaEditContextInput.ts` and diffing in `textAreaEditContextState.ts`; the existing `Android backspace` unit test only covers a simple single-character deletion case.
- **history check:** Local `git log` searches for `224920`, Android/editor input, and backspace-related fixes did not find a commit explicitly fixing https://github.com/microsoft/vscode/issues/224920.

---
Reviewed by vscodesweeper (codex) — proposal only, not applied.
