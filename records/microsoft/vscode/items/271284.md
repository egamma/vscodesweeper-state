---
number: 271284
url: "https://github.com/microsoft/vscode/issues/271284"
engine: copilot
owner: anthonykim1
labels: bug, confirmation-pending, terminal-process
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-25T16:48:08.376Z"
itemUpdatedAt: "2025-12-05T01:46:12Z"
itemCreatedAt: "2025-10-14T10:05:54Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 1
milestone: Backlog
contentHash: b96c213ed80ddb8a903c2c16efd3f64725d6139206227ea1f55540f95aa7002b
policyHash: ef2efec6827348f6
---

# Issue #271284 — VSCode occupies all my folders in PATH environment variable, so I got folder access denied when delete these folder

[https://github.com/microsoft/vscode/issues/271284](https://github.com/microsoft/vscode/issues/271284)

## Summary

The report lacks detail to determine whether the directory handles are held by VS Code's own process or by the spawned shell subprocess (which naturally holds handles to PATH directories on Windows). Labeled `confirmation-pending` with no team confirmation; requesting more info.

## Change summary

VS Code (or its terminal subprocess) holds file handles on directories listed in the PATH environment variable on Windows, preventing their deletion.

## Best solution

The reporter should clarify: (1) whether the handles are on `Code.exe` itself or on the shell child process (e.g. `powershell.exe`, `cmd.exe`), (2) whether the issue reproduces when the terminal panel is never opened, and (3) whether closing the terminal tab releases the handles. If the handles are on the shell subprocess, this is expected OS behavior; if on `Code.exe` itself, the terminal process launch or PATH-scanning code in `src/vs/workbench/contrib/terminal/` would need investigation.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** terminal-process
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the report. To help us investigate, could you clarify a few things?
  > 
  > 1. **Which process holds the handles?** In Windows Resource Monitor, `Code.exe` can include child processes (like the terminal's shell). Could you check whether the handles are on the main VS Code process or on a child `powershell.exe`/`cmd.exe` process?
  > 2. **Does it reproduce without the terminal?** If you open VS Code with no terminal panel visible (close all terminal tabs first, then restart), do the PATH directory handles still appear?
  > 3. **Does closing the terminal release the handles?** If you close the terminal tab (not just hide the panel), do the handles go away?
  > 
  > On Windows, shell processes naturally hold handles to directories in PATH — this may be expected OS behavior from the spawned shell rather than a VS Code bug. The answers above will help us determine whether VS Code itself is doing something unexpected.
  > 
  > Please also test on the latest [Insiders build](https://code.visualstudio.com/insiders/) if possible.
  > 
  > *Note: `info-needed` issues are auto-closed after 7 days without a reply.*

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction unclear — cannot confirm this is a VS Code defect vs. expected OS/shell behavior.

## Evidence

- **thin reproduction:** The reporter shows a Resource Monitor screenshot listing handles under 'Code.exe' but does not distinguish between the main VS Code process and child shell processes; Windows Resource Monitor groups child processes under the parent.
- **expected OS behavior possible:** On Windows, spawned shell processes (cmd.exe, powershell.exe) naturally resolve and hold handles to PATH directories — this is standard Windows behavior, not necessarily a VS Code defect.
- **confirmation-pending label:** The issue carries `confirmation-pending`, indicating the team has not yet confirmed the behavior as a bug.
- **no community confirmation:** 0 upvotes; the only comment is from the original reporter (crying emojis), with no independent reproduction.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
