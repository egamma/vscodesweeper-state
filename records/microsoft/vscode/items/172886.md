---
number: 172886
url: "https://github.com/microsoft/vscode/issues/172886"
engine: copilot
owner: chrmarti
labels: bug, proxy
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.76
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-25T21:42:08.690Z"
itemUpdatedAt: "2024-01-19T13:16:35Z"
itemCreatedAt: "2023-01-31T03:55:06Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: df65c4c4af4696dca3780701961251634db185b3eecb3e24de40ce3faaaf70e1
policyHash: 278656bf1e7c0255
---

# Issue #172886 — The Hosts file is not applied in the network proxy.

[https://github.com/microsoft/vscode/issues/172886](https://github.com/microsoft/vscode/issues/172886)

## Summary

The reporter confirmed the failure through January 2024, but current source delegates the SOCKS endpoint connection to Node's normal socket lookup, which should consult `/etc/hosts`; current-main behavior and the failing resolution stage therefore need fresh diagnostics.

## Change summary

A SOCKS proxy hostname defined in WSL's `/etc/hosts` cannot be reached, while using the corresponding literal IPv6 address succeeds; the report also asks for `ALL_PROXY` support.

## Best solution

Confirm the failure on current Insiders and isolate whether proxy-host DNS lookup, IPv4/IPv6 address selection, or the socket connection fails. If confirmed, cover the WSL hosts-file proxy endpoint in the proxy-agent integration tests and ensure proxy endpoint resolution follows the remote environment's normal resolver; treat `ALL_PROXY` support as a separate configuration decision.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** proxy
- **Confidence:** 0.76
- **Proposed comment:**

  > Thanks for the report and the earlier confirmations. The proxy stack has changed since the last update, and the current code ultimately connects to the SOCKS proxy hostname through Node's normal socket resolver, so could you please retry with the latest Insiders under WSL and provide the proxy log plus the output of `getent hosts Proxy.local` and a minimal `node -e "require('dns').lookup('Proxy.local', { all: true }, console.log)"` run from the same environment? Please also confirm whether the literal IPv4 address works, since the two `/etc/hosts` entries may make address-family selection relevant. The separate `ALL_PROXY` variable is still not consumed by the current resolver, although HTTP_PROXY/HTTPS_PROXY should cover the reported reproduction. Issues labeled `info-needed` are automatically closed after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** Current behavior is unconfirmed and the source path should already use normal host resolution, so an agent lacks a diagnosed defect and validation target.

## Evidence

- **historical reproduction:** The reporter reproduced the same `_PacProxyAgent` SOCKS connection failure in VS Code 1.76.1 and again on 2024-01-18: https://github.com/microsoft/vscode/issues/172886#issuecomment-1897825747.
- **current integration path:** Current `src/vs/workbench/api/node/proxyResolver.ts` passes `process.env` to `@vscode/proxy-agent`; that package constructs a `SocksProxyAgent` for SOCKS results, while the underlying `socks` client connects to the proxy hostname through `net.Socket.connect`.
- **ALL_PROXY gap:** Current `@vscode/proxy-agent` source selects only `https_proxy`/`HTTPS_PROXY` and `http_proxy`/`HTTP_PROXY`; it does not read `ALL_PROXY`. This does not explain the reported run because HTTP_PROXY and HTTPS_PROXY were also set.
- **dependency changes:** VS Code now uses `@vscode/proxy-agent` 0.45.0 and `socks-proxy-agent` 8.0.1, substantially newer than the report-era stack, but no concrete fixing commit for `/etc/hosts` proxy hostname resolution was found.
- **canonical search:** Repository searches for SOCKS proxy hosts-file failures found no separate open canonical issue; https://github.com/microsoft/vscode/issues/167015 concerns disabling proxy redirection and does not own this failure.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
