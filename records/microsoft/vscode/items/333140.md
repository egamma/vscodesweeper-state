---
number: 333140
url: "https://github.com/microsoft/vscode/issues/333140"
engine: copilot
owner: TylerLeonhardt
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-31T16:23:40.504Z"
itemUpdatedAt: "2026-08-31T14:44:48Z"
itemCreatedAt: "2026-08-28T10:53:45Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: a50fd141c31d949fde14c54d87f30afae79b70240ad3125019dfeb9a44ca9e47
policyHash: 3d6943c3a15c721c
---

# Issue #333140 — VS Code Agents Window mcp server integration is a blocker to use

[https://github.com/microsoft/vscode/issues/333140](https://github.com/microsoft/vscode/issues/333140)

## Summary

The report is too thin to act on (no agent provider, MCP server, or logs named), and the codebase already has substantial machinery for exactly this scenario — an 'Authenticate' action for MCP servers in the AuthRequired state — so it's unclear whether the reporter hit a real gap in that flow or simply didn't find the existing affordance.

## Change summary

When an MCP server configured for the Agents Window requires an OAuth flow, the session appears to get permanently stuck with no way to trigger re-authentication.

## Best solution

Confirm with the reporter which agent provider (Copilot/Claude/Codex) and MCP server they used, and whether the MCP server list ever surfaced 'Authentication required' status; if the AuthRequired detection genuinely never fires for their server/provider (e.g. the regex-based 'not logged in' heuristic in codexMcpServers.ts:codexStartupErrorNeedsAuth, or the equivalent in copilotAgentSession.ts, missing their server's specific error text), that heuristic needs to be widened. If the status does appear but the Authenticate action isn't discoverable from inside the Agents Window itself, that's a smaller UX/discoverability fix in the sessions MCP UI.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** mcp
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report. VS Code already has an 'Authenticate' action for MCP servers stuck needing OAuth (it shows up when a server's status is 'Authentication required' — reachable from the MCP servers list), but it depends on the agent provider correctly detecting that the server needs auth in the first place.
  > 
  > Could you share: which agent/provider you were using (Copilot, Claude, or Codex), the MCP server in question, and the output from its diagnostics channel ('Show Output') when it gets stuck? That'll help pin down whether the auth-required detection is missing your server's specific error, or whether the Authenticate action just isn't easy to find from the Agents Window.
  > 
  > This issue will auto-close in 7 days if there's no reply with more details, but please just reopen if that happens before we get to it.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Cannot confirm the exact defect (detection heuristic miss vs. UI discoverability) without knowing the agent provider and MCP server involved, so there's no concrete validation to target yet.

## Evidence

- **existing auth-required infra:** McpServerStatus.AuthRequired is modeled and handled across all three agent providers (src/vs/platform/agentHost/node/codex/codexMcpServers.ts, .../copilot/copilotAgentSession.ts, .../shared/mcpCustomizationController.ts), each surfacing an auth-gated MCP server as AuthRequired rather than a fatal error.
- **existing Authenticate action:** src/vs/workbench/contrib/mcp/browser/mcpCommands.ts (around line 532-560) already offers an 'Authenticate' quick-pick item when `server.state.kind === McpServerStatus.AuthRequired`, calling `agentHostCustomizations.authenticateMcpServer(...)` — this is the mechanism that would let a user re-trigger the OAuth browser flow for an agent-host MCP server.
- **heuristic detection risk:** codexStartupErrorNeedsAuth() in codexMcpServers.ts matches auth-required states via a regex on codex's human-readable error text ('not logged in|mcp login|...'); if the reporter's specific MCP server/provider emits a different error phrasing, the session could stay stuck in a generic 'Starting'/'Error' state without ever reaching AuthRequired, which would match their report of no reauth affordance appearing.
- **report is thin:** No MCP server name, no agent provider, no logs or screenshots were included, so it isn't possible to tell from the checkout alone whether this is the discoverability gap or a detection-heuristic miss.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
