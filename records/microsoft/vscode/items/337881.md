---
number: 337881
url: "https://github.com/microsoft/vscode/issues/337881"
engine: copilot
owner: roblourens, connor4312, vritant24
labels: bug
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: plan
reviewedAt: "2026-09-25T13:33:35.812Z"
itemUpdatedAt: "2026-09-25T04:13:19Z"
itemCreatedAt: "2026-09-25T03:49:34Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 1
milestone: none
contentHash: 20275e3716a1534e833d53772b56c06dcf5e064c169656362e2f98c7147cf894
policyHash: d13126680f5ee38a
---

# Issue #337881 — Agents Window: Figma Sign In action targets slack-github

[https://github.com/microsoft/vscode/issues/337881](https://github.com/microsoft/vscode/issues/337881)

## Summary

Well-diagnosed, credible bug: the reporter's AHP debug logs directly show the Figma row's Sign In control repeatedly issuing Start requests for a different server (slack-github). I traced the two Sign In wiring paths in mcpListWidget.ts (McpServerItemRenderer.updateStatus and appendInstalledServerSignIn) and both correctly bind to the row's own activeSessionServer.id with rowKey/signature-based invalidation, so the actual defect likely lives in virtualized-list row recycling or stale click-listener retention that I could not fully trace from source alone (shallow clone also blocks git-history verification of a possible prior fix).

## Change summary

Clicking Sign In on a Figma MCP server row instead sends a Start Server request for an unrelated server (slack-github), so Figma authentication never opens.

## Best solution

Add the reporter's suggested renderer-level regression test (render Figma + Slack active-session rows, transition/recycle states, click Sign In on the Figma row, assert only the Figma ID is authenticated) to pin the row-identity bug, then trace where the mismatch actually occurs — likely in virtualized row template reuse or a retained click handler from a prior row/action — since the currently-reviewed code paths in mcpListWidget.ts already look correctly scoped per row.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** reproduced
- **Evidence:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** plan
- **Rationale:** Real, well-evidenced bug, but the two Sign In wiring paths I traced already scope the auth call correctly per row — the actual root cause (likely virtualized row recycling or a retained click handler) is unconfirmed and unbounded across candidate subsystems, so this needs a diagnosis pass before a fix can be written.

## Plan brief

- **Likely files:** `src/vs/workbench/contrib/chat/browser/aiCustomization/mcpListWidget.ts`
- **Validation:** Renderer-level regression test per the reporter's suggestion: render active-session rows for Figma and Slack, transition/recycle them through stopped/starting/authRequired, click Sign In on the row labeled Figma, and assert authentication is invoked with the Figma server id while slack-github.start()/auth is never called.
- **Behavior:**

  > Clicking the Sign In control on a row labeled with one MCP server (e.g. Figma) must authenticate that exact server; it must never issue a start/auth request for a different server (e.g. slack-github), regardless of prior row reordering, recycling, or state transitions in the virtualized list. Acceptance: the suggested regression test (render two active-session rows, transition/reorder/recycle, click Sign In on the visibly-labeled row, assert the correct server id receives the auth call and the other server sees no start/auth call) passes.
- **Trace:**

  > The two currently-reviewed Sign In wiring paths look correct in isolation: McpServerItemRenderer.updateStatus (mcpListWidget.ts:449-519) resolves currentEntry/activeSessionServer freshly per render and keys action-node invalidation off getMcpRowKey(element) via renderedRowKey (line 309-315) and a renderedStatusSignature that includes rowKey and activeSessionServerId (lines 469-486); appendInstalledServerSignIn (mcpListWidget.ts:2348-2382) similarly re-resolves getActiveSessionServer(getEntry()) inside the click handler. Neither shows the literal pattern the reporter flagged (a stale getActiveSessionServerLifecycleAction()'s server.start() callback firing instead of authenticateMcpServer). The actual defect is therefore likely in a layer not fully traced here — e.g. the underlying virtualized WorkbenchList's DOM/template recycling, or a Button click-listener not torn down on disposeElement — which needs first-hand reproduction (the reporter's suggested test) to pin down. Boundary: do not restructure the row-identity/signature invalidation scheme already in place unless the regression test proves it insufficient; related issue for context: https://github.com/microsoft/vscode/issues/337874.

## Evidence

- **reporter log evidence:** Six consecutive c2s session/mcpServerStartRequested AHP messages all target slack-github while clicking the visible Figma Sign In control; no request for the Figma customization ID or https://mcp.figma.com appears.
- **code trace 1:** src/vs/workbench/contrib/chat/browser/aiCustomization/mcpListWidget.ts:500-505 (McpServerItemRenderer.updateStatus) wires the Sign In button to authenticateMcpServer(agentHostCustomizationService, activeSessionResource, activeSessionServer.id) — the id belongs to the currentEntry resolved for that specific row, and renderedRowKey/renderedStatusSignature (lines 310-312, 469-486) invalidate cached actions when the row's identity changes.
- **code trace 2:** src/vs/workbench/contrib/chat/browser/aiCustomization/mcpListWidget.ts:2348-2382 (appendInstalledServerSignIn, the 'card' rendering path) similarly re-resolves getActiveSessionServer(getEntry()).id inside the click handler and on each onDidChangeCustomizations update, keyed by rowKey (line 2326).
- **limited history:** Repository checkout is a shallow clone (depth 1, single commit 11e39c07), so I cannot confirm via git log/blame whether a fix already landed after this report or trace when this code was last touched.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
