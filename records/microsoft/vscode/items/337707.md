---
number: 337707
url: "https://github.com/microsoft/vscode/issues/337707"
engine: copilot
owner: justschen
labels: ""
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.65
severity: none
breadth: niche
priority: none
visionFit: aligned
autoFixable: false
agentReadiness: plan
reviewedAt: "2026-09-25T13:43:50.484Z"
itemUpdatedAt: "2026-09-24T14:17:37Z"
itemCreatedAt: "2026-09-24T14:17:36Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 68bc63d5f2cdb47460d3de53963d749a0e7293d30436004faa2e711be3f33835
policyHash: d13126680f5ee38a
---

# Issue #337707 — Responsive chat input fixture produces nondeterministic screenshot hashes

[https://github.com/microsoft/vscode/issues/337707](https://github.com/microsoft/vscode/issues/337707)

## Summary

Legitimate, fresh test-infra issue: a component-fixture screenshot (`ResponsiveModelResizeCycleMinimal`) flips between two near-identical PNG hashes on the Ubuntu runner, and the reporter already landed a temporary mitigation (removing `blocksCi` for this fixture) in #337699. This is a genuine follow-up task, not something to close.

## Change summary

The chat-input responsive resize screenshot fixture produces two different (visually indistinguishable, 1-channel-delta) PNG hashes across CI runs, making the blocking screenshot gate flaky.

## Best solution

Investigate the sub-pixel rendering variance (likely font/antialiasing or subpixel layout nondeterminism in headless Chromium on Ubuntu) for `sessions/chat/input/chatInput/ResponsiveModelResizeCycleMinimal`, confirm a stable hash across repeated Ubuntu runs, then restore `blocksCi: true` and re-add the Dark/Light entries to `test/componentFixtures/blocks-ci-screenshots.md`.

## Vision fit

- **Fit:** aligned
- **Reason:** Stabilizing a flaky CI screenshot gate is squarely engineering-system/OSS-triage work — build and test health that the project continuously invests in.
- **Evidence:** Durable theme: "Engineering system & OSS triage — issue bots and community triage, help wanted / good first issue hygiene, smoke tests, build health."

## Proposed action

- **Triage action:** keep-open
- **Item type:** debt
- **Reproduction:** not_applicable
- **Evidence:** not applicable
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.65
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** plan
- **Rationale:** Real, bounded-in-intent debt item, but the root cause (Ubuntu-runner rendering nondeterminism) isn't diagnosable from source alone — it needs actual repeated screenshot runs on the CI environment to confirm stability, which isn't an executable check available in this checkout.

## Plan brief

- **Likely files:** `src/vs/sessions/contrib/chat/test/browser/chatInput.fixture.ts`, `test/componentFixtures/blocks-ci-screenshots.md`, `src/vs/workbench/test/browser/componentFixtures/fixtureUtils.ts`
- **Validation:** After a proposed fix, run the screenshot capture for `ResponsiveModelResizeCycleMinimal` repeatedly (multiple CI runs) on the Ubuntu runner and confirm the PNG hash is stable before restoring `blocksCi: true` and re-adding its Dark/Light entries to blocks-ci-screenshots.md.
- **Behavior:**

  > The `ResponsiveModelResizeCycleMinimal` screenshot fixture should render byte-identical (or hash-stable) output across repeated runs on the same platform/runner, so the blocking CI screenshot gate does not fail on unrelated changes. Acceptance: multiple consecutive Ubuntu CI runs of this fixture produce the same accepted hash, after which `blocksCi` and its Dark/Light `blocks-ci-screenshots.md` entries are restored.
- **Trace:**

  > The fixture is defined at src/vs/sessions/contrib/chat/test/browser/chatInput.fixture.ts:128 with `labels: { kind: 'screenshot' }` (no `blocksCi`, per the temporary mitigation from https://github.com/microsoft/vscode/pull/337699). The reported diffs were 28 pixels with max channel delta of 1 — consistent with subpixel antialiasing/font-hinting nondeterminism rather than a layout bug. Where the uncertainty starts: no commit or trace confirms the actual root cause yet (e.g. whether it's font rendering, GPU compositor timing, or a settle-delay race in the fixture's resize-cycle rendering) — that diagnosis has to happen against the real Ubuntu runner, which this checkout cannot exercise.
- **Open decisions:**
  - Is the fix a rendering-determinism change (e.g. disabling subpixel antialiasing, forcing consistent font rendering) versus a fixture-level change (e.g. an added settle delay after the last resize in `resizeWidths`)?
  - If a stable hash still isn't achievable, should this fixture instead be marked `flaky: true` (the existing label fixtureUtils.ts already supports) rather than continuing to chase `blocksCi` restoration?

## Evidence

- **current fixture state:** src/vs/sessions/contrib/chat/test/browser/chatInput.fixture.ts:128 defines `ResponsiveModelResizeCycleMinimal` with `labels: { kind: 'screenshot' }` and no `blocksCi`, matching the described mitigation (blocksCi removed).
- **blocks-ci-screenshots.md:** test/componentFixtures/blocks-ci-screenshots.md has no Dark/Light entries for this fixture — consistent with the reported removal, confirming the mitigation from #337699 is already on main.
- **fixture label vocabulary:** src/vs/workbench/test/browser/componentFixtures/fixtureUtils.ts defines `ThemedFixtureGroupLabels` with `kind`, `blocksCi`, and a `flaky` flag — the infra already has a concept for exactly this kind of instability, supporting that this is a known, addressable class of issue rather than a one-off.
- **shallow checkout:** The provided checkout is a 1-commit shallow clone (HEAD only), so history/blame for the actual mitigation commit or diff could not be traced further; state was verified by inspecting current file contents only.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
