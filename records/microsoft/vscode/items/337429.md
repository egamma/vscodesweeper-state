---
number: 337429
url: "https://github.com/microsoft/vscode/issues/337429"
engine: copilot
owner: osortega
labels: ""
proposedLabel: none
closeReason: none
triageAction: route-to-area
confidence: 0.6
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-24T13:46:42.870Z"
itemUpdatedAt: "2026-09-24T10:12:42Z"
itemCreatedAt: "2026-09-23T08:01:13Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 04f0a121e3708d2fd57e2f6683386a711ca77058890d8ea4f2ce5e9bb984568d
policyHash: d13126680f5ee38a
---

# Issue #337429 — Codebase Semantic Index: triggerIndexing(manual) returns 401 despite valid GitHub session

[https://github.com/microsoft/vscode/issues/337429](https://github.com/microsoft/vscode/issues/337429)

## Summary

Detailed, consistent report of a 401 on the code-search trigger-indexing endpoint despite a valid, authenticated GitHub session; traced the client code and the auth token acquisition (`getGithubAccessToken`) and request construction are identical between the successful GET status check and the failing POST trigger, so this looks like a backend/CAPI-side authorization inconsistency rather than a bug in this repo's client code — routing to the area owner for backend investigation.

## Change summary

GithubCodeSearchService.triggerIndexing(manual) returns 401 'Requires authentication' even though getRemoteIndexState succeeds with the same session, blocking Codebase Semantic Index creation for any GitHub repo.

## Best solution

Have the chat-codebase/CAPI owners compare the POST trigger-indexing request against the GET status request server-side (token scope requirements, org/SSO enforcement, or a backend permission check specific to the write endpoint) since the client sends identical auth headers for both; no client-side code defect was found in extensions/copilot/src/platform/remoteCodeSearch/common/githubCodeSearchService.ts.

## Proposed action

- **Triage action:** route-to-area
- **Item type:** bug
- **Reproduction:** reproduced
- **Evidence:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** chat-codebase
- **Confidence:** 0.6
- **Proposed comment:**

  > Thanks for the detailed report and logs. This looks distinct from https://github.com/microsoft/vscode/issues/318023 (which fixed the misclassified-401/infinite-spinner bug, already shipped) — here the underlying 401 on the trigger-indexing call itself is still happening even though the status check with the same session succeeds. Looking at the client code, both requests use the identical auth-token acquisition and header construction, so this looks like it needs investigation on the code-search/CAPI service side rather than a client fix. Routing to the area owners for backend investigation.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** Client-side auth flow is identical between the working and failing requests; the discrepancy points to server-side (CAPI/backend) behavior outside this repo's code, so there's no bounded client fix to hand an agent yet.

## Evidence

- **identical auth path:** Both getRemoteIndexState (GET) and triggerIndexing (POST) in extensions/copilot/src/platform/remoteCodeSearch/common/githubCodeSearchService.ts call the same `getGithubAccessToken(auth.silent)` (lines 149/226) and pass the token via the same Authorization header pattern through `this._capiClientService.makeRequest`, so no client-side difference explains a 401 on one call but not the other.
- **prior related issue:** https://github.com/microsoft/vscode/issues/318023 (closed, completed, PR https://github.com/microsoft/vscode/pull/319965) fixed a *different* bug — 401/403 responses being misclassified as generic errors and an infinite spinner — but did NOT address why the backend returns 401 to the trigger-indexing call in the first place; that root cause appears unresolved and is what this new report describes.
- **no recent related fix:** `git log` on extensions/copilot/src/platform/remoteCodeSearch/ shows no commits touching this auth/request logic beyond the already-shipped 318023 fix.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
