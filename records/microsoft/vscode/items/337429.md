---
number: 337429
url: "https://github.com/microsoft/vscode/issues/337429"
engine: copilot
owner: osortega
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-23T13:33:27.450Z"
itemUpdatedAt: "2026-09-23T08:15:03Z"
itemCreatedAt: "2026-09-23T08:01:13Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 5f3ed7072afe9d8f8bc39a1eddf78f4b070f4031ff026629b5ba88f23e761380
policyHash: d13126680f5ee38a
---

# Issue #337429 — Codebase Semantic Index: triggerIndexing(manual) returns 401 despite valid GitHub session

[https://github.com/microsoft/vscode/issues/337429](https://github.com/microsoft/vscode/issues/337429)

## Summary

Fresh, well-logged report (0 days old, no comments yet) of a 401 on GithubCodeSearchService.triggerIndexing while the paired getRemoteIndexState call with the same token succeeds. Source inspection shows both calls build the auth header identically, so the discrepancy isn't visible as a client bug in this file — it likely needs live diagnosis (server-side scope/permission behavior) that can't be settled from the checkout alone, so this goes to needs-info rather than a source-confirmed close.

## Change summary

Building the Codebase Semantic Index fails with a 401 'Requires authentication' specifically on the manual triggerIndexing call, even though the preceding index-status check succeeds with the same GitHub session.

## Best solution

A maintainer with access to CAPI/GitHub backend logs should check whether the embeddings-index write path requires a different token scope/permission than the read path used by getRemoteIndexState; if it's confirmed to be the same recurring condition as #318023, tie the two together, otherwise diagnose as a new regression.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** chat-codebase
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed report and logs. This looks similar to https://github.com/microsoft/vscode/issues/318023, but that one was closed after https://github.com/microsoft/vscode/pull/319965 only improved how the 401 is surfaced (mapping it to a 'not-authorized' state) — it didn't change anything that would prevent the backend from returning 401 on the trigger-indexing call in the first place, so this could be the same underlying condition recurring rather than something newly broken.
  > 
  > Could you confirm this still happens on the latest VS Code/Copilot Chat Insiders build, and let us know if fully signing out of GitHub (Accounts menu) and back in changes anything? That'll help narrow down whether this is a token-scope issue on the backend side. This issue will auto-close in 7 days if there's no reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** The client-side auth construction is identical between the failing and succeeding calls, so there's no traceable client bug to fix without live backend diagnosis first.

## Evidence

- **identical client-side auth path:** extensions/copilot/src/platform/remoteCodeSearch/common/githubCodeSearchService.ts: getRemoteIndexState (lines 142-218) and triggerIndexing (lines 220-270) both call the same private getGithubAccessToken(auth.silent) (line 452-455) and build an identical Authorization header before calling this._capiClientService.makeRequest with { type: RequestType.EmbeddingsIndex, repoWithOwner }. No client-side difference explains why one succeeds and the other 401s.
- **closely related, already-fixed issue:** https://github.com/microsoft/vscode/issues/318023 reported the exact same 'GithubCodeSearchService.triggerIndexing... Response: 401' symptom and was closed as fixed by https://github.com/microsoft/vscode/pull/319965 (merged 2026-06-04). That PR only added 401/403 -> 'not-authorized' error classification (so the UI can prompt re-auth) and fixed a truthiness check in codeSearchRepo.ts — it did not change anything that would prevent the backend from returning 401 in the first place.
- **no reproduction attempt possible from checkout:** Verifying this requires a live authenticated request to the CAPI embeddings-index endpoint, which isn't something that can be traced or executed from a read-only source checkout.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
