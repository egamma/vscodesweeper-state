---
number: 307166
url: "https://github.com/microsoft/vscode/issues/307166"
engine: copilot
owner: meganrogge, anthonykim1
labels: confirmation-pending
proposedLabel: none
triageAction: keep-open
confidence: 0.88
severity: correctness
breadth: common
priority: none
visionFit: aligned
autoFixable: true
reviewedAt: "2026-07-21T06:45:15.019Z"
itemUpdatedAt: "2026-04-08T20:29:42Z"
itemCreatedAt: "2026-04-01T15:01:26Z"
upvotes: 1
reactionsTotal: 1
commentCount: 1
relatedOpenIssues: 1
milestone: none
contentHash: fa5ce5c37ab6d17a18a332f9a0502bcc121a6f97ce23ba7ef85d97c88da066fd
policyHash: ebd3763732abe97d
---

# Issue #307166 — Copilot cannot read the output of `node -v`

[https://github.com/microsoft/vscode/issues/307166](https://github.com/microsoft/vscode/issues/307166)

## Summary

Confirmed bug: the suffix-match logic in `findCommandEcho` falsely identifies the `v` at the start of `v24.14.1` as a wrapped continuation of the command `node -v`, stripping the actual output and leaving it empty.

## Change summary

Copilot's `run_in_terminal` tool reports 'Command produced no output' for `node -v` because the output-stripping heuristic incorrectly removes the real output.

## Best solution

Fix the suffix-match guard in `findCommandEcho` (strategyHelpers.ts) to reject very short suffix matches (e.g. require suffixLen >= some minimum like 3–4 characters, or reject single-character suffix matches whose char is a common output-starting character like `v`). The existing test suite in `strategyHelpers.test.ts` should gain a case for `node -v` producing `v24.14.1`.

## Vision fit

- **Fit:** aligned
- **Reason:** Correct terminal output capture is foundational to agent tool reliability, directly supporting the AI-native editor direction.
- **Evidence:** Aligns with current agent-experience priorities around tool quality and terminal integration.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** terminal-shell-integration
- **Confidence:** 0.88
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small, localized fix in the suffix-match guard of findCommandEcho; existing test suite covers the function and can validate the fix with a new case.
- **Likely files:** `src/vs/workbench/contrib/terminalContrib/chatAgentTools/browser/executeStrategy/strategyHelpers.ts`, `src/vs/workbench/contrib/terminalContrib/chatAgentTools/test/browser/strategyHelpers.test.ts`
- **Validation:** Add a test case: `stripCommandEchoAndPrompt('v24.14.1\nuser@host:~$', 'node -v')` should return `'v24.14.1'` (currently returns empty string). Run `scripts/test.sh src/vs/workbench/contrib/terminalContrib/chatAgentTools/test/browser/strategyHelpers.test.ts`.

## Evidence

- **root cause in source:** In `src/vs/workbench/contrib/terminalContrib/chatAgentTools/browser/executeStrategy/strategyHelpers.ts` line 224–236, `findCommandEcho` with `allowSuffixMatch=true` iterates suffix lengths from longest to 1. For `node -v`, the 1-char suffix `v` matches the start of `v24.14.1`, and `charBefore` is `-` which passes the non-space check, so the output line is incorrectly stripped as a command echo.
- **explains all observations:** The pattern explains all data in the issue: `node --version` ends in `n` (no suffix match with `v...` output); `npm -v` output starts with `1` not `v`; `node --version && node -v` ends in `v` and loses one output line.
- **no fix on main:** git log since 2026-04-01 shows no changes to strategyHelpers.ts; the bug persists on current main.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
