---
number: 307166
url: "https://github.com/microsoft/vscode/issues/307166"
engine: copilot
owner: meganrogge, anthonykim1
labels: confirmation-pending
proposedLabel: none
triageAction: keep-open
confidence: 0.88
severity: correctness
breadth: common
priority: none
visionFit: aligned
autoFixable: true
reviewedAt: "2026-07-24T09:37:29.431Z"
itemUpdatedAt: "2026-04-08T20:29:42Z"
itemCreatedAt: "2026-04-01T15:01:26Z"
upvotes: 1
reactionsTotal: 1
commentCount: 1
relatedOpenIssues: 1
milestone: none
contentHash: fa5ce5c37ab6d17a18a332f9a0502bcc121a6f97ce23ba7ef85d97c88da066fd
policyHash: 0c32deb9982e8caf
---

# Issue #307166 — Copilot cannot read the output of `node -v`

[https://github.com/microsoft/vscode/issues/307166](https://github.com/microsoft/vscode/issues/307166)

## Summary

Confirmed from source: the `allowSuffixMatch` path in `findCommandEcho` accepts a 1-character suffix (`v` from `node -v`) that matches the start of the output `v24.14.1`, causing the entire output line to be stripped as command echo. The bug is unpatched on current main.

## Change summary

Copilot's run_in_terminal tool fails to capture the output of `node -v` because the output-stripping logic incorrectly treats the `v` at the start of `v24.14.1` as a command echo suffix.

## Best solution

Add a minimum suffix length guard (e.g. ≥3 characters) in `findCommandEcho`'s `allowSuffixMatch` path in `src/vs/workbench/contrib/terminalContrib/chatAgentTools/browser/executeStrategy/strategyHelpers.ts`. A 1-character suffix is too ambiguous to be a reliable command-echo signal.

## Vision fit

- **Fit:** aligned
- **Reason:** Fixing terminal tool output capture directly supports the agent-experience reliability that current priorities center on.
- **Evidence:** Aligns with current agent-experience and terminal tool priorities.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** chat-terminal
- **Confidence:** 0.88
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small, localized guard in the suffix-match loop of findCommandEcho; reproducible from source with a clear test case.
- **Likely files:** `src/vs/workbench/contrib/terminalContrib/chatAgentTools/browser/executeStrategy/strategyHelpers.ts`, `src/vs/workbench/contrib/terminalContrib/chatAgentTools/test/browser/strategyHelpers.test.ts`
- **Validation:** Add a test: `stripCommandEchoAndPrompt('v24.14.1', 'node -v')` must return `'v24.14.1'` (not empty string). Currently it returns `''`.
- **Fix prompt:**

  > In `src/vs/workbench/contrib/terminalContrib/chatAgentTools/browser/executeStrategy/strategyHelpers.ts`, function `findCommandEcho` (line ~218–237), the `allowSuffixMatch` branch loops suffix lengths from `trimmedCommand.length - 1` down to `1`. A 1-character suffix is too short to be a reliable signal of a wrapped command echo — for `node -v`, suffix `v` matches the start of output `v24.14.1`, causing the entire output to be stripped. Fix: raise the minimum suffix length to at least 3 (change `len >= 1` to `len >= 3` in the for-loop on line 224). This preserves the intended behavior for genuine long-command wraps while preventing false positives from single-char coincidences. Do NOT change the full-match path (line 209) or the prompt-stripping logic below. Add a unit test in `src/vs/workbench/contrib/terminalContrib/chatAgentTools/test/browser/strategyHelpers.test.ts` asserting `stripCommandEchoAndPrompt('v24.14.1', 'node -v')` returns `'v24.14.1'`, and also that the existing suffix-match tests still pass (they use much longer suffixes). Related issue: https://github.com/microsoft/vscode/issues/307166.

## Evidence

- **root cause in source:** In `src/vs/workbench/contrib/terminalContrib/chatAgentTools/browser/executeStrategy/strategyHelpers.ts` line 224–237, `findCommandEcho` with `allowSuffixMatch=true` loops down to `len=1`. For command `node -v`, the suffix `v` matches the start of output `v24.14.1`. The char before (`-`) is not a space, so the match is accepted, causing the output line to be treated as command echo and stripped entirely.
- **execute strategy usage:** `noneExecuteStrategy.ts` line 167 calls `stripCommandEchoAndPrompt(output, commandLine, ...)` with the stripped output. After `findCommandEcho` treats line 0 as echo, `linesAfter` is empty, yielding trimmed output of length 0 → 'Command produced no output'.
- **no fix on main:** git log shows no changes to strategyHelpers.ts since before the issue was filed (2026-04-01). The bug persists on current main.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
