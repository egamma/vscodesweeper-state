---
number: 328555
url: "https://github.com/microsoft/vscode/issues/328555"
engine: copilot
owner: amunger
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.55
severity: visual
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-07T08:22:02.245Z"
itemUpdatedAt: "2026-08-03T11:10:52Z"
itemCreatedAt: "2026-08-01T11:53:00Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 34ba482badbb0d5344e878160eb95abf585ca5bc83e79ad4beb9341ba2d58cf2
policyHash: 70e952cc2fb28823
---

# Issue #328555 — Webview panels (Release Notes, extension panels) render with unreadable dark-on-dark contrast on cold start until theme is manually re-toggled

[https://github.com/microsoft/vscode/issues/328555](https://github.com/microsoft/vscode/issues/328555)

## Summary

Detailed report of a plausible-sounding cold-start theming race, but tracing the current webview message pipeline shows a mechanism that should already prevent it, and there's no confirming comment or attached repro (video/screenshot) — asking the reporter for more concrete evidence before deciding.

## Change summary

Webview panels (Release Notes, restored extension panels) allegedly render with unreadable dark-on-dark contrast on VS Code cold start until the color theme is manually re-toggled.

## Best solution

Ask the reporter for a screen recording/screenshot of the actual dark-on-dark render and, ideally, `--verbose` logs around webview creation on a fresh cold start, since source inspection of src/vs/workbench/contrib/webview/browser/webviewElement.ts doesn't show an obvious path for the described race (styles messages are queued via the `_send`/pendingMessages mechanism until the webview signals ready, so they shouldn't be lost or sent before injection).

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** visual
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** webview
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed write-up. Looking at the webview theming code (`src/vs/workbench/contrib/webview/browser/webviewElement.ts` and `themeing.ts`), the `styles` message that injects the `--vscode-*` CSS variables is pushed as soon as the webview element is constructed, and any message sent before the webview signals it's ready gets queued and flushed once it is — so this specific race isn't obvious from the source.
  > 
  > Could you attach a screenshot or short screen recording of the actual dark-on-dark Release Notes panel on a cold start, along with the exact repro steps you used (fresh profile vs. existing one, any custom settings around webview retention)? That'll help confirm whether this is a real timing issue versus something else (e.g. a slow-loading theme extension, OS-level dark/light mismatch, or GPU/rendering quirk).
  > 
  > This issue will auto-close in 7 days if there's no reply, but feel free to comment any time after with more details and we'll take another look.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction is unclear and no concrete failing code path was confirmed — the existing message-queuing mechanism argues against the described race, so there's nothing safe to auto-fix without a confirmed repro.

## Evidence

- **theme push on construction:** webviewElement.ts:309 calls `Event.runAndSubscribe(webviewThemeDataProvider.onThemeDataChanged, () => this.style())`, so `style()` (which sends the `--vscode-*` CSS variables via the `styles` message) runs immediately when the webview element is constructed, not lazily after some later broadcast.
- **message queuing on cold start:** webviewElement.ts:405-413 — `_send()` queues any message (including `styles` and `content`) into `pendingMessages` while `this._state.type === WebviewState.Type.Initializing`, and only calls `doPostMessage` once initialization completes, which should prevent styles from being dropped or racing content on a slow cold start.
- **no confirming discussion:** 0 reactions, no comments, and the reporter's own steps rely on a workaround (manually re-toggling the theme) rather than a video/screenshot of the actual glitch — the report reads as speculative/inferred rather than directly observed.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
