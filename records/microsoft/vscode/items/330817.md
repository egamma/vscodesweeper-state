---
number: 330817
url: "https://github.com/microsoft/vscode/issues/330817"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.35
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-18T09:07:58.035Z"
itemUpdatedAt: "2026-08-17T22:29:55Z"
itemCreatedAt: "2026-08-14T09:42:19Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: a3a8ce46f356ea98ec2070a195f296afc275bb8a64cc400038ee10a7d47ba735
policyHash: 4721e56ad78fdd17
---

# Issue #330817 — Explorer View does not refresh U "Untracked" files after adding them using git add -f

[https://github.com/microsoft/vscode/issues/330817](https://github.com/microsoft/vscode/issues/330817)

## Summary

Plausible stale-decoration report, but it's a single, uncorroborated repro (no other reporters, no comments) and I couldn't confirm the defect from source alone — the git decoration refresh path (extensions/git/src/decorationProvider.ts, GitDecorationProvider.onDidRunGitStatus) looks like it should clear the 'U' badge once the file drops out of the untracked group after commit, so I can't tell if this is a real gap or an environment/timing quirk without more detail.

## Change summary

After un-ignoring a file, force-adding it, and committing/syncing, the Explorer still shows the old 'U' (untracked) decoration until VS Code is restarted.

## Best solution

If a maintainer or the reporter can confirm this reproduces reliably (e.g. does it happen when `git add -f` is run from VS Code's own integrated terminal vs. an external shell, and does manually toggling the file or reloading the window without a full restart also fix it?), the fix likely belongs in extensions/git/src/repository.ts's status/decoration refresh flow (updateModelState / GitDecorationProvider) — otherwise this may just need the reporter to confirm on a clean profile.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git
- **Confidence:** 0.35
- **Proposed comment:**

  > Thanks for the detailed report. I traced the decoration refresh code in extensions/git/src/decorationProvider.ts and it looks like it should clear the 'U' badge once a file leaves the untracked group after a commit, so I wasn't able to confirm the gap purely from source.
  > 
  > Could you help narrow this down: does it reproduce if `git add -f` is run from VS Code's own integrated terminal (not an external shell), and does a window reload (`Developer: Reload Window`) fix it without a full restart? Also, does this happen with all other extensions disabled (`code --disable-extensions`)? This issue will auto-close in 7 days if there's no reply, but feel free to comment any time to reopen the discussion.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction status is unclear from source alone — no confirmed failing path to point a fix at yet.

## Evidence

- **decoration refresh code:** extensions/git/src/decorationProvider.ts GitDecorationProvider.onDidRunGitStatus rebuilds decorations from indexGroup/untrackedGroup/workingTreeGroup/mergeGroup on every status run and fires onDidChangeDecorations for the union of old+new URIs, which should clear a stale 'U' badge once the file leaves the untracked group — no obvious gap found by reading the code.
- **no related recent fix:** git log on extensions/git/src/decorationProvider.ts and repository.ts shows no recent commit addressing stale untracked decorations after add -f/commit.
- **single report:** No comments, no upvotes, no other reporters corroborating; steps are clear but untested by anyone else.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
