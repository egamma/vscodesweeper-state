---
number: 289048
url: "https://github.com/microsoft/vscode/issues/289048"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: none
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-21T20:46:13.410Z"
itemUpdatedAt: "2026-01-20T10:14:41Z"
itemCreatedAt: "2026-01-20T08:42:59Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 724c4a824f46c3302e23063e37e0a3a89119312e3d5e2ff01c0501592254f898
policyHash: ebd3763732abe97d
---

# Issue #289048 — commit & push button renders source control unusable when no remotes

[https://github.com/microsoft/vscode/issues/289048](https://github.com/microsoft/vscode/issues/289048)

## Summary

The report describes the SCM view being stuck in a busy state after clicking 'Commit & Push' with no remotes configured. Code tracing shows the PostCommitCommand operation should complete normally after the user dismisses the 'no remotes' warning — the `_push` method returns without throwing, and the operation's `finally` block always calls `_operations.end()`. The report lacks step-by-step reproduction details (e.g., whether dismissing the warning dialog recovers the state, or whether the view stays stuck indefinitely). Requesting more information.

## Change summary

Clicking 'Commit & Push' in a git repo with no remotes reportedly leaves the Source Control view permanently in a busy/spinning state.

## Best solution

Request confirmation on the latest Insiders build and detailed reproduction steps — specifically whether the busy state persists after dismissing the 'no remotes configured' warning dialog. If confirmed, the fix likely belongs in `extensions/git/src/commands.ts` (`_push` method) or `extensions/git/src/postCommitCommands.ts`, ensuring the PostCommitCommand operation completes promptly rather than blocking on a user-facing dialog.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** git
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the report. I traced the commit & push flow in the code and the PostCommitCommand operation should complete normally after the 'no remotes' warning is dismissed — the busy state should clear at that point.
  > 
  > Could you confirm whether the busy state persists **after** you dismiss the warning dialog ("Your repository has no remotes configured to push to.")? And could you try reproducing this on the latest VS Code Insiders build?
  > 
  > Detailed steps to reproduce would help us investigate further. Note that issues marked `info-needed` are auto-closed after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Cannot confirm the defect from the code — the operation lifecycle appears correct; need reproduction confirmation before fixing.

## Evidence

- **code trace: _push no-remotes path:** `extensions/git/src/commands.ts` line 4022–4037: `_push` checks `remotes.length === 0`, shows `window.showWarningMessage(...)` with an 'Add Remote' button, and returns normally (no throw). The PostCommitCommand operation wrapping this call should end via the `finally` block in `extensions/git/src/repository.ts` line 2738.
- **code trace: operation lifecycle:** `extensions/git/src/repository.ts` line 2702–2740: the `run` method always calls `this._operations.end(operation)` in a `finally` block, so `OperationKind.PostCommitCommand` should clear regardless of how `_push` resolves.
- **code trace: busy indicator:** `extensions/git/src/actionButton.ts` line 263–266: `isCommitInProgress` tracks `OperationKind.Commit`, `OperationKind.PostCommitCommand`, and `OperationKind.RebaseContinue`. While the warning dialog is open, PostCommitCommand is still running, so the button shows a spinner — but it should clear once the dialog is dismissed.
- **no fix found:** No related changes to `actionButton.ts`, `postCommitCommands.ts`, or the `_push` path in `commands.ts` since the issue was filed (2026-01-20).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
