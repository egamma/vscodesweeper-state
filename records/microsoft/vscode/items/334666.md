---
number: 334666
url: "https://github.com/microsoft/vscode/issues/334666"
engine: copilot
owner: hediet
labels: ""
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.68
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
agentReadiness: implement
reviewedAt: "2026-09-22T13:39:07.839Z"
itemUpdatedAt: "2026-09-22T00:59:51Z"
itemCreatedAt: "2026-09-04T23:32:21Z"
upvotes: 0
reactionsTotal: 0
commentCount: 3
relatedOpenIssues: 0
milestone: none
contentHash: bccb6400606d45012a61b5169abd335ab1171324df21a4fa2bbbd0c9a279adc7
policyHash: d13126680f5ee38a
---

# Issue #334666 — Refactor test configuration file to use robust path joining and modern standards

[https://github.com/microsoft/vscode/issues/334666](https://github.com/microsoft/vscode/issues/334666)

## Summary

Real, minor code-hygiene request confirmed in the checkout: `.vscode-test.js` still builds `--crash-reporter-directory`, `--logsPath`, and the Electron path via `${__dirname}/...` string interpolation instead of `path.join`. No fix or PR exists yet (the collaborator's comment about a 'draft PR' was misdirected), so this stays open as a small debt item.

## Change summary

Replace string-interpolated `${__dirname}/...` path concatenations in `.vscode-test.js` with `path.join(__dirname, ...)` for cross-platform robustness.

## Best solution

A small, self-contained PR converting the three interpolated paths at lines 101 and 140 of `.vscode-test.js` to `path.join`, verified to produce identical output on the current platform before/after the change.

## Proposed action

- **Triage action:** keep-open
- **Item type:** debt
- **Reproduction:** source_reproducible
- **Evidence:** 4/6 Traced
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.68
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Agent readiness:** implement
- **Rationale:** Confirmed from source, single small file, bounded change (swap string interpolation for path.join at the three call sites), with a manual before/after equivalence check nameable as validation.
- **Likely files:** `.vscode-test.js`
- **Validation:** Run `node -e "import('./.vscode-test.js').then(c => console.log(JSON.stringify(c.default)))"` before and after the change and diff the output — the launchArgs and useInstallation.fromPath values must resolve to the same absolute paths on the current platform.
- **Behavior:**

  > `.vscode-test.js` must produce identical launch arguments and installation paths after the refactor as before it, on both POSIX and Windows path semantics — only the construction method changes (from string interpolation to `path.join`), not the resulting values. Acceptance: dumping the resolved config before and after shows byte-identical `crash-reporter-directory`, `logsPath`, and `useInstallation.fromPath` values.
- **Trace:**

  > The three interpolation sites are `.vscode-test.js:101` (`--crash-reporter-directory=${__dirname}/.build/crashes`, `--logsPath=${__dirname}/.build/logs/integration-tests`) and `.vscode-test.js:140` (`${__dirname}/scripts/code.${process.platform === 'win32' ? 'bat' : 'sh'}`). The file already imports `path` and uses `path.join` elsewhere (lines 32, 37, 47, 57, 62, 67, 129-132), so the fix is consistent with existing style. Boundary: do not touch the `extensions` array structure, the mocha/reporter logic, or any other config semantics — only the path-construction expressions change.

## Evidence

- **confirmed pattern:** .vscode-test.js line 101 uses `--crash-reporter-directory=${__dirname}/.build/crashes` and `--logsPath=${__dirname}/.build/logs/integration-tests`; line 140 uses `${__dirname}/scripts/code.${...}` — all string interpolation rather than path.join, while the rest of the file (e.g. lines 32, 37, 47, 57, 62, 67, 129-132) already uses path.join/os.tmpdir consistently.
- **no existing PR:** The collaborator's comment referencing a 'draft PR to standardize these' was clarified as misdirected ('that was a message for Henning'); the reporter confirmed no PR exists yet but offered to open one.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
