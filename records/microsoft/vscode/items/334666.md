---
number: 334666
url: "https://github.com/microsoft/vscode/issues/334666"
engine: copilot
owner: TylerLeonhardt
labels: triage-needed
proposedLabel: "*out-of-scope"
closeReason: out-of-scope
triageAction: propose-close
confidence: 0.62
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-05T11:46:51.366Z"
itemUpdatedAt: "2026-09-04T23:47:19Z"
itemCreatedAt: "2026-09-04T23:32:21Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: f23b1a11458bc989f1ea44c1b3657cc62b181895192b3b7e2031c629b6770acd
policyHash: 5fa429198b8c8ed6
---

# Issue #334666 — Refactor test configuration file to use robust path joining and modern standards

[https://github.com/microsoft/vscode/issues/334666](https://github.com/microsoft/vscode/issues/334666)

## Summary

The cited code (`.vscode-test.js` line 101) already works cross-platform today — `--crash-reporter-directory=${__dirname}/.build/crashes` and `--logsPath=${__dirname}/.build/logs/integration-tests` are just CLI flag string values built from an already-normalized `__dirname`, and forward slashes are accepted fine by Electron/Node on Windows here — no platform bug is demonstrated, so this reads as a speculative style nitpick rather than an actionable fix.

## Change summary

Requests refactoring `.vscode-test.js` to use `path.join()` instead of template-literal string interpolation for a couple of CLI launch-arg paths, for 'modern standards'.

## Best solution

No functional problem exists to fix; if a maintainer still wants stylistic consistency, it's a trivial one-line tidy-up in `.vscode-test.js`, not something that needs a tracked issue.

## Proposed action

- **Triage action:** propose-close
- **Item type:** polish
- **Reproduction:** not_applicable
- **Evidence:** not applicable
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *out-of-scope
- **Area labels:** none
- **Confidence:** 0.62
- **Proposed comment:**

  > Thanks for the suggestion. Looking at `.vscode-test.js:101`, those two paths are CLI launch-argument strings built from an already-normalized `__dirname`, and forward slashes there work fine cross-platform (including on the Windows CI runners that already exercise this file) — there's no reported or reproducible path issue driving this change. Since it's a style-only refactor without a concrete problem to fix, I don't think this needs to stay open as a tracked issue; a maintainer is welcome to tidy it up in passing if desired.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** No confirmed defect — this is a speculative style preference, not a bug with a validation to check against.

## Evidence

- **current code:** .vscode-test.js:101 builds `--crash-reporter-directory=${__dirname}/.build/crashes` and `--logsPath=${__dirname}/.build/logs/integration-tests` as CLI flag values via template literals; `__dirname` is derived from `path.dirname(fileURLToPath(import.meta.url))`, already OS-normalized.
- **no reported breakage:** Issue body describes a hypothetical 'can cause platform-specific path separator issues' without any observed failure, error, or reproduction; CI (e.g. build/azure-pipelines/win32/*) runs these same test configs on Windows without reported path issues.
- **elsewhere in same file:** Lines 32/37/47/57/62/67/129-132 already use path.join for filesystem paths that matter (workspaceFolder, mochaFile) — the two flagged spots are Electron CLI argument strings, not filesystem joins the OS resolves directly.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
