---
number: 333456
url: "https://github.com/microsoft/vscode/issues/333456"
engine: copilot
owner: lszomoru
labels: info-needed
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-31T16:10:07.028Z"
itemUpdatedAt: "2026-08-31T10:29:24Z"
itemCreatedAt: "2026-08-31T05:09:52Z"
upvotes: 0
reactionsTotal: 0
commentCount: 4
relatedOpenIssues: 0
milestone: none
contentHash: 4a689ff7e83273076aefad63a045ad4a23ad59ef2b6f7f8be4450a1e3764b536
policyHash: 3d6943c3a15c721c
---

# Issue #333456 — More lines in commit message not seen properly in Source Control

[https://github.com/microsoft/vscode/issues/333456](https://github.com/microsoft/vscode/issues/333456)

## Summary

Report is too thin to act on: the reporter's screenshots show a cropped commit message in the Source Control Graph, but it's unclear whether they mean the single-line list row (which is expected to show only the subject) or the hover tooltip (which already renders the full multi-line message). A maintainer already asked for a gif and the reporter has not yet supplied one, so info-needed is the right lane.

## Change summary

Long, multi-line commit messages are reportedly cropped in the Source Control Graph view and the reporter wants a way to see the full message.

## Best solution

Confirm via a screen recording whether the reporter means the graph list row (expected to show only the subject/first line) or the hover tooltip; if it's the row, point them to hovering an entry, which already renders the full message via toHistoryItemHoverContent in src/vs/workbench/contrib/scm/browser/scmHistory.ts — if a real gap remains (e.g. the hover itself truncates), it belongs in src/vs/workbench/contrib/scm/browser/scmHistoryViewPane.ts / scmHistory.ts.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git, scm
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report and the screenshots. It looks like you're seeing only the first line of the commit message in the Graph list row — that's expected, since each row shows just the subject line by design. The full multi-line message should be available by hovering over the commit entry (a tooltip pops up with the complete message). Could you confirm whether you tried hovering, and if it still doesn't show the full text there, a short screen recording (as requested above) showing the hover would help us pin down what's actually going wrong. Note: this issue will auto-close in 7 days if there's no reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Report is unclear whether a real defect exists (row vs. hover) — needs reporter confirmation before any fix can be scoped.

## Evidence

- **existing hover behavior:** src/vs/workbench/contrib/scm/browser/scmHistoryViewPane.ts:481-483 wires up `toHistoryItemHoverContent` (src/vs/workbench/contrib/scm/browser/scmHistory.ts:560-565) which passes the full `historyItem.message` (not just the subject) as hover content for each graph row — no max-height/truncation CSS was found for `.history-item-hover` in src/vs/workbench/contrib/scm/browser/media/scm.css.
- **list row uses subject only:** scmHistoryViewPane.ts:496 calls `templateData.label.setLabel(historyItem.subject, ...)` for the row label itself, so the single-line row intentionally shows only the subject — full text is meant to be surfaced via hover, not the row.
- **maintainer request:** lszomoru (MEMBER) and the vs-code-engineering bot already asked for a `.gif`/screen recording on 2026-08-31; reporter has only posted static screenshots so far, which don't clearly show whether hovering was attempted.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
