---
number: 327293
url: "https://github.com/microsoft/vscode/issues/327293"
engine: copilot
owner: DonJayamanne
labels: new release
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-28T07:00:36.673Z"
itemUpdatedAt: "2026-07-24T13:08:00Z"
itemCreatedAt: "2026-07-24T12:58:30Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 5374d7d0a640cc479f00b837e3f4d38b7559f43c2147147ffd04b3352d96b0ef
policyHash: 26158a1d7dfd26ac
---

# Issue #327293 — Notebook editor shows stale cell source during Copilot notebook edits

[https://github.com/microsoft/vscode/issues/327293](https://github.com/microsoft/vscode/issues/327293)

## Summary

The report describes stale notebook content after Copilot edits, but the reproduction steps are too vague to verify — step 4 ('Use Copilot to edit a notebook cell (or apply a tool-driven .ipynb rewrite)') doesn't specify which Copilot flow triggers the staleness, and the mechanism matters: the built-in edit_notebook tool uses the VS Code NotebookEdit API (which updates the in-memory model directly), whereas an external file-system write would rely on the file watcher and the stored-file working-copy reload path, which intentionally skips dirty working copies. Requesting clarification before routing.

## Change summary

Notebook editor may display stale cell source after a Copilot-driven notebook modification, requiring a manual close/reopen or window reload to resync.

## Best solution

Clarify the exact Copilot flow (agent mode tool call, Copilot CLI, external script, etc.) and whether the notebook is dirty at the time of the edit. If the issue is that an external process writes the .ipynb on disk while the notebook is clean and the file watcher doesn't pick it up, that's a real file-watching or notebook-reload gap in `StoredFileWorkingCopyManager.queueWorkingCopyReloads`. If the notebook is dirty, the current behavior (skip reload) is by design and the user needs the conflict dialog. The attached logs may hold the answer, but the issue body doesn't surface it.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** chat-notebook, notebook-ipynb
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the detailed write-up and attached logs. To narrow this down, could you clarify a few things?
  > 
  > 1. **Which Copilot flow** triggers the stale state? Are you using agent mode (the chat panel), inline chat (`Ctrl+I`), the Copilot CLI, or an external script/tool that writes to the `.ipynb` file on disk?
  > 2. **Is the notebook dirty** (has unsaved changes) at the time Copilot modifies it? If the notebook has pending edits, VS Code intentionally skips auto-reload from disk changes.
  > 3. **Is VS Code in the foreground** when the modification happens, or does it happen while VS Code is minimized/unfocused?
  > 4. **Can you reproduce this consistently**, or is it intermittent?
  > 
  > Understanding the exact path is important because the built-in notebook edit tool updates the in-memory model directly (no disk round-trip needed), whereas an external file write relies on the file watcher — these are very different code paths.
  > 
  > Please note that this issue will be closed automatically in 7 days if we don't hear back.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction is unclear — can't identify the defective code path without knowing the exact Copilot flow.

## Evidence

- **vague repro step:** Step 4 says 'Use Copilot to edit a notebook cell (or apply a tool-driven .ipynb rewrite)' without specifying the exact Copilot feature or tool — agent mode, inline chat, Copilot CLI, or an external script all have different code paths.
- **edit_notebook tool uses VS Code API:** extensions/copilot/src/extension/tools/node/editNotebookTool.tsx applies changes via the NotebookEdit API, which updates the in-memory model directly — this path should not produce staleness.
- **working copy reload path:** src/vs/workbench/services/workingCopy/common/storedFileWorkingCopyManager.ts:277-279 — dirty working copies are intentionally never reloaded from file-change events; if the notebook is dirty when the external write happens, no reload occurs (by design).
- **clean notebook should auto-reload:** For a non-dirty notebook, the file watcher → onDidFilesChange → queueWorkingCopyReloads path should trigger a reload when the .ipynb changes on disk (storedFileWorkingCopyManager.ts:264-291).
- **attached logs not examined:** The reporter attached cli.log, JSONL traces, and a notebook file as GitHub attachments, but the issue body doesn't summarize what they show.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
