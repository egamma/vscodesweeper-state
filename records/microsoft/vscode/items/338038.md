---
number: 338038
url: "https://github.com/microsoft/vscode/issues/338038"
engine: copilot
owner: sandy081
labels: ""
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.55
severity: visual
breadth: niche
priority: none
visionFit: aligned
autoFixable: false
agentReadiness: plan
reviewedAt: "2026-09-26T12:56:31.414Z"
itemUpdatedAt: "2026-09-25T19:34:59Z"
itemCreatedAt: "2026-09-25T19:34:59Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 3126da8ddf92f85fdbf00cdaec7fa5cbf21bf8a704df40760840d33b5c64c43f
policyHash: 278656bf1e7c0255
---

# Issue #338038 — Session list keeps scrolling down automatically

[https://github.com/microsoft/vscode/issues/338038](https://github.com/microsoft/vscode/issues/338038)

## Summary

Fresh team-authored bug report (0 days old) with an attached video showing the Agents Window session list auto-scrolling on refresh/window-focus changes; no fix exists yet and team-authored reports are protected from judgment closes, so this stays open pending investigation.

## Change summary

The sessions list in the Agents Window scrolls to a different position on its own, apparently whenever the list refreshes (e.g. triggered by switching window focus).

## Best solution

Trace what `SessionsListWidget.update()` (src/vs/sessions/contrib/sessions/browser/views/sessionsList.ts) does to the tree on a refresh triggered by focus/session-state changes — likely `tree.setChildren()` recomputing sections — and ensure the scroll position/anchor is preserved across that refresh instead of being reset.

## Vision fit

- **Fit:** aligned
- **Reason:** The Agents Window sessions list is core workbench/AI-assisted-development UX that the roadmap explicitly prioritizes; scroll stability during list refresh is exactly the kind of workbench UX polish that theme covers.
- **Evidence:** Durable theme: 'Workbench & core UX — layout flexibility, profiles, settings fidelity, first-run experience, notifications hygiene' and 'AI-assisted development … grown into the project's center of gravity.'

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** reproduced
- **Evidence:** 5/6 Source-confirmed
- **Severity:** visual
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** plan
- **Rationale:** Confirmed complaint with video evidence, but the exact trigger for the scroll reset (which refresh path, and whether the fix belongs in the tree widget or the update() rebuild) isn't pinned down from static inspection — needs a short investigation before a bounded fix can be scoped.

## Plan brief

- **Likely files:** `src/vs/sessions/contrib/sessions/browser/views/sessionsList.ts`
- **Validation:** A manual repro: open the Agents Window session list, switch focus away and back (or trigger whatever causes `update()`/`refresh()` to run), and confirm the scroll position is unchanged; ideally add/adjust a test around `SessionsListWidget.update()` asserting `tree` scroll offset is stable across a no-op data refresh.
- **Behavior:**

  > Switching focus away from and back to the VS Code window (or any other trigger that causes the sessions list to refresh) must not change the scroll position of the Agents Window session list. Acceptance: repeatedly toggling window focus with the list scrolled mid-way leaves the visible scroll position unchanged.
- **Trace:**

  > `SessionsListWidget.update()` in src/vs/sessions/contrib/sessions/browser/views/sessionsList.ts (~line 3993) rebuilds the filtered/sorted/grouped session sections and pushes them into the tree via `this.tree.setChildren(null, ...)` (~lines 4355/4365); no scroll-position capture/restore was found around these calls (`grep` for scrollTop/setInput/preserveScroll in the file returns nothing). `update()` is wired to many event sources (provider capability changes, viewport transitions, collapse-state changes, etc.) — which one fires on a window-focus switch is not yet confirmed. Do not change unrelated filtering/grouping/sorting logic; the fix should be scoped to preserving/anchoring scroll position across a children rebuild.
- **Open decisions:**
  - Which specific event handler fires `update()`/`refresh()` on a window-focus switch, and should the fix live there or generically in the tree-children-rebuild path?

## Evidence

- **no scroll-preservation logic found:** `grep -n "scrollTop|setInput|preserveScroll" src/vs/sessions/contrib/sessions/browser/views/sessionsList.ts` returns no matches — the widget's `update()` (line 3993) rebuilds sections and calls `this.tree.setChildren(null, ...)` (lines 4355/4365) with no explicit scroll-position capture/restore around it.
- **frequent update triggers:** `update()` is invoked from many event handlers in sessionsList.ts (viewport transitions, provider capability changes, etc.), any of which firing on a window-focus change could explain the reported 'switching windows triggers scrolling'.
- **team-authored, same-day report:** Author roblourens is a VS Code core maintainer (MEMBER); issue was filed today with no comments or linked PR yet, so no fix commit exists to cite for an implemented-on-main close.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
