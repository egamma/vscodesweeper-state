---
number: 331810
url: "https://github.com/microsoft/vscode/issues/331810"
engine: copilot
owner: joshspicer
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: common
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-25T09:15:03.260Z"
itemUpdatedAt: "2026-08-24T18:41:55Z"
itemCreatedAt: "2026-08-20T15:19:25Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 421766af1826b40bd93fe0e612e4c7e285cfd664fd9b2d168ac3a89d19df6fb3
policyHash: 3d6943c3a15c721c
---

# Issue #331810 — Remote Tunnel Access is broken

[https://github.com/microsoft/vscode/issues/331810](https://github.com/microsoft/vscode/issues/331810)

## Summary

Real failure reported (code-tunnel.exe rejects `--user-data-dir` on VS Code 1.134.0), but current main's `cli/src/commands/args.rs` `TunnelServeArgs` struct already declares a hidden `--user-data-dir` field that `tunnelProcessCoordinator.ts` relies on — so this may already be fixed in a newer CLI build; asking the reporter to confirm on the latest Insiders build rather than closing on source inspection alone.

## Change summary

Starting Remote Tunnel Access on Windows fails because the bundled `code-tunnel.exe` doesn't recognize the `--user-data-dir` flag the editor passes it, only `--server-data-dir`.

## Best solution

If reproducible on the latest Insiders build, the fix is aligning `src/vs/platform/remoteTunnel/node/tunnelProcessCoordinator.ts` (which pushes `--user-data-dir` for the plain `tunnel` and `tunnel service install` invocations) with the Rust CLI's argument surface in `cli/src/commands/args.rs`/`tunnels.rs`; on current main, `TunnelServeArgs` already has a matching hidden `user_data_dir` field, but `TunnelServiceInstallArgs` (used by the service-install path) still does not, which would keep the same class of failure alive for that code path.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** common
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report and the log. Looking at current `main`, the `tunnel` command's Rust CLI args (`cli/src/commands/args.rs`, `TunnelServeArgs`) already declare a hidden `--user-data-dir` option that matches what the editor passes — so this may already be resolved in a newer `code-tunnel` build than the one bundled with 1.134.0. Could you try Remote Tunnel Access again on the latest VS Code Insiders build and let us know if it still fails? If there's no update within 7 days this issue will be automatically closed.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Needs a reporter confirmation on a newer build before any fix work is scoped; source inspection suggests the primary path may already be resolved, and the shallow clone prevents citing a concrete fix commit.

## Evidence

- **reported failure:** code-tunnel.exe on VS Code 1.134.0 rejects `--user-data-dir` for the `tunnel` command with 'unexpected argument... tip: a similar argument exists: --server-data-dir'.
- **current main - TunnelServeArgs:** cli/src/commands/args.rs lines 906-917 declares `#[clap(long, hide = true)] pub user_data_dir: Option<String>` on `TunnelServeArgs`, the struct backing the plain `tunnel` subcommand — this matches exactly what src/vs/platform/remoteTunnel/node/tunnelProcessCoordinator.ts:319 passes, suggesting the flag is now accepted.
- **possible remaining gap:** cli/src/commands/args.rs lines 1042-1050 `TunnelServiceInstallArgs` (used by the `tunnel service install` path invoked from tunnelProcessCoordinator.ts:371) has no `user_data_dir` field, so that code path could still hit the same error class even if the primary `tunnel` command is fixed.
- **shallow clone limitation:** This checkout is a depth-1 shallow clone (single commit 5aaf0974), so the actual commit that introduced `user_data_dir` on `TunnelServeArgs` cannot be identified or cited; git blame only points to the clone boundary, not a real fix commit.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
