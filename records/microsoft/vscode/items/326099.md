---
number: 326099
url: "https://github.com/microsoft/vscode/issues/326099"
engine: copilot
owner: deepak1556
labels: triage-needed, stale
proposedLabel: "*caused-by-extension"
triageAction: propose-close
confidence: 0.82
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-07T13:41:37.821Z"
itemUpdatedAt: "2026-07-23T15:46:29Z"
itemCreatedAt: "2026-07-16T03:29:34Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: 0809cf8b0d04f056ccf9b0cc46f70c13bee5940f4874547a8f38a2d47b922e2c
policyHash: 70e952cc2fb28823
---

# Issue #326099 — vscode

[https://github.com/microsoft/vscode/issues/326099](https://github.com/microsoft/vscode/issues/326099)

## Summary

The failing write happens entirely inside the third-party CodeBuddy extension's own private data folder, not any VS Code-managed path, and VS Code's own atomic rename (`Promises.rename` in src/vs/base/node/pfs.ts) already has an EXDEV fallback (copy+delete) that this raw error bypassed — so the write is not going through VS Code's file service at all.

## Change summary

User hit an unhandled EXDEV (cross-device rename) error while an extension wrote a history/index.json file.

## Best solution

Report this to Tencent Cloud's CodeBuddy extension (tencent-cloud.coding-copilot) — the failing path `...\CodeBuddyExtension\Data\...\VSCode\...\history\...\index.json.tmp...` is the extension's own private storage, and its temp-file+rename write pattern needs the same EXDEV cross-device fallback VS Code itself implements in pfs.ts's `rename()`.

## Proposed action

- **Triage action:** propose-close
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *caused-by-extension
- **Area labels:** none
- **Confidence:** 0.82
- **Proposed comment:**

  > Thanks for the report. Looking at the path in the error — `...\CodeBuddyExtension\Data\...\VSCode\...\history\...\index.json.tmp...` — this write is happening inside the CodeBuddy extension's own private data folder, not anywhere VS Code manages. VS Code's own atomic-rename helper (`rename()` in `src/vs/base/node/pfs.ts`) already falls back to copy+delete on an `EXDEV` cross-device error, so a raw unhandled EXDEV like this one indicates the extension is doing its own temp-file-then-rename write without that fallback. Could you file this with the CodeBuddy extension (tencent-cloud.coding-copilot) instead? They'll need to add the same cross-device fallback to their write path.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** The defect is in third-party extension code (CodeBuddy's own file writer), not in this repository, so there is nothing here to auto-fix.

## Evidence

- **path ownership:** Failing path is `C:\Users\ei\AppData\Local\CodeBuddyExtension\Data\...\VSCode\...\history\...\index.json` — a private data directory named after the extension (CodeBuddyExtension), not any VS Code user-data/global-storage path.
- **installed extension:** The issue's Extensions list includes `腾讯云代码助手 CodeBuddy | tencent-cloud.coding-copilot | Tencent-Cloud | 4.10.32994164`, matching the folder name in the error path.
- **VS Code already handles EXDEV:** src/vs/base/node/pfs.ts `rename()` (used by DiskFileSystemProvider.rename via `Promises.rename`) explicitly catches EXDEV and falls back to `copy()` + `rimraf()` — a write going through VS Code's own file service would not surface a raw unhandled 'EXDEV: cross-device link not permitted' error.
- **report format:** The 'User-Friendly Information' / 'Technical Details' sections and 'generated by issue reporter' footer are not VS Code's standard issue-reporter template, consistent with this being auto-filed by the extension's own error-reporting hook against the wrong repo.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
