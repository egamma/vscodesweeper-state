---
number: 332867
url: "https://github.com/microsoft/vscode/issues/332867"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.4
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-27T19:08:13.265Z"
itemUpdatedAt: "2026-08-27T04:02:51Z"
itemCreatedAt: "2026-08-27T03:52:16Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: 855fbf9f79d156792ce46c0829d46dad4eb3075fe5ac0f5684640fc1c7722d3d
policyHash: 3d6943c3a15c721c
---

# Issue #332867 — macOS: VS Code prompts for Command Line Developer Tools on every startup

[https://github.com/microsoft/vscode/issues/332867](https://github.com/microsoft/vscode/issues/332867)

## Summary

Plausible macOS-only regression, but it's a single hours-old report and the behavior is an OS-level installer prompt that can't be verified from source alone; existing guard logic in git.ts looks correct on its face, so more diagnostic detail is needed before this can be routed with confidence.

## Change summary

On macOS 26.6 with VS Code 1.132+, the OS 'Command Line Developer Tools' install dialog appears on every VS Code startup even without CLT installed and without disabling Git.

## Best solution

Needs a maintainer/community reproduction on a clean macOS machine without CLT installed, likely in the built-in Git extension's Darwin git-detection path (extensions/git/src/git.ts, findGitDarwin), to confirm whether the xcode-select guard is actually being bypassed somewhere in the 1.132+ startup flow.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git
- **Confidence:** 0.4
- **Proposed comment:**

  > Thanks for the detailed report. This is tricky to verify without a macOS machine that's missing the Command Line Developer Tools, and the git extension's Darwin detection code (extensions/git/src/git.ts) already has a guard that checks `xcode-select -p` before touching `/usr/bin/git` to avoid triggering the OS prompt — so it's not obvious from the source alone what changed in 1.132.
  > 
  > Could you help narrow this down: does running `xcode-select -p` in a terminal on your machine return an error, and does the prompt still appear if you fully quit VS Code, run `defaults delete com.microsoft.VSCode` (or check `git.path`/`git.enabled` aren't set to something custom), and relaunch? Also, does `code --disable-extensions --verbose` (with the built-in Git extension truly disabled via the Extensions view) still show the prompt? That will help isolate whether Git extension code or something else in core is triggering it.
  > 
  > This will auto-close in 7 days if there's no reply, but please add the requested info and it'll stay open for investigation.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Cannot confirm the defect or its exact trigger without macOS reproduction; the existing guard code looks correct on inspection, so there's no localized fix to identify yet.

## Evidence

- **existing guard:** extensions/git/src/git.ts findGitDarwin() already checks `xcode-select -p` and rejects with an error before ever invoking `git --version` when the git path resolves to /usr/bin/git and CLT is missing (lines ~119-136), which should prevent triggering the OS install prompt.
- **no history available:** This checkout has squashed history (only one commit touches extensions/git/src/git.ts), so no diff between 1.131 and 1.132 could be inspected to confirm or rule out a regression in this logic.
- **environment-dependent:** The reported behavior is a macOS OS-level dialog tied to xcode-select/CLT installation state, which cannot be observed or ruled out by static source reading in this sandbox (no macOS, no CLT-less environment available).
- **single report:** Filed 0 days ago by a NONE-association author with 0 reactions and no other commenters confirming; only an automated version-check bot has replied.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
