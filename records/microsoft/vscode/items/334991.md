---
number: 334991
url: "https://github.com/microsoft/vscode/issues/334991"
engine: copilot
owner: hawkticehurst
labels: bug, agents-window
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.9
severity: visual
breadth: common
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-09-08T12:49:08.469Z"
itemUpdatedAt: "2026-09-08T05:05:10Z"
itemCreatedAt: "2026-09-08T00:11:26Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: 1.138.0
contentHash: ece6043fcb869f9a277518325fcf54f403dadfd50a61c4800dbea3fec4afcdce
policyHash: 5fa429198b8c8ed6
---

# Issue #334991 — Customizations UI: Random circle icon in MCP Server Management Page

[https://github.com/microsoft/vscode/issues/334991](https://github.com/microsoft/vscode/issues/334991)

## Summary

Real, well-diagnosed bug confirmed in source: the MCP server enable/disable toggle is rendered as `.plugin-enable-switch`/`.plugin-enable-switch-thumb` but no CSS rules exist for these classes anywhere in the repo, so it renders as an unstyled native button — matching the reporter's own comment. Team-authored, has a milestone (1.138.0), and no fixing PR yet, so this stays open for the fix.

## Change summary

The MCP server enable/disable toggle in the Customizations UI renders as an unstyled native button (looks like a stray circle icon) instead of a styled switch.

## Best solution

Add CSS styling for `.plugin-enable-switch` and `.plugin-enable-switch-thumb` (used identically in mcpListWidget.ts and pluginListWidget.ts) in the aiCustomization media stylesheet, likely aiCustomizationManagement.css, so the toggle renders as a proper switch.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Evidence:** 5/6 Source-confirmed
- **Severity:** visual
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** agents-window
- **Confidence:** 0.9
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small, localized CSS-only fix; root cause already traced to a specific missing selector by the reporter and confirmed in source, with a clear visual validation.
- **Likely files:** `src/vs/workbench/contrib/chat/browser/aiCustomization/media/aiCustomizationManagement.css`, `src/vs/workbench/contrib/chat/browser/aiCustomization/mcpListWidget.ts`, `src/vs/workbench/contrib/chat/browser/aiCustomization/pluginListWidget.ts`
- **Validation:** Add/verify CSS rules for `.plugin-enable-switch` (track/pill shape) and `.plugin-enable-switch-thumb` (thumb), including `.checked` state styling; visually confirm in a running build that the MCP server list toggle renders as a switch rather than a bare circle, and that `aria-checked`/`.checked` toggling moves the thumb.
- **Fix prompt:**

  > In the AI Customizations UI (MCP server management page), the enable/disable toggle for each installed MCP server renders as an unstyled native HTML button instead of a switch control, appearing as a stray circle between the checkmark and the '…' menu button. Root cause (confirmed by reading source, not by running it): `renderMcpListActions` in src/vs/workbench/contrib/chat/browser/aiCustomization/mcpListWidget.ts (~lines 1757-1774) creates `DOM.append(actions, $('button.plugin-enable-switch'))` and appends a `.plugin-enable-switch-thumb` child, toggling a `.checked` class and `aria-checked` attribute — but a repo-wide grep for these two class names found zero matching CSS rules in any stylesheet. The identical unstyled markup pattern is also used in src/vs/workbench/contrib/chat/browser/aiCustomization/pluginListWidget.ts (~line 1467-1470), so the same underlying issue likely affects the plugin list toggle too, though this issue only reports the MCP page. Add CSS rules for `.plugin-enable-switch` (a pill/track shaped toggle button, sized appropriately for the list row, with a resting/unchecked appearance) and `.plugin-enable-switch-thumb` (the inner circular thumb that should visually move/highlight based on the `.checked` class), most likely in src/vs/workbench/contrib/chat/browser/aiCustomization/media/aiCustomizationManagement.css alongside the other aiCustomization styles — respect existing VS Code toggle/switch visual conventions and theme color tokens (see ux-theming guidance) rather than introducing new hardcoded colors. Do not change the TS logic in mcpListWidget.ts/pluginListWidget.ts (the class names and toggle behavior are already correct) — this is a pure CSS addition. Validate by confirming the MCP list toggle now renders as a recognizable switch shape (not a stray circle) in both checked and unchecked states, matching styling on the equivalent plugin list toggle if that one is also fixed. Related: https://github.com/microsoft/vscode/issues/334991.

## Evidence

- **missing CSS:** grep for 'plugin-enable-switch' across src/ finds zero CSS rules in any .css file; only TS usages in mcpListWidget.ts and pluginListWidget.ts.
- **source:** renderMcpListActions in src/vs/workbench/contrib/chat/browser/aiCustomization/mcpListWidget.ts (around lines 1757-1774) creates `button.plugin-enable-switch` and appends `.plugin-enable-switch-thumb`, with no styling applied.
- **reporter diagnosis:** Author (rwoll, MEMBER) already identified the root cause in a follow-up comment, pointing at the same code path.
- **milestone:** Issue carries milestone 1.138.0, a real scheduling milestone — keeping open per the milestone hard-keep-open rule (not a holding bin).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
