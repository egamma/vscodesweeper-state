---
number: 332136
url: "https://github.com/microsoft/vscode/issues/332136"
engine: copilot
owner: connor4312
labels: new release
proposedLabel: "*not-reproducible"
closeReason: not-reproducible
triageAction: propose-close
confidence: 0.72
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-23T08:36:44.018Z"
itemUpdatedAt: "2026-08-22T18:54:44Z"
itemCreatedAt: "2026-08-22T18:38:09Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: 598366cb6de36f65ed40102d9d7994b7494a49b5904fd103a874b325085eee8b
policyHash: 4721e56ad78fdd17
---

# Issue #332136 — Code, on Linux, is bleeding internal env vars into delve sessions.

[https://github.com/microsoft/vscode/issues/332136](https://github.com/microsoft/vscode/issues/332136)

## Summary

The report's central claim — that VS Code core injects `DEBUG=release` into the markdown-language-features server worker's environment, which then leaks into Delve debug sessions — has no basis anywhere in this repository's source; a targeted search finds no such env var assignment.

## Change summary

Reporter claims VS Code injects an internal build-time env var (`DEBUG=release`) into extension host child processes, which then leaks into spawned Delve (Go) debug sessions.

## Best solution

Close as not reproducible; if the reporter can show the actual env var leaking into a real `dlv` process (e.g. `cat /proc/<pid>/environ` for the Delve process itself), reopen with that concrete evidence — the current report's mechanism (build-time `DEBUG=release` in the markdown server worker) doesn't exist in the source.

## Proposed action

- **Triage action:** propose-close
- **Item type:** bug
- **Reproduction:** not_reproduced
- **Rating:** 2/6 Unverified
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *not-reproducible
- **Area labels:** none
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the detailed write-up. I searched the codebase for the specific mechanism described — VS Code's build pipeline setting `DEBUG=release` and that leaking through the extension host into a Delve child process — and couldn't find any such env var assignment anywhere in this repo (the only `DEBUG=` env set is an unrelated Playwright CI logging flag). The markdown-language-features server worker and the extension host environment construction code don't reference `DEBUG` at all, so I can't confirm this is happening as described.
  > 
  > If you can share the actual output of `cat /proc/<pid>/environ` for the live Delve (`dlv`) process itself showing `DEBUG=release` present, that would help pin down where it's really coming from (it may originate from your shell profile, a wrapper script, or the Go extension rather than VS Code core). Closing as not reproducible for now — happy to take another look with that evidence.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** No confirmed defect in this repo's source — the claimed env-injection mechanism doesn't exist, so there is nothing concrete to fix.

## Evidence

- **no matching env assignment:** Searched the full repo for `DEBUG=release`, `DEBUG['"]?\s*[:=]\s*['"]release`, and `process.env.DEBUG =` — the only hit is build/azure-pipelines/common/installPlaywright.js setting `DEBUG = 'pw:install'` for Playwright logging in CI, unrelated to the extension host or markdown server worker.
- **no DEBUG usage in extension host / env code:** Grepped src/vs/workbench/services/extensions and src/vs/platform/environment for `DEBUG` — no matches. Nothing in the environment-construction code for extension host or debug adapter child processes sets or forwards a build-only `DEBUG=release` flag.
- **report provenance:** The reporter states the trace was produced by an AI assistant (Claude Sonnet 5) querying their system; the specific mechanism described (VS Code build pipeline injecting `DEBUG=release` into a Node service worker, then Delve inheriting it via the extension host) does not match how markdown-language-features' serverWorkerMain or the Go/Delve debug adapter spawn is implemented in this repo.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
