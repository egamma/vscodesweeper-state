---
number: 336578
url: "https://github.com/microsoft/vscode/issues/336578"
engine: copilot
owner: sandy081
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-19T12:43:52.830Z"
itemUpdatedAt: "2026-09-17T14:31:15Z"
itemCreatedAt: "2026-09-17T14:20:20Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: dc3f9895a9a0a5c908243981c10d533e09a78b1e152d7ca178f2644ff2385eea
policyHash: fd90e4d8a068f279
---

# Issue #336578 — Portable .mcp.json is not read into configuration

[https://github.com/microsoft/vscode/issues/336578](https://github.com/microsoft/vscode/issues/336578)

## Summary

Current main already implements workspace-root `.mcp.json` discovery and management-UI listing end-to-end and independently of the `chat.mcp.workspaceRootConfig.enabled` setting, so this looks like a specific repro detail (JSON validity, workspace shape, or a missing reload) rather than a missing feature — asking for more detail before deciding.

## Change summary

A workspace-root `.mcp.json` file is not shown as an installed MCP server in the 'configure custom agents' MCP servers tab, even though `.vscode/mcp.json` works.

## Best solution

Get a precise repro (exact `.mcp.json` contents, single- vs multi-root workspace, whether a window reload was tried, any Output/log errors) — if the capability truly fails to surface after that, it's a bounded fix in WorkspaceDotMcpDiscovery/mcpWorkbenchManagementService; if it's confirmed working, this can close as already-implemented.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report and for confirming it persists after updating. Looking at the current source, workspace-root `.mcp.json` discovery is implemented and actively tested (it's watched and parsed independently of the .vscode/mcp.json path, and the setting mentioned in the docs only affects where new servers get added, not whether existing files are read). To help track down what's different in your case, could you share: the exact contents of your `.mcp.json`, whether this is a single-folder or multi-root workspace, whether a full window reload (not just the tab) was tried, and any errors in the MCP output channel or Developer Tools console? This issue will auto-close in 7 days if there's no reply, but a bit more detail will help us pin down whether there's a real gap or a specific misconfiguration.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** Source shows the capability already exists and is well-tested; without a confirmed reproduction there's no defect to hand to an agent yet.

## Evidence

- **discovery is unconditional:** src/vs/workbench/contrib/mcp/browser/mcp.contribution.ts:66 registers `WorkspaceDotMcpDiscovery` (src/vs/workbench/contrib/mcp/common/discovery/workspaceDotMcpDiscovery.ts) unconditionally; it watches `<folder>/.mcp.json` and registers server definitions as a Trusted collection.
- **management UI supports it:** src/vs/workbench/services/mcp/common/mcpWorkbenchManagementService.ts uses `McpResourceFormat.WorkspaceRoot` and `WORKSPACE_ROOT_MCP_CONFIG_FILE = '.mcp.json'` (src/vs/platform/mcp/common/mcpWorkspaceConfiguration.ts) to scan and dedupe workspace-root configs against `.vscode/mcp.json`, with dedicated test coverage in mcpWorkbenchService.test.ts (e.g. lines 955-1150).
- **setting only gates the 'add' flow:** The `chat.mcp.workspaceRootConfig.enabled` description in chat.shared.contribution.ts:1945 explicitly states existing `.mcp.json` files 'can be opened and read independently of this setting' — so the read/discovery path the reporter is missing shouldn't depend on that setting.
- **no fix commit to cite:** The checkout is a shallow single-commit clone (git log shows only one commit), so no history is available to confirm when this landed relative to 1.138.0/1.139.0 — cannot cite a fixedSha even though the capability is present on main.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
