---
number: 331190
url: "https://github.com/microsoft/vscode/issues/331190"
engine: copilot
owner: roblourens
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.45
severity: crash
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-18T08:57:37.682Z"
itemUpdatedAt: "2026-08-17T12:46:38Z"
itemCreatedAt: "2026-08-17T12:23:06Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 26722f51fc5071fb9d8c15697ef4b7c7c1c3763192fb10b72b4881af466ed9a5
policyHash: 4721e56ad78fdd17
---

# Issue #331190 — I can reproduce this on Windows with VS Code 1.133.0 Stable and its bundled GitHub Copilot 0.61.0.

[https://github.com/microsoft/vscode/issues/331190](https://github.com/microsoft/vscode/issues/331190)

## Summary

A single, detailed report of repeated extension-host crashes (code 134) traced to a `TypeError: e is not iterable` in Copilot Chat's Git repository observer, but the code path that throws (`ObservableGit.init` in `src/platform/inlineEdits/common/observableGit.ts`) already guards `gitApi.repositories` with `?? []`, so the trigger isn't confirmed in source and no other user has hit this — needs a repro or logs before it can be routed as a confirmed defect.

## Change summary

Bundled GitHub Copilot Chat 0.61.0 repeatedly crashes the extension host on Windows with a `TypeError: e is not iterable` originating from its Git repository observer, correlated with a separate 'unable to open database file' error.

## Best solution

Ask the reporter to confirm reproduction on the latest Insiders build and share the full diagnostic package/logs so the Copilot Chat team can determine what makes `gitApi.repositories` (or the observable feeding `mapObservableArrayCached` in `extensions/copilot/src/platform/inlineEdits/common/observableGit.ts`) non-iterable in their environment — the existing `?? []` guard there should normally prevent this, so the real trigger (possibly a git-extension state issue or environment/disk problem tied to the separate SQLite 'unable to open database file' error) needs more evidence.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** crash
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the very detailed report and diagnostics. I traced the stack to Copilot Chat's Git repository observer (`ObservableGit.init`), but the code there already guards against a missing repositories list, so it's not clear from source what makes it non-iterable in your case — it may be tied to the separate database/disk issue you're also seeing, or something environment-specific. Could you confirm whether this still reproduces on the latest VS Code Insiders build, and share whether the SQLite database path mentioned in the 'unable to open database file' error has any permission/disk-space/AV issues on this machine? This will auto-close in 7 days if there's no reply, but reopening is easy once we have that info.
  > 
  > Also worth noting: https://github.com/microsoft/vscode/issues/331197 looks like a duplicate of this report filed shortly after.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** The suspected code path already has a null-safety guard, so there is no clearly localized missing-guard fix to apply without first confirming what value actually reaches `setItems`; needs reporter follow-up, not an automatic fix.

## Evidence

- **code trace:** Stack (setItems → _computeFn → _recompute → recomputeInitiallyAndOnChange → NI.init) matches `extensions/copilot/src/platform/inlineEdits/common/observableGit.ts` at commit 7fee81510403f95dff38c5f4a528dbc4bd4d4298, which calls `mapObservableArrayCached(this, repos, ...)` inside `init()`; `setItems` does `for (const item of items)` in `src/util/vs/base/common/observableInternal/utils/utils.ts` (line ~319), matching the reported TypeError.
- **existing guard:** `repos` is built via `observableFromEvent(this, ..., () => gitApi.repositories ?? [])`, so a null/undefined `repositories` is already handled; the built-in `git` extension's `repositories` getters (`extensions/git/src/api/api1.ts:423`, `extensions/git/src/model.ts:201`) always return arrays via `.map()`, so the trigger for a non-iterable value isn't evident from source alone.
- **unrelated db error:** 'Failed to create database. Falling back to in-memory db' is logged from `extensions/copilot/src/platform/workspaceChunkSearch/node/codeSearch/externalIngestIndex.ts:180`, a separate code-search subsystem — the reporter's causal link between this and the Git-observer crash is speculative, not proven.
- **single report:** No comments, no other confirmations; a near-identical duplicate report (https://github.com/microsoft/vscode/issues/331197) was filed 22 minutes later by the same author (title literally references '#331190'), suggesting an accidental double-submission rather than independent corroboration.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
