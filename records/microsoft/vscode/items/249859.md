---
number: 249859
url: "https://github.com/microsoft/vscode/issues/249859"
engine: copilot
owner: osortega
labels: bug, search
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.75
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-24T15:08:02.013Z"
itemUpdatedAt: "2025-12-10T23:20:16Z"
itemCreatedAt: "2025-05-27T05:40:42Z"
upvotes: 2
reactionsTotal: 2
commentCount: 2
relatedOpenIssues: 1
milestone: none
contentHash: a0dc93c7e0aaeef2829b71df56df2d9d8284534986f378daa54a74e4e13ab8ea
policyHash: a68119f215da75e1
---

# Issue #249859 — VS Code Search Shows Results from Files of a Previous Git Branch

[https://github.com/microsoft/vscode/issues/249859](https://github.com/microsoft/vscode/issues/249859)

## Summary

The report lacks essential environment details (VS Code version, OS, Git version), and VS Code's text search delegates to ripgrep which reads files directly from disk with no content caching — if git checkout properly removes files from the working tree, ripgrep will not match them. Without knowing the reporter's OS, filesystem type, or whether a remote/network workspace is involved, the bug cannot be verified. Requesting more information.

## Change summary

After switching Git branches, Ctrl+Shift+F search returns matches from files that belonged to the previous branch.

## Best solution

Request reproduction details — VS Code version, OS, filesystem type (local/network/WSL), and whether the files are truly absent from disk after checkout. VS Code's text search uses ripgrep against the live filesystem with no content cache, so stale results likely point to a filesystem sync issue, a network/remote mount, or files that weren't actually removed by the checkout (e.g. untracked files). If the issue persists with a local filesystem on current Insiders, a verbose log (`Developer: Set Log Level` → Trace for file watcher) would help identify whether file-watcher events are delayed or lost.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** search
- **Confidence:** 0.75
- **Proposed comment:**

  > Thanks for the report. VS Code's text search (Ctrl+Shift+F) uses ripgrep under the hood, which searches files directly on disk — there's no content cache that would persist results across branch switches. If `git checkout` properly removes those files from your working tree, ripgrep should not find them.
  > 
  > To help us investigate, could you please provide:
  > 
  > 1. **VS Code version** (Help → About)
  > 2. **OS** (Windows / macOS / Linux — and if Linux, which distro)
  > 3. **Filesystem type** — is the project on a local disk, a network mount (NFS/SMB/SSHFS), WSL, or a VS Code Remote workspace?
  > 4. **Git version** (`git --version`)
  > 5. After switching branches, can you confirm the files are actually gone from disk? (e.g. `ls path/to/file` in the integrated terminal)
  > 6. Does the issue reproduce on the latest [VS Code Insiders](https://code.visualstudio.com/insiders/)?
  > 
  > This will help us understand whether the issue is in VS Code's file watching, a filesystem sync delay, or something else.
  > 
  > *Note: issues labeled `info-needed` are auto-closed after 7 days without a reply.*

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Insufficient reproduction information — cannot confirm a defect in VS Code's code; likely an environment-specific issue.

## Evidence

- **no search content cache:** VS Code's text search (Ctrl+Shift+F) delegates to ripgrep via src/vs/workbench/services/search/node/ripgrepSearchProvider.ts, which searches files on disk directly. There is no content caching layer that would persist results across branch switches.
- **file search cache is unrelated:** The cache in src/vs/workbench/services/search/node/rawSearchService.ts is for Quick Open file-pattern matching, not for text content search.
- **missing environment info:** Reporter did not fill in VS Code version, OS, or Git version — all marked as '[Fill in]'. No information about filesystem type (local, network, WSL, remote).
- **no confirmed repro:** One commenter (wxtim) says 'This is happening to me' but provides no environment details either. The other comment is off-topic.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
