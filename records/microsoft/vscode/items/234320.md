---
number: 234320
url: "https://github.com/microsoft/vscode/issues/234320"
engine: copilot
owner: rzhao271, justschen
labels: bug, workbench-os-integration, issue-reporter
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.78
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-18T21:16:14.590Z"
itemUpdatedAt: "2024-12-12T05:15:32Z"
itemCreatedAt: "2024-11-21T06:12:33Z"
upvotes: 0
reactionsTotal: 0
commentCount: 3
relatedOpenIssues: 0
milestone: Backlog
contentHash: e755282421312db487d03f0de55b5132e77307e6df93b154f316cdca1d489c8c
policyHash: ebd3763732abe97d
---

# Issue #234320 — Can't launch additional windows via `code` from cli.

[https://github.com/microsoft/vscode/issues/234320](https://github.com/microsoft/vscode/issues/234320)

## Summary

The user's own logs reveal an unexplained `extensionDevelopmentPath` argument being injected into the CLI args, which diverts the launch into extension-development-host mode and causes window reuse instead of opening a new window. This is almost certainly environment-specific (custom `.vscode-oss` build or desktop launcher wrapper on Arch/Hyprland), not a VS Code core bug. More info is needed to confirm.

## Change summary

Running `code <folder>` from the CLI while a VS Code window is already open fails to open a new window; the existing window reloads instead.

## Best solution

Ask the reporter to investigate why `extensionDevelopmentPath` appears in their CLI args when they didn't pass it — likely a custom `.desktop` file, shell alias, or Hyprland launcher wrapper on their Arch Linux setup is injecting it. Without reproducing on a stock VS Code install, this is not actionable.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** workbench-os-integration
- **Confidence:** 0.78
- **Proposed comment:**

  > Thanks for the detailed logs — they're actually very revealing. Looking at your log output, the second instance's args include `extensionDevelopmentPath: [ '$HOME/.vscode-oss/extensions/undefined_publisher.wallbash-0.0.1' ]`, even though you only ran `code --disable-extensions --verbose ./tmp2`. When `extensionDevelopmentPath` is present, VS Code routes the launch into extension-development-host mode, which reuses the existing window instead of opening a new one — that matches the reload behavior you described.
  > 
  > Could you investigate where that `extensionDevelopmentPath` argument is coming from? Likely candidates:
  > - A shell alias or wrapper script for `code`
  > - A `.desktop` file or Hyprland launcher rule
  > - A custom VS Code OSS build (the `.vscode-oss` path suggests a community/AUR build rather than the official release)
  > 
  > Try running `which code` and `type code` in your terminal to check for aliases, and inspect any `.desktop` files. If you can reproduce this on an official VS Code build without the injected `extensionDevelopmentPath`, please reopen with updated logs.
  > 
  > This issue will auto-close in 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Likely environment-specific, not a VS Code core defect — the injected extensionDevelopmentPath arg is external to VS Code.

## Evidence

- **suspicious CLI args:** The user's own log (1.txt) shows `extensionDevelopmentPath: [ '$HOME/.vscode-oss/extensions/undefined_publisher.wallbash-0.0.1' ]` in the received second-instance data, even though the user only ran `code --disable-extensions --verbose ./tmp2`. This arg was never passed by the user.
- **code path diversion:** In `src/vs/platform/launch/electron-main/launchMainService.ts:141-143`, when `extensionDevelopmentPath` is present, the launch is routed to `openExtensionDevelopmentHostWindow` which reuses an existing dev-host window — explaining the reload-instead-of-new-window behavior.
- **non-standard environment:** The `.vscode-oss` path and `undefined_publisher.wallbash` extension suggest a custom/community VS Code OSS build (likely from AUR), not the official Microsoft build. The desktop environment (Hyprland on Wayland) is also uncommon.
- **no independent reproduction:** No other user has reported this issue; 0 upvotes over 19 months. The team member's follow-up in December 2024 did not receive a response addressing the root cause.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
