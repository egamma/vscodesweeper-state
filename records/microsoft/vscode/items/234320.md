---
number: 234320
url: "https://github.com/microsoft/vscode/issues/234320"
engine: copilot
owner: rzhao271, justschen
labels: bug, workbench-os-integration, issue-reporter
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-26T11:53:27.658Z"
itemUpdatedAt: "2024-12-12T05:15:32Z"
itemCreatedAt: "2024-11-21T06:12:33Z"
upvotes: 0
reactionsTotal: 0
commentCount: 3
relatedOpenIssues: 0
milestone: Backlog
contentHash: e755282421312db487d03f0de55b5132e77307e6df93b154f316cdca1d489c8c
policyHash: 26158a1d7dfd26ac
---

# Issue #234320 — Can't launch additional windows via `code` from cli.

[https://github.com/microsoft/vscode/issues/234320](https://github.com/microsoft/vscode/issues/234320)

## Summary

The logs reveal `extensionDevelopmentPath` is unexpectedly present in the CLI args, which diverts the second-instance handling into the extension development host path instead of normal window opening. This likely stems from the user's environment (Arch Linux Code-OSS package, a wrapper script, or shell alias) rather than a core VS Code bug — but more info is needed to confirm.

## Change summary

Opening a second project via `code ./path` from the CLI does nothing when a VS Code window is already open.

## Best solution

Clarify with the reporter why `extensionDevelopmentPath` appears in their CLI args (visible in their own logs). If the Arch Linux Code-OSS package wrapper injects it, the fix belongs there. If VS Code's own CLI launcher is at fault, the fix is in the argument parsing in `src/vs/platform/environment/`.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** workbench-os-integration
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the detailed logs — they actually reveal the likely cause. In your log output, the second instance's args include:
  > 
  > ```
  > extensionDevelopmentPath: [ '$HOME/.vscode-oss/extensions/undefined_publisher.wallbash-0.0.1' ]
  > ```
  > 
  > This argument shouldn't be present when you just run `code --disable-extensions --verbose ./tmp2`. When it's present, VS Code routes the request through the extension development host path instead of opening a normal new window — which explains the observed behavior.
  > 
  > Could you check:
  > 1. What does `which code` or `type code` return? Is it a wrapper script (common on Arch with Code-OSS packages)?
  > 2. Does the issue persist if you launch directly via the binary (e.g. `/usr/lib/code-oss/code-oss ./tmp2`)?
  > 3. Is there anything in your shell startup (`.zshrc`, etc.) that aliases or wraps the `code` command?
  > 
  > The `undefined_publisher.wallbash-0.0.1` extension path suggests something in your environment is injecting `--extensionDevelopmentPath` into the launch arguments.
  > 
  > Note: this issue will auto-close in 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Root cause appears to be the user's environment injecting unexpected CLI args — not a VS Code core defect. Needs confirmation.

## Evidence

- **extensionDevelopmentPath in args:** The user's own log (1.txt) shows `extensionDevelopmentPath: [ '$HOME/.vscode-oss/extensions/undefined_publisher.wallbash-0.0.1' ]` in the received second-instance data, even though they only ran `code --disable-extensions --verbose ./tmp2`.
- **launch service routing:** In src/vs/platform/launch/electron-main/launchMainService.ts:142, when `args.extensionDevelopmentPath` is truthy the launch service calls `openExtensionDevelopmentHostWindow` instead of the normal window-open flow — this explains why no new window appears.
- **user environment:** The user runs Code-OSS on Arch Linux (`.vscode-oss` paths visible in logs). The `code` command on Arch is often a wrapper script that may inject unexpected arguments.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
