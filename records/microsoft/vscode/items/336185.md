---
number: 336185
url: "https://github.com/microsoft/vscode/issues/336185"
engine: copilot
owner: dbaeumer
labels: triage-needed
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-15T13:27:09.749Z"
itemUpdatedAt: "2026-09-15T11:38:12Z"
itemCreatedAt: "2026-09-14T18:36:02Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: a8dad71cb4e925c3e26a6f24dc41a66e71c796152cc805c122527d38da9a3574
policyHash: 5fa429198b8c8ed6
---

# Issue #336185 — Copilot Chat: external-agent (Claude) delegated sessions missing from Copilot Chat's own history

[https://github.com/microsoft/vscode/issues/336185](https://github.com/microsoft/vscode/issues/336185)

## Summary

Plausible, well-diagnosed report, but the reporter's transcript evidence doesn't rule out an alternative, mundane explanation: Copilot's own sidebar deliberately caps external-agent session visibility (`chat.agentSessions.showExternal`, default `recent` = up to 2 sessions from the last 7 days, further pruned at startup). Need the reporter to confirm the session is still missing after switching that setting to `last30Days` before treating this as a genuine persistence bug in the Copilot-side scratch directory.

## Change summary

A completed Claude-delegated (external-agent) chat session is missing from Copilot Chat's own sidebar history, even though the Claude Code SDK's own transcript for it persists correctly on disk.

## Best solution

Confirm with the reporter whether raising `chat.agentSessions.showExternal` surfaces the session; if it still doesn't appear, trace why the workspaceless scratch dir at `~/.copilot/chats/<uuid>/` isn't retained/registered for Claude-targeted external-agent runs (see `ensureWorkspacelessScratchDir` in src/vs/platform/agentHost/node/workspacelessScratchDir.ts and Claude's external-chat discovery classification in src/vs/platform/agentHost/node/claude/claudeAgent.ts) versus Copilot's own scratch-dir cleanup path in src/vs/platform/agentHost/node/copilot/copilotAgent.ts.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** chat
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed write-up and transcript evidence. Before treating this as a scratch-directory persistence bug, can you confirm one thing: Copilot's sidebar caps how many external-agent sessions it shows via `chat.agentSessions.showExternal` (default is `recent`, which only shows up to 2 external sessions from the last 7 days, and prunes further at startup). Could you try setting that to `last30Days` and reloading the window to see if the Claude-delegated session appears? That will help tell us whether this is a visibility-setting effect or a real gap in how the Copilot-side scratch directory is persisted for Claude-targeted runs.
  > 
  > This will auto-close in 7 days if we don't hear back, but please re-open or comment with the result and we'll keep digging.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Not confirmed as a code defect yet — plausible alternate explanation (default external-session visibility cap) needs to be ruled out with the reporter before any fix is scoped.

## Evidence

- **visibility setting:** src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts registers `chat.agentSessions.showExternal` (ChatConfiguration.ShowExternalAgentSessions) with default `AgentHostExternalSessionsMode.Recent` — 'up to 2 most recent external sessions updated in the last 7 days... older than the second-most-recently updated local session are hidden at startup'. A Claude-delegated session is exactly the 'external session' category this setting governs.
- **discovery classification:** AGENTS.md for src/vs/platform/agentHost documents that Claude 'classif[ies] their unknown native chats as external' via onDidDiscoverChats, and Agent Service applies visibility filtering based on that provenance — independent of whatever happens to the Copilot-side scratch directory.
- **scratch dir lifecycle:** src/vs/platform/agentHost/node/workspacelessScratchDir.ts creates `<userHome>/.copilot/chats/<sessionId>` via mkdir -p and nothing in copilotAgent.ts removes it except explicit session delete (`_cleanupWorkspacelessScratchDir`, copilotAgent.ts:3082), so the reporter's claim that the directory 'does not exist on disk afterward' is not yet corroborated in the code paths reviewed.
- **unrelated comment:** A commenter (ghsama) reports a completely different root cause for missing session history — a `chat.viewSessions.enabled` setting stuck at false — which doesn't confirm this issue's specific external-agent scenario and adds noise rather than a repro.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
