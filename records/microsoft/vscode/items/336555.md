---
number: 336555
url: "https://github.com/microsoft/vscode/issues/336555"
engine: copilot
owner: TylerLeonhardt
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-17T13:15:16.864Z"
itemUpdatedAt: "2026-09-17T11:18:51Z"
itemCreatedAt: "2026-09-17T11:07:22Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 6f9eb330149f7ce25ea3c2a0d089fc0dc6dcc5a1c6dd830e3dd81edd813e83f5
policyHash: 5fa429198b8c8ed6
---

# Issue #336555 — Agent host GitLab MCP returns 401 when using a Codex subscription in VS Code 1.138

[https://github.com/microsoft/vscode/issues/336555](https://github.com/microsoft/vscode/issues/336555)

## Summary

Plausible new-feature bug (Codex support in the Agent Host, shipped 1.138) but unconfirmed and under-specified — the exact MCP auth path involved (static header vs. workbench OAuth token injection) isn't stated, so I can't pin the defect from source alone; asking for that detail before triaging further.

## Change summary

GitLab MCP server calls return HTTP 401 when the Agent Host session uses a Codex/ChatGPT subscription, though the same MCP server works fine via Codex CLI/Desktop.

## Best solution

Have the reporter clarify how the GitLab MCP server's Authorization is supplied (static `headers` config vs. OAuth login flow) and whether the failure also happens with Copilot/Claude sessions in VS Code — that narrows whether the bug is in the codex-specific OAuth token injection path (src/vs/platform/agentHost/node/codex/codexMcpServers.ts, injectCodexMcpAuthTokens) or elsewhere in header plumbing to the codex SDK.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed report. To help track this down: could you clarify how the GitLab MCP server's Authorization is configured (a static token/header in your MCP server config, or via an OAuth login flow VS Code prompted you through)? Also, does the same GitLab MCP server work from a VS Code Chat session using a Copilot or Claude subscription (not Codex)? That will help narrow down whether this is specific to the new Codex auth-token injection path or a more general MCP header issue. This issue will auto-close in 7 days if there's no reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** A plausibly relevant code path exists (codex MCP OAuth token injection) but the defect isn't confirmed and depends on details of the reporter's MCP server auth configuration that aren't yet known — not safe to auto-fix without confirming the actual failing path.

## Evidence

- **relevant code path:** src/vs/platform/agentHost/node/codex/codexMcpServers.ts implements a codex-specific mechanism (injectCodexMcpAuthTokens/withoutAuthorizationHeaders) that strips any existing Authorization header on a matched http MCP server URL and replaces it with a workbench-acquired OAuth bearer token — this only runs for servers with a token in `tokensByNormalizedUrl`, so it wouldn't explain a static-PAT config failing, but could explain an OAuth-configured server getting an unexpected/invalid token.
- **feature is new:** Codex subscription support in the Agent Host was introduced in VS Code 1.138 per the linked release notes, so this auth-injection code path is recent and largely unexercised for third-party OAuth MCP servers like GitLab.
- **no confirmation:** Issue is 0 days old with no comments; only the reporter's own account of the 401, no independent confirmation or logs attached.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
