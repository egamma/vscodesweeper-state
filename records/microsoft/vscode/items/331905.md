---
number: 331905
url: "https://github.com/microsoft/vscode/issues/331905"
engine: copilot
owner: hediet
labels: new release
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-21T08:46:53.290Z"
itemUpdatedAt: "2026-08-21T06:34:10Z"
itemCreatedAt: "2026-08-21T06:12:25Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: fc98d9e3ce7174fc88c75dfdbc5c6efc83a763c8abc82d78675b45cb127db260
policyHash: 4721e56ad78fdd17
---

# Issue #331905 — Code Lens needs to manually reload VSCode to showup Clickable lreferences links, after first startup

[https://github.com/microsoft/vscode/issues/331905](https://github.com/microsoft/vscode/issues/331905)

## Summary

Single, same-day report with no comments or confirmed reproduction; core CodeLens already listens for provider-registration changes and should refresh automatically, so this needs more detail (does it happen with extensions disabled, is it specific to the C# reference CodeLens) before it can be routed or closed.

## Change summary

On first opening a file, CodeLens (reference links) shows blank/empty spots until the window is manually reloaded, after which it renders correctly.

## Best solution

Ask the reporter to confirm whether this reproduces with only the relevant language extension enabled (this workspace only has the C#/C# Dev Kit and Unity extensions installed) and on the latest build; if it's specific to the C# reference CodeLens provider's activation timing, the fix likely belongs in that extension rather than core, since CodeLensContribution already subscribes to `_languageFeaturesService.codeLensProvider.onDidChange` and re-renders when a provider registers late.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report. VS Code's CodeLens contribution already listens for language providers registering late and should refresh automatically without a manual reload, so this looks like it may be tied to how a specific extension (e.g. the C# reference CodeLens provider) activates and reports its lenses on first load.
  > 
  > Could you confirm: does this still happen on the latest VS Code build, and does it reproduce with only the relevant language extension enabled (e.g. via `--disable-extensions` plus that one extension)? That will help determine whether this belongs here or in that extension's tracker.
  > 
  > Note: this issue will auto-close in 7 days if there's no reply with the requested details.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction is environment/timing-dependent (extension activation ordering on first load) and not conclusively traceable to a specific core code path from source alone — needs reporter confirmation before any fix can be scoped.

## Evidence

- **core refresh mechanism:** src/vs/editor/contrib/codelens/browser/codelensController.ts constructor registers `_disposables.add(_languageFeaturesService.codeLensProvider.onDidChange(this._onModelChange, this))`, so CodeLens should re-render automatically once a language provider registers/activates late, without needing a manual window reload.
- **placeholder cache:** src/vs/editor/contrib/codelens/browser/codeLensCache.ts stores blank placeholder lenses (`command: item.symbol.command && { id: '', title: ... }`) from a prior session to reserve layout space until real providers respond — this could explain transient blank CodeLens rows, but is by design and should self-heal once the provider responds.
- **extensions installed:** Only 6 extensions are listed, including ms-dotnettools.csharp and ms-dotnettools.csdevkit, whose reference CodeLens is provided entirely by that extension's language service, not core.
- **single report:** Issue created 0 days ago with no comments and no other reporters; not enough to confirm this is a general core defect versus an extension activation/timing issue.
- **label-validation:** Engine proposed areaLabels that do not exist in the repo — dropped: editor-codelens (real labels only).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
