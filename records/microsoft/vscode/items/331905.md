---
number: 331905
url: "https://github.com/microsoft/vscode/issues/331905"
engine: copilot
owner: hediet
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.4
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-25T09:10:31.508Z"
itemUpdatedAt: "2026-08-24T18:41:39Z"
itemCreatedAt: "2026-08-21T06:12:25Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: bacf12728c8eceb2be88fe2cf9516cac8a1fb2c566c3854e1df8b564f5f674e0
policyHash: 3d6943c3a15c721c
---

# Issue #331905 — Code Lens needs to manually reload VSCode to showup Clickable references links, after first startup

[https://github.com/microsoft/vscode/issues/331905](https://github.com/microsoft/vscode/issues/331905)

## Summary

Plausible report, but the evidence points more toward the C#/.NET language extension than a core CodeLens defect — VS Code's editor already re-renders CodeLens when a provider signals a change, so this needs the reporter to isolate whether it happens with a built-in language (no extensions) before we can call it a core bug.

## Change summary

CodeLens references show as blank/non-clickable on VS Code's very first startup, and only a full window reload (not just reopening the file) fixes it.

## Best solution

Ask the reporter to reproduce with extensions disabled or against a plain JS/TS file (no C# Dev Kit/Roslyn) to determine whether the defect is in core editor CodeLens (src/vs/editor/contrib/codelens/browser/codelensController.ts) or in how the C# extension's provider reports readiness/refreshes; if it's extension-specific, this belongs with dotnet/vscode-csharp, not microsoft/vscode.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.4
- **Proposed comment:**

  > Thanks for the report. Looking at the CodeLens implementation in core (`src/vs/editor/contrib/codelens/browser/codelensController.ts`), the editor already re-renders CodeLens automatically whenever a provider signals a change — so a fix that only takes effect after a full window reload points more toward the C# language extension (Roslyn) not being fully ready/not notifying VS Code, rather than a core defect.
  > 
  > Could you confirm whether this also happens with a built-in language (e.g. plain JavaScript/TypeScript, no other extensions) on first startup? If it's specific to C#/Unity projects, this is likely better tracked in https://github.com/dotnet/vscode-csharp.
  > 
  > This issue will auto-close in 7 days if there's no reply with that info.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Root cause is unclear pending reporter confirmation — likely extension-side (C# language server readiness), not a localized core defect to fix automatically.

## Evidence

- **core refresh mechanism exists:** src/vs/editor/contrib/codelens/browser/codelensController.ts:69 and :166 already subscribe to `codeLensProvider.onDidChange` / each provider's own `onDidChange` and re-run `_onModelChange`/scheduler — so core CodeLens is designed to refresh automatically once a provider announces new data, without requiring a full window reload.
- **installed extensions:** The reporter's extension list is dominated by ms-dotnettools.csharp / csdevkit (Roslyn-based C# language server) and visualstudiotoolsforunity — CodeLens 'references' links in this scenario are provided by that extension, not by any codelens provider in this repo.
- **no core fix found:** git log on codelensController.ts and codelensWidget.ts shows no related recent change; `computeIfNecessary`/`updateCommands` in codelensWidget.ts always re-resolve when triggered, so a stuck-blank state after first load is more consistent with the extension not firing its `onDidChange` (or the language server not being ready yet) than with a bug in the editor's rendering pipeline.
- **label-validation:** Engine proposed areaLabels that do not exist in the repo — dropped: editor-code-lens (real labels only).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
