---
number: 331905
url: "https://github.com/microsoft/vscode/issues/331905"
engine: copilot
owner: hediet
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.45
severity: visual
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-28T20:18:33.171Z"
itemUpdatedAt: "2026-08-28T04:39:09Z"
itemCreatedAt: "2026-08-21T06:12:25Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 0462d2c6034c267c10acdcc17a7951980d94d5ef1d41bc0a323de121863ff70b
policyHash: 3d6943c3a15c721c
---

# Issue #331905 — Code Lens needs to manually reload VSCode to showup Clickable references links, after first startup

[https://github.com/microsoft/vscode/issues/331905](https://github.com/microsoft/vscode/issues/331905)

## Summary

Plausible but unconfirmed: core's CodeLensContribution already listens for `onDidChange` on the code lens provider registry and re-renders when a provider registers late (src/vs/editor/contrib/codelens/browser/codelensController.ts), so a core refresh bug is not obviously supported by the code — this more likely traces to a slow-activating language extension (C# / C# Dev Kit, given the `.cs` file) not firing that event, but that's circumstantial without confirmation.

## Change summary

On first VS Code startup, CodeLens reference links render blank over methods/variables until the window is reloaded; a second startup shows them correctly.

## Best solution

Ask the reporter to bisect with `--disable-extensions` (or selectively disable ms-dotnettools.csharp / csdevkit) to confirm whether this is caused by the C# extension's CodeLens provider not firing a refresh once project load completes, versus a genuine core CodeLens refresh gap; route accordingly once confirmed.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** visual
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the report. VS Code's editor already listens for CodeLens providers changing/registering and re-renders automatically, so this is more likely tied to how one of your installed language extensions (e.g. the C# / C# Dev Kit extension, given the `.cs` file) activates and refreshes its CodeLens on first load.
  > 
  > Could you help narrow this down: run `code --disable-extensions` and reopen the same folder to see if CodeLens still starts blank, and if it does, try re-enabling extensions one at a time (or just C#/C# Dev Kit) to see which one reproduces it? That will tell us whether this belongs in VS Code core or in the C# extension.
  > 
  > This issue will auto-close in 7 days if there's no reply with that information.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Root cause is unclear between core and an installed language extension; needs reporter bisection before any fix can be scoped.

## Evidence

- **core refresh mechanism:** src/vs/editor/contrib/codelens/browser/codelensController.ts registers `_languageFeaturesService.codeLensProvider.onDidChange(this._onModelChange, this)` in the constructor, so core already re-renders CodeLens when a provider is registered or changes after the editor opens — a late-registering provider should not by itself require a manual reload.
- **likely extension origin:** The referenced file is `TurnFlowManager.cs` and the installed extensions include ms-dotnettools.csharp and ms-dotnettools.csdevkit (Roslyn), which are the actual source of 'N references' CodeLens for C# — core has no built-in C# reference CodeLens provider.
- **no reproduction in this repo:** No commenters have confirmed the repro, and it depends on extension activation timing that cannot be traced from the vscode core checkout alone.
- **label-validation:** Engine proposed areaLabels that do not exist in the repo — dropped: editor (real labels only).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
