---
number: 336772
url: "https://github.com/microsoft/vscode/issues/336772"
engine: copilot
owner: bhavyaus
labels: triage-needed
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.45
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-21T15:18:14.029Z"
itemUpdatedAt: "2026-09-21T08:16:51Z"
itemCreatedAt: "2026-09-18T14:17:33Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 88352677201da8b336cd755f36c5e025b2a46c672f2eb80769315e2994528946
policyHash: d13126680f5ee38a
---

# Issue #336772 — /remote on fails in VS Code 1.137+ with "Failed to set up remote session" and "Session is not an adoptable legacy chat"

[https://github.com/microsoft/vscode/issues/336772](https://github.com/microsoft/vscode/issues/336772)

## Summary

Credible regression report (works on 1.136, fails on 1.137/1.138, reproduced on two machines/multiple repos with a fresh profile), but I could not pin the exact defect from source — the failure spans a closed-source Copilot CLI SDK call (`session.commands.invoke`, `Failed to set up remote session`) and a distinct core error string (`Session is not an adoptable legacy chat`) from Agent Host's legacy-session restore/adoption path, and it's unclear whether these are the same failure or two overlapping symptoms.

## Change summary

`/remote on` (Copilot CLI remote control) fails in the classic GitHub Chat panel (not the Agents window) starting in VS Code 1.137, throwing a `session.commands.invoke` / 'Session is not an adoptable legacy chat' error.

## Best solution

A maintainer with live repro access (and Agent Host debug logs) should confirm whether the classic chat panel's Copilot agent session is failing legacy-chat adoption in `restoreSession` (src/vs/platform/agentHost/node/agentService.ts around line 6633-6641) versus a genuine SDK-side `/remote` regression; if it's the adoption path, it may already be addressed by the ongoing peer-chat-catalog migration fixes landing this week (e.g. commits 3e0ff278, b80ed11d), which should be checked against this report before assuming a new fix is needed.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** chat
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the detailed report and for confirming this across two machines, multiple repos, and a fresh profile — that's very helpful. The 'Session is not an adoptable legacy chat' message comes from Agent Host's legacy-chat restore path, but the user-facing 'Failed to set up remote session' string isn't in the VS Code or Copilot Chat source, so it likely originates in the Copilot CLI SDK itself. To help narrow this down, could you share an Agent Host debug log capture (Command Palette: 'Export Agent Host Debug Logs') from a session where `/remote on` fails, and confirm whether the same chat works if you start a brand-new chat instead of reusing an older one? This will auto-close in 7 days without a reply, but please keep it open if you can attach the logs.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** The failure spans a closed-source SDK call and an internal Agent Host adoption guard under active, unrelated churn — root cause is not confirmed from source, so no bounded fix can be scoped yet.

## Evidence

- **core error string match:** 'Session is not an adoptable legacy chat' is thrown only from src/vs/platform/agentHost/node/agentService.ts:6640, in restoreSession's guard for unregistered sessions that are neither an adoptable legacy chat nor already-known Agent Host metadata.
- **no match for the user-facing message:** 'Failed to set up remote session' does not appear anywhere in the vscode or extensions/copilot source, so that string likely originates in the closed-source @github/copilot/sdk package, outside this checkout.
- **extension-side /remote handler:** extensions/copilot/src/extension/chatSessions/copilotcli/node/copilotcliSession.ts:_handleRemoteControl implements /remote by calling the GitHub API directly (no session.commands.invoke), so the failing path in this report may be a different, Agent-Host-routed Copilot chat agent (node/copilot/copilotAgentSession.ts, which does call `this._wrapper.session.rpc.commands.invoke`) rather than this CLI session handler.
- **adjacent very-recent fixes:** Commits 3e0ff278 (2026-09-21, 'Fix peer chat catalog migration racing with chat creation') and b80ed11d (2026-09-21, 'keep a provider readable after a partial catalog import') touch the same legacy-chat migration/adoption machinery but do not reference this issue number, so they cannot be cited as a confirmed fix.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
