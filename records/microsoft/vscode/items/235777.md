---
number: 235777
url: "https://github.com/microsoft/vscode/issues/235777"
engine: copilot
owner: anthonykim1
labels: bug, terminal-layout
proposedLabel: none
triageAction: keep-open
confidence: 0.8
severity: visual
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-07-24T06:07:53.141Z"
itemUpdatedAt: "2026-01-16T01:54:50Z"
itemCreatedAt: "2024-12-10T20:49:44Z"
upvotes: 0
reactionsTotal: 0
commentCount: 3
relatedOpenIssues: 1
milestone: Backlog
contentHash: 0a67be7a52248222fb6cf0e52f89bcfc9b25d2502ba0262e97d1d549ab00a886
policyHash: 0c32deb9982e8caf
---

# Issue #235777 — Terminal toggle size to content width lacks horizontal scroll bar

[https://github.com/microsoft/vscode/issues/235777](https://github.com/microsoft/vscode/issues/235777)

## Summary

The bug is still present on main: the horizontal scrollbar logic in `_addScrollbar()` has a likely timing issue where `setScrollDimensions` is called before the DOM has fully laid out after the resize, so the scrollbar doesn't render until a subsequent resize forces a re-layout. No fix commit was found.

## Change summary

After toggling terminal content width (Alt+Z), the horizontal scrollbar does not appear until the terminal is manually resized.

## Best solution

Fix the timing in `_addScrollbar()` in `terminalInstance.ts` — the `setScrollDimensions` call at line 2265 likely needs to happen after the DOM layout settles (e.g., after a rAF or microtask), or `scanDomNode()` needs to be called on the `DomScrollableElement` to force it to pick up the new dimensions.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** visual
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** terminal-layout
- **Confidence:** 0.8
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small localized timing fix in _addScrollbar(); the scrollbar is created but dimensions aren't picked up until a resize forces re-layout.
- **Likely files:** `src/vs/workbench/contrib/terminal/browser/terminalInstance.ts`
- **Validation:** Toggle Alt+Z on a terminal with wrapped lines: the horizontal scrollbar should appear immediately without needing a manual resize. Add a test that after `toggleSizeToContentWidth()`, the `_horizontalScrollbar` is defined and its scroll dimensions reflect the fixed column width.
- **Fix prompt:**

  > In src/vs/workbench/contrib/terminal/browser/terminalInstance.ts, the `_addScrollbar()` method (line 2245) creates a `DomScrollableElement` and calls `setScrollDimensions()`, but the scrollbar does not render until a subsequent resize. The likely fix: after setting scroll dimensions at line 2265-2268 and appending the scrollbar DOM node, call `this._horizontalScrollbar.scanDomNode()` to force the scrollable element to measure its container and render the scrollbar track. The `DomScrollableElement` (from src/vs/base/browser/ui/scrollbar/scrollableElement.ts) inherits `scanDomNode()` from `AbstractScrollableElement` which re-measures container dimensions. Additionally, ensure the order in `_addScrollbar` is correct: the DOM node should be in the document before `setScrollDimensions` is called, and a `scanDomNode()` call after both ensures the scrollbar widget picks up the actual layout. Do not restructure the toggle flow or change the scrollbar visibility mode. Validate by checking that `_horizontalScrollbar` renders its track immediately after `toggleSizeToContentWidth()` without requiring a manual resize.

## Evidence

- **scrollbar code:** src/vs/workbench/contrib/terminal/browser/terminalInstance.ts lines 2245-2283 implement `_addScrollbar()` which creates a `DomScrollableElement` with `horizontal: ScrollbarVisibility.Auto` and sets scroll dimensions.
- **toggle flow:** `toggleSizeToContentWidth()` at line 2211 calls `_refreshScrollbar()` which calls `_addScrollbar()`, but the scrollbar dimensions are set before the container may have finished layout from the preceding `_resize()` call.
- **no fix found:** git log shows no commit referencing issue #235777; the scrollbar-related commits (4bcfe945487, 9aa4f663d06) address different scrollbar issues.
- **reporter comment:** Author (Tyriar) confirms at https://github.com/microsoft/vscode/issues/235777#issuecomment-2531920965 that the scrollbar appears after resizing, suggesting a layout-timing issue.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
