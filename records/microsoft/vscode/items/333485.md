---
number: 333485
url: "https://github.com/microsoft/vscode/issues/333485"
engine: copilot
owner: sandy081
labels: bug, agents-window
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.85
severity: visual
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-09-08T12:57:56.721Z"
itemUpdatedAt: "2026-09-07T14:58:11Z"
itemCreatedAt: "2026-08-31T09:07:03Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: 1.138.0
contentHash: 24daffddda91b19413560b3a3c07da6bdae54a916b65452373c06a479f341387
policyHash: 5fa429198b8c8ed6
---

# Issue #333485 — Agents - chat tab still uses the old spinner progress indicator

[https://github.com/microsoft/vscode/issues/333485](https://github.com/microsoft/vscode/issues/333485)

## Summary

Team-authored (protected) and milestoned to 1.138.0, so this stays keep-open regardless — but I also confirmed the defect directly in the checkout: the chat tab bar's in-progress indicator still uses the legacy `Codicon.loading` spin animation while the sessions list, header, and automations views were migrated to the new pixel-spinner `SessionStatusIcon` widget.

## Change summary

The chat composite tab bar's in-progress indicator still shows the old spinning codicon instead of the new pixel-spinner status icon used elsewhere in the Agents window.

## Best solution

Update `chatCompositeBar.ts`'s tab indicator to host a `SessionStatusIcon` (or its pixel-spinner path) instead of `ThemeIcon.modify(Codicon.loading, 'spin')`, matching sessionHeader.ts/sessionsList.ts, and adjust chatCompositeBar.css's `.in-progress` selectors accordingly.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Evidence:** 5/6 Source-confirmed
- **Severity:** visual
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** agents-window
- **Confidence:** 0.85
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small, localized swap of the tab indicator's icon-building code from the legacy codicon spinner to the existing SessionStatusIcon/pixel-spinner pattern already used by sibling surfaces; the defect is directly confirmed in source.
- **Likely files:** `src/vs/sessions/browser/parts/chatCompositeBar.ts`, `src/vs/sessions/browser/parts/media/chatCompositeBar.css`
- **Validation:** Add/extend a test that renders a chat tab with SessionStatus.InProgress and asserts the tab indicator no longer carries a `codicon-loading` class but instead hosts the pixel-spinner element/class used by SessionStatusIcon (mirroring assertions in src/vs/sessions/test/browser/sessionStatusIcon.test.ts); it should fail before the fix and pass after.
- **Fix prompt:**

  > In src/vs/sessions/browser/parts/chatCompositeBar.ts (around line 345-373), the per-tab 'in-progress' indicator builds its icon via `indicatorIcon.classList.add(...ThemeIcon.asClassNameArray(ThemeIcon.modify(Codicon.loading, 'spin')))` — this is the old spinning codicon. Every other Agents-window surface that shows session status (src/vs/sessions/browser/parts/sessionHeader.ts, src/vs/sessions/contrib/sessions/browser/views/sessionsList.ts, src/vs/sessions/contrib/sessions/browser/views/automationsView.ts) has been migrated to host the `SessionStatusIcon` widget from src/vs/sessions/browser/sessionStatusIcon.ts, which renders an animated pixel spinner (createPixelSpinner from src/vs/base/browser/ui/pixelSpinner/pixelSpinner.ts) respecting reduced-motion. Replace the manual codicon class toggling in chatCompositeBar.ts's tab-indicator autorun with a `SessionStatusIcon` instance hosted in the `.chat-composite-bar-tab-indicator-icon` container, calling `setStatus(status, isRead, isArchived, completedStateIcon, chat.resource)` instead of manually computing the 'mode' string and codicon classes for the in-progress case (keep the existing needs-input/unread class toggling on `tab`/`indicator` for CSS styling, since those aren't part of this bug). Update chatCompositeBar.css's `.in-progress .chat-composite-bar-tab-indicator-icon` rule (around line 401) if it assumed a codicon element rather than the pixel-spinner markup. Do not touch unrelated tab behavior (renaming, close button, drag-and-drop). Validate with a test rendering a tab in SessionStatus.InProgress and asserting the pixel-spinner markup/class appears instead of any `codicon-loading` class, following the pattern in src/vs/sessions/test/browser/sessionStatusIcon.test.ts.

## Evidence

- **old spinner still in use:** src/vs/sessions/browser/parts/chatCompositeBar.ts:371 sets `indicatorIcon.classList.add(...ThemeIcon.asClassNameArray(ThemeIcon.modify(Codicon.loading, 'spin')))` for the in-progress tab state — the legacy codicon spinner.
- **new widget used elsewhere:** src/vs/sessions/browser/sessionStatusIcon.ts implements the modern pixel spinner (createPixelSpinner), and is consumed by src/vs/sessions/browser/parts/sessionHeader.ts, src/vs/sessions/contrib/sessions/browser/views/sessionsList.ts, and automationsView.ts — but not by chatCompositeBar.ts.
- **related PR:** https://github.com/microsoft/vscode/pull/333804 ('sessions: Refresh chat status after background completion') is merged and referenced, but the old spinner code path is still present on current main, so it does not resolve this specific report — not citable as the fix.
- **team-authored + milestone:** Author lszomoru is a MEMBER and the issue carries milestone 1.138.0 (a real release milestone, not a holding bin), so a judgment close is barred outright; only a mechanical fix citing #333485 would qualify, and none was found in this squashed-history checkout.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
