---
number: 159465
url: "https://github.com/microsoft/vscode/issues/159465"
engine: copilot
owner: meganrogge
labels: bug, tasks
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.75
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-20T21:52:37.817Z"
itemUpdatedAt: "2026-03-19T15:37:38Z"
itemCreatedAt: "2022-08-29T14:34:22Z"
upvotes: 0
reactionsTotal: 0
commentCount: 13
relatedOpenIssues: 0
milestone: none
contentHash: 6ce1977cf6db47b456b34bce69af11cdf1000bfb9f69aadec2f5ca7fb31e95fd
policyHash: ebd3763732abe97d
---

# Issue #159465 — VSCode uses yarn to run npm tasks

[https://github.com/microsoft/vscode/issues/159465](https://github.com/microsoft/vscode/issues/159465)

## Summary

The report lacks the key diagnostic info (whether a yarn.lock, .yarn-integrity, or yarn workspace root existed in a parent directory). The auto-detection logic in preferred-pm.ts is working as designed — it checks lockfiles and yarn workspace roots up the directory tree — but without knowing what was in the reporter's filesystem, we cannot confirm a defect.

## Change summary

npm task type incorrectly uses yarn when npm.packageManager is set to auto, even though the user never installed or used yarn.

## Best solution

Ask the reporter to confirm on a current build whether the issue persists, and to share (1) whether any parent directory contains a yarn.lock or package.json with a `workspaces` field, and (2) whether node_modules/.yarn-integrity exists. Without that context, the detection logic in extensions/npm/src/preferred-pm.ts appears to be working correctly — it traverses parent directories via findWorkspaceRoot and whichPM.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** tasks
- **Confidence:** 0.75
- **Proposed comment:**

  > Thanks for the report. We still need a bit more information to investigate this properly — could you confirm on the latest VS Code whether this still reproduces, and check:
  > 
  > 1. Does any parent directory of your project contain a `yarn.lock` file?
  > 2. Does any parent directory have a `package.json` with a `"workspaces"` field?
  > 3. Does `node_modules/.yarn-integrity` exist in your project?
  > 
  > The `auto` detection looks at these signals (including up the directory tree), so one of them is likely causing yarn to be selected.
  > 
  > Note: issues labeled `info-needed` will be auto-closed after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Cannot confirm a defect — the detection logic appears correct; the issue likely stems from an unrecognized yarn artifact in the user's filesystem.

## Evidence

- **detection logic:** extensions/npm/src/preferred-pm.ts isYarnPreferred() calls findWorkspaceRoot(pkgPath) which traverses parent directories for yarn workspace roots — this could detect yarn without a local yarn.lock.
- **whichPM fallback:** extensions/npm/node_modules/which-pm/index.js returns {name:'yarn'} if node_modules/.yarn-integrity exists, which the user may not be aware of.
- **missing info:** alexr00 asked (comment 12) whether the user had a yarn.lock file; no response was ever given.
- **stale:** Issue is from 2022 (VS Code 1.70.2); the detection logic has been refined since then but the core flow is unchanged.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
