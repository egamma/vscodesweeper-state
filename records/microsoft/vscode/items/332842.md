---
number: 332842
url: "https://github.com/microsoft/vscode/issues/332842"
engine: copilot
owner: benibenj
labels: info-needed, agents-window
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.6
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-02T12:56:43.983Z"
itemUpdatedAt: "2026-09-02T11:09:03Z"
itemCreatedAt: "2026-08-26T23:21:55Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: ed97c691fad916007d854ce89686d4643ea5119581339003383aefeceffd47ec
policyHash: 5fa429198b8c8ed6
---

# Issue #332842 — Session artifacts appear duplicated with unreadable absolute paths

[https://github.com/microsoft/vscode/issues/332842](https://github.com/microsoft/vscode/issues/332842)

## Summary

The maintainer already flagged this as hard to reproduce and asked for logs; that matches what I found — the dedup logic in `sessionArtifacts.ts` looks sound on its own, so confirming this needs the actual raw session-artifact stream from the reporter's session, not a source-only diagnosis.

## Change summary

Session artifacts picker shows the same artifact twice: once as a truncated absolute path and again as a short file name.

## Best solution

Get the session logs/artifact stream from the reporter (as benibenj asked) to see whether two distinct raw artifact entries (e.g. a `Resource`-kind entry carrying only `link` and a `File`-kind entry carrying `uri`) point at the same file — `buildSessionArtifactSections` in src/vs/sessions/contrib/chat/browser/sessionArtifacts.ts dedupes via `artifactValueKey`, which compares URIs, so two entries would only collide if they share the same URI comparison key; anything else (mismatched schemes/casing, or a link-only vs uri-only pair for the same file) would slip through undetected.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** agents-window
- **Confidence:** 0.6
- **Proposed comment:**

  > Thanks for flagging this — agreeing with benibenj that it's hard to pin down from the report alone. The dedup logic in `buildSessionArtifactSections` (src/vs/sessions/contrib/chat/browser/sessionArtifacts.ts) keys entries by a URI comparison key, so this would only happen if two raw artifact entries for the same file don't produce matching keys (e.g. one recorded with only a `link`, the other with a `uri`). If you can share the session logs or reproduce this again, that would help narrow down which artifact entries are involved. This will auto-close in 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction is unclear and depends on the actual raw session-artifact stream from a live session, which isn't available from source inspection alone — no concrete failing case to validate a fix against yet.

## Evidence

- **maintainer response:** benibenj (CONTRIBUTOR) commented asking for logs, saying they can't tell how this could happen — no confirmed repro yet.
- **dedup logic:** src/vs/sessions/contrib/chat/browser/sessionArtifacts.ts:89-97 (`artifactValueKey`) and buildSessionArtifactSections (lines ~215-247) dedupe by a URI comparison key (or link/hash) via a `seen` Set shared across the whole artifact list; a File artifact renders `basename(uri)` (line ~159) while a Resource artifact renders the full label/path, so two raw entries of different kinds pointing at the same file would only be caught as duplicates if their `artifactValueKey` matches exactly.
- **no recent fix:** git log on sessionArtifacts.ts and agentHostSessionArtifacts.ts shows no commit touching the dedup path since this was filed; only an unrelated 'ready message' change (359cba2f).

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
