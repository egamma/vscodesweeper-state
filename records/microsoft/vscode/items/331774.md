---
number: 331774
url: "https://github.com/microsoft/vscode/issues/331774"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-25T09:15:34.397Z"
itemUpdatedAt: "2026-08-24T18:42:03Z"
itemCreatedAt: "2026-08-20T10:46:14Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: fd2c97aa5330939bd51615f9c60b1d860c28ee4e6ad10aec0672fe9cdeb27360
policyHash: 3d6943c3a15c721c
---

# Issue #331774 — Git diff fails with GitFileSystemProvider Repository not found on Windows

[https://github.com/microsoft/vscode/issues/331774](https://github.com/microsoft/vscode/issues/331774)

## Summary

Detailed, plausible Windows-only Git diff failure with matching source-level log strings, but it's a single unconfirmed report and I couldn't conclusively pin the defect in `GitFileSystemProvider`/`Model.getRepository` from source alone — asking the reporter to confirm on the latest build and share a couple of diagnostic details.

## Change summary

Opening the diff for a modified file fails with 'Repository not found' when the Git repository root itself (not the drive root) is opened directly as the VS Code workspace on Windows.

## Best solution

A `git` area owner should verify whether this shares the root cause of the already-assigned https://github.com/microsoft/vscode/issues/297275 (drive-root case) or is a distinct path-matching bug in `Model.getOpenRepository`/`isDescendant` (extensions/git/src/model.ts, extensions/git/src/util.ts) — likely something environment-specific (e.g. a substituted/mapped drive or realpath mismatch) since this is otherwise a very common workflow that would surface far more reports if universally broken.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed report and logs — this looks related to https://github.com/microsoft/vscode/issues/297275, which tracks a similar 'git:' URI failure on Windows, though that one is specifically about opening a drive root as the workspace rather than the repo root itself as you describe.
  > 
  > A couple of things that would help narrow this down:
  > - Can you confirm this still reproduces on the latest Insiders build?
  > - Is the workspace path on a real physical drive, or could it involve a substituted (`subst`) or mapped network drive?
  > 
  > This issue will auto-close in 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Root cause not conclusively traced from source (path-matching logic appears sound for the simple case); needs a Windows repro/confirmation before a targeted fix can be scoped.

## Evidence

- **source match:** The reported log lines ('[GitFileSystemProvider][readFile] Repository not found - ...') match exactly extensions/git/src/fileSystemProvider.ts:221, confirming the failure is in Model.getOrOpenRepository failing to resolve an already-open repository via Model.getRepository/getOpenRepository (extensions/git/src/model.ts:929-1013).
- **path matching traced:** getOpenRepository uses isDescendant(repository.root, resourcePath) with normalizePath lower-casing both sides on Windows (extensions/git/src/util.ts:308-340), so a simple drive-letter case mismatch should not explain this; the exact trigger (e.g. subst drive, realpath divergence) is not confirmed from source alone.
- **related but distinct issue:** https://github.com/microsoft/vscode/issues/297275 tracks the same 'git:' URI error but only for opening a drive root (e.g. D:\) as the workspace; it's open, assigned to a maintainer (lszomoru), no milestone, and the reporter of this issue explicitly noted their case (repo root itself as workspace) is different, so it isn't a clean duplicate in either direction.
- **shallow checkout:** Repo checkout here is shallow (depth 1, git log only shows one commit), so I could not check recent history on extensions/git/src/model.ts or fileSystemProvider.ts for a related regression fix.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
