---
number: 193698
url: "https://github.com/microsoft/vscode/issues/193698"
engine: copilot
owner: deepak1556, dmitrivMS
labels: install-update, under-discussion, ssh
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.75
severity: crash
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-26T13:56:52.885Z"
itemUpdatedAt: "2026-02-21T09:15:20Z"
itemCreatedAt: "2023-09-21T15:32:19Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: a5e2c43137ea9244cc4b15c76716f481875c3e3f4c968fcd2a0845f7ea6986fa
policyHash: 26158a1d7dfd26ac
---

# Issue #193698 — Kernel Panic/crash Debian (12-bookworm) on VSCode Upgrader Installer running

[https://github.com/microsoft/vscode/issues/193698](https://github.com/microsoft/vscode/issues/193698)

## Summary

The report alleges kernel panics during VS Code Remote SSH auto-updates on a Debian 12 headless server, but no kernel logs, dmesg output, or SSH traces have ever been provided despite being requested — the issue is unverifiable without that evidence, and a kernel panic is almost certainly an OS/hardware-level problem rather than a userspace VS Code defect.

## Change summary

Kernel panics occur on a Debian 12 headless server during VS Code Remote SSH auto-update, roughly 1 in 3 times.

## Best solution

Request kernel-level evidence (dmesg/journalctl logs from a surviving boot after the panic, SSH verbose traces). If the panic is reproducible, it is almost certainly a kernel or hardware issue (memory pressure triggering a kernel bug, failing RAM, etc.) rather than a VS Code defect. Without such evidence after 1000+ days, the established info-needed process should retire this.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** crash
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** install-update, ssh
- **Confidence:** 0.75
- **Proposed comment:**

  > Thanks for the detailed description. Unfortunately, without kernel-level logs we can't investigate further — a kernel panic is an OS-level crash that VS Code (a userspace application) cannot directly cause, though it's possible heavy I/O during the update could surface a pre-existing kernel or hardware issue.
  > 
  > Could you provide:
  > 1. Kernel panic traces from a subsequent boot: `journalctl --boot=-1` or `dmesg` output from the boot after the crash.
  > 2. SSH verbose logs: connect with `ssh -vvv` to capture the session around the time of the hang.
  > 3. Memory/system state: confirm whether the 7.5 GB system is under memory pressure during the update (e.g. `free -h` before triggering the update).
  > 
  > Without this evidence it's not possible to determine whether this is a VS Code issue or an underlying OS/hardware problem. This issue will auto-close in 7 days if we don't hear back — please reopen with the requested info if you can still reproduce this on a current build.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** No evidence that VS Code code is at fault — kernel panics are OS-level; needs diagnostic data first.

## Evidence

- **no logs provided:** Team member @deepak1556 requested SSH trace logs in September 2023; the reporter explained they lose the logs when the machine crashes but never provided kernel panic traces (dmesg, journalctl --boot=-1) from a subsequent boot.
- **kernel panic is OS-level:** A kernel panic is a kernel-space crash; userspace applications like VS Code cannot directly cause one. The most common causes are hardware faults, driver bugs, or kernel bugs under memory/IO pressure.
- **unreproducible by reporter:** Reporter states 'I cannot prove it' and estimates it happens '1 out of 3 auto-upgrades' but has never captured diagnostic output.
- **age without progress:** Issue is 1038 days old with no actionable diagnostic data and no other reporters confirming the behavior.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
