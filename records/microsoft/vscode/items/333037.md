---
number: 333037
url: "https://github.com/microsoft/vscode/issues/333037"
engine: copilot
owner: lszomoru
labels: new release
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.4
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-28T20:14:41.127Z"
itemUpdatedAt: "2026-08-27T21:07:36Z"
itemCreatedAt: "2026-08-27T20:58:38Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: f9a2a33098ec64939e7b3ed3aac2f8bd9baddc92a6e8cf649a9a48f895e19e4e
policyHash: 3d6943c3a15c721c
---

# Issue #333037 — Git: Clone destination folder dialog never appears for a repo with no prior clone history

[https://github.com/microsoft/vscode/issues/333037](https://github.com/microsoft/vscode/issues/333037)

## Summary

Tracing the current clone flow in extensions/git/src/cloneManager.ts shows `cloneRepository()` always calls `window.showOpenDialog` when `parentPath` is falsy, regardless of whether the repo has cache history — I can't find a code path on main that skips the picker for never-cloned repos. Since the reporter explicitly hasn't tested with `--disable-extensions` and flags the GitHub Pull Requests extension as enabled, this needs confirmation before treating it as a core Git extension defect.

## Change summary

Git: Clone never shows the native destination-folder picker for a repository with no prior local clone history, skipping straight to the post-clone prompt without ever running `git clone`.

## Best solution

Ask the reporter to reproduce with `code --disable-extensions` (or Extension Bisect) to rule out interference from the GitHub Pull Requests extension's remote-source provider; if it still reproduces with only built-in extensions, revisit `CloneManager.clone()`/`cloneRepository()` in extensions/git/src/cloneManager.ts, since current source shows the dialog firing correctly whenever `parentPath` is unset.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git
- **Confidence:** 0.4
- **Proposed comment:**

  > Thanks for the detailed report. Reading through the current clone flow in extensions/git/src/cloneManager.ts, the destination-folder dialog is shown whenever no parent path is supplied — I can't find a path on main that skips it purely because a repo lacks prior clone history. Since you noted this happens with the GitHub Pull Requests extension enabled and haven't yet tried with it (or all extensions) disabled, could you reproduce with `code --disable-extensions`, or run 'Help: Start Extension Bisect', and let us know if the picker is still skipped? That will tell us whether this is a core Git extension issue or something introduced by the GitHub Pull Requests extension's remote-source provider. This issue will auto-close in 7 days if there's no reply, but feel free to re-open with the extra info anytime.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Core clone code as read on main appears correct; without a confirmed reproduction isolating it from the GitHub Pull Requests extension, there's no confirmed defect to fix.

## Evidence

- **code trace:** extensions/git/src/cloneManager.ts lines 30-88: clone() checks repositoryCache.get(url); when there is no cache (or it's empty) it calls cloneRepository(url, options.parentPath, options) directly, and cloneRepository() shows window.showOpenDialog whenever parentPath is falsy — this matches the expected behavior the reporter wants, not the skip they describe.
- **reporter's own caveat:** Issue body: 'Does this issue occur when all extensions are disabled?: No (occurs with GitHub Pull Requests extension enabled; not tested with all extensions disabled)' — the report itself flags this as unverified against core-only behavior.
- **shallow checkout:** This checkout is a 1-commit shallow clone (`git log` shows only HEAD), so no history is available to confirm whether a recent fix already landed; judgment relies solely on reading current main source.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
