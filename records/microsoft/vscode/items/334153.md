---
number: 334153
url: "https://github.com/microsoft/vscode/issues/334153"
engine: copilot
owner: pwang347
labels: ""
proposedLabel: none
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-03T12:50:22.437Z"
itemUpdatedAt: "2026-09-03T00:43:50Z"
itemCreatedAt: "2026-09-03T00:33:01Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: 9054a2a331259c3d5f4e460492ffb10e322673b196b5a6f582f484a1f0df7c89
policyHash: 5fa429198b8c8ed6
---

# Issue #334153 — Agents not Recognized loading plugin with only .copilot repository Or hooks not found with com.github.copilot

[https://github.com/microsoft/vscode/issues/334153](https://github.com/microsoft/vscode/issues/334153)

## Summary

Plausible confusion between two unrelated conventions, but the report doesn't show whether a valid `plugin.json` manifest was present — without that detail I can't confirm this is a real gap versus expected manifest-based format detection.

## Change summary

Reporter says a plugin with only a `.copilot/` folder or only a `com.github.copilot/` folder silently fails to load agents/hooks/instructions.

## Best solution

Ask the reporter to share the plugin's `plugin.json` (or confirm its absence) and the exact folder layout; if no manifest with the Agent Plugins `$schema` was present, this is as-designed per `requiresManifest` in pluginParsers.ts and the fix is clearer messaging/docs rather than a code change. If a valid manifest was present and content still didn't load, that's a real bug in `detectPluginFormat`/`readAgentPluginManifest`.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report. Looking at the code, `.copilot/` isn't a recognized plugin-content directory anywhere — that path is reserved for the Copilot CLI's own install location (`~/.copilot/installed-plugins/...`), not for a plugin's own layout. And `com.github.copilot/` is only read when it's paired with a root `plugin.json` carrying the Agent Plugins `$schema` (see `AGENTS_PLUGINS.md`); without that manifest, plugins fall back to the legacy Copilot format, which looks for `agents/`, `rules/`, and `hooks.json` at the plugin root, not inside `com.github.copilot/`.
  > 
  > Could you confirm whether your plugin included a `plugin.json` with `$schema` pointing at the Agent Plugins schema? If not, this is expected — the `com.github.copilot/` layout needs that manifest to be recognized. If you did include a valid manifest and it still didn't work, please share the exact file layout and manifest contents so we can dig further.
  > 
  > This issue will auto-close in 7 days without a reply, per the `info-needed` process.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Report is missing the key detail (was a plugin.json manifest present?) needed to confirm whether this is a real defect or expected manifest-required behavior — needs reporter confirmation, not an automatic fix.

## Evidence

- **no `.copilot/` plugin convention:** src/vs/platform/agentPlugins/common/pluginParsers.ts has no format that reads content from a `.copilot/` directory; `.copilot` only appears as the Copilot CLI's own install root `~/.copilot/installed-plugins/...` (agentPluginServiceImpl.ts:731-856, agentPluginEnablement.ts:30), an unrelated convention.
- **`com.github.copilot/` requires a manifest:** AGENT_PLUGIN_FORMAT in pluginParsers.ts:202-221 sets `requiresManifest: true`; detectPluginFormat() (line 223) only selects AgentPlugin format via `readAgentPluginManifest`, which needs a root `plugin.json` with the Agent Plugins $schema. Without it, format falls back to legacy COPILOT_FORMAT (line 236), which reads root-level `agents/`, `rules/`, `hooks.json` — not inside `com.github.copilot/` — matching the reporter's 'hooks not found' symptom.
- **documented design:** src/vs/workbench/contrib/chat/common/plugins/AGENTS_PLUGINS.md describes `com.github.copilot/` as valid only under the Agent Plugins v1 manifest; the scaffolding in createPluginAction.ts always writes a root plugin.json alongside com.github.copilot/, so the supported path pairs the two.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
