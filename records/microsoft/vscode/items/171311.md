---
number: 171311
url: "https://github.com/microsoft/vscode/issues/171311"
engine: copilot
owner: alexdima
proposedLabel: none
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-08T18:17:20.990Z"
itemUpdatedAt: "2024-12-19T23:00:04Z"
itemCreatedAt: "2023-01-14T07:30:56Z"
upvotes: 1
reactionsTotal: 1
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: a6b715b600c4504534f3d7cade922985d287622c93b5fde3f685638013971546
policyHash: 5ec37a1f28b7de7b
---

# Issue #171311 — vscode.TextEditor.edit breaks undo/redo history

[https://github.com/microsoft/vscode/issues/171311](https://github.com/microsoft/vscode/issues/171311)

## Summary

The report lacks a minimal reproduction — no specific code, steps, or observed-vs-expected behavior. The undo stop logic in `mainThreadEditor.ts` looks correct on current main; `undoStopBefore`/`undoStopAfter` are respected. Without a concrete repro, this cannot be verified.

## Change summary

Extension edits via `vscode.TextEditor.edit` with `undoStopBefore: false` / `undoStopAfter: false` allegedly still create separate undo stops, breaking undo grouping.

## Best solution

Request a minimal reproduction extension from the reporter. The undo stop logic at `src/vs/workbench/api/browser/mainThreadEditor.ts:503-509` is straightforward and appears correct; the likely culprit is the async model-version check (line 480) silently rejecting a second edit when the first hasn't round-tripped yet, which is a usage pattern issue. If confirmed, documentation or an API improvement in the extension host editor proxy would be the fix site.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Verification strength:** Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** undo-redo
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for raising this. The undo stop logic on current VS Code looks correct — `pushUndoStop()` is only called when the corresponding flag is `true`. The referenced issue #38535 was also closed for lack of reproduction.
  > 
  > Could you provide a **minimal reproduction extension** (or a few lines of extension code) that demonstrates the problem? Specifically:
  > 1. The exact sequence of `editor.edit()` calls with their `undoStopBefore`/`undoStopAfter` options
  > 2. What you expect undo to do vs. what actually happens
  > 3. Your current VS Code version
  > 
  > One common pitfall: if you call `editor.edit()` multiple times without `await`ing each one, later edits may silently fail due to model version checks. Ensure you `await` each edit before issuing the next.
  > 
  > This issue will auto-close in 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** No confirmed defect — the undo stop logic is correct on main; needs a concrete reproduction to identify any real bug.

## Evidence

- **undo stop implementation:** src/vs/workbench/api/browser/mainThreadEditor.ts lines 503-509: `pushUndoStop()` is called only when `undoStopBefore`/`undoStopAfter` is `true` — logic is correct.
- **referenced issue closed:** https://github.com/microsoft/vscode/issues/38535 was closed as `info-needed` in 2020; no fix was applied because no reproduction was provided.
- **no reproduction provided:** Reporter references the old closed issue but provides no minimal extension code, specific steps, or observed-vs-expected output.
- **potential async pitfall:** The model version check at line 480 silently returns `false` if the model changed between edits — sequential `await editor.edit(...)` calls can fail because each edit increments the version, and the extension host may hold a stale version.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
