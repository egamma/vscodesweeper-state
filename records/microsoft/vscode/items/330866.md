---
number: 330866
url: "https://github.com/microsoft/vscode/issues/330866"
engine: copilot
owner: ulugbekna
labels: ""
proposedLabel: none
closeReason: none
triageAction: route-to-area
confidence: 0.85
severity: papercut
breadth: common
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-08-18T09:05:06.579Z"
itemUpdatedAt: "2026-08-17T22:29:51Z"
itemCreatedAt: "2026-08-14T14:30:59Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: 7a0c42e3ec9da5ee480ffefaa04e4313f74da7cf6fda7910c5acdfe351a68db3
policyHash: 4721e56ad78fdd17
---

# Issue #330866 — Built-in Copilot Inline Chat slash commands are missing from autocomplete

[https://github.com/microsoft/vscode/issues/330866](https://github.com/microsoft/vscode/issues/330866)

## Summary

Confirmed on main: the Inline Chat (editor-location) chat participant `github.copilot.editingSessionEditor` in extensions/copilot/package.json declares `"commands": []`, while the panel participant `github.copilot.default` lists `explain, tests, fix, new, newNotebook, semanticSearch, setupTests` — so Inline Chat's completion list has no built-in commands registered, matching the report exactly.

## Change summary

Typing `/` in editor Inline Chat shows no built-in slash commands (doc, explain, fix, tests, help) in autocomplete, even though they still execute when typed manually.

## Best solution

Populate the `commands` array for the `github.copilot.editingSessionEditor` participant in extensions/copilot/package.json with the built-in Inline Chat commands (doc, explain, fix, tests, help) so they surface in the `/` completion list, mirroring how github.copilot.default declares its commands.

## Proposed action

- **Triage action:** route-to-area
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** papercut
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** chat
- **Confidence:** 0.85
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small, localized package.json contribution fix: add the built-in command names to the `commands` array for the `github.copilot.editingSessionEditor` chat participant so they register for autocomplete.
- **Likely files:** `extensions/copilot/package.json`
- **Validation:** Add/extend an extension test (e.g. under extensions/copilot/src/extension/conversation/vscode-node/test/) that reads the contributed chatParticipants from package.json and asserts the editor-location participant's `commands` array includes the expected built-in command names (doc, explain, fix, tests, help); it should fail before the fix (empty array) and pass after.
- **Fix prompt:**

  > The Inline Chat editor participant is missing built-in slash commands from its autocomplete list. In extensions/copilot/package.json, the `contributes.chatParticipants` entry with `"id": "github.copilot.editingSessionEditor"` and `"locations": ["editor"]` declares `"commands": []` (around line 1498), while the sibling panel participant `github.copilot.default` (same file, ~line 1358) declares `commands` entries for `explain`, `review`, `tests`, `fix`, `new`, `newNotebook`, `semanticSearch`, `setupTests` with names/descriptions. The reporter confirms manually typing `/doc` still executes correctly (handled in extensions/copilot/src/extension/inlineChat/vscode-node/inlineChatCommands.ts and the intent-detection/request-handler code in extensions/copilot/src/extension/prompt/node/chatParticipantRequestHandler.ts / defaultIntentRequestHandler.ts), so this is purely a missing autocomplete registration, not a broken handler. Fix by adding the applicable built-in commands (doc, explain, fix, tests, help — whichever intents are actually wired for the editor/Inline Chat location; check inlineChatIntent.ts and related intent registrations in extensions/copilot/src/extension/inlineChat2/node/ to confirm the exact set and correct `name`/`description` strings, localizing descriptions via the existing `%...%` nls keys like the panel participant does) to the `commands` array of the `github.copilot.editingSessionEditor` entry only. Do not touch the panel (`github.copilot.default`), notebook, or terminal participants' command lists, and do not change the request-handling/intent-detection logic. Validate with a test asserting the editor-location participant's `commands` array contains the expected command names.

## Evidence

- **package.json contribution:** extensions/copilot/package.json line ~1498: the `github.copilot.editingSessionEditor` chatParticipant (locations: ["editor"]) has `"commands": []`, verified via `python3 -c "json.load(...)"` against current main (commit 7fee81510403f95dff38c5f4a528dbc4bd4d4298).
- **contrast with panel participant:** `github.copilot.default` (locations: ["panel"]) declares commands explain, review, tests, fix, new, newNotebook, semanticSearch, setupTests in the same file — the editor-scoped participant is the one missing entries.
- **reporter confirmation:** Author's own comment confirms the side/panel chat correctly suggests doc/explain/fix/tests/help, and it's specific to Inline Chat (editor location).
- **no duplicate found:** gh issue search for 'inline chat slash command' on microsoft/vscode turned up no other open issue tracking this exact gap.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
