---
number: 328359
url: "https://github.com/microsoft/vscode/issues/328359"
engine: copilot
owner: benibenj
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.45
severity: correctness
breadth: niche
priority: none
visionFit: aligned
autoFixable: false
reviewedAt: "2026-08-07T08:27:09.657Z"
itemUpdatedAt: "2026-08-03T11:11:08Z"
itemCreatedAt: "2026-07-31T08:07:31Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: bf8068add493f10a26491f17a3e961b1e1b39be85cdb089ae7c64f47dcb135c8
policyHash: 70e952cc2fb28823
---

# Issue #328359 — Agent: submit / act on feedback buttons broken in 1.131.0

[https://github.com/microsoft/vscode/issues/328359](https://github.com/microsoft/vscode/issues/328359)

## Summary

Plausible real regression in the Submit Feedback → act-on-feedback flow, but the current source looks structurally correct (whenWidgetForSession, submitFeedback, _sendActOnFeedbackRequest in src/vs/sessions/contrib/agentFeedback/browser/agentFeedbackService.ts), and the reporter themselves says it's inconsistent across sessions — that intermittency plus the shallow checkout (only one commit of history available) means I can't confirm or rule out a defect from source alone, so this needs more diagnostic detail rather than a guessed close.

## Change summary

Clicking Submit on an agent feedback comment silently does nothing (no act-on-feedback request reaches the agent) in some sessions on 1.131.0, though the `/act-on-feedback` slash command still works as a manual workaround.

## Best solution

Get the reporter to attach Agent Host debug logs (or browser console errors) from a session where Submit fails, and note whether it's an agent-host-backed session vs. a classic chat session — that will show whether `submitFeedback`/`_sendActOnFeedbackRequest` in agentFeedbackService.ts is failing to resolve a chat widget, failing the `widget.acceptInput('/act-on-feedback', …)` call, or something upstream (e.g. annotations backend) is dropping the request before it reaches that code.

## Vision fit

- **Fit:** aligned
- **Reason:** Agent feedback / act-on-feedback is part of the Agents window and agent-host feedback loop, both named as current focus areas.
- **Evidence:** Current priorities include the Agents window ("multi-chat UX, layout, PR/changes review flows") and agent host/harness parity work — the Submit Feedback → act-on-feedback loop sits squarely in that surface.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** agents-window
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the report, and for flagging that it's intermittent — that detail is useful. From reading the current `agentFeedbackService.ts` code, the Submit → act-on-feedback path (resolving the chat widget, then sending `/act-on-feedback`) looks structurally sound, so this may depend on session state or timing that isn't obvious from source.
  > 
  > Could you help narrow it down: when Submit does nothing, is that in an Agent Host-backed session or a classic chat session? Could you attach the Agent Host debug logs (or any errors in the developer console) from a session where it fails, ideally alongside one where it works, so we can compare? This issue will auto-close in 7 days if there's no reply, but we'd like to get to the bottom of it.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction status is unclear (intermittent, not confirmed on current main) — no confirmed defect or validation to point an automatic fix at.

## Evidence

- **code path traced:** src/vs/sessions/contrib/agentFeedback/browser/agentFeedbackService.ts: submitFeedback() (line 904) resolves the widget via whenWidgetForSession (line 64, 10s timeout), then for agent-host sessions attaches feedback and calls _sendActOnFeedbackRequest (line 950), which sends widget.acceptInput('/act-on-feedback', …) and only marks feedback submitted onRequestAccepted.
- **no obvious defect on main:** The logic looks internally consistent (errors are logged via this._logService.error on both widget-not-found and acceptInput failure paths), so nothing conclusively reproduces from a static read.
- **shallow checkout:** git log --oneline on the file shows only a single commit (b6b77233, from the initial checkout), so history-based verification of a recent regression or fix is not possible here.
- **reporter's own comment:** Reporter states 'I have some sessions where this works, but some do not' — consistent with an intermittent/race condition rather than a deterministic code defect visible from source.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
