---
number: 172791
url: "https://github.com/microsoft/vscode/issues/172791"
engine: codex
owner: alexdima
proposedLabel: none
triageAction: keep-open
confidence: 0.94
reviewedAt: "2026-06-26T18:09:30.391Z"
contentHash: 2026c4d367e033670f6ab235799ff452800ca0209d89967b46ad7fafbe414873
policyHash: 17b7468985688190
---

# Issue #172791 — Uncaught Error: Illegal value for lineNumber

[https://github.com/microsoft/vscode/issues/172791](https://github.com/microsoft/vscode/issues/172791)

## Summary

Keep open: this is a team-authored error-telemetry bug, and the hard keep-open rule applies to MEMBER-authored issues. The stack still routes to the editor hover path, where current main calls `getLineMaxColumn` from `MarkdownHoverParticipant.computeSync` without enough issue context to prove a completed fix or safe close.

## Change summary

Hover computation can call `TextModel.getLineMaxColumn` with an illegal line number and throw during markdown hover rendering.

## Best solution

Route to the editor hover owners to decide whether current telemetry still shows this stack; if it does, guard or validate the hover anchor range in `src/vs/editor/contrib/hover/browser/markdownHoverParticipant.ts` before querying the model.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Proposed label:** none
- **Area labels:** editor
- **Confidence:** 0.94
- **Proposed comment:** none

## Evidence

- **hard keep-open:** Issue author association is MEMBER, so the cleanup policy says not to propose closing even if stale or possibly fixed.
- **current labels:** The issue is already classified as `bug` and `error-telemetry`, with no milestone and no verification labels.
- **stack routing:** The issue stack points to `src/vs/editor/contrib/hover/browser/markdownHoverParticipant.ts` and `src/vs/editor/common/model/textModel.ts`, which are editor hover/model code paths.
- **current code:** On current main, `MarkdownHoverParticipant.computeSync` reads `anchor.range.startLineNumber` and calls `model.getLineMaxColumn(lineNumber)`; `TextModel.getLineMaxColumn` throws `BugIndicatingError('Illegal value for lineNumber')` when the line is outside `1..getLineCount()`.

---
Reviewed by vscodesweeper (codex) — proposal only, not applied.
