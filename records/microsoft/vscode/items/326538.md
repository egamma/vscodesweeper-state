---
number: 326538
url: "https://github.com/microsoft/vscode/issues/326538"
engine: copilot
owner: vritant24
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: visual
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-27T15:14:46.378Z"
itemUpdatedAt: "2026-07-19T22:42:56Z"
itemCreatedAt: "2026-07-19T22:34:35Z"
upvotes: 1
reactionsTotal: 1
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: baa3f493193f4f0aba1e461d5bad38161a6cc3939a27b2efa379cf0e134127bf
policyHash: 26158a1d7dfd26ac
---

# Issue #326538 — Language Models panel mixes LLM providers and chat session agents, appearing as confusing duplicates

[https://github.com/microsoft/vscode/issues/326538](https://github.com/microsoft/vscode/issues/326538)

## Summary

Cannot reproduce on current main: the `copilotcli` and `claude-code` language model provider entries in copilot/package.json have `"when": "false"`, and `getVendors()` in languageModels.ts filters them out before the Language Models panel ever sees them. The reporter may be seeing entries from a third-party extension or an older Insiders build.

## Change summary

The Language Models panel allegedly shows greyed-out chat session agent entries (Claude Code, Copilot CLI) alongside actual LLM providers, causing confusion about duplicates.

## Best solution

Ask the reporter to reproduce on the latest Insiders build and provide the full list of installed extensions. The `when: "false"` guard on the `copilotcli` and `claude-code` vendor entries in `extensions/copilot/package.json` should prevent them from appearing in the Language Models panel via the `getVendors()` filter in `src/vs/workbench/contrib/chat/common/languageModels.ts` (line ~1012). If the issue persists, the reporter should check whether a third-party extension is contributing these entries.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** visual
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** chat
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the detailed report. I looked into this on current `main` and the `copilotcli` and `claude-code` entries in the built-in Copilot extension's `languageModelChatProviders` have `"when": "false"`, which means `getVendors()` filters them out before the Language Models panel renders them. The panel also doesn't pull from `chatSessions` — it only shows language model providers.
  > 
  > Could you try reproducing this on the latest Insiders build? If it still happens, could you share:
  > - Your full list of installed extensions (`code-insiders --list-extensions`)
  > - A screenshot of the Language Models panel
  > 
  > It's possible the entries you're seeing come from a third-party extension rather than the built-in Copilot. Note that `info-needed` issues auto-close after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Cannot confirm the defect on current main — the when-clause guard should prevent the reported entries from appearing.

## Evidence

- **when clause guard:** extensions/copilot/package.json registers `copilotcli` and `claude-code` as languageModelChatProviders with `"when": "false"`, which should prevent them from appearing in the UI.
- **getVendors filter:** src/vs/workbench/contrib/chat/common/languageModels.ts line ~1012: `getVendors()` filters vendors by their `when` clause via `contextMatchesRules`, so `when: "false"` entries are excluded.
- **no chatSessions in panel:** The chatModelsViewModel.ts uses only `ILanguageModelsService` (language model providers), not `chatSessions`. The claim that chatSessions entries appear in the panel is not supported by the current code.
- **possible third-party source:** Reporter has `cpulvermacher.claude-model-provider` installed, which could be contributing additional entries that look like duplicates.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
