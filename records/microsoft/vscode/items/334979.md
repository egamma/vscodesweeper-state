---
number: 334979
url: "https://github.com/microsoft/vscode/issues/334979"
engine: copilot
owner: meganrogge
labels: ""
proposedLabel: none
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-15T13:42:22.976Z"
itemUpdatedAt: "2026-09-14T21:25:19Z"
itemCreatedAt: "2026-09-07T21:43:23Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: 52e900315517c0c80169d2fd77c65a46535e7d9ab9dcf73bde82b7642d726837
policyHash: 5fa429198b8c8ed6
---

# Issue #334979 — Terminal memory leak (oom-kill)

[https://github.com/microsoft/vscode/issues/334979](https://github.com/microsoft/vscode/issues/334979)

## Summary

The report is too thin and internally inconsistent to act on: the OOM log shows the kernel killing an `rg` (ripgrep) process at ~11GB RSS, not a VS Code terminal shell process, yet the title and steps frame it as a 'terminal' leak from running terraform/journalctl. No extensions-disabled test was done, and the process attribution doesn't match the described trigger, so this needs clarification before it can be routed.

## Change summary

Reporter says the integrated terminal (or something correlated with terminal usage) leaks memory until the OOM killer terminates a process, crashing the pty host.

## Best solution

Ask the reporter to confirm which process is actually ballooning (the log names `rg`, which is VS Code's bundled search binary, not a terminal shell), whether it reproduces with `--disable-extensions`, and to attach a memory profile or `top`/`ps` output captured while memory is climbing — this is unlikely to be terminal-owned code without that clarification.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** terminal
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report. One thing that doesn't quite add up: the kernel log names the killed process as `rg` (ripgrep, VS Code's bundled search tool), not a terminal shell or the terraform/journalctl process you mention running — so it's not yet clear this is a terminal issue at all.
  > 
  > Could you help narrow this down:
  > - Does this reproduce when starting VS Code with `code --disable-extensions`?
  > - Can you capture `ps`/`top` output (or a memory profile) while memory is climbing, to confirm which process is actually growing?
  > - Is there a large workspace or search open at the same time that might be triggering a big `rg` search?
  > 
  > This issue will auto-close in 7 days if there's no follow-up, but feel free to reopen once you have more detail.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction is unclear and the process attribution in the report doesn't match the described trigger — needs reporter clarification before any code investigation, not an automatic fix.

## Evidence

- **oom log mismatch:** The kernel log names the killed process as `rg` (pid 419562, total-vm 13.6GB, anon-rss ~10.9GB) — `rg` is the ripgrep binary VS Code bundles for search/grep, not a terminal shell or the reported terraform/journalctl process.
- **no extension bisect:** The template's 'Does this occur with all extensions disabled?' question was left as the literal placeholder 'Yes/No', and no bisect was attempted.
- **no repro steps for the leak itself:** Steps say 'occasionally' the terminal flashes and a process is OOM-killed — no minimal, deterministic reproduction, and only one other user comment confirming it persists on 1.137.0 without new detail.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
