---
number: 334979
url: "https://github.com/microsoft/vscode/issues/334979"
engine: copilot
owner: meganrogge
labels: new release
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: crash
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-08T12:49:11.880Z"
itemUpdatedAt: "2026-09-07T21:54:01Z"
itemCreatedAt: "2026-09-07T21:43:23Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 6fcb16ddc5e18e0a4416803b45237e14411e6d198b9d902d9e46040ecc2b09f3
policyHash: 5fa429198b8c8ed6
---

# Issue #334979 — Terminal memory leak (oom-kill)

[https://github.com/microsoft/vscode/issues/334979](https://github.com/microsoft/vscode/issues/334979)

## Summary

Freshly filed report of an OOM-kill affecting a terminal-spawned `rg` process alongside a pty host crash, but the repro steps ('run terraform or systemd journal, occasionally...') are too vague to trace or reproduce — asking for more detail before deciding whether this is a VS Code-side leak or an environment/workload issue.

## Change summary

User reports the Linux OOM killer terminates a terminal-run `rg` process, and the pty host/extension host crash shortly after, suggesting a possible memory leak.

## Best solution

Ask the reporter for a memory profile (Help: Open Process Explorer, or a growth trend of the pty host/rg process RSS over time) and a more deterministic repro; if a leak is confirmed in the pty host or terminal shell integration, it likely belongs in src/vs/platform/terminal or src/vs/workbench/contrib/terminal.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** crash
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** terminal
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report. This is hard to act on as written — the oom-kill journal shows the killed process is `rg` (ripgrep), and it's not clear whether that's a process VS Code spawned or one you ran yourself in the terminal, nor what makes it grow to ~13GB virtual memory. Could you share: (1) exactly what command triggers the growth, (2) whether `rg` was invoked by you directly or by VS Code's search, and (3) a screenshot/output of Help: Open Process Explorer while the memory is climbing? Also, if you believe a specific 1.134+ change caused this, a bisect (Help: Start Extension Bisect, or testing an older release) would help narrow it down. This issue will auto-close in 7 days if there's no reply with more detail.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction status is unclear from a single thin report with no traceable code path — needs more information before any fix can be scoped.

## Evidence

- **thin repro:** Steps are 'Run processes in terminal like terraform for example or systemd journal. Occasionally the terminal will flash and the process is killed by oom-kill' — not a deterministic sequence to trace or reproduce.
- **rg process, not VS Code process:** The oom-kill journal line targets `task=rg,pid=419562...total-vm:13676836kB` — this is the ripgrep binary itself (likely invoked by the user in the shell, since the report says 'terraform for example or systemd journal', not vscode's search rg), not a VS Code/Electron/pty-host process; unclear if this is caused by VS Code at all.
- **downstream crash:** main.log shows the shared-process, ptyHost, and extension host all crashed with code 15 (SIGTERM/kill) around the same time as the OOM event, consistent with the OS OOM killer targeting multiple processes under memory pressure rather than a single leaking component.
- **no maintainer or community confirmation:** Issue is 0 days old with no comments; no other report ties this to a specific VS Code regression despite the claim that 'minor changes' since 1.134.0 introduced this.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
