---
number: 291865
url: "https://github.com/microsoft/vscode/issues/291865"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-21T20:48:06.148Z"
itemUpdatedAt: "2026-01-30T13:32:31Z"
itemCreatedAt: "2026-01-30T12:21:37Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: f613672977811fe0760d05f3e1e06864db065a24d77bafcd06f2a046ea582d2b
policyHash: ebd3763732abe97d
---

# Issue #291865 — Source Control panel has severly restricted path that doesn't include local bin

[https://github.com/microsoft/vscode/issues/291865](https://github.com/microsoft/vscode/issues/291865)

## Summary

The report lacks the diagnostic information needed to reproduce: how each VS Code version was launched (Dock vs CLI), the actual PATH values in each, and Developer Tools console output showing whether shell environment resolution succeeded or timed out.

## Change summary

Git hooks (lefthook) run from the Source Control panel in VS Code Insiders cannot find `npx` because the PATH is restricted compared to regular VS Code and the terminal.

## Best solution

Ask the reporter for diagnostic details: launch method (Dock vs CLI), actual PATH values from both VS Code versions (via `echo $PATH` in the integrated terminal), and Developer Tools console output (Help > Toggle Developer Tools) showing shell environment resolution logs. The root cause is almost certainly in how VS Code resolves the user's shell environment on macOS (`src/vs/platform/shell/node/shellEnv.ts`) — whether the login shell spawned to capture PATH succeeded, timed out, or was skipped due to CLI launch detection.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the report. This is likely related to how VS Code resolves your shell environment on macOS — it spawns a login shell to pick up your PATH, and if that process fails or is skipped, tools like `npx` won't be found by git hooks.
  > 
  > To help diagnose this, could you provide:
  > 
  > 1. **How do you launch each VS Code version?** (From the Dock, Spotlight, or the `code`/`code-insiders` CLI command?)
  > 2. **What shell do you use?** (zsh, bash, fish, etc.)
  > 3. **The actual PATH in each version** — open the integrated terminal and run `echo $PATH` in both regular VS Code and Insiders.
  > 4. **Developer Tools console output** — open Help > Toggle Developer Tools > Console tab and look for lines containing `resolveShellEnv` or `getUnixShellEnvironment`.
  > 
  > This will help us determine whether the shell environment resolution is timing out, being skipped, or producing a different result in Insiders.
  > 
  > *Note: issues labeled `info-needed` will be closed automatically after 7 days without a reply.*

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Insufficient diagnostic information to identify a concrete code defect — may be user-configuration or launch-method dependent.

## Evidence

- **shell env resolution:** On macOS, VS Code spawns a login shell to resolve the user's environment (src/vs/platform/shell/node/shellEnv.ts). If this times out, fails, or is skipped (e.g. VSCODE_CLI is set), the PATH will be restricted.
- **git extension env:** extensions/git/src/git.ts line 689 merges process.env into git spawn options — git hooks inherit whatever PATH VS Code's process has.
- **missing diagnostics:** The reporter does not state how either VS Code version was launched, does not provide PATH values, and does not include Developer Tools console output — all essential for diagnosing shell environment resolution issues.
- **recent shell env work:** Multiple recent commits touched shellEnv.ts (e.g. timeout handling, PowerShell fixes, JSON parsing), but without knowing the reporter's shell and launch method, it's unclear if any are related.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
