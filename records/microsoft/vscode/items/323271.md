---
number: 323271
url: "https://github.com/microsoft/vscode/issues/323271"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: none
triageAction: needs-info
confidence: 0.8
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-22T05:47:37.685Z"
itemUpdatedAt: "2026-06-27T16:23:23Z"
itemCreatedAt: "2026-06-27T16:13:28Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: ffc63ead2ac4417e48f5bae919dbe5090689948bfb86e3d03de2b73222eb4368
policyHash: ebd3763732abe97d
---

# Issue #323271 — ENH: Add a setting to write `[credential]` helper to ~/.gitconfig.local

[https://github.com/microsoft/vscode/issues/323271](https://github.com/microsoft/vscode/issues/323271)

## Summary

VS Code does not write a `[credential] helper` entry to `~/.gitconfig` — it uses the `GIT_ASKPASS` environment variable mechanism instead. The credential helper in `~/.gitconfig` is most likely written by Git Credential Manager (GCM), which ships with Git itself, not with VS Code. Requesting more information to clarify which component is actually writing the config.

## Change summary

Request to have VS Code write its git credential helper configuration to a separate file like `~/.gitconfig.local` instead of `~/.gitconfig`.

## Best solution

Clarify the reporter's setup: the `[credential] helper` in `~/.gitconfig` is almost certainly written by Git Credential Manager (a component of Git itself), not by VS Code. If confirmed, this request belongs in the GCM repo (https://github.com/git-ecosystem/git-credential-manager), not VS Code. If the reporter can show VS Code is writing this entry, the issue should be re-evaluated.

## Proposed action

- **Triage action:** needs-info
- **Item type:** feature-request
- **Reproduction:** not_applicable
- **Rating:** not applicable
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** git
- **Confidence:** 0.8
- **Proposed comment:**

  > Thanks for the suggestion! After investigating, VS Code doesn't actually write a `[credential] helper` entry to `~/.gitconfig` — it uses the `GIT_ASKPASS` environment variable to handle credential prompts at runtime, without modifying your git config files.
  > 
  > The `[credential] helper` entry in your `~/.gitconfig` is most likely written by **Git Credential Manager (GCM)**, which ships with Git itself (especially on Windows and macOS). You might want to file this request at https://github.com/git-ecosystem/git-credential-manager instead.
  > 
  > Could you confirm which tool or setup step added the credential helper to your `~/.gitconfig`? If you can show that VS Code is responsible, we'd be happy to re-evaluate.
  > 
  > Note: issues labeled `info-needed` will be auto-closed after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** VS Code does not write credential.helper to gitconfig — the premise appears incorrect; needs clarification from reporter.

## Evidence

- **no credential.helper writes in VS Code:** Searched the entire VS Code codebase (extensions/git/ and src/) for `credential.helper`, `gitconfig`, and `git config --global` — VS Code never writes a credential helper to ~/.gitconfig. The only hit is a completion suggestion in extensions/terminal-suggest/src/completions/git.ts.
- **GIT_ASKPASS mechanism:** VS Code uses the GIT_ASKPASS environment variable (set in extensions/git/src/askpass.ts) to handle credential prompts, not a gitconfig credential helper entry.
- **likely GCM:** Git Credential Manager (bundled with Git for Windows and available for macOS/Linux) is the component that writes `[credential] helper = ...` to ~/.gitconfig during its setup.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
