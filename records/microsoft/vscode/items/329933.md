---
number: 329933
url: "https://github.com/microsoft/vscode/issues/329933"
engine: copilot
owner: meganrogge
labels: tasks, confirmation-pending
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-11T09:26:23.209Z"
itemUpdatedAt: "2026-08-10T16:57:57Z"
itemCreatedAt: "2026-08-10T10:49:08Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: dd430359540cf0e5c5a3573db546d0e5241158cb312585ed5737266dd4f5f99e
policyHash: f13d7795d7aa28b9
---

# Issue #329933 — Tasks with quoting escape does not escape “ and ” character when run under PS

[https://github.com/microsoft/vscode/issues/329933](https://github.com/microsoft/vscode/issues/329933)

## Summary

The report's own sample task uses `"quoting": "strong"`, but the code location it cites (`charsToEscape` in `_buildShellCommandLine`) is only used for `ShellQuoting.Escape` — strong quoting just wraps the value in single quotes with no per-character escaping. So the given repro doesn't clearly hit the cited path, and curly quotes aren't PowerShell string delimiters, so it's unclear what actually breaks; asking the reporter to confirm the exact quoting mode and observed vs. expected output.

## Change summary

Task args containing “ / ” (curly quote) characters aren't escaped correctly when run under PowerShell via VS Code's task quoting.

## Best solution

Have the reporter confirm whether they tested `"quoting": "escape"` (not `"strong"` as shown) and share the actual PowerShell output/error; if the escape path is indeed involved, adding U+201C/U+201D to `charsToEscape` in src/vs/workbench/contrib/tasks/browser/terminalTaskSystem.ts would be the narrow fix, but that isn't established yet from the current repro.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** tasks
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report. Looking at the code, `charsToEscape` (terminalTaskSystem.ts) is only consulted for `"quoting": "escape"`, but your sample task uses `"quoting": "strong"`, which just wraps the value in single quotes without character-level escaping — and curly quotes aren't string delimiters in PowerShell, so it's not obvious yet what's actually going wrong. Could you confirm whether you also reproduced this with `"quoting": "escape"`, and share the exact command VS Code sends to the terminal plus the actual vs. expected output? This issue will auto-close in 7 days if there's no reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** The exact defect and affected code path aren't yet confirmed — the reporter's own sample doesn't exercise the cited escape logic, so a fix can't be scoped until reproduction details are clarified.

## Evidence

- **code path mismatch:** src/vs/workbench/contrib/tasks/browser/terminalTaskSystem.ts, quote() function: ShellQuoting.Strong just wraps the value in shellQuoteOptions.strong (') with no character escaping; charsToEscape (line ~129) is only read under the ShellQuoting.Escape branch. The reporter's sample task uses "quoting": "strong", not "escape".
- **curly quotes not PS delimiters:** PowerShell's parser only recognizes straight ' and " as string delimiters; U+201C/U+201D have no special meaning inside a single-quoted string, so it's unclear why strong-quoting would need to escape them.
- **freshness:** Issue opened 0 days ago, no comments yet, already carries confirmation-pending — no maintainer or community confirmation of the exact failure mode.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
