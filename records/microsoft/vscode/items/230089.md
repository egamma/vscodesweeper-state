---
number: 230089
url: "https://github.com/microsoft/vscode/issues/230089"
engine: codex
owner: alexdima, rzhao271
proposedLabel: none
triageAction: keep-open
confidence: 0.72
reviewedAt: "2026-06-26T17:45:22.650Z"
contentHash: cdce608caf36fd08301bb5028b347ac845299a6b47db2ed2b9fa5e64d729f768
policyHash: 17b7468985688190
---

# Issue #230089 — `editor.fontLigatuers`  doesn't wotk if the string is long

[https://github.com/microsoft/vscode/issues/230089](https://github.com/microsoft/vscode/issues/230089)

## Summary

Keep this open for font-rendering confirmation. The checked code path appears to pass the full `editor.fontLigatures` string through to CSS without an obvious VS Code-side length cap, so there is not enough evidence for a cleanup close.

## Change summary

A long `editor.fontLigatures` OpenType feature string for Iosevka stops taking effect, while a shorter feature string works.

## Best solution

Have the font-rendering owner confirm whether the generated editor DOM retains the full `font-feature-settings` value; if it does, the remaining failure likely belongs in the Chromium/Skia font stack rather than a VS Code string-length change.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Proposed label:** none
- **Area labels:** font-rendering
- **Confidence:** 0.72
- **Proposed comment:** none

## Evidence

- **issue state:** https://github.com/microsoft/vscode/issues/230089 is open, authored by a non-member, has no milestone, and carries `bug`, `font-rendering`, and `confirmation-pending`; there is no linked duplicate, fix PR, or maintainer resolution in the provided context.
- **validation path:** `src/vs/editor/common/config/editorOptions.ts` `EditorFontLigatures.validate` returns non-empty string inputs unchanged, except for the literal strings `true` and `false`; no length threshold is visible there.
- **font info path:** `src/vs/editor/common/config/fontInfoFromSettings.ts` reads `EditorOption.fontLigatures` into `fontFeatureSettings`, and `src/vs/editor/common/config/fontInfo.ts` stores that value directly in `BareFontInfo`; no truncation is visible in that path.
- **DOM application path:** `src/vs/editor/browser/config/domFontInfo.ts` assigns `fontInfo.fontFeatureSettings` to `style.fontFeatureSettings`, and `src/vs/base/browser/fastDomNode.ts` does the same in `setFontFeatureSettings`; this points to browser/font-engine behavior if the CSS value is preserved.
- **history check:** A read-only `git log --all --grep='fontLigatures\|font ligatures\|font-feature-settings\|fontFeatureSettings'` did not reveal a concrete fixing commit, so an implemented-on-main close is not supported.

---
Reviewed by vscodesweeper (codex) — proposal only, not applied.
