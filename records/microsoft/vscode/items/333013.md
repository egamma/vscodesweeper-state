---
number: 333013
url: "https://github.com/microsoft/vscode/issues/333013"
engine: copilot
owner: bryanchen-d
labels: bug, error-telemetry, recent-regression, stable-anomaly, ai-generated, *fix
proposedLabel: none
closeReason: none
triageAction: route-to-area
confidence: 0.6
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-27T19:01:21.458Z"
itemUpdatedAt: "2026-08-27T18:49:01Z"
itemCreatedAt: "2026-08-27T18:45:35Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: e9474307af5488da6e0001ffaf622ff0e5a433136969921de3c0cc577b41cabb
policyHash: 3d6943c3a15c721c
---

# Issue #333013 — [Error] unhandlederror-View already awaiting revival

[https://github.com/microsoft/vscode/issues/333013](https://github.com/microsoft/vscode/issues/333013)

## Summary

This is a regression/incomplete-fix follow-up to the closed https://github.com/microsoft/vscode/issues/320623 — the prior fix in `webviewViewService.ts`/`webviewViewPane.ts` only clears the pending `_awaitingRevival` entry when the webview pane's `dispose()` callback runs `_webviewDisposables.clear()`, but current main still throws `View already awaiting revival` whenever `resolve()` is called a second time for the same viewType while an earlier pending entry hasn't been cancelled through that path (e.g. a re-activation that doesn't route through the dispose callback). No fix commit exists for this new occurrence, so it can't be closed as implemented-on-main.

## Change summary

Auto-generated error-telemetry report: a stable-release regression where the webview view revival guard still throws for 54 users on 1.135.0, matching a previously 'fixed' bucket.

## Best solution

Have the webview view owners re-examine the reentrancy path in src/vs/workbench/contrib/webviewView/browser/webviewViewPane.ts (`activate()`/`_activated` reset via the `dispose()` callback) and src/vs/workbench/contrib/webviewView/browser/webviewViewService.ts (`resolve()`/`_awaitingRevival`) to find the remaining path where a second `resolve()` call for the same viewType still sees a stale pending entry, since the 1.132.0 fix (per #320623) evidently doesn't cover every re-activation trigger.

## Proposed action

- **Triage action:** route-to-area
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 4/6 Traced
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** webview
- **Confidence:** 0.6
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** The remaining reentrancy path that still triggers the throw isn't pinned down precisely enough (which caller re-activates without going through the `dispose()`/cancellation path) to write a safe, validated fix without further investigation.

## Evidence

- **prior fix present but incomplete:** src/vs/workbench/contrib/webviewView/browser/webviewViewService.ts lines 128-160 show the cancellation-cleanup logic (cancellation listener deletes `_awaitingRevival` entry) that was clearly added to address 'View already awaiting revival', yet the throw at line 132 (`if (this._awaitingRevival.has(viewType)) { throw new Error('View already awaiting revival'); }`) is still reachable.
- **re-activation path:** webviewViewPane.ts `activate()` (lines 161-238) only cancels the previous CancellationTokenSource via `_webviewDisposables.clear()` inside the webviewView `dispose()` callback (line 229) — `_webviewDisposables` is a single, never-reset DisposableStore, so a re-activation that doesn't go through this exact dispose callback would leave the earlier pending `_awaitingRevival` entry uncancelled, reproducing the throw.
- **prior closed issue:** https://github.com/microsoft/vscode/issues/320623 was closed as completed with a fix shipped in 1.132.0; this new report (1.135.0, 54 users, same stack) is flagged by the bot itself as a likely regression or incomplete fix.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
