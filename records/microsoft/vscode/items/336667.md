---
number: 336667
url: "https://github.com/microsoft/vscode/issues/336667"
engine: copilot
owner: roblourens
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-19T12:39:16.052Z"
itemUpdatedAt: "2026-09-18T01:44:29Z"
itemCreatedAt: "2026-09-18T01:35:03Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 3efdd38820edbc928a59eb8d95586020912f8a6dc6531e82b1fbe6b74bc471c1
policyHash: fd90e4d8a068f279
---

# Issue #336667 — Codex / ChatGPT sign-in is missing from Agents Window after enabling documented settings

[https://github.com/microsoft/vscode/issues/336667](https://github.com/microsoft/vscode/issues/336667)

## Summary

The Codex Agent Host sign-in path exists and, per the code, should surface once `chat.agentHost.codexAgent.enabled` is on — but I can't confirm from source alone whether this report reflects a real defect or an environment-specific gap (SDK distribution, org policy, or an agent-host restart that didn't actually happen), so more diagnostic detail is needed before this can be triaged further.

## Change summary

After enabling the documented settings for signed-out Codex/ChatGPT usage in the Agents Window, no 'Sign in to ChatGPT' option or Codex entry appears in the session/account picker.

## Best solution

Ask the reporter for Agent Host logs (Output > Agent Host) and confirm whether the Codex SDK actually downloaded/registered, since `chat.agentHost.codexAgent.enabled` defaults to off in Stable builds and Codex registration also depends on `product.agentSdks.codex`/policy gating (`Codex3PIntegration`) that isn't visible from this checkout — a maintainer with access to production telemetry or the reporter's Agent Host log can pin down whether this is a genuine visibility bug in `shouldShowCodexAccount`/`codexAccountService.ts` or an SDK/policy issue outside this repo's OSS source.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed report. The sign-in path you're describing does exist in the code (`codexAccountService.ts`'s `shouldShowCodexAccount`/`createCodexAccountMenuActions`), so this looks like it should work once `chat.agentHost.codexAgent.enabled` is set — but I can't tell from here whether the Codex SDK actually registered in your build. Could you check the "Agent Host" output channel (or `Developer: Show Agent Host Log`) after enabling the setting and share what it shows for Codex provider registration, and confirm whether you're on the same behavior on Insiders? This helps rule out an SDK-distribution or policy issue versus a UI bug. This issue will auto-close in 7 days if there's no reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** Reproduction status is unclear — no confirmed defect in source, and the likely causes span outside this OSS checkout (SDK distribution, policy), so nothing to hand to an agent yet.

## Evidence

- **visibility gate:** src/vs/workbench/services/agentHost/browser/codexAccountService.ts:51 `shouldShowCodexAccount` only requires `chat.agentHost.codexAgent.enabled` (for the Agents/sessions window) plus AI not disabled — matching the documented setup, so the reported gap isn't obviously explained by that gate alone.
- **stable default:** src/vs/platform/agentHost/common/agentHostStarter.config.contribution.ts:289 shows `chat.agentHost.codexAgent.enabled` defaults to `product.quality !== 'stable'` (off by default in Stable) and is also gated by the `Codex3PIntegration` policy — an org policy or an SDK not yet distributed for this Stable build (`product.agentSdks.codex`, not present in this OSS checkout) could silently prevent Codex from registering even with the setting enabled.
- **sign-in action exists:** createCodexAccountMenuActions in the same file does produce a 'Sign in to ChatGPT' action when the account status is signedOut/unknown/error, confirming the UI path is implemented and should appear when the account service reports a non-signed-in Codex account.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
