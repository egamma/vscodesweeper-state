---
number: 332442
url: "https://github.com/microsoft/vscode/issues/332442"
engine: copilot
owner: benvillalobos
labels: ""
proposedLabel: "*not-reproducible"
closeReason: not-reproducible
triageAction: propose-close
confidence: 0.8
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-25T08:51:03.319Z"
itemUpdatedAt: "2026-08-24T23:08:08Z"
itemCreatedAt: "2026-08-24T22:51:47Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: c772de6703ab000fc84a71ac51a69b2291b2210d1d1a190d30275401839d7e57
policyHash: 3d6943c3a15c721c
---

# Issue #332442 — Sessions: deleting a session with a worktree should also delete the worktree

[https://github.com/microsoft/vscode/issues/332442](https://github.com/microsoft/vscode/issues/332442)

## Summary

Traced the described deletion path on current main and the worktree cleanup already happens: this report's premise doesn't hold against the current source.

## Change summary

Requests that deleting a session with an active worktree also delete the worktree, mirroring the cleanup that already happens on archive.

## Best solution

No action needed — `AgentService._doDisposeSession` (src/vs/platform/agentHost/node/agentService.ts) already resolves and force-removes the worktree via `IAgentHostWorktreeIsolation.prepareSessionDeletion`/`removeSessionWorktree` before the session is torn down; close as not reproducible against current main.

## Proposed action

- **Triage action:** propose-close
- **Item type:** bug
- **Reproduction:** not_reproduced
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *not-reproducible
- **Area labels:** none
- **Confidence:** 0.8
- **Proposed comment:**

  > Thanks for the report. I traced the deletion path on current `main` and this already works: `AgentService._doDisposeSession` (src/vs/platform/agentHost/node/agentService.ts) resolves the session's worktree via `IAgentHostWorktreeIsolation.prepareSessionDeletion` and force-removes it via `removeSessionWorktree` (which calls `IGitService.removeWorktree(..., { force: true })` in src/vs/platform/agentHost/node/shared/worktreeIsolation.ts) before the session data is torn down. This runs for every session deletion, not just archive, so no worktree should be left behind. Please reopen with repro steps if you can still reproduce an orphaned worktree on the latest build.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Source tracing shows the requested cleanup is already implemented on current main — nothing to fix.

## Evidence

- **deletion path already cleans up worktree:** src/vs/platform/agentHost/node/agentService.ts `_doDisposeSession` (around line 3666) calls `const worktree = await this._worktree.prepareSessionDeletion(session, sessionId);` and later (line 3694) `await this._worktree.removeSessionWorktree(sessionId, worktree);` — this runs on every session deletion, not just archive.
- **removal implementation:** src/vs/platform/agentHost/node/shared/worktreeIsolation.ts `_removeSessionWorktree` (line 845) calls `this._gitService.removeWorktree(worktree.repositoryRoot, worktree.worktree, { force: true })`, force-deleting the worktree directory on disk.
- **issue's premise vs. code:** The issue only inspected the archive/unarchive side-effect handler (`SessionIsArchivedChanged` in agentSideEffects.ts) and assumed deletion had no equivalent path; a dedicated deletion-specific worktree cleanup already exists and is wired into `disposeSession`.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
