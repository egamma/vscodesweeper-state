---
number: 329764
url: "https://github.com/microsoft/vscode/issues/329764"
engine: copilot
owner: lramos15, justschen
labels: ""
proposedLabel: "*question"
closeReason: question
triageAction: propose-close
confidence: 0.82
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-12T09:30:58.996Z"
itemUpdatedAt: "2026-08-11T14:46:14Z"
itemCreatedAt: "2026-08-08T15:06:02Z"
upvotes: 0
reactionsTotal: 0
commentCount: 4
relatedOpenIssues: 0
milestone: none
contentHash: f8b595d475a395eedaac07893b075b26cd9a7b1eac233d9e32258d0ff2032f64
policyHash: f13d7795d7aa28b9
currentState: closed
closedAt: "2026-08-13T20:29:46Z"
stateReason: completed
closedBy: lramos15
reconciledAt: "2026-08-14T09:39:28.747Z"
proposedCommentUsed: none
---

# Issue #329764 — [regression] GitHub Copilot chat up arrow deletes current chat content.

[https://github.com/microsoft/vscode/issues/329764](https://github.com/microsoft/vscode/issues/329764)

## Summary

Not a VS Code defect: PR #328269 intentionally removed the redundant `not('isComposing')` clause from the built-in `history.showPrevious`/`showNext` keybinding `when` clauses (composing keystrokes now normalize to `KEY_IN_COMPOSITION` earlier in the pipeline, so the guard was dead code). Because keybinding negation overrides must match the built-in `when` clause exactly, the reporter's existing override (which still includes `!isComposing`) no longer matches and stops suppressing the command — this is expected keybinding-override mechanics, not a regression in behavior, and the reporter has already correctly worked out the fix (drop `!isComposing` from their override).

## Change summary

Reporter's custom keybindings.json override that disables Up/Down history navigation in chat stopped working after a VS Code update changed the built-in `when` clause it needs to match exactly.

## Best solution

Confirm for the reporter that the `when` clause changed deliberately in https://github.com/microsoft/vscode/pull/328269 (isComposing guard removed as redundant) and that they should update their `keybindings.json` overrides to drop `!isComposing`, matching the new built-in clause exactly — no core change needed.

## Proposed action

- **Triage action:** propose-close
- **Item type:** question
- **Reproduction:** not_applicable
- **Rating:** not applicable
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *question
- **Area labels:** keybindings
- **Confidence:** 0.82
- **Proposed comment:**

  > Thanks for the detailed writeup. This traces back to https://github.com/microsoft/vscode/pull/328269, which intentionally removed the `!isComposing` clause from the built-in `history.showPrevious`/`history.showNext` keybindings — it had become dead code once composing keystrokes are normalized to `KEY_IN_COMPOSITION` earlier in the pipeline. Keybinding overrides need to match the built-in `when` clause exactly to suppress it, so your existing override (which still includes `!isComposing`) no longer disables the command. You've already found the right answer: drop `!isComposing` from your override so it reads `historyNavigationBackwardsEnabled && historyNavigationWidgetFocus && !suggestWidgetVisible` (and the same for `showNext`). I'll close this as a question since it's not a product defect, but flagging it here so the discussion is documented for anyone else hitting the same stale-override issue.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Not a code defect — the built-in when-clause change was deliberate and the user-side fix is a keybindings.json update, not a VS Code fix.

## Evidence

- **PR that changed the when clause:** https://github.com/microsoft/vscode/pull/328269 ('Suppress keybindings and filtering during IME composition', merged 2026-07-30) removed the `ContextKeyExpr.not('isComposing')` clause from src/vs/platform/history/browser/contextScopedHistoryWidget.ts (diff shows 2 deletions in that file) because composing keystrokes are now normalized to `KeyCode.KEY_IN_COMPOSITION` earlier (in StandardKeyboardEvent), making the per-command guard dead code.
- **current main when clause:** src/vs/platform/history/browser/contextScopedHistoryWidget.ts currently registers history.showPrevious/showNext with `when: HistoryNavigationWidgetFocusContext && HistoryNavigationBackwardsEnablementContext==true && suggestWidgetVisible==false` — no `isComposing` term, matching the reporter's own observation.
- **maintainer repro attempt:** lramos15 (MEMBER) tried the up/down history flow without a custom override and found it behaves as expected — the actual issue only manifests for users with a stale custom override from https://github.com/microsoft/vscode/issues/276373 whose `when` string no longer matches.
- **keybinding override mechanics:** VS Code keybinding negation (`-command` overrides) require the `when` clause to match the built-in rule's `when` string exactly; a partial/stale `when` silently fails to suppress the command, which is the root of the reporter's confusion, not a product defect.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
