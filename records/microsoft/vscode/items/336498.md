---
number: 336498
url: "https://github.com/microsoft/vscode/issues/336498"
engine: copilot
owner: roblourens, connor4312
labels: ""
proposedLabel: bug
closeReason: none
triageAction: keep-open
confidence: 0.82
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: plan
reviewedAt: "2026-09-19T20:02:05.777Z"
itemUpdatedAt: "2026-09-16T22:41:58Z"
itemCreatedAt: "2026-09-16T22:41:58Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 1
milestone: none
contentHash: ac609b3898228daca91680a4dbe590886eadacf366e106a78eb52102d9d0261d
policyHash: e8f657b9f60e3480
---

# Issue #336498 — Copilot asks me to run `/mcp auth`

[https://github.com/microsoft/vscode/issues/336498](https://github.com/microsoft/vscode/issues/336498)

## Summary

Keep open: the team-authored report directly captures Copilot asking the user to run CLI-only `/mcp auth` commands instead of exposing the Agent Host authentication flow. Current source has a structured MCP-authentication path, but the WorkIQ failure is not reaching it, and no issue-referencing fix establishes completion.

## Change summary

When WorkIQ MCP connections are unauthenticated, Copilot asks the user to run `/mcp auth workiq` and `/mcp auth workiq-me` rather than presenting a usable VS Code authentication action.

## Best solution

Ensure WorkIQ authentication requirements reach the existing structured Agent Host MCP-authentication path so VS Code presents an actionable sign-in request and retries the blocked tool call, without instructing users to run unsupported CLI slash commands.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** reproduced
- **Evidence:** 6/6 Confirmed
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** bug
- **Area labels:** mcp
- **Confidence:** 0.82
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** plan
- **Rationale:** The user-visible failure is confirmed, but diagnosis must first determine why WorkIQ does not produce or reach the existing structured MCP-authentication callback.

## Plan brief

- **Likely files:** `src/vs/platform/agentHost/node/copilot/copilotAgentSession.ts`, `src/vs/platform/agentHost/node/chatContributions/sessionInputNeeded/sessionInputNeededContribution.ts`, `src/vs/platform/agentHost/test/node/copilotAgentSession.test.ts`
- **Validation:** Add an Agent Host test covering an unauthenticated WorkIQ MCP tool call and assert that it produces a structured `ChatToolCallAuthRequired`/`ToolAuthentication` request, completes after credentials are supplied, and never falls back to asking the user to run `/mcp auth`.
- **Behavior:**

  > When a WorkIQ MCP tool needs authentication, VS Code presents an actionable authentication request rather than having Copilot ask the user to run `/mcp auth`. Completing authentication retries or unblocks the affected tool call; cancelling it reports cancellation without an unusable command suggestion. Existing successful MCP connections remain unchanged.
- **Trace:**

  > The issue screenshot confirms that unauthenticated `workiq` and `workiq-me` connections currently surface as a model-generated input request containing CLI commands. Current main already handles SDK `McpAuthRequest` callbacks in `CopilotAgentSession._handleMcpAuthRequest`, emits `ChatToolCallAuthRequired`, and mirrors that through `SessionInputNeededContribution` as `ToolAuthentication`. The uncertainty begins before that handler: establish whether the bundled Copilot runtime emits an MCP auth request for WorkIQ and, if it does, where the event loses its active tool-call association. https://github.com/microsoft/vscode/issues/336555 is related evidence for a runtime-level authentication fallback but is newer and concerns GitLab, so do not consolidate the issues without proving the same observable path and root cause.

## Evidence

- **direct observation:** The screenshot on https://github.com/microsoft/vscode/issues/336498 shows Copilot reporting both `workiq` and `workiq-me` as unauthenticated and asking the user to run `/mcp auth` commands.
- **current source path:** src/vs/platform/agentHost/node/copilot/copilotAgentSession.ts implements `_handleMcpAuthRequest`, which publishes `McpServerStatus.AuthRequired` and emits `ChatToolCallAuthRequired`; src/vs/platform/agentHost/node/chatContributions/sessionInputNeeded/sessionInputNeededContribution.ts mirrors that state as a `ToolAuthentication` input request. The screenshot indicates this structured path was not used for the reported WorkIQ request.
- **related but not duplicate:** https://github.com/microsoft/vscode/issues/336555 independently reports the same `/mcp auth` fallback text for a GitLab MCP server, but it was filed after this re-reviewed issue and concerns a different server and diagnosed OAuth challenge path, so it cannot serve as the canonical duplicate.
- **protected authorship:** The issue was filed by a repository member. There is no issue-referencing fix commit or verified older canonical issue that would permit a factual close under the team-authored-item rule.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
