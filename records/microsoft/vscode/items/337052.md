---
number: 337052
url: "https://github.com/microsoft/vscode/issues/337052"
engine: copilot
owner: TylerLeonhardt
labels: agent-host
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-22T13:25:41.677Z"
itemUpdatedAt: "2026-09-21T18:38:29Z"
itemCreatedAt: "2026-09-21T14:44:20Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: f26e1aa1f6583085d84242f9a90ec4d73c441e0e70b1f76bda4eb2450f7fb140
policyHash: d13126680f5ee38a
---

# Issue #337052 — Copilot MCP OAuth corrupts dynamically registered client ID and repeatedly prompts for sign-in

[https://github.com/microsoft/vscode/issues/337052](https://github.com/microsoft/vscode/issues/337052)

## Summary

Detailed, well-diagnosed report of a corrupted MCP OAuth client_id/resource in VS Code's agent-host handoff, but I can't confirm the defect from the open checkout — the request-building logic (`_handleMcpAuthRequest`/`_protectedResourceFromMcpAuthRequest` in copilotAgentSession.ts) only forwards `staticClientConfig`/`resourceMetadata` values that originate in the bundled Copilot CLI runtime, which isn't part of this repo's source. No independent confirmation yet, so this should stay open pending the logs the reporter offered.

## Change summary

VS Code's Copilot agent-host MCP OAuth flow sends a malformed client_id ("qw4+v") and falls back to the authorization-server root resource instead of the path-specific resource/registered client id, causing repeated failed sign-in prompts for a remote HTTP MCP server.

## Best solution

Have the reporter (or a maintainer) capture the sanitized agent-host debug logs covering the MCP discovery → dynamic-registration → authorization-request sequence so the handoff in copilotAgentSession.ts's `_handleMcpAuthRequest`/`_protectedResourceFromMcpAuthRequest` (and whatever populates `request.staticClientConfig`/`request.resourceMetadata` in the bundled Copilot CLI runtime) can be compared against the values the reporter captured; fix wherever the client id/resource gets overwritten between registration and the browser redirect.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** agent-host
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the very thorough writeup and diagnostics — this is a strong isolation of the problem to VS Code's agent-host MCP auth handoff rather than the ECS server or the Copilot CLI itself.
  > 
  > To help trace this further, could you capture the agent-host debug logs covering the sequence from MCP discovery through dynamic client registration to the authorization request (sanitized, without tokens/codes/cookies) and attach or share them here? That will let us compare the registered client id/resource against what actually goes out in the browser request.
  > 
  > Note this issue will auto-close in 7 days if there's no follow-up, so please reply with the logs when you can.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** Root cause not confirmed from source — the likely mangling point sits in the bundled Copilot CLI runtime's OAuth handling, which isn't traceable in this repo; needs a reproduction/log trace before any fix can be scoped.

## Evidence

- **code path traced:** src/vs/platform/agentHost/node/copilot/copilotAgentSession.ts:2575-2689 (`_handleMcpAuthRequest`, `_protectedResourceFromMcpAuthRequest`) builds the `resource` and `oauthClient` fields surfaced to VS Code from `request.resourceMetadata` and `request.staticClientConfig`/`request.wwwAuthenticateParams`, which are supplied by the Copilot CLI SDK/runtime — not visible in this checkout — so the actual mangling point (registration → authorization-request) can't be confirmed from source alone.
- **reporter diagnostics:** Reporter isolated the failure to VS Code's handoff by showing the standalone Copilot CLI (same MCP config) succeeds and stays authenticated, while VS Code fails with both the bundled 1.0.84-4 and newer 1.0.87-0 runtimes — a strong signal but not independently reproduced by anyone else or traceable in this repo's code.
- **offer to share logs:** Reporter offered to share local protocol/authentication logs privately but hasn't done so yet; comment 2 shows they need guidance on what to collect.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
