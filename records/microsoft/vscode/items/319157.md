---
number: 319157
url: "https://github.com/microsoft/vscode/issues/319157"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.78
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-22T05:47:31.003Z"
itemUpdatedAt: "2026-06-27T14:15:14Z"
itemCreatedAt: "2026-05-30T16:46:57Z"
upvotes: 0
reactionsTotal: 0
commentCount: 3
relatedOpenIssues: 0
milestone: none
contentHash: 1561ff12ca17fe4bac66be894db586925e83a3ccd0e030e9e423ff7dbb47e0b9
policyHash: ebd3763732abe97d
---

# Issue #319157 — Git auto fetch is not working on Linux

[https://github.com/microsoft/vscode/issues/319157](https://github.com/microsoft/vscode/issues/319157)

## Summary

The report lacks the detail needed to diagnose: no Git output log, no confirmation whether extensions were disabled, no indication of display server (Wayland vs X11) or network/metered-connection state. Requesting info so the reporter can supply actionable reproduction steps.

## Change summary

Git auto fetch does not periodically run on Linux, requiring manual fetch to see remote changes.

## Best solution

Ask the reporter for the Git output channel log (`Output > Git`), whether `git.autofetch` is explicitly set to `true` in their settings, whether the issue reproduces with extensions disabled, and whether the system reports a metered connection (since `fa6cfe12c69` added a metered-connection guard that disables auto fetch). The `whenIdleAndFocused` gate in `extensions/git/src/repository.ts:3205` also blocks fetch until the window is focused — Wayland compositors sometimes fail to report focus correctly, which could stall auto fetch indefinitely.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git
- **Confidence:** 0.78
- **Proposed comment:**

  > Thanks for the report. To narrow this down, could you share a few details?
  > 
  > 1. **Git output log** — open the Output panel (`View > Output`) and select **Git** from the dropdown, then share what it shows after waiting a few minutes with the window focused.
  > 2. **Settings check** — confirm that `git.autofetch` is set to `true` (not `false` or unset) in your User settings (`Preferences: Open User Settings (JSON)`).
  > 3. **Extensions disabled** — does the issue reproduce when you launch with `code --disable-extensions`?
  > 4. **Display server** — are you using Wayland or X11? (`echo $XDG_SESSION_TYPE` in a terminal.)
  > 5. **Metered connection** — does your NetworkManager report the connection as metered? (Check `nmcli -t -f GENERAL.METERED dev show` — a recent VS Code change disables auto fetch on metered connections.)
  > 
  > This additional context will help us identify whether this is a focus-detection issue, a metered-connection false positive, or something else. Note that issues marked `info-needed` are automatically closed after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Insufficient reproduction information — cannot confirm a defect to fix.

## Evidence

- **metered-connection guard:** Commit fa6cfe12c69 (Feb 2026) added `env.isMeteredConnection` check to `autofetch.ts:71` that silently disables auto fetch on metered connections — could affect Linux NetworkManager setups that report metered status.
- **window focus gate:** `whenIdleAndFocused()` at extensions/git/src/repository.ts:3205 blocks auto fetch until `window.state.focused` is true; on some Linux display servers (especially Wayland), focus reporting can be unreliable.
- **missing reproduction detail:** Reporter did not answer whether the issue occurs with extensions disabled, provided no Git output log, and did not specify X11 vs Wayland or network configuration.
- **no other confirmation:** No other user has confirmed the issue; one commenter asked for updates but did not confirm the same behavior.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
