---
number: 328390
url: "https://github.com/microsoft/vscode/issues/328390"
engine: copilot
owner: connor4312
labels: ""
proposedLabel: bug
triageAction: keep-open
confidence: 0.96
severity: freeze
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-08-01T14:13:28.809Z"
itemUpdatedAt: "2026-07-31T11:30:11Z"
itemCreatedAt: "2026-07-31T11:30:11Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 1
milestone: none
contentHash: e012de6b1b2acf78e94c93c00a7c649f154038dfce9273de15052b91e1676f8f
policyHash: ff6556e82b018d01
---

# Issue #328390 — Agent Host session can remain stuck on Working after config-driven refresh

[https://github.com/microsoft/vscode/issues/328390](https://github.com/microsoft/vscode/issues/328390)

## Summary

The deadlock remains source-reproducible on current main: config refresh awaits SDK disconnect without first cancelling pending guarded callbacks. Keep this team-authored issue open for the localized Agent Host fix.

## Change summary

Cancel pending Agent Host interactions before disconnecting a config-stale Copilot SDK session.

## Best solution

Call `_beginAbort()` at the start of `CopilotAgentSession.destroySession()` and add a regression test proving a pending permission request is rejected before SDK disconnect begins.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** freeze
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** bug
- **Area labels:** agent-host
- **Confidence:** 0.96
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** The defect is a localized teardown-ordering bug with a deterministic mock-session regression test.
- **Likely files:** `src/vs/platform/agentHost/node/copilot/copilotAgentSession.ts`, `src/vs/platform/agentHost/test/node/copilotAgentSession.test.ts`
- **Validation:** Add an Agent Host unit test that leaves `runtime.handlePermissionRequest(...)` pending, calls `session.destroySession()`, and asserts from the mock SDK disconnect hook that the request has already resolved to `{ kind: 'reject' }`.
- **Fix prompt:**

  > Fix https://github.com/microsoft/vscode/issues/328390 in `CopilotAgentSession.destroySession()`. Current main flushes edit attribution and then awaits SDK disconnect without calling `_beginAbort()`, while abort and dispose use `_beginAbort()` to cancel all guarded interactions. Invoke `_beginAbort()` before attribution flushing and disconnect so pending permission, input, authentication, elicitation, planning, command, and client-tool callbacks settle before the SDK teardown starts; preserve the existing flush error handling and disconnect semantics. Extend the existing Agent Host session tests with a mock disconnect hook that observes a pending permission callback and verifies it has already resolved to `{ kind: 'reject' }` when disconnect begins. The regression test must fail with the current ordering and pass after the narrow fix.

## Evidence

- **current main:** At GitHub main commit 7234ef01c2cace7cfa911d792ce9c5b1f333fca5, src/vs/platform/agentHost/node/copilot/copilotAgentSession.ts calls `flushAttribution()` and then `session.disconnect()` in `destroySession()` without calling `_beginAbort()`.
- **blocking flow:** src/vs/platform/agentHost/node/copilot/copilotAgent.ts awaits `entry.destroySession()` inside the per-session sequencer when `requiresRestart(...)` is true, so a blocked disconnect prevents resume and subsequent queued operations.
- **existing cancellation behavior:** In copilotAgentSession.ts, both `abort()` and `dispose()` call `_beginAbort()`, which cancels the token and invokes `_cancelAllPendingInteractions()`; `destroySession()` is the uncovered teardown path.
- **test gap:** src/vs/platform/agentHost/test/node/copilotAgentSession.test.ts tests pending permission rejection on abort and dispose but has no corresponding destroy-session ordering test.
- **no competing fix:** https://github.com/microsoft/vscode/issues/328390 is open with no linked or searchable open fixing pull request.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
