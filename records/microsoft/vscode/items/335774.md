---
number: 335774
url: "https://github.com/microsoft/vscode/issues/335774"
engine: copilot
owner: hediet
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-17T13:27:08.188Z"
itemUpdatedAt: "2026-09-16T16:49:12Z"
itemCreatedAt: "2026-09-11T16:05:28Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 1
milestone: none
contentHash: e4830f0d5f05e91144bbaa11960ea5026687a9111d6bcce6744eb65d54ec0768
policyHash: 5fa429198b8c8ed6
---

# Issue #335774 — Inline suggestions (ghost text) never trigger for documents from third-party FileSystemProvider schemes (e.g. Code for IBM i "member:" scheme)

[https://github.com/microsoft/vscode/issues/335774](https://github.com/microsoft/vscode/issues/335774)

## Summary

Traced the ghost-text registration path on main and didn't find the scheme allowlist the reporter theorizes — the provider is registered with `{ pattern: '**' }` and the only scheme-based logic is a small denylist (`output`, `search-editor`, `comment`, `git`, `chat-editing-snapshot-text-model`) that doesn't include custom schemes like `member:`. Since I can't reproduce the actual failure without the third-party FileSystemProvider extension live, this needs more diagnostic info from the reporter before it can be confirmed as a core defect.

## Change summary

Copilot inline suggestions (ghost text) never trigger for documents opened via a third-party FileSystemProvider scheme (e.g. Code for IBM i's `member:` scheme), even though the document is a valid, editable TextDocument with a correct language ID.

## Best solution

If reproducible, the fix likely belongs in extensions/copilot/src/extension/completions-core/vscode-node/extension/src/textDocumentManager.ts (the `ignoreUriSchemes` denylist) or wherever document eligibility is actually gated before a completion request is issued — but that gate wasn't found in the code paths inspected here, so the next step is collecting Copilot debug logs / dev tools errors from the reporter to locate where the request is actually being dropped.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed report. I looked through the current Copilot ghost-text registration path on main and didn't find the scheme allowlist you describe — the inline completion provider is registered against `{ pattern: '**' }`, and the only scheme-based filtering I could find is a small denylist (`output`, `search-editor`, `comment`, `git`, `chat-editing-snapshot-text-model`) that doesn't include custom schemes like `member:`. That means the silent drop you're seeing likely happens somewhere else in the pipeline that I couldn't trace from source alone.
  > 
  > Could you help narrow it down further? A few things that would help:
  > - Enable verbose Copilot logging (`github.copilot.advanced.debug.overrideLogLevels` or similar) and share the relevant `GitHub Copilot Chat` output for a member: document vs. a local file.
  > - Check the Help > Toggle Developer Tools console for any errors when typing in the member: document.
  > - Try reproducing with another custom-scheme document (e.g. a `vscode-remote://` or another virtual FS provider) to see if this is scheme-agnostic or specific to the `member:` provider's implementation (e.g. missing FileSystemProvider capabilities).
  > 
  > This issue will auto-close in 7 days if there's no follow-up, but please add the requested details any time and it'll get another look.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Could not confirm the defect or its location in source — the reporter's theorized scheme allowlist doesn't match what's actually in the code, so more diagnostic info is needed before any fix can be scoped.

## Evidence

- **provider registration:** extensions/copilot/src/extension/completions/vscode-node/completionsCoreContribution.ts registers the inline completion provider with `{ pattern: '**' }`, not scoped to specific schemes.
- **scheme denylist:** extensions/copilot/src/extension/completions-core/vscode-node/extension/src/textDocumentManager.ts defines `ignoreUriSchemes = new Set(['output','search-editor','comment','git','chat-editing-snapshot-text-model'])` — `member:` is not in this list.
- **enablement check:** extensions/copilot/src/extension/completions-core/vscode-node/extension/src/config.ts `isCompletionEnabledForDocument` only special-cases `Schemas.vscodeChatInput`; otherwise it's purely a per-language enable check, no scheme gate.
- **related tracker issue:** https://github.com/codefori/vscode-ibmi/issues/3444 tracks the same symptom from the extension side but doesn't pinpoint a root cause either.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
