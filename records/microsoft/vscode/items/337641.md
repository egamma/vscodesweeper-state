---
number: 337641
url: "https://github.com/microsoft/vscode/issues/337641"
engine: copilot
owner: meganrogge
labels: bug, agents-window
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: plan
reviewedAt: "2026-09-25T13:44:45.694Z"
itemUpdatedAt: "2026-09-24T20:56:54Z"
itemCreatedAt: "2026-09-24T07:20:56Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: bb63b01533514a0fbf8a188dbf256dfbbb8d63714b305d75bf910b5c63e2229f
policyHash: d13126680f5ee38a
---

# Issue #337641 — Cannot click on "Chat" in the New Session workspace picker

[https://github.com/microsoft/vscode/issues/337641](https://github.com/microsoft/vscode/issues/337641)

## Summary

Team-authored bug report (video only, no text repro) about the 'Chat' entry in the New Session workspace picker being unclickable; already correctly labeled `bug`/`agents-window`, so this simply stays open — team-authored items are protected from judgment closes and there's no fix commit referencing this issue number or a verified duplicate.

## Change summary

In the New Session workspace picker, clicking the 'Chat' (no-workspace) entry does not do anything.

## Best solution

Investigate the no-workspace/'Chat' picker item in src/vs/sessions/contrib/chat/browser/sessionWorkspacePicker.ts and newChatWidget.ts's `_getNoWorkspaceOption` — in particular whether `submenuActions` is unexpectedly populated (forcing `openSubmenuOnClick` and leaving `item.run` undefined) when only a single quick-chat-capable provider is active, which would make the row swallow a plain click.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** agents-window
- **Confidence:** 0.55
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** plan
- **Rationale:** Bug is plausible and bounded to the workspace-picker no-workspace item, but the exact trigger (submenu vs. direct click regression) isn't confirmed from source alone — needs a quick repro/diagnosis pass before implementation.

## Plan brief

- **Likely files:** `src/vs/sessions/contrib/chat/browser/sessionWorkspacePicker.ts`, `src/vs/sessions/contrib/chat/browser/newChatWidget.ts`
- **Validation:** Manually reproduce in the New Session composer with the unified workspace picker enabled: open the workspace picker, click the 'Chat' entry, and confirm it either selects no-workspace mode or correctly opens its provider submenu — never silently no-ops.
- **Behavior:**

  > Clicking the 'Chat' item in the New Session workspace picker dropdown should either immediately select no-workspace/quick-chat mode (single or no quick-chat-capable provider) or open the provider submenu (multiple providers) — it must never appear inert to a click. Acceptance: manual click-through of the 'Chat' row in both single- and multi-provider quick-chat configurations produces a visible selection or submenu, never a no-op.
- **Trace:**

  > `_getNoWorkspaceOption` in newChatWidget.ts:917 returns `submenuActions` only when more than one quick-chat-capable agent host provider is active; `sessionWorkspacePicker.ts`'s renderer (~line 1856) uses that to decide whether the item gets a direct `run` handler or becomes submenu-only via `openSubmenuOnClick`. Where the uncertainty starts: whether the reporter's setup actually has `submenuActions.length > 0` (making the click legitimately open a submenu, possibly failing to open) or whether it's empty/undefined and the click handler itself is failing elsewhere (e.g. `selectNoWorkspace` -> `_openQuickChat` in newChatWidget.ts:906-913). No history is available in this shallow checkout to see if a recent change already addressed it.
- **Open decisions:**
  - Does the reporter's setup have zero, one, or multiple quick-chat-capable providers (which determines whether a submenu should even open)?
  - Is the intended behavior for the video's scenario a direct selection or a submenu, i.e. is the picker's `run`/`openSubmenuOnClick` branching itself correct?

## Evidence

- **candidate code path:** src/vs/sessions/contrib/chat/browser/sessionWorkspacePicker.ts:1856-1880 builds the 'Chat' (no-workspace) action-list item with `run: noWorkspaceOption.submenuActions?.length ? undefined : () => {...}` and `openSubmenuOnClick: !!noWorkspaceOption.submenuActions?.length`; if `submenuActions` is populated in a case where a direct click should still select the option, the click would appear to do nothing.
- **submenu construction:** src/vs/sessions/contrib/chat/browser/newChatWidget.ts:917-955 (`_getNoWorkspaceOption`) only sets `submenuActions` when `providers.length > 1`, gated behind the experimental `chat.agentSessions.unifiedWorkspacePicker` (formerly `consolidatedRemoteWorkspaces`) setting — not yet confirmed as the exact cause without running the UI.
- **no fix / duplicate found:** Repo checkout is a shallow single-commit clone of main with no history to check for a subsequent fix; no open issue in the candidate canonicals covers this specific symptom.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
