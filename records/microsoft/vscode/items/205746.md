---
number: 205746
url: "https://github.com/microsoft/vscode/issues/205746"
engine: copilot
owner: Yoyokrazy
labels: bug, notebook-statusbar
proposedLabel: none
triageAction: keep-open
confidence: 0.75
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-07-26T21:08:58.518Z"
itemUpdatedAt: "2024-04-02T17:29:36Z"
itemCreatedAt: "2024-02-20T19:36:45Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: 769aa05cb05b2a4735cb9cfe56ff69500eda3ba1e29ae0bc55d3199b8f8bdf4c
policyHash: 26158a1d7dfd26ac
---

# Issue #205746 — "Select Indentation" popup

[https://github.com/microsoft/vscode/issues/205746](https://github.com/microsoft/vscode/issues/205746)

## Summary

Still reproduces on main: the notebook indentation status bar quickpick has a focus-handling issue when selecting 'Convert to Spaces' while already using spaces, causing a spurious popup. No fix has landed since the feature was introduced in PR #205052.

## Change summary

Selecting 'Convert to Spaces' in a notebook when already using spaces triggers a stray status bar popup due to improper focus handling after the quickpick closes.

## Best solution

Fix the focus management in the notebook indentation quickpick flow — likely in `notebookIndentationActions.ts` — so that after executing a convert action that doesn't open a secondary quickpick, focus returns cleanly to the notebook editor rather than re-triggering the status bar indentation entry.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 4/6 Traced
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** notebook-statusbar
- **Confidence:** 0.75
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small localized focus-management fix in one file; the defect path is traced and a test can verify no spurious quickpick appears.
- **Likely files:** `src/vs/workbench/contrib/notebook/browser/controller/notebookIndentationActions.ts`
- **Validation:** Add a test that invokes notebook.action.convertIndentationToSpaces when indentation is already spaces, and asserts that no new quickpick/quickinput is shown afterward (the IQuickInputService should not be triggered). The test should fail before the fix (spurious popup) and pass after.
- **Fix prompt:**

  > The notebook indentation status bar has a focus-handling bug: when the user selects 'Convert to Spaces' while already using spaces, the quickpick closes but focus returns to the status bar entry, causing it to re-trigger its popup. The root cause is in src/vs/workbench/contrib/notebook/browser/controller/notebookIndentationActions.ts. The `convertNotebookIndentation` function completes the conversion but does not explicitly restore focus to the notebook editor. When the first quickpick (status bar indentation picker) closes, focus falls back to the status bar item, which may re-open its picker. Fix: after the conversion completes (in the `.then()` callback of `convertNotebookIndentation`), explicitly focus the active notebook editor pane (e.g., via `editorService.activeEditorPane?.focus()`). Do NOT change `changeNotebookIndentation` or its 50ms setTimeout pattern — that handles the legitimate tab-size picker flow. Validate with a test asserting that invoking `notebook.action.convertIndentationToSpaces` when already on spaces does not trigger IQuickInputService.

## Evidence

- **code path:** src/vs/workbench/contrib/notebook/browser/controller/notebookIndentationActions.ts — the `changeNotebookIndentation` function uses a `setTimeout(50ms)` hack for the secondary quickpick, and `convertNotebookIndentation` has no explicit focus restoration after completing.
- **no fix since report:** git log shows only refactoring commits (ESM, lint, type cleanup) since the issue was filed; no focus-related fix has landed on this file.
- **comment diagnosis:** Yoyokrazy (MEMBER) identified the root cause as a focus change when the quickpick closes — suggests focus should return to the notebook or activeEditorPane.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
