---
number: 332056
url: "https://github.com/microsoft/vscode/issues/332056"
engine: copilot
owner: osortega
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.4
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-22T08:37:59.012Z"
itemUpdatedAt: "2026-08-21T22:37:18Z"
itemCreatedAt: "2026-08-21T22:28:31Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 58032b42e006c7f82ce55a8719f6e29aea3008fbdebd2da1e0110ee48632aaaf
policyHash: 4721e56ad78fdd17
---

# Issue #332056 — Open in Editor fails to display Agent conversation despite successful session discovery

[https://github.com/microsoft/vscode/issues/332056](https://github.com/microsoft/vscode/issues/332056)

## Summary

Thanks for the detailed report. The described symptom (a session discovered but 0 chats emitted, then Open in Editor doing nothing) is plausible, but the report was AI-authored from log analysis and cites a renderer dependency error on a service (`chatSessionRoutingProviderService`) that doesn't exist anywhere in this codebase — that specific detail looks fabricated/hallucinated, which undermines confidence in the rest of the log interpretation. Given the thin, partly unreliable diagnosis and no reproduction steps confirmed independently, this needs clarification before it can be actioned.

## Change summary

Selecting "Open in Editor" on an Agent conversation over a VS Code Tunnel connection does not open the conversation, even though Agent Host discovery logs show the session was found.

## Best solution

Ask the reporter to confirm the exact repro on a clean session (ideally non-tunnel) and clarify whether the session shown in the Agents UI is a discovered/external session or one created in this window; if it's the discovery path, it likely relates to src/vs/platform/agentHost/node/copilot/copilotAgent.ts's adoptable/import-window classification, and if it's a live session, check src/vs/sessions/electron-browser/actions/vscodeActions.ts:getChatSessionToOpenInEditor, which only returns a URI when `session.isCreated` is true.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.4
- **Proposed comment:**

  > Thanks for the report and the logs. A couple of things make this hard to action as-is:
  > 
  > - The renderer error you cited (`chatSessionRoutingProviderService` as an unknown DI dependency) doesn't match any service that exists in the current codebase, so that specific diagnosis may not be accurate.
  > - It's unclear whether the session you tried to open was a freshly discovered/external session (which has its own import-window filtering) or an already-active session in this window (whose "Open in Editor" path depends on the session being fully materialized).
  > 
  > Could you confirm: (1) whether the conversation was one you started in this window vs. one that appeared via discovery, and (2) whether this reproduces without the VS Code Tunnel, on the latest build? That'll help narrow down which code path is actually failing.
  > 
  > This issue will auto-close in 7 days if there's no reply, but feel free to re-open or file a fresh issue with confirmed details.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction is unclear and the cited log evidence is partly unreliable (a nonexistent service name); no concrete failing code path can be confirmed without more info from the reporter.

## Evidence

- **no such service:** Grepped the full src/vs tree for `chatSessionRoutingProviderService` and any `SessionRouting`/`routingProvider` symbol — zero matches. The renderer log excerpt citing this as a missing DI dependency does not correspond to any real service in this repo, suggesting the AI-authored report may have fabricated or misattributed this log line.
- **discovery filtering is real but may be unrelated:** The "outside the import window" log line comes from a genuine code path (src/vs/platform/agentHost/node/copilot/copilotAgent.ts, `outsideImportWindow` counter), which only applies to non-adoptable (external) sessions older than `COPILOT_EXTERNAL_SESSION_MAX_AGE_MS`. It's unclear if this is the actual session the reporter tried to open, or unrelated discovery noise happening at the same time.
- **Open in Editor gating:** src/vs/sessions/electron-browser/actions/vscodeActions.ts:getChatSessionToOpenInEditor only returns a resource URI when `session.isCreated.get()` is true — a provisional/uncreated session returns undefined and the command would silently no-op, which is a plausible alternate explanation not covered by the report's log analysis.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
