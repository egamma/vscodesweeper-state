---
number: 328958
url: "https://github.com/microsoft/vscode/issues/328958"
engine: copilot
owner: deepak1556
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.45
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-07T06:47:25.966Z"
itemUpdatedAt: "2026-08-04T13:18:31Z"
itemCreatedAt: "2026-08-04T13:08:02Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: dce185f4b7ea2777cd3ca8c7dd1c09cccaa73b03a5df0e9e42ff38b0552aead9
policyHash: 70e952cc2fb28823
---

# Issue #328958 — Windows taskbar Jump List offers to pin `.code-workspace` entries as folders instead of workspace files

[https://github.com/microsoft/vscode/issues/328958](https://github.com/microsoft/vscode/issues/328958)

## Summary

Detailed report, but tracing the exact code path in workspacesHistoryMainService.ts shows it already builds a correct `--file-uri` arg and `(Workspace)` title for workspace jump-list entries, so the described mismatch isn't visible from source alone; this is Windows Jump List/taskbar OS integration behavior that needs a fresh confirmed repro rather than a source-only close.

## Change summary

On Windows, the taskbar Jump List's 'Recent Folders & Workspaces' entry for a `.code-workspace` file offers to pin it as a plain folder instead of the workspace file, unlike the correctly-labeled `File > Open Recent` menu.

## Best solution

Have the reporter confirm on the latest Insiders build with a completely fresh pin (unpin/re-pin, or a new taskbar icon) to rule out a stale OS-cached Jump List entry, and, if it still reproduces, capture the actual generated shortcut/task args (e.g. via a Jump List inspection tool) so a maintainer can compare them against `updateWindowsJumpList` in src/vs/platform/workspaces/electron-main/workspacesHistoryMainService.ts.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** windows, workbench-multiroot
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the very thorough investigation here. Looking at the current source (`updateWindowsJumpList` / `getWindowsJumpListLabel` in `src/vs/platform/workspaces/electron-main/workspacesHistoryMainService.ts`), the code already builds a `--file-uri` argument and a `"{name} (Workspace)"` title for workspace entries, so this isn't obviously reproducible just from reading the source — it may be tied to a stale/cached Windows Jump List entry rather than a fresh generation.
  > 
  > Could you try completely unpinning and re-pinning the taskbar icon (or testing on a fresh user profile) on the latest build, and check whether the issue persists? If so, capturing the actual generated shortcut target/arguments for the pinned item (via a Jump List/shortcut inspector) would help narrow down where the mismatch happens.
  > 
  > This issue will auto-close in 7 days if there's no reply with more details, but feel free to comment any time after that to reopen the conversation.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Source tracing shows the relevant code already appears correct, so there's no confirmed defect or localized fix target to hand to an automatic fix — needs a real repro/confirmation first.

## Evidence

- **current logic looks correct:** src/vs/platform/workspaces/electron-main/workspacesHistoryMainService.ts lines 414-424: for `isRecentWorkspace(recent)` entries it sets `hasWorkspaces = true` and builds `args = --file-uri "${workspace.configPath}"`, not a folder-uri, and getWindowsJumpListLabel (line 460-483) returns a title like `"{0} (Workspace)"` for workspace items.
- **no obviously related recent change:** `git log --oneline` on this file shows only an unrelated commit (a6162282, agent plugin actions); no recent regression is visible in this area.
- **reporter's own note:** Some previously-pinned `.code-workspace` entries from older VS Code versions remain correctly pinned on the same machine, suggesting possible OS-level Jump List caching/staleness rather than a clear code defect, though this isn't conclusively ruled in or out.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
