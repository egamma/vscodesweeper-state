---
number: 212551
url: "https://github.com/microsoft/vscode/issues/212551"
engine: codex
owner: alexdima
proposedLabel: none
triageAction: keep-open
confidence: 0.72
reviewedAt: "2026-06-26T17:35:17.404Z"
contentHash: cb5b25b59e40cc1b8f9521f280f951c55a3a1e62c97533541a9a8065b56067a8
policyHash: 17b7468985688190
---

# Issue #212551 — redo command doesn't focus cursor

[https://github.com/microsoft/vscode/issues/212551](https://github.com/microsoft/vscode/issues/212551)

## Summary

Keep open for editor command/API triage: the report is plausible and I did not find a concrete fixing commit or duplicate that would justify closure. Current code has direct editor redo cursor-state handling, but the reported paths involve command service focus behavior, which is not covered by the old cursor redo regression test.

## Change summary

Redo invoked through `commands.executeCommand('redo')` or the Command Palette should focus/reveal the editor cursor at the restored insertion or deletion point.

## Best solution

Confirm the command-service path for `redo` and add a focused regression test if invoking redo from Quick Pick/API fails to refocus the active editor and reveal the restored selection. The likely owner is editor commands/API around `src/vs/editor/browser/coreCommands.ts` and the command registration in `src/vs/editor/browser/editorExtensions.ts`.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Proposed label:** none
- **Area labels:** editor-commands, editor-api
- **Confidence:** 0.72
- **Proposed comment:** none

## Evidence

- **issue context:** https://github.com/microsoft/vscode/issues/212551 is still open, has no milestone, no comments, and is already labeled `bug`, `editor-commands`, and `editor-api`.
- **command registration:** `src/vs/editor/browser/editorExtensions.ts` registers the `redo` MultiCommand with Command Palette menu metadata, so the Command Palette path is part of the core editor command surface.
- **focus-sensitive command path:** `src/vs/editor/browser/coreCommands.ts` implements `EditorOrNativeTextInputCommand`: redo runs against the focused editor when it has text focus, against the active editable DOM element when one is focused, or focuses the active code editor only in the fallback path.
- **existing direct-editor coverage:** `src/vs/editor/test/browser/controller/cursor.test.ts` has `issue #15761: Cursor doesn't move in a redo operation`, but it exercises `editor.runCommand(CoreEditingCommands.Redo, null)` directly, not `commands.executeCommand('redo')` or the Command Palette focus path.
- **no concrete fix found:** Repository history search found no commit referencing `#212551`; without a fixing SHA or confirmed duplicate, this should not be closed as completed or duplicate.

---
Reviewed by vscodesweeper (codex) — proposal only, not applied.
