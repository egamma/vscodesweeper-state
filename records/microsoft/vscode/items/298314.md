---
number: 298314
url: "https://github.com/microsoft/vscode/issues/298314"
engine: copilot
owner: anthonykim1
labels: terminal-process
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-25T17:12:12.650Z"
itemUpdatedAt: "2026-02-27T20:09:51Z"
itemCreatedAt: "2026-02-27T18:24:48Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 1
milestone: none
contentHash: d12fbd6f5efc548508ae31f46c99a28e0fb21c1dda4f9683df7ad48f91fcfc3e
policyHash: ef2efec6827348f6
---

# Issue #298314 — Integrated terminal loses PATH in tcsh when Copilot/Copilot Chat are forced to UI via `remote.extensionKind`

[https://github.com/microsoft/vscode/issues/298314](https://github.com/microsoft/vscode/issues/298314)

## Summary

The report describes a PATH loss in tcsh when Copilot Chat is forced to UI via remote.extensionKind, but the causal mechanism is unclear — how an extension's execution location would strip the terminal's PATH is not established, and no other users have confirmed. Requesting more info.

## Change summary

Integrated terminal loses PATH entries in tcsh on Remote SSH when Copilot Chat is forced to run as a UI extension via remote.extensionKind.

## Best solution

Clarify the causal chain: capture the terminal environment with and without the setting (using `env` in a raw pty) and compare. If confirmed, the fix likely lives in the remote server's environment resolution (src/vs/server/ or src/vs/platform/terminal/node/) where the shell environment is constructed before spawning the pty. May also be a tcsh-specific interaction with shell integration probing (the `/bin/sh -c` metadata probe could clobber inherited env in tcsh's init).

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** terminal-process
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the detailed report. The causal link between forcing `github.copilot-chat` to `["ui"]` via `remote.extensionKind` and the terminal losing its PATH isn't immediately clear — extension kind controls where the extension process runs, but shouldn't directly affect how the remote terminal's environment is constructed.
  > 
  > Could you help narrow this down?
  > 1. When you set copilot-chat to `["ui"]`, does the remote extension host still start? (Check Remote Explorer → Installed Extensions to see what's running remotely.)
  > 2. Can you capture `process.env` from the remote server logs (Developer: Show Logs → Remote Server) with and without the setting?
  > 3. Does the same PATH loss happen if you force a *different* extension to UI (e.g. any other workspace extension), or is it specific to copilot-chat?
  > 
  > This would help determine whether it's a general remote extension host lifecycle issue or something specific to the Copilot extension.
  > 
  > Note: issues labeled `info-needed` will auto-close after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Causal mechanism is unclear — cannot confirm defect location without more info from the reporter.

## Evidence

- **tcsh shell env handling:** src/vs/platform/shell/node/shellEnv.ts line 146 has tcsh-specific arg handling (uses '-ic' instead of '-i -l -c'), but this is for resolving the shell environment, not for the terminal spawn itself.
- **shell integration unsupported:** src/vs/platform/terminal/node/terminalEnvironment.ts line 141 logs 'Shell integration cannot be enabled' for unsupported shells including tcsh — this is expected and not the PATH issue itself.
- **no fix on main:** git log since 2025-01 on terminalEnvironment.ts and shellEnv.ts shows no changes related to extensionKind or PATH stripping in remote scenarios.
- **unclear mechanism:** The report does not explain why forcing copilot-chat to ['ui'] would cause PATH loss — extension kind affects where the extension runs, not directly how terminal environment is constructed on the remote.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
