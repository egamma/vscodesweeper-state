---
number: 336743
url: "https://github.com/microsoft/vscode/issues/336743"
engine: copilot
owner: connor4312
labels: new release
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: common
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-19T12:34:20.249Z"
itemUpdatedAt: "2026-09-18T09:56:47Z"
itemCreatedAt: "2026-09-18T09:47:54Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: a2029c4f85d8ec474db970ab43def746d04695286dcb14645bfe2f77174e5ac2
policyHash: fd90e4d8a068f279
---

# Issue #336743 — Debugger auto attach no longer works with Node.js

[https://github.com/microsoft/vscode/issues/336743](https://github.com/microsoft/vscode/issues/336743)

## Summary

Fresh (1-day-old), single-report bug with clear steps but no confirmation from other users or maintainers yet, and the actual attach mechanism lives partly in the separate ms-vscode.js-debug extension (microsoft/vscode-js-debug) that isn't in this checkout, so I can't confirm the defect from source alone — needs-info to gather logs/confirmation before it can be triaged further.

## Change summary

Setting `debug.javascript.autoAttachFilter` to `always` no longer causes the debugger to attach to a Node.js process started with `--inspect` in the integrated/debug terminal, and the status bar no longer shows a 'Node: Auto Attach' item as documented.

## Best solution

Get the reporter to confirm reproduction on a clean profile (to rule out the several extensions listed, e.g. Devcontainer/Playwright/GitLens/Oxc) and share the Output panel logs for 'Auto Attach' and the js-debug extension; if confirmed, the fix likely lands in microsoft/vscode-js-debug (auto-attach IPC/env-var wiring) or in extensions/debug-auto-launch/src/extension.ts if the IPC server itself isn't starting.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** common
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** debug
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed report. Since the actual attach happens via the bundled JS debugger (a separate component from this extension), it'd help to narrow this down before we can act on it:
  > 
  > - Does this reproduce with all other extensions disabled (`code --disable-extensions`)?
  > - Can you check the "Auto Attach" output channel (or enable `debug.javascript.autoAttachSmartPattern`/`autoAttachFilter` again) for any errors when the terminal starts?
  > - Does the debug terminal show any output from the debugger at all, and does `node --inspect` alone (without auto attach) let you attach manually via "Attach to Node Process"?
  > 
  > This will help confirm whether the issue is in `debug-auto-launch` or the JS debugger itself. This issue will auto-close in 7 days if there's no reply, but feel free to add the info any time before or after that.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** Reproduction is unconfirmed and the likely root cause spans a separate repo (vscode-js-debug) not present in this checkout — not agent-ready until more information narrows the cause.

## Evidence

- **code path in this repo:** extensions/debug-auto-launch/src/extension.ts wires the `debug.javascript.autoAttachFilter` setting to an IPC server and delegates the actual attach to the external 'extension.js-debug.autoAttachToProcess' command (line 262-265); the extension logic itself looks intact in current main.
- **js-debug is a separate repo:** product.json lines 58-61 show `ms-vscode.js-debug` is pulled from https://github.com/microsoft/vscode-js-debug, which is outside this checkout, so the actual attach failure (or the missing 'Node: Auto Attach' status text) can't be confirmed or ruled out here.
- **shallow clone:** This checkout is a depth-1 shallow clone (`git rev-list --count HEAD` = 1), so no history is available to check for a recent regression in extensions/debug-auto-launch.
- **no corroboration:** Issue is 1 day old with zero comments and zero reactions; several third-party/other-Microsoft extensions are installed (Playwright, GitLens, Oxc, Dev Containers) that could interact with the debug terminal.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
