---
number: 182441
url: "https://github.com/microsoft/vscode/issues/182441"
engine: codex
owner: alexdima
proposedLabel: none
triageAction: keep-open
confidence: 0.74
reviewedAt: "2026-06-26T19:34:39.509Z"
contentHash: 392f223349a8d68f402b33b70858fdcd97a5d4544165fe82034d11ab6d80fc39
policyHash: 17b7468985688190
---

# Issue #182441 — Regression: TextEditorEdit.replace does not select empty selection anymore

[https://github.com/microsoft/vscode/issues/182441](https://github.com/microsoft/vscode/issues/182441)

## Summary

Keep this open for API/editor-api owner review; the report is actionable and I did not find a fixing commit or canonical duplicate. Current code still routes `TextEditorEdit.replace` through the editor without an explicit resulting selection, while the API docs explicitly distinguish `insert` from `replace` selection behavior.

## Change summary

Restore or clarify the resulting selection behavior when `TextEditorEdit.replace` is called with an empty selection.

## Best solution

Add an API regression test for `TextEditorEdit.replace` on an empty `Selection`, then either restore the old selected-inserted-text behavior in the `TextEditorEdit`/main-thread editor path or make an explicit API-owner decision that the new behavior is intentional and document it.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Proposed label:** none
- **Area labels:** api, editor-api
- **Confidence:** 0.74
- **Proposed comment:**

  > Thanks for the report. This still looks like it needs API/editor-api owner review: `TextEditorEdit.replace` and `insert` deliberately carry different marker semantics, and the d.ts docs call out different resulting selections, but the main-thread edit path does not set an explicit resulting selection for this case. A focused regression test for replacing an empty `Selection` would help decide whether to restore the old selected-inserted-text behavior or document the current behavior as intentional.

## Evidence

- **current implementation:** In `src/vs/workbench/api/common/extHostTextEditor.ts:87-100`, `replace` accepts a `Selection` via the `Range` branch and pushes the edit with `forceMoveMarkers: false`; `insert` at `src/vs/workbench/api/common/extHostTextEditor.ts:102-105` uses `forceMoveMarkers: true`, so the API intentionally has distinct selection semantics.
- **main-thread application:** `src/vs/workbench/api/browser/mainThreadEditor.ts:495-507` forwards the edit operations to `this._codeEditor.executeEdits('MainThreadTextEditor', transformedEdits)` without an explicit end cursor/selection state, so current behavior depends on editor marker/cursor recovery rather than a TextEditorEdit-specific selection contract.
- **API docs:** `src/vscode-dts/vscode.d.ts:1410-1414` states that `insert` and the equivalent `replace` produce different resulting selections, which supports treating this as an API behavior regression or contract clarification rather than closing as out-of-scope.
- **history search:** `git log --all --grep='182441|TextEditorEdit.replace|replace does not select|empty selection'` returned no matching fixing commit in the checkout, and the provided issue context contains no duplicate or maintainer resolution beyond the initial stale-version bot comment.

---
Reviewed by vscodesweeper (codex) — proposal only, not applied.
