---
number: 329649
url: "https://github.com/microsoft/vscode/issues/329649"
engine: copilot
owner: sandy081
labels: bug, agents-window, agent-host
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-31T16:32:14.411Z"
itemUpdatedAt: "2026-08-30T14:55:23Z"
itemCreatedAt: "2026-08-07T17:18:40Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: 1.137.0
contentHash: d7b95bfe16b46cb376a5b5723d85c8920f04d123211c0438bc75434ce7ba2d5b
policyHash: 3d6943c3a15c721c
---

# Issue #329649 — send_message server tool does not block a chat from messaging itself (Codex + Claude)

[https://github.com/microsoft/vscode/issues/329649](https://github.com/microsoft/vscode/issues/329649)

## Summary

Real bug with a milestone (1.137.0) assigned, which is a scheduling decision — the hard keep-open rule for milestoned issues applies, and I found no commit referencing #329649, so no implemented-on-main close is available even though the code has visibly moved since this was filed.

## Change summary

The send_message server tool's self-target guard is a plain string comparison of chat vs. channel URIs, and only works when the provider's "current channel" is already chat-shaped, which Codex and Claude did not supply at report time.

## Best solution

Land the proposed shared `currentChatUri`-normalization fix in `applySendMessageTool` (sessionServerTools.ts) so the guard compares like-with-like for every provider, then update the E2E gate/KNOWN_ISSUES.md entries once verified fixed for each provider; this is milestoned work already tracked for 1.137.0.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 4/6 Traced
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** agents-window, agent-host
- **Confidence:** 0.55
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Milestoned bug already assigned to the team for 1.137.0, and the Claude-side root cause on current main no longer matches the issue's original diagnosis (the `_storageUri` symbol it cites is gone) — the exact remaining defect needs a maintainer/author re-trace before an automatic fix could target it safely.

## Evidence

- **milestone:** Issue carries milestone 1.137.0, a real release milestone (not Backlog/On Deck) — per policy this bars any close regardless of other findings.
- **guard still present:** src/vs/platform/agentHost/node/shared/sessionServerTools.ts applySendMessageTool (~line 1049) still does `chat.toString() === URI.parse(currentChannel).toString()`, exactly the comparison the issue flags as fragile; an unexported `currentChatUri` helper exists (~line 900) but is only used by the rename-chat path, not by send_message.
- **Codex looks fixed:** src/vs/platform/agentHost/node/codex/codexAgent.ts now passes `chatUri: session.chatChannel` (line ~7304), and `chatChannel` is assigned from real chat URIs (e.g. lines 4086, 4604) rather than the raw session URI the issue describes — matching the test suite, where `supportsSelfSendRejection` now excludes only 'claude', not 'codex' (serverToolsSuite.ts:75).
- **Claude still failing per KNOWN_ISSUES.md:** KNOWN_ISSUES.md 'Claude can send a message to the chat that is already running the tool' (~line 620) is still present and still gated by `supportsSelfSendRejection`; however, the `_storageUri` symbol the issue cites in claudeAgentSession.ts:133-135 no longer exists on main — `_chatChannelUri` is used directly (line ~837), so the exact URI-collapsing root cause described for Claude may already be partially addressed, and the residual failure could now be a different (e.g. SDK-side) behavior than the one this issue's diagnosis targets.
- **related but distinct:** https://github.com/microsoft/vscode/issues/325611 ('Self messaging works but loops can still form with two sessions') looks like a broader multi-session loop concern, not clearly the same narrow single-chat guard bug; not verified as canonical, so not proposed as duplicate.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
