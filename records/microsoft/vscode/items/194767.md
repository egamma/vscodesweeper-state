---
number: 194767
url: "https://github.com/microsoft/vscode/issues/194767"
engine: copilot
owner: lszomoru
labels: bug, help wanted, git
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-21T20:01:51.561Z"
itemUpdatedAt: "2025-12-10T14:59:51Z"
itemCreatedAt: "2023-10-04T02:04:45Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: Backlog
contentHash: eeb18cbd7fd48f1e5ef7fafd59b5043dc1160bfe39dc9252fec52e4d65ba6d56
policyHash: ebd3763732abe97d
---

# Issue #194767 — Git - Git not obeying `commit.gpgsign = false` via `git config --local commit.gpgsign false`

[https://github.com/microsoft/vscode/issues/194767](https://github.com/microsoft/vscode/issues/194767)

## Summary

The report lacks critical details — we don't know whether the user had `git.enableCommitSigning` enabled in VS Code settings or `commit.gpgsign = true` in their global git config. The code on current main correctly defers to git's own config when `git.enableCommitSigning` is not enabled (no signing flag is passed), and a recent change (f3d99dee2f4) further improved this by supporting explicit `--no-gpg-sign` via the API. Without knowing the reporter's full config, the bug cannot be verified.

## Change summary

VS Code's Git integration reportedly ignores a local `commit.gpgsign = false` git config and still triggers a GPG signing prompt on commit.

## Best solution

Ask the reporter to confirm on a current build with details about their VS Code `git.enableCommitSigning` setting and global git `commit.gpgsign` config. On current main, the code correctly omits any signing flag when `enableCommitSigning` is not set to `true`, so git's local config should be respected. If the issue was that VS Code's setting overrode the local git config, that is by design — the VS Code setting takes precedence.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the report. Could you help us reproduce this? On current VS Code, when `git.enableCommitSigning` is not set to `true`, the Git extension does not pass any signing flag, so git should respect your local `commit.gpgsign = false` config.
  > 
  > Could you confirm:
  > 1. What is your `git.enableCommitSigning` setting in VS Code?
  > 2. What does `git config --global commit.gpgsign` return?
  > 3. Does this still reproduce on the latest VS Code version?
  > 
  > Note: issues labeled `info-needed` will auto-close after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Report is too thin to verify; the code appears correct on main and may already address whatever the reporter experienced.

## Evidence

- **current code path:** extensions/git/src/commands.ts:2455 — `opts.signCommit = config.get<boolean>('enableCommitSigning') === true ? true : undefined;` — when the VS Code setting is not enabled, `signCommit` is `undefined` and no signing flag is passed to git, letting git's own config (including local `commit.gpgsign`) take effect.
- **code at filing time:** At the time of the report (Oct 2023, commit 9434d943), the code was `opts.signCommit = enableCommitSigning;` which set it to `false` when the setting was off. In git.ts, `if (opts.signCommit)` was falsy for `false`, so no flag was passed — same effective behavior.
- **recent improvement:** Commit https://github.com/microsoft/vscode/commit/f3d99dee2f4 (Feb 2026) added explicit `--no-gpg-sign` support when signCommit is explicitly set to `false` via the API, and changed the default-off path to `undefined` for clarity.
- **missing context:** The report does not mention the user's `git.enableCommitSigning` VS Code setting value, their global git `commit.gpgsign` config, or whether any extensions were involved. No comments or confirmations exist after 1000+ days.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
