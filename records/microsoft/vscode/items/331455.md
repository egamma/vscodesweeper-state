---
number: 331455
url: "https://github.com/microsoft/vscode/issues/331455"
engine: copilot
owner: zhichli
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.45
severity: correctness
breadth: niche
priority: none
visionFit: aligned
autoFixable: false
reviewedAt: "2026-08-19T08:56:47.797Z"
itemUpdatedAt: "2026-08-18T11:27:45Z"
itemCreatedAt: "2026-08-18T11:06:53Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 863377177523e7c470fcce122e661f12009b63913189066f0b0e036eac71bfac
policyHash: 4721e56ad78fdd17
---

# Issue #331455 — Copilot Chat OTel export only emits utility-model spans, never the active chat model (GPT-5/Claude)

[https://github.com/microsoft/vscode/issues/331455](https://github.com/microsoft/vscode/issues/331455)

## Summary

Detailed report with raw OTLP captures, but tracing the actual chat-instrumentation code on main shows the primary agent path (ToolCallingLoop's `invoke_agent` span plus chatMLFetcher's `chat <model>` span) does set `gen_ai.request.model` to the real selected model, which conflicts with what the reporter observed — asking for confirmation on a current build and a fuller capture before deciding this is a live defect.

## Change summary

OTel export allegedly only ever emits spans for internal utility-model calls (title summarization, embeddings) and never a span for the actual selected chat model (GPT-5/Claude) used in the visible conversation turn.

## Best solution

If reproduced on the latest build, this likely belongs in the agent-observability area alongside the existing OTel work; the maintainers should check whether `askAgentIntent`/`ToolCallingLoop.run()` is even being entered for the reporter's exact scenario (vs. some other code path that bypasses `invoke_agent`/`chat <model>` span creation) and whether the SQLite/OTLP exporter is dropping those specific spans before flush.

## Vision fit

- **Fit:** aligned
- **Reason:** Correct, complete OTel span emission is part of the agent observability workstream the project is actively investing in.
- **Evidence:** Durable theme: engineering system & OSS triage / agent host — and current priorities name evaluations/observability for agent quality as an active workstream.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** agent-observability
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the very detailed report and captures. Looking at the current instrumentation, the main agent turn should be emitting an `invoke_agent <agentName>` span with a nested `chat <model>` span carrying `gen_ai.request.model` set to the actual selected model (see ToolCallingLoop.run() and chatMLFetcher in the Copilot Chat source) — the `copilotLanguageModelWrapper` span you're seeing is a separate code path used for `vscode.lm` API/utility calls (like turn-title summarization), not the visible conversation.
  > 
  > Could you confirm on the latest Copilot Chat build (0.63.0+) and share whether your capture contains ANY `invoke_agent` span at all (not just `chat`/embedding spans)? That will help narrow down whether the top-level agent span is being created but not exported, or not created at all for your scenario. This issue will auto-close in 7 days if there's no follow-up.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Cannot confirm the defect exists on current main — source tracing shows the expected instrumentation is present — so there is no confirmed bug to localize a fix for yet.

## Evidence

- **wrapper span is for extension/utility calls:** The `copilotLanguageModelWrapper` debugName the reporter sees comes from extensions/copilot/src/extension/conversation/vscode-node/languageModelAccess.ts:807, which wraps the `vscode.lm` API surface used by extensions and internal utility calls (e.g. turn-title summarization), not the visible agent conversation.
- **main agent path is instrumented:** extensions/copilot/src/extension/intents/node/toolCallingLoop.ts creates an `invoke_agent ${agentName}` span (~line 1266) and sets GenAiAttr.REQUEST_MODEL from the resolved chat endpoint (~line 1315); extensions/copilot/src/extension/prompt/node/chatMLFetcher.ts creates a nested `chat ${chatEndpointInfo.model}` span (~line 1041) with GenAiAttr.REQUEST_MODEL set to the real model — this contradicts the reporter's capture showing no such span.
- **unrelated meta/tracking issue:** https://github.com/microsoft/vscode/issues/293225 is a broad 'Meta: Agent Observability' tracking issue (labels feature-request/under-discussion/agent-observability); it does not own this specific model-attribution bug, so it is not a duplicate canonical.
- **version delta:** Reporter tested Copilot Chat 0.61.0; current main package.json shows 0.63.0, and no specific commit could be found fixing this exact behavior in the interim.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
