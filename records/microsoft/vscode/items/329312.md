---
number: 329312
url: "https://github.com/microsoft/vscode/issues/329312"
engine: copilot
owner: hediet
labels: new release
proposedLabel: none
triageAction: route-to-area
confidence: 0.55
severity: freeze
breadth: common
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-07T06:29:44.625Z"
itemUpdatedAt: "2026-08-06T03:46:28Z"
itemCreatedAt: "2026-08-06T02:21:54Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: c5c21e57c52b1fa3a2b08e6387adcd0a2955457f6ec68bdb997e4c55b4719446
policyHash: 70e952cc2fb28823
---

# Issue #329312 — Copilot config/chat view crashes when multiple VS Code windows are open (TypeError: e is not iterable)

[https://github.com/microsoft/vscode/issues/329312](https://github.com/microsoft/vscode/issues/329312)

## Summary

The reporter's own source trace is partially confirmed on main: extensions/copilot/src/extension/chatSessions/vscode-node/chatSessions.ts:165 still unconditionally calls vscode.chat.createChatParticipant('claude-code', ...) without checking the same agent-host/preferAgentHost gating used by the package.json `chatSessions` contribution's `when` clause — so the registration mismatch they describe is real and unfixed on main, but I could not independently confirm it's the exact cause of the reported 'e is not iterable' crash trace (that stack is in a different bundled module, N8e.setItems).

## Change summary

Copilot chat/config view becomes unresponsive and a TypeError crashes it when multiple VS Code windows are open with Copilot Chat active.

## Best solution

Have a Copilot Chat maintainer confirm whether the unconditional `createChatParticipant(ClaudeSessionUri.scheme, ...)` call in chatSessions.ts (not gated by the same agentHostEnabled/preferAgentHost condition as the claude-code chatSessions contribution's `when` clause in package.json) is the actual cause, and add a guard there so registration only happens when the contribution would also be considered available — mirroring the `_isClaudeAvailable()` pattern already used on the workbench side in src/vs/sessions/contrib/providers/copilotChatSessions/browser/copilotChatSessionsProvider.ts.

## Proposed action

- **Triage action:** route-to-area
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 4/6 Traced
- **Severity:** freeze
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** chat
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed trace. I checked current `main`: the workbench-side Claude availability check you found (`_isClaudeAvailable()` in `copilotChatSessionsProvider.ts`) is on the UI/session-picker side, but the bundled extension's own registration in `extensions/copilot/src/extension/chatSessions/vscode-node/chatSessions.ts` still calls `vscode.chat.createChatParticipant(ClaudeSessionUri.scheme, ...)` unconditionally, without checking the same `agentHostEnabled`/`preferAgentHost` condition used by its own `chatSessions` contribution's `when` clause in `package.json`. So the mismatch you describe does appear to still exist on `main` as of this review. That said, I can't confirm from source alone that this is the exact defect producing the 'e is not iterable' TypeError in your log (that trace is in a different bundled module). Routing this to the chat/Copilot area for a maintainer to confirm the root cause and decide on a fix.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** A plausible registration-gating mismatch was traced in source, but the link to the specific reported crash trace ('e is not iterable' in a different module) isn't confirmed, so this needs maintainer investigation before a validated automatic fix can be scoped.

## Evidence

- **unconditional registration on main:** extensions/copilot/src/extension/chatSessions/vscode-node/chatSessions.ts:165 (as of commit a61622829b436d1714f647da5c9f6cfad4dd37fe) calls `vscode.chat.createChatParticipant(ClaudeSessionUri.scheme, ...)` unconditionally, with no check of chat.agentHost.enabled or chat.editor/agents.claude.preferAgentHost.
- **gating mismatch:** The claude-code chatSessions contribution in extensions/copilot/package.json line 6883 has `when: config.github.copilot.chat.claudeAgent.enabled && (!agentHostEnabled || ...preferAgentHost checks...)`, but the extension code that registers the participant for the same scheme/id has no equivalent check — so getAllChatSessionContributions() (src/vs/workbench/contrib/chat/browser/chatSessions/chatSessions.contribution.ts:859-863) can omit 'claude-code' from available contributions while the extension still registers it, and there is no static 'claude-code' entry in extensions/copilot/package.json's chatParticipants array (only github.copilot.* ids are declared).
- **throw path:** src/vs/workbench/api/browser/mainThreadChatAgents2.ts $registerAgent throws 'chatParticipant must be declared in package.json' when neither a static declaration nor an available chatSessions contribution nor dynamicProps exist for the id — matching the reporter's traced chain, though not the literal 'e is not iterable' error text seen in the log.
- **unverified link to exact crash:** The captured stack trace (N8e.setItems / fp._computeFn in extension.js:1162) is in a different minified module than the registration path described above, so I can't confirm this specific mismatch is what produces the 'e is not iterable' TypeError without the actual source map.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
