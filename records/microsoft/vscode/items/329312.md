---
number: 329312
url: "https://github.com/microsoft/vscode/issues/329312"
engine: copilot
owner: hediet
labels: ""
proposedLabel: none
closeReason: none
triageAction: route-to-area
confidence: 0.68
severity: freeze
breadth: niche
priority: none
visionFit: aligned
autoFixable: false
reviewedAt: "2026-08-12T09:34:07.922Z"
itemUpdatedAt: "2026-08-12T02:25:47Z"
itemCreatedAt: "2026-08-06T02:21:54Z"
upvotes: 0
reactionsTotal: 0
commentCount: 3
relatedOpenIssues: 0
milestone: none
contentHash: ddd405018ec466eae297dc855aa552366b499311cc07c5185facbe3fcb5cab80
policyHash: f13d7795d7aa28b9
---

# Issue #329312 — Copilot config/chat view crashes when multiple VS Code windows are open (TypeError: e is not iterable)

[https://github.com/microsoft/vscode/issues/329312](https://github.com/microsoft/vscode/issues/329312)

## Summary

Real, well-diagnosed crash/hang in the Copilot chat view tied to the 1.132 Agent Host rollout defaults, confirmed by a second commenter — but the exact code path the reporter traced (an unconditional `createChatParticipant` call for the `claude-code` scheme in the bundled extension) no longer exists in the current source tree, so this needs a maintainer to confirm whether the ongoing Agent Host migration already covers it rather than an automated close.

## Change summary

Opening the Copilot chat/configuration view crashes or hangs the extension host after the 1.132 Agent Host rollout changed `chat.agentHost.enabled`/`preferAgentHost` defaults, throwing `TypeError: e is not iterable` from an unconditionally-registered `claude-code` chat participant.

## Best solution

A maintainer should verify whether the `claude-code` chatSessions contribution and its unconditional `createChatParticipant` call have been fully superseded by the new Agent Host architecture (now under src/vs/sessions/contrib/providers/copilotChatSessions/, with `_isClaudeAvailable`-style gating) with no remaining unconditional registration path; if so, close with a citation to the specific migrating commit, otherwise land a guard so the participant registration respects the same `when` clause as the package.json contribution.

## Vision fit

- **Fit:** aligned
- **Reason:** This sits squarely in the current Agent Host rollout workstream (multi-harness agent host, Claude integration, parity work), so getting it right matters for the stated current priority even though the specific bug appears to predate the latest refactor.
- **Evidence:** Current priorities: 'Agent host & harnesses — one agent host... rolling out as the default for agent sessions, with parity work on tools, terminal integration, permissions, and context.'

## Proposed action

- **Triage action:** route-to-area
- **Item type:** bug
- **Reproduction:** reproduced
- **Rating:** 5/6 Source-confirmed
- **Severity:** freeze
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** chat-claude-agent, agent-host, freeze-slow-crash-leak
- **Confidence:** 0.68
- **Proposed comment:**

  > Thanks for the very thorough diagnosis here — tracing the exact `createChatParticipant`/`when`-clause mismatch from the bundled extension logs is great work. I checked current `main`: the `claude-code` chatSessions contribution and that unconditional registration path you found in 1.132 aren't present in today's source (Claude sessions now go through the newer Agent Host provider in `src/vs/sessions/contrib/providers/copilotChatSessions/`), so this may already be resolved by that migration. I can't point to a specific commit that closes this issue by number, though, so routing this to the chat/agent-host area for a maintainer to confirm whether the new architecture fully avoids the crash or whether a similar gap remains.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** The exact code path has already changed via an ongoing architectural migration (Agent Host rollout); confirming whether the new implementation avoids this crash, and whether any residual gap needs a fix, requires maintainer/product judgment rather than a small isolated patch.

## Evidence

- **reporter's traced chain:** Comment 1 traces the crash to extensions/copilot's `claude-code` chatSessions contribution being filtered out by the `when` clause under new 1.132 agent-host defaults, while the bundled extension.js still unconditionally calls `createChatParticipant(ClaudeSessionUri.scheme, ...)`, causing `MainThreadChatAgents.$registerAgent` to throw.
- **mechanism absent on main:** Current extensions/copilot/package.json `chatSessions` contribution array only lists `copilotcli` and `copilot-cloud-agent` — no `claude-code` type at all (verified via `python3 -c "json.load(...)"`), and `git log -S"ClaudeSessionUri"` for extensions/copilot/ returns nothing, meaning that exact registration path isn't present in this source tree.
- **new architecture location:** Claude chat-session handling now lives in core at src/vs/sessions/contrib/providers/copilotChatSessions/ (see its test file referencing `claude-code` scheme and `providerType: 'claude-code'`), a different implementation than the one the reporter traced in the 1.132 bundled extension.
- **no citable fix commit for this issue:** The most recent commit touching that provider, 802ccf1e ('agentHost: fix read-state, model, and open-after-toggle issues for legacy Copilot CLI migration', #330369), does not reference issue #329312, so it cannot be cited as an implemented-on-main fix per policy.
- **independent confirmation:** Comment 3 by DefTruth independently reports 'crash/hang while copilot is using, 132.1', corroborating the reporter's own finding that it's not actually window-count dependent.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
