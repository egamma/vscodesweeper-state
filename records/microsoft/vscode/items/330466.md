---
number: 330466
url: "https://github.com/microsoft/vscode/issues/330466"
engine: copilot
owner: bryanchen-d
labels: bug, error-telemetry, recent-regression, extension, ai-generated, ai-analyzed
proposedLabel: none
closeReason: none
triageAction: route-to-area
confidence: 0.82
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-08-13T09:31:09.111Z"
itemUpdatedAt: "2026-08-12T15:14:21Z"
itemCreatedAt: "2026-08-12T14:57:59Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: f61d4b53565b63e54d53410a0635eac8fe06111d433d9ad57ac44ae5400c2503
policyHash: f13d7795d7aa28b9
---

# Issue #330466 — [Error] [GitHub.copilot-chat] unhandlederror-database disk image is malformed

[https://github.com/microsoft/vscode/issues/330466](https://github.com/microsoft/vscode/issues/330466)

## Summary

Confirmed in source: `reconcileDbFiles`'s stale-file removal loop calls the unguarded `delete()` (raw `_db.prepare(...).run()`) outside any try/catch, so a corrupted on-disk SQLite file throws unhandled — exactly matching the reported stack trace. No fix has landed on main yet.

## Change summary

The copilot-chat external-ingest SQLite index throws an unhandled 'database disk image is malformed' error when reconciling stale file entries against a corrupted local database file, observed exclusively on darwin.

## Best solution

Wrap the stale-file removal loop (and/or the `delete()` method itself) in `externalIngestIndex.ts` with a try/catch that logs and falls back to recreating/reinitializing the local DB on corruption, mirroring the guard already present around the per-folder scan loop just above it; the bot's own follow-up issue #330470 already has a candidate diff on an unmerged branch that a maintainer can review and land.

## Proposed action

- **Triage action:** route-to-area
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.82
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small, localized fix (guard the stale-file removal loop / delete() against a corrupted DB) confirmed by tracing the exact code path on main; a concrete test can validate the guard.
- **Likely files:** `extensions/copilot/src/platform/workspaceChunkSearch/node/codeSearch/externalIngestIndex.ts`
- **Validation:** Add a unit test that simulates `_db.prepare(...).run()` throwing 'database disk image is malformed' (or any SQLite error) during the stale-file removal phase of `reconcileDbFiles`, and assert that `reconcileDbFiles`/`initialize` resolves without throwing (the error is caught and logged) instead of rejecting unhandled; the test should fail before the fix and pass after.
- **Fix prompt:**

  > In extensions/copilot/src/platform/workspaceChunkSearch/node/codeSearch/externalIngestIndex.ts, the private `reconcileDbFiles()` method has a 'Remove files that no longer exist' loop (around lines 946-952) that calls `this.delete(uri)` for each stale entry. `delete()` (lines 762-764) does a raw `this._db.prepare('DELETE FROM Files WHERE path = ?').run(uri.toString())` with no error handling. When the on-disk SQLite database is corrupted ('database disk image is malformed'), this throws synchronously and the error escapes uncaught out of `reconcileDbFiles` and up through `ensureExternalIngestInitialized`/`initialize`, producing an unhandled rejection (see stack trace in microsoft/vscode#330466). Note the sibling per-folder scan loop just above (lines 909-944) already has a try/catch around it from a prior fix (commit 4a09cdbe / PR #313401), but that guard does not cover this later removal loop. Add error handling around the removal loop (or inside `delete()` itself) that catches the error, logs it via `this._logService.error(...)` the same way the per-folder loop does, and lets `reconcileDbFiles` continue/return normally instead of throwing. Do not change the per-folder scan loop's existing behavior or the DB schema. If feasible, consider whether a caught corruption error here should also trigger the same DB-recreation/recovery path used elsewhere for corrupted local stores, but keep the change minimal and scoped to this method. Validate with a unit/vitest test that stubs `_db.prepare` to throw a 'disk image is malformed' error during the removal phase and asserts `reconcileDbFiles`/`initialize` completes without throwing. Related: the bot's own fallback analysis and candidate diff live in microsoft/vscode#330470 on branch fix/external-ingest-malformed-db-330466, which can be used as a starting reference.

## Evidence

- **unguarded call site:** extensions/copilot/src/platform/workspaceChunkSearch/node/codeSearch/externalIngestIndex.ts lines 946-952: the stale-file removal loop calls `this.delete(uri)` (line 949) with no try/catch, while the sibling per-folder scan loop just above (lines 909-944) is guarded.
- **underlying throw:** `delete()` (lines 762-764) runs a raw `this._db.prepare('DELETE FROM Files WHERE path = ?').run(...)` with no error handling, so a malformed SQLite file throws synchronously and propagates up through `reconcileDbFiles` -> `ensureExternalIngestInitialized`, matching the reported stack trace exactly.
- **no fix on main:** `git log` on this file shows no commit addressing this; the bot's follow-up comment states the intended PR was not created and a fallback analysis issue (#330470) was filed instead, with the fix only on an unmerged branch `fix/external-ingest-malformed-db-330466`.
- **related prior corruption reports:** Bot analysis references prior SQLite-store corruption issue #93428 (state.vscdb) as a similar class of problem.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
