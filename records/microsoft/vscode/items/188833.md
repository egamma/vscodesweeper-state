---
number: 188833
url: "https://github.com/microsoft/vscode/issues/188833"
engine: codex
owner: alexdima
proposedLabel: none
triageAction: keep-open
confidence: 0.74
reviewedAt: "2026-06-26T17:34:46.855Z"
contentHash: 3ae04500d175f6e0b5c06b7a20f16c8ee92738f34f97e6ba25b3a03b5de1174e
policyHash: 17b7468985688190
---

# Issue #188833 — Odd order of extension activation

[https://github.com/microsoft/vscode/issues/188833](https://github.com/microsoft/vscode/issues/188833)

## Summary

Keep open for extension-activation owner judgment: current main still has the behavior that motivated the report, and the issue is asking for a product/API-doc decision rather than presenting a clearly closeable support question.

## Change summary

Clarify the startup precedence of extension activation events, especially `onLanguage`/`onFileSystem` relative to `*`, and whether Emmet should use a narrower activation strategy.

## Best solution

Extension activation maintainers should decide whether the current startup ordering and 1s pending-activation wait are intentional API behavior; if so, document that ordering in the activation-events docs, and only change Emmet's `activationEvents` if there is a confirmed startup/perf reason.

## Proposed action

- **Triage action:** keep-open
- **Item type:** under-discussion
- **Proposed label:** none
- **Area labels:** extension-activation
- **Confidence:** 0.74
- **Proposed comment:** none

## Evidence

- **current state:** The issue is open, labeled `bug`, `confirmation-pending`, and `extension-activation`, with milestone `Backlog`; none of the hard close-blocking labels apply, but Backlog also does not by itself justify closing.
- **Emmet activation:** Current `extensions/emmet/package.json` still declares `activationEvents` as `onCommand:emmet.expandAbbreviation` and broad `onLanguage`, so the Emmet concern described in https://github.com/microsoft/vscode/issues/188833 remains present on main.
- **activation ordering code:** Commit e440a9bb7dfbe8a0b76fc76894fcf189b8d2e6b8 (`Reorder eager extension activation (#176706)`) added a wait for pending activation operations, capped at 1000ms, before `_handleEagerExtensions()` runs.
- **current eager activation code:** In `src/vs/workbench/api/common/extHostExtensionService.ts`, startup waits for already activating extensions and then handles eager `*`, workspace-contains, and remote-resolver activation; `onStartupFinished` is triggered only after those eager activations or a timeout.
- **language activation trigger:** `src/vs/workbench/services/language/common/languageService.ts` still fires both `onLanguage:${languageId}` and broad `onLanguage` when a language is requested, matching the mechanism the reporter called out.

---
Reviewed by vscodesweeper (codex) — proposal only, not applied.
