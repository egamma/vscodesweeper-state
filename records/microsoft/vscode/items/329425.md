---
number: 329425
url: "https://github.com/microsoft/vscode/issues/329425"
engine: copilot
owner: bryanchen-d
labels: bug, error-telemetry, agents, ai-generated, *fix
proposedLabel: none
triageAction: keep-open
confidence: 0.85
severity: correctness
breadth: common
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-08-07T06:18:34.043Z"
itemUpdatedAt: "2026-08-06T15:04:13Z"
itemCreatedAt: "2026-08-06T15:04:13Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 1
milestone: none
contentHash: 08ff87177eb651b65b87e6cf91cb1c0e114a25ac511d39412bd2fe2c53be8c67
policyHash: 70e952cc2fb28823
---

# Issue #329425 — [Error] unhandlederror-Response got filtered.

[https://github.com/microsoft/vscode/issues/329425](https://github.com/microsoft/vscode/issues/329425)

## Summary

Real, still-open bug: on current main, `CopilotLanguageModelWrapper._provideLanguageModelResponse` has no branch for `ChatFetchResponseType.Filtered`/`PromptFiltered`, so it falls through to a bare `throw new Error(result.reason)`, matching the reported unhandled error exactly. Small, well-scoped fix; not merged yet.

## Change summary

Calling the Language Model API through Copilot and getting a content-filtered response throws a generic `Error('Response got filtered.')` instead of a typed `vscode.LanguageModelError`, which the extension host logs as an unhandled error.

## Best solution

Add explicit `Filtered`/`PromptFiltered` branches in the `result.type !== ChatFetchResponseType.Success` block of extensions/copilot/src/extension/conversation/vscode-node/languageModelAccess.ts, throwing `vscode.LanguageModelError.Blocked(getFilteredMessage(result.category, false))`, consistent with the existing `ExtensionBlocked`/`QuotaExceeded`/`RateLimited` handling.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** error-telemetry
- **Confidence:** 0.85
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small, localized, well-understood gap: adding one branch for Filtered/PromptFiltered mirroring the existing ExtensionBlocked/QuotaExceeded pattern, using an already-exported helper; validated with a focused unit test.
- **Likely files:** `extensions/copilot/src/extension/conversation/vscode-node/languageModelAccess.ts`
- **Validation:** Add a unit test that mocks makeChatRequest2 to resolve with result.type === ChatFetchResponseType.Filtered (and PromptFiltered) and asserts _provideLanguageModelResponse rejects with a vscode.LanguageModelError whose name/code indicates Blocked, rather than a bare Error; the test should fail before the fix (generic Error is thrown) and pass after.
- **Fix prompt:**

  > In extensions/copilot/src/extension/conversation/vscode-node/languageModelAccess.ts, the `_provideLanguageModelResponse` method's `if (result.type !== ChatFetchResponseType.Success)` block (around lines 854-874) has explicit branches for `ChatFetchResponseType.ExtensionBlocked`, `QuotaExceeded`, and `RateLimited`, but no branch for `ChatFetchResponseType.Filtered` or `ChatFetchResponseType.PromptFiltered`. Those two cases fall through to the generic `throw new Error(result.reason)` at the end of the block, which the extension host logs as an unhandled error ('Response got filtered.') — filed as https://github.com/microsoft/vscode/issues/329425. Add an `else if (result.type === ChatFetchResponseType.Filtered || result.type === ChatFetchResponseType.PromptFiltered)` branch before the final generic throw, that throws `vscode.LanguageModelError.Blocked(getFilteredMessage(result.category, false))` — mirroring how `ExtensionBlocked` already throws `vscode.LanguageModelError.Blocked(...)`. Import `getFilteredMessage` from '../../../platform/chat/common/commonTypes' (it's already exported there, used elsewhere for the same message text) alongside the existing `ChatFetchResponseType`/`ChatLocation`/`getErrorDetailsFromChatFetchError` import. Do not change the `Success`, `Length`, `ExtensionBlocked`, `QuotaExceeded`, or `RateLimited` branches, and do not alter the generic fallback throw for any other result types. Add a unit test (near existing tests for this wrapper, likely under extensions/copilot/src/extension/conversation) that stubs the request to resolve with `result.type = ChatFetchResponseType.Filtered` (and separately `PromptFiltered`) and asserts the rejection is a `vscode.LanguageModelError` (not a bare `Error`), so the unhandled-error telemetry stops firing. Reference: https://github.com/microsoft/vscode/issues/329425 and the proposed patch in https://github.com/microsoft/vscode/issues/329432.

## Evidence

- **source trace:** extensions/copilot/src/extension/conversation/vscode-node/languageModelAccess.ts lines 854-874: the branch chain handles ExtensionBlocked, QuotaExceeded, RateLimited, but has no case for ChatFetchResponseType.Filtered or PromptFiltered, so both fall through to `throw new Error(result.reason)` at line 873, matching the reported stack trace at line 603/864.
- **helper already exists:** getFilteredMessage(category, supportsMarkdown) is exported from extensions/copilot/src/platform/chat/common/commonTypes.ts:492 and already used to classify Filtered/PromptFiltered as ChatErrorLevel.Info elsewhere, confirming filtering is treated as an expected outcome.
- **no merged PR:** The linked https://github.com/microsoft/vscode/issues/329432 is another auto-generated issue (not a merged/open human-owned PR — its actual PR creation failed) that proposes this exact diff; the fix has not landed on main.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
