---
number: 335505
url: "https://github.com/microsoft/vscode/issues/335505"
engine: copilot
owner: justschen
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.4
severity: papercut
breadth: common
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-11T12:54:29.374Z"
itemUpdatedAt: "2026-09-10T16:43:04Z"
itemCreatedAt: "2026-09-10T16:34:03Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 7bb478fbe218ed46b9724a18f96b33666a9dd20ad56cc4f34ea0cd6310e7e226
policyHash: 5fa429198b8c8ed6
---

# Issue #335505 — Quick Fix / Code Action widget: ArrowDown invokes selectNextCodeAction but selection never moves

[https://github.com/microsoft/vscode/issues/335505](https://github.com/microsoft/vscode/issues/335505)

## Summary

Plausible keyboard-navigation bug in the Code Action / Quick Fix widget, but I traced `selectNextCodeAction` → `ActionWidget.focusNext()` → `ActionList.focusNext()` on current main and the standard (non-filter) path looks correct — it calls `WorkbenchList.focusNext()` and reveals the new focus, so I can't confirm the defect from source alone; this needs a live repro or extension-disabled confirmation from the reporter.

## Change summary

In the Code Action/Quick Fix widget, ArrowDown correctly triggers `selectNextCodeAction` (confirmed via keybinding troubleshooting log) but the highlighted item never advances.

## Best solution

Ask the reporter to retest with `--disable-extensions` and on the latest Insiders build, and ideally attach a screen recording; if confirmed, the defect likely lives in src/vs/platform/actionWidget/browser/actionList.ts `focusNext()`/list rendering rather than the keybinding dispatch, which is already shown to fire correctly.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 2/6 Unverified
- **Severity:** papercut
- **Breadth:** common
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** editor-contrib
- **Confidence:** 0.4
- **Proposed comment:**

  > Thanks for the detailed report and the keybinding troubleshooting log — that's helpful. I traced `selectNextCodeAction` through `ActionWidget.focusNext()` into `ActionList.focusNext()` (src/vs/platform/actionWidget/browser/actionList.ts) and didn't spot an obvious defect in the code path for the standard case, so I wasn't able to confirm this from source alone. Could you confirm this still reproduces with `code --disable-extensions` and on the latest Insiders build? A short screen recording of the widget while pressing ArrowDown would also help narrow this down. This issue will auto-close in 7 days if there's no reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** No confirmed reproduction or concrete failing test yet; the static code trace didn't reveal an obvious defect, so this needs a live repro before any fix can be scoped.

## Evidence

- **command wiring:** src/vs/platform/actionWidget/browser/actionWidget.ts:362-383 registers `selectNextCodeAction` -> `IActionWidgetService.focusNext()` -> `ActionWidget.focusNext()` (line 118) -> `this._list.value?.focusNext()`.
- **list focusNext logic:** src/vs/platform/actionWidget/browser/actionList.ts:1891-1926 `focusNext()`: for the default Quick Fix widget (no `showFilter`, so `_filterInput` is undefined per lines 1006-1020), it falls straight to `this._list.focusNext(1, true, undefined, this.focusCondition)` and reveals the new focus — no obvious defect found in this path via static read.
- **base list:** src/vs/base/browser/ui/list/listWidget.ts:1814-1823 `List.focusNext` is a standard `findNextIndex` + `setFocus`, unchanged-looking and not obviously broken.
- **reporter's own diagnostic:** Keybinding troubleshooting log shows `selectNextCodeAction` correctly resolved and invoked on every ArrowDown press, ruling out a keybinding conflict but not narrowing further.
- **referenced prior issue:** Reporter cites #160062 as a similar past regression, but that is not verified here as the same root cause.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
