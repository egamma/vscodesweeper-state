---
number: 338099
url: "https://github.com/microsoft/vscode/issues/338099"
engine: copilot
owner: vritant24
labels: model-byok
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-26T12:54:32.101Z"
itemUpdatedAt: "2026-09-26T04:25:34Z"
itemCreatedAt: "2026-09-26T04:16:06Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 47a6ec835cd527c80ce15f9b029c2d52973d757861f356228e427ca91e7cdd35
policyHash: 278656bf1e7c0255
---

# Issue #338099 — Copilot Agent Window: BYOK custom models register in Chat but fail in agent sessions (session.model.switchTo 'not registered')

[https://github.com/microsoft/vscode/issues/338099](https://github.com/microsoft/vscode/issues/338099)

## Summary

Plausible, technically detailed report, but source inspection complicates the stated root cause and it closely overlaps an older, more thoroughly diagnosed issue (#337742) whose scope may or may not fully cover this one — asking for confirmation before deciding whether to consolidate.

## Change summary

A BYOK custom-endpoint model that is registered in Chat fails to switch to inside an Agent Host session with 'session.model.switchTo ... is not registered'.

## Best solution

If this is the same startup/restore race already traced in https://github.com/microsoft/vscode/issues/337742 (a session created/restored before the BYOK bridge hydrates never gets the provider/model config applied), consolidate there. If it's a distinct case — a session that already logged successful BYOK wiring still failing switchTo — the fix likely belongs in `_buildSessionConfig`/`_resolveByokSessionConfig` in `copilotSessionLauncher.ts`, re-applying the resolved BYOK config to the specific session attempting the switch.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** model-byok
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed trace and logs. Looking at current `main`, the session launcher already merges the resolved BYOK `providers`/`models` into every session's create/resume config as a single batch (`_buildSessionConfig` spreading `...byok`), so the 'nothing plumbs them into the registry' read may not fully match what's there today. Your own logs show the wiring succeeding for one session tag ('boot') while the switch fails in a different session ('c7f7565c') that never logs its own `Wired ...` line — that's the same pattern traced in https://github.com/microsoft/vscode/issues/337742, which points at sessions created/restored before the BYOK bridge hydrates.
  > 
  > Could you confirm: was the failing session (`c7f7565c`) freshly created in that Agent Window run, or auto-restored from a previous window/session? Full `agenthost.log` lines for that specific session's 'Preparing SDK session' entry (and whether any 'Wired N BYOK model(s)' line follows it) would help determine whether this is the same defect as #337742 or a distinct one. This issue will auto-close in 7 days without a reply, so please follow up when you get a chance.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** Needs-info: the reported root cause conflicts with what's traceable in current source, and it may be the same defect as an older, better-diagnosed related issue — more logs are needed before any diagnosis or fix attempt.

## Evidence

- **existing plumbing contradicts part of the report:** `_buildSessionConfig` in src/vs/platform/agentHost/node/copilot/copilotSessionLauncher.ts already spreads `...byok` (the resolved BYOK `providers`/`models`, computed by `_resolveByokSessionConfig`/`resolveByokSessionConfig`, lines 531-591 and 841-851) into every create/resume session config — this appears to already implement the reporter's proposed single-batch registration, so the claim 'nothing plumbs them into the session registry at boot' does not match current main as read.
- **genuine runtime logs, but for two different sessions:** The quoted log line `Wired N BYOK model(s) across M provider(s) via loopback proxy ...` matches `resolveByokSessionConfig`'s `logService.info` call verbatim (confirming it's a real trace), but it is tagged for a session labeled 'boot' while the failing `setModel`/`switchTo` call happens in a different session ('c7f7565c') that never logs its own `Wired ...` line — the same pattern as the confirmed race in #337742.
- **related, older, more thoroughly diagnosed issue:** https://github.com/microsoft/vscode/issues/337742 (opened 2026-09-24, 2 days earlier) reports the identical error text `session.model.switchTo failed ... is not registered` for BYOK models in Agent Host sessions, with a detailed timeline tracing it to a session created/restored before the BYOK bridge hydrates. It states 'new sessions always get the BYOK wiring' as a workaround, which would need reconciling against this report's scenario before treating them as the same defect.
- **switchTo is SDK-internal:** `CopilotAgentSession.setModel` (src/vs/platform/agentHost/node/copilot/copilotAgentSession.ts:3931) calls `this._wrapper.session.setModel(...)`, which is a vendored `@github/copilot-sdk` call — the actual model-registry lookup and 'is not registered' rejection live outside this repo.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
