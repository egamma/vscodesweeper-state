---
number: 226737
url: "https://github.com/microsoft/vscode/issues/226737"
engine: copilot
owner: osortega
labels: bug, search, search-api
proposedLabel: none
triageAction: keep-open
confidence: 0.9
severity: papercut
breadth: niche
priority: none
visionFit: aligned
autoFixable: true
reviewedAt: "2026-07-24T12:37:01.506Z"
itemUpdatedAt: "2024-12-11T22:36:55Z"
itemCreatedAt: "2024-08-27T11:01:57Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: On Deck
contentHash: cf5f2a024df859109c2f6b1d6866512d0639149dd99228c06717492a941ce471
policyHash: 0c32deb9982e8caf
---

# Issue #226737 — Properties of `FileSearchProviderOptions` should be marked `readonly`

[https://github.com/microsoft/vscode/issues/226737](https://github.com/microsoft/vscode/issues/226737)

## Summary

Still unfixed on main: the `FileSearchProviderOptions` interface in `vscode.proposed.fileSearchProvider2.d.ts` lacks `readonly` modifiers on all its properties. The issue has an 'On Deck' milestone, so keeping open per policy.

## Change summary

Mark all properties of `FileSearchProviderOptions` (and its nested object types) as `readonly` to signal to extension authors that these are input-only options.

## Best solution

Add `readonly` to every property in the `FileSearchProviderOptions` interface and its nested object literals in `src/vscode-dts/vscode.proposed.fileSearchProvider2.d.ts`, matching the convention used elsewhere in the extension API for options passed to extensions.

## Vision fit

- **Fit:** aligned
- **Reason:** Maintaining extension API hygiene is part of the durable extension-ecosystem theme.
- **Evidence:** Durable theme: Extension ecosystem — API hygiene (finalize or clean out proposed APIs), authoring tooling.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** search, search-api
- **Confidence:** 0.9
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Small, localized type-level change (adding `readonly` to interface properties); no behavioral change, validated by typecheck.
- **Likely files:** `src/vscode-dts/vscode.proposed.fileSearchProvider2.d.ts`
- **Validation:** Run `npm run typecheck-client` — it must pass after adding `readonly` modifiers. Any extension code that attempts to assign to these properties should now produce a compile error.
- **Fix prompt:**

  > The `FileSearchProviderOptions` interface in `src/vscode-dts/vscode.proposed.fileSearchProvider2.d.ts` is missing `readonly` modifiers on all its properties. VS Code's convention for options objects passed to extensions is to mark them `readonly` (and arrays as `readonly T[]`) so extension authors know they cannot mutate these values. Add `readonly` to every property in the interface: `folderOptions` (make it `readonly folderOptions: readonly { ... }[]`), and inside the object literal type mark `folder`, `includes` (as `readonly includes: readonly string[]`), `excludes` (as `readonly excludes: readonly GlobPattern[]`), `followSymlinks`, `useIgnoreFiles`, and its nested `local`, `parent`, `global` properties all `readonly`. Also mark `session` and `maxResults` readonly at the top level. Do NOT change any runtime code — this is purely a `.d.ts` type change. After the change, `npm run typecheck-client` must pass. Reference issue: https://github.com/microsoft/vscode/issues/226737.

## Evidence

- **source verification:** src/vscode-dts/vscode.proposed.fileSearchProvider2.d.ts on current main: `FileSearchProviderOptions` has no `readonly` modifiers on `folderOptions`, `session`, or nested properties like `folder`, `includes`, `excludes`, `followSymlinks`, `useIgnoreFiles`.
- **no fix in history:** git log --grep='FileSearchProviderOptions' returns no commits addressing readonly — the issue remains open on main.
- **milestone:** Issue carries 'On Deck' milestone — hard keep-open rule applies.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
