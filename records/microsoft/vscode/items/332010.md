---
number: 332010
url: "https://github.com/microsoft/vscode/issues/332010"
engine: copilot
owner: aeschli
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-22T08:42:26.300Z"
itemUpdatedAt: "2026-08-21T18:25:33Z"
itemCreatedAt: "2026-08-21T18:13:45Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: 7da92b0459831ad7e27250cad5ce0e13130123cfd4ea02ac1a461ba21929fb9b
policyHash: 4721e56ad78fdd17
---

# Issue #332010 — CMD/bat comments are missing a space after REM

[https://github.com/microsoft/vscode/issues/332010](https://github.com/microsoft/vscode/issues/332010)

## Summary

Tracing the toggle-line-comment code on main shows the add-comment path always appends a space after the line-comment token when `editor.comments.insertSpace` is true (the default), so `@REM` + space should be inserted before `text` — the reported `@REMtext` doesn't match what the current source does by default, and the report gives no settings detail to explain the gap.

## Change summary

Toggling a line comment (Ctrl+/) in a .bat/.cmd file allegedly inserts `@REMtext` with no space, instead of `@REM text`.

## Best solution

Ask the reporter to confirm `editor.comments.insertSpace` (should be true by default) and reproduce with a clean profile/settings; if it still reproduces on the current stable build, the fix (if any) would live in src/vs/editor/contrib/comment/browser/lineCommentCommand.ts's `_createAddLineCommentsOperations`, which the source trace shows already appends a space by default.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report. Looking at the comment-toggling code (`src/vs/editor/contrib/comment/browser/lineCommentCommand.ts`), the add-comment path inserts a space after the `@REM` token whenever `editor.comments.insertSpace` is enabled, which it is by default — so this should normally produce `@REM text`, not `@REMtext`. Could you confirm you don't have `editor.comments.insertSpace` set to `false` in your settings, and retest on the latest stable release (1.134.0)? This will help pin down whether there's a real regression here.
  > 
  > This issue will auto-close in 7 days if there's no reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Source tracing suggests the reported symptom contradicts default behavior; without a confirmed reproduction on a clean profile, there's no defect to localize or validate a fix against.

## Evidence

- **language config:** extensions/bat/language-configuration.json defines `"lineComment": "@REM"` with no trailing space baked in.
- **space-insertion logic:** src/vs/editor/contrib/comment/browser/lineCommentCommand.ts:410-422 `_createAddLineCommentsOperations` computes `afterCommentStr = this._insertSpace ? ' ' : ''` and inserts `commentStr + afterCommentStr`, and `editor.comments.insertSpace` defaults to `true` (src/vs/editor/common/config/editorOptions.ts:1520-1524), so toggling on an uncommented line should yield `@REM text`, not `@REMtext`.
- **existing test coverage:** src/vs/editor/contrib/comment/test/browser/lineCommentCommand.test.ts:56-69 confirms `!@#` (also multi-char, no trailing space in config) becomes `!@# some text` after toggle, matching the expected space-insertion behavior for tokens like `@REM`.
- **bot reply:** vs-code-engineering bot asked the reporter to retest on the latest stable (1.134.0 vs. reported 1.130.0), which hasn't happened yet.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
