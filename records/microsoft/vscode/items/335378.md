---
number: 335378
url: "https://github.com/microsoft/vscode/issues/335378"
engine: copilot
owner: Giuspepe
labels: triage-needed
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.45
severity: freeze
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-11T12:59:53.040Z"
itemUpdatedAt: "2026-09-10T00:56:22Z"
itemCreatedAt: "2026-09-10T00:47:48Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: f0beb1acb3cb6f0d0539c2823c6094b1fb36a072483e814af559460325871e35
policyHash: 5fa429198b8c8ed6
---

# Issue #335378 — Browser client tools never return: completions forwarded with source=client-dispatch are never delivered to the pending tool_use

[https://github.com/microsoft/vscode/issues/335378](https://github.com/microsoft/vscode/issues/335378)

## Summary

Detailed report, but tracing the actual delivery path on main (AgentSideEffects._notifyClientToolCallComplete → ClaudeAgent.onClientToolCallComplete → ClaudeAgentSession.completeClientToolCall → PendingRequestRegistry.respond) shows nothing browser-tool-specific — it's the same generic client-tool completion path used for every client tool, so the described 1:1 correlation with `client-dispatch`/browser tools isn't explained by anything I can find in source. Several specifics cited as evidence (the `withContext=true` log field, the exact 'Running client tool' log line format) don't appear anywhere in the current tree, which makes it hard to tell whether this matches current main or an older/different build — asking the reporter to confirm on latest Insiders is the right next step before deciding.

## Change summary

Client tool calls dispatched via the `client-dispatch` path (specifically browser tools like readPage/openBrowserPage/runPlaywrightCode) allegedly never deliver their completion back to the pending tool_use, hanging the turn indefinitely and leaving it uncancellable.

## Best solution

Have the reporter confirm reproduction on a current Insiders build with Agent Host debug logs enabled (see the agent-host-logs export flow) so the exact `ChatToolCallComplete`/`tool_use_id` pairing can be checked against `claudeAgentSession.ts`'s `_pendingClientToolCalls` registry; if it reproduces, the fix likely lives in how the client-tool MCP handler's `register()` keys line up with the completion's `toolCallId` for these specific tools.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 2/6 Unverified
- **Severity:** freeze
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the very detailed write-up. I traced the client-tool completion path in `agentSideEffects.ts` / `claudeAgentSession.ts` on current main and it's generic to all client tools (not special-cased for browser tools), so I can't yet see why only `openBrowserPage`/`runPlaywrightCode`/`readPage` would be affected. A couple of the log details in the report (e.g. `withContext=true`) don't match any current log format in the source, so it'd help to confirm this reproduces on the latest Insiders build with a fresh Agent Host debug log export, plus the exact `tool_use_id`/`toolCallId` values from both the 'Running client tool' and the 'Forwarding client tool completion' lines for one hang.
  > 
  > This will auto-close in 7 days without a reply, but please do reopen/comment with the requested info if you can still repro it.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Bug with unclear reproduction — source tracing found the generic delivery path intact, so no confirmed defect or concrete validation to build an automated fix from.

## Evidence

- **delivery path traced:** src/vs/platform/agentHost/node/agentSideEffects.ts:1272 _notifyClientToolCallComplete → src/vs/platform/agentHost/node/claude/claudeAgent.ts:2495 onClientToolCallComplete → src/vs/platform/agentHost/node/claude/claudeAgentSession.ts:1384 completeClientToolCall → common/pendingRequestRegistry.ts:74 respond(). This is generic to all client tools, not special-cased for browser tools.
- **unmatched log strings:** The issue's exact log fragments ('withContext=true', 'Running client tool: ... (callId=..., withContext=true)') do not appear anywhere under src/vs/platform/agentHost — could indicate a different build/version than current main, or a misremembered/paraphrased log.
- **known related race documented in code:** common/pendingRequestRegistry.ts's respondOrBuffer doc explicitly calls out a register/respond race for 'the Copilot client-tool round-trip' (src/vs/platform/agentHost/node/copilot/copilotAgentSession.ts uses respondOrBuffer), but Claude's client-tool path uses plain register()/respond() without that buffering — worth the reporter confirming if this is Claude-specific, since the report is about the `anthropic.claude-code` extension.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
