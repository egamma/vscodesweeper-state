---
number: 330690
url: "https://github.com/microsoft/vscode/issues/330690"
engine: copilot
owner: hediet
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.35
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-18T09:08:49.786Z"
itemUpdatedAt: "2026-08-17T22:30:06Z"
itemCreatedAt: "2026-08-13T17:02:02Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 660fc735905177c45a411a0c0c64721b337572cd13c1aebd50b6d8b71734ae1f
policyHash: 4721e56ad78fdd17
---

# Issue #330690 — Unnecessary scroll that causes limited vision

[https://github.com/microsoft/vscode/issues/330690](https://github.com/microsoft/vscode/issues/330690)

## Summary

Plausible but underspecified report of unwanted horizontal scroll in wrapped Markdown after a split-editor resize; the code path that would explain it (viewport-wrap content width recompute) can't be pinned down without a clearer repro, screenshot, or GIF, so this needs more info before it can be routed or closed.

## Change summary

Markdown editor (word-wrap on by default) shows a horizontal scrollbar and auto-scrolls left while typing after using split editor, even though wrapped text shouldn't need horizontal scroll.

## Best solution

Ask the reporter for a short recording and to confirm whether a single long unwrapped token (URL, long code span) is present on the line — if so this is expected (a line that can't wrap still needs horizontal scroll under `editor.wordWrap: on`, per `viewLayout.ts`'s `_computeContentWidth`); if not, the likely culprit is stale `maxLineWidth`/content-width recomputation on editor resize in `src/vs/editor/common/viewLayout/viewLayout.ts`, which would need a real reproduction to confirm before any code investigation.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** editor-core
- **Confidence:** 0.35
- **Proposed comment:**

  > Thanks for the report. By default Markdown files wrap (`editor.wordWrap: on`), and wrapped lines shouldn't normally need horizontal scroll unless a single line contains an unbroken token (a long URL or unbroken code span) wider than the viewport — in that case the scrollbar is expected. Could you share a short screen recording or GIF of the split + typing behavior, and confirm whether any of your paragraphs contain a very long unbroken word/URL? That'll help pin down whether this is a real layout bug after resizing or expected behavior for long tokens. This issue will auto-close in 7 days if there's no reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction is unclear from the report alone; without a confirmed repro there's nothing concrete to trace or validate a fix against.

## Evidence

- **markdown default wrap:** extensions/markdown-language-features/package.json sets `configurationDefaults.[markdown].editor.wordWrap: "on"`, so markdown files wrap by default.
- **content width logic:** src/vs/editor/common/viewLayout/viewLayout.ts `_computeContentWidth()` skips `scrollBeyondLastColumn` when `wrappingInfo.isViewportWrapping` is true and only returns extra width beyond the viewport when `maxLineWidth > layoutInfo.contentWidth + typicalHalfwidthCharacterWidth` — i.e. horizontal scroll under wrap should only appear when a single unbroken token is wider than the viewport.
- **report is thin:** No screenshot/recording, no confirmation of a long unwrapped token on the line, and no comments corroborating the described auto-scroll-while-typing behavior.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
