---
number: 332936
url: "https://github.com/microsoft/vscode/issues/332936"
engine: copilot
owner: roblourens
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-28T20:16:40.014Z"
itemUpdatedAt: "2026-08-28T11:19:26Z"
itemCreatedAt: "2026-08-27T13:37:49Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: 6db25c892ee7e8b45bc5c0f765d88132236933626a35a0f730af8e19eede0670
policyHash: 3d6943c3a15c721c
---

# Issue #332936 — Agents can not output more than 500 characters in task_complete

[https://github.com/microsoft/vscode/issues/332936](https://github.com/microsoft/vscode/issues/332936)

## Summary

Thanks for the report. Current main already special-cases `task_complete` so the completion text shown to the user comes from the tool call's original `summary` argument rather than the offloaded/truncated tool output — but the report is thin (no exact repro steps or VS Code channel) and this checkout's git history is squashed to a single commit, so I can't date that fix against the report or rule out a distinct root cause (e.g. the CLI truncating the argument itself before it reaches VS Code).

## Change summary

Agent task-completion summaries longer than ~500 characters get offloaded to a temp file as "tool output" instead of staying visible in the chat UI.

## Best solution

If reproducible on the latest Insiders build, this likely needs a fix (or confirmation of an existing fix) in src/vs/platform/agentHost/node/copilot/mapSessionEvents.ts / copilotToolDisplay.ts, which already contain a `getTaskCompleteSummary`/`getTaskCompleteMarkdown` fallback specifically for the 'tool output truncated to a file' scenario for `task_complete` — worth confirming whether the reporter's build predates that fallback or whether the underlying `@github/copilot-sdk` truncates the `summary` argument itself before VS Code ever sees it.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** chat
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report and for confirming it persists after updating. We found that VS Code's Agent Host already has specific handling for `task_complete` so completion text shown in chat comes from the original summary the agent provided, rather than a truncated/offloaded tool-output file — but we can't tell from here whether your build predates that handling or whether the truncation is happening earlier (e.g. in the underlying CLI/SDK before VS Code sees it). Could you confirm: (1) whether you're using the built-in Agent chat or a Copilot CLI session, (2) the exact VS Code build/channel (Stable vs Insiders), and (3) a screenshot or the exact completion text length involved? This issue will auto-close in 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Root cause location (VS Code's existing fallback vs. an external CLI/SDK truncation point) is unconfirmed, and the report lacks a concrete, executable reproduction to validate against.

## Evidence

- **existing fallback:** src/vs/platform/agentHost/node/copilot/copilotToolDisplay.ts defines getTaskCompleteSummary/getTaskCompleteMarkdown, which render the tool call's input `summary` argument and only fall back to the (possibly truncated/offloaded) tool result content when no summary was supplied.
- **targeted test coverage:** src/vs/platform/agentHost/test/node/mapSessionEvents.test.ts, 'task_complete renders the input summary when tool output is truncated', simulates a tool.execution_complete result of 'Output too large to read at once (11.3 KB). Saved to: /tmp/task-complete.txt' and asserts the rendered markdown uses the original summary argument instead.
- **shallow checkout:** This clone's git log has only 1 commit (114237ee, dated 2026-08-28), so I cannot determine when the fallback landed relative to the issue's filing (2026-08-27) or whether it predates/postdates the reporter's 1.135.0 stable test.
- **possible external root cause:** The truncation-to-file behavior itself (e.g. '@github/copilot-sdk' referenced in copilotToolDisplay.test.ts) may originate in the external Copilot CLI/SDK, which could also be truncating the `summary` argument before VS Code receives it — a scenario the existing fallback would not help with.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
