---
number: 245909
url: "https://github.com/microsoft/vscode/issues/245909"
engine: copilot
owner: connor4312
labels: feature-request, chat-mcp
proposedLabel: none
triageAction: keep-open
confidence: 0.75
severity: none
breadth: common
priority: none
visionFit: aligned
autoFixable: false
reviewedAt: "2026-07-25T07:36:37.495Z"
itemUpdatedAt: "2025-04-09T14:11:24Z"
itemCreatedAt: "2025-04-07T21:19:51Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 1
milestone: On Deck
contentHash: 2b5c03c0aee4ae29019322dd90a25940b65c1ca69bcd2572472c5b6b394322c5
policyHash: a68119f215da75e1
---

# Issue #245909 — MCP support substitutions as command line args

[https://github.com/microsoft/vscode/issues/245909](https://github.com/microsoft/vscode/issues/245909)

## Summary

Team-authored issue (hard keep-open). The underlying capability — variable substitution in MCP `args` — appears to already be implemented on current main via `ConfigurationResolverExpression.parse` applied to the entire `McpServerLaunch` object (including `args`), which recursively resolves `${env:…}`, `${workspaceFolder}`, etc. A maintainer should verify whether the original ask is now fully satisfied and close if so.

## Change summary

Support variable substitutions (e.g. `${env:CONNECTION_STRING}`) in MCP server command-line `args`.

## Best solution

Verify the feature works end-to-end (the infrastructure is in place on main: `_replaceVariablesInLaunch` in `mcpRegistry.ts` feeds the entire `McpServerLaunch` object — including `args` — through `ConfigurationResolverExpression.parse` and `resolveAsync`, which handles `${env:…}` and other substitutions). If confirmed working, close as implemented; if edge cases remain (e.g. `envFile`-sourced variables not being available to `${env:…}` in `args`), document what's missing.

## Vision fit

- **Fit:** aligned
- **Reason:** MCP server configuration is part of the current agent-experience investment.
- **Evidence:** Aligns with current agent host and MCP infrastructure priorities.

## Proposed action

- **Triage action:** keep-open
- **Item type:** feature-request
- **Reproduction:** not_applicable
- **Rating:** not applicable
- **Severity:** none
- **Breadth:** common
- **Priority:** none
- **Proposed label:** none
- **Area labels:** chat-mcp
- **Confidence:** 0.75
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Feature request authored by a team member; the capability appears already implemented — needs maintainer verification, not a code fix.

## Evidence

- **variable resolution pipeline:** `_replaceVariablesInLaunch` in `src/vs/workbench/contrib/mcp/common/mcpRegistry.ts` (line 451) calls `delegate.substituteVariables(definition, launch)` followed by `ConfigurationResolverExpression.parse(withRemoteFilled)` on the full `McpServerLaunch` object, which includes `args`.
- **recursive resolution:** `ConfigurationResolverExpression.parse<T>` in `src/vs/workbench/services/configurationResolver/common/configurationResolverExpression.ts` (line 94) walks the entire object tree, resolving `${env:…}` and other substitutions in all string values including array elements.
- **test evidence:** Tests in `configurationResolverService.test.ts` (line 945) demonstrate resolving `${env:…}` inside array elements of parsed objects, confirming the resolver handles arrays recursively.
- **team-authored:** Author association is MEMBER — hard keep-open rule applies.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
