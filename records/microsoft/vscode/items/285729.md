---
number: 285729
url: "https://github.com/microsoft/vscode/issues/285729"
engine: copilot
owner: anthonykim1
labels: bug, tasks, terminal-shell-pwsh
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.7
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-25T16:59:25.961Z"
itemUpdatedAt: "2026-01-06T18:09:48Z"
itemCreatedAt: "2026-01-03T11:47:08Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 4
milestone: none
contentHash: d497cd39c9a352ae271ec5eee98ae04d413285ca83f65208ac4320981ce482ee
policyHash: ef2efec6827348f6
---

# Issue #285729 — Problem in VS Code task with -Command parameter

[https://github.com/microsoft/vscode/issues/285729](https://github.com/microsoft/vscode/issues/285729)

## Summary

The reported error — `Set-Location '<cwd>' -Command <script>` — cannot be traced in the current codebase (1.129.0). The task system sets `shellLaunchConfig.cwd` natively via node-pty and adds `-Command` to shell args separately; nothing constructs a `Set-Location` prefix in the current code. The reporter should confirm whether this still reproduces on the latest stable (1.128+). Requesting info.

## Change summary

Running a shell task with a `cwd` option on Windows PowerShell produces a `Set-Location` error because `-Command` is injected as a parameter of `Set-Location` instead of as a PowerShell argument.

## Best solution

Ask the reporter to reproduce on the latest stable build. If it still reproduces, a verbose terminal log (Developer: Open Terminal Log Level…) and the exact default terminal profile configuration would help identify where `Set-Location` is being injected. If it no longer reproduces, close as fixed.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** tasks, terminal-shell-pwsh
- **Confidence:** 0.7
- **Proposed comment:**

  > Thanks for the report. I wasn't able to trace where `Set-Location` is being injected into the command line in the current codebase — the task system sets the working directory natively via the PTY, not via a PowerShell `Set-Location` command, and `-Command` is added as a separate shell argument.
  > 
  > Could you try reproducing this on the latest VS Code stable (1.128+) or Insiders? Several task system fixes have shipped since 1.107. If it still reproduces:
  > 
  > 1. Share the output of **Terminal: Show Shell Integration Log** (or enable `terminal.integrated.shellIntegration.showWelcome`)
  > 2. Share your default terminal profile configuration (`terminal.integrated.defaultProfile.windows`)
  > 3. Check whether any extensions modify the terminal launch (e.g. PowerShell extension)
  > 
  > Note: `info-needed` issues auto-close after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Cannot trace the defect in current source — need reproduction confirmation on latest build first.

## Evidence

- **no Set-Location in task code:** Searched the entire codebase for `Set-Location` in TypeScript task/terminal code — it does not appear in `src/vs/workbench/contrib/tasks/` or `src/vs/platform/terminal/`. The task system sets `shellLaunchConfig.cwd` (terminalTaskSystem.ts line 1398) which is passed to node-pty natively, not via a PowerShell command.
- **task system -Command injection:** In terminalTaskSystem.ts line 1281–1283, `-Command` is added to PowerShell args (`toAdd.push('-Command')`) and the command line is appended separately. The two should not collide with `Set-Location`.
- **stale report:** Filed against VS Code 1.107.1 (December 2025), 203 days old with zero community upvotes and no external reproduction. Current main is at 1.129.0 with multiple task system changes since then.
- **workaround exists:** The reporter confirmed in a comment that `type: "process"` with `command: "powershell.exe"` works correctly.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
