---
number: 336647
url: "https://github.com/microsoft/vscode/issues/336647"
engine: copilot
owner: roblourens
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-19T12:43:06.698Z"
itemUpdatedAt: "2026-09-17T22:54:13Z"
itemCreatedAt: "2026-09-17T22:39:21Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 74cda099d2d7e1462361ae2113be4871c5d9d6202f38fab50239c7593da9f48b
policyHash: fd90e4d8a068f279
---

# Issue #336647 — Asking to list open sessions on a follow-up turn reliably breaks Agent Host session (Error: query - No response was returned)

[https://github.com/microsoft/vscode/issues/336647](https://github.com/microsoft/vscode/issues/336647)

## Summary

Thanks for the detailed diagnostic work — but this can't be confirmed from source alone (the failure is a model/API-side empty response, not a deterministic code path), and the report is missing the actual VS Code/Copilot Chat version numbers, so I'd ask for those plus the offered debug log bundles before triaging further.

## Change summary

Asking the assistant to list open Agent Host sessions on a non-first turn reliably makes Claude Sonnet return an empty model response, which the Agent Host surfaces as a generic, unrecoverable `errorType: 'query'` "No response was returned" error.

## Best solution

A maintainer with access to real model traffic should capture the request payload sent when `list_sessions` is invoked mid-session and compare it against a working turn to find what differs (tool schema, prior tool-result content, etc.) — the generic empty-response guard itself is working as designed, so the fix (if any) is in what triggers the empty completion, not in the error-reporting guard.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the thorough repro work here — the isolation testing (MCP disabled, control turns, 3/3 reproduction) is genuinely useful.
  > 
  > The generic `errorType: 'query'` / "No response was returned" path itself is intentional (there's existing test coverage for HTTP-200-but-empty model responses being surfaced this way), so the interesting question is why asking to list sessions specifically triggers an empty completion from Claude Sonnet — that's likely in the request payload built for that turn rather than in the error-reporting code.
  > 
  > Could you fill in the actual Copilot Chat extension version and VS Code version in the issue body, and attach (or link) the two Agent Host debug log exports you mentioned? That'll help narrow down whether this is tied to a specific build. This will auto-close in 7 days without a reply, so please follow up when you get a chance.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** Needs-info verdict and the defect is model/API-side (empty completion), not a bounded, source-confirmable code fix — no validation can be named without live reproduction.

## Evidence

- **generic error is by design:** src/vs/platform/agentHost/test/node/e2e/suites/copilotRuntimeToolsSuite.ts:111 has an existing, passing e2e test ('an accepted empty response reports a query error instead of completing silently') that intentionally maps an HTTP-200-but-empty model response to `errorType: 'query'`, `message: 'No response was returned...'` — matching exactly what the reporter cites from copilotAgentSession.ts. This confirms the *reporting* path is intentional generic behavior, not itself a defect.
- **PR reference checks out:** PR https://github.com/microsoft/vscode/pull/334818 ('agentHost: Add Copilot runtime regression E2E coverage') is merged and added exactly this empty-response test coverage, corroborating the reporter's pointer.
- **related but not a duplicate canonical:** https://github.com/microsoft/vscode/issues/253126 ('Meta: Sorry, no response was returned') is an open, broader umbrella tracking the same user-visible error message across models generically. It doesn't own this issue's specific, isolated trigger (list_sessions requests) and folding this report in would bury the reporter's isolation testing; keeping this open separately preserves the lead.
- **tool schema looks unremarkable:** src/vs/platform/agentHost/node/shared/sessionServerTools.ts defines the `list_sessions` tool and its input schema; nothing in the schema or description stands out as malformed from a read of the source, so the trigger is likely in request construction or model-side behavior rather than an obvious schema bug.
- **missing version info:** The issue body still has the Copilot Chat Extension Version and VS Code Version fields as unfilled template placeholders.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
