---
number: 144115
url: "https://github.com/microsoft/vscode/issues/144115"
engine: copilot
owner: connor4312
labels: bug, extensions-development
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.7
severity: papercut
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-20T15:37:34.769Z"
itemUpdatedAt: "2024-12-12T21:46:24Z"
itemCreatedAt: "2022-02-17T23:56:04Z"
upvotes: 0
reactionsTotal: 0
commentCount: 7
relatedOpenIssues: 0
milestone: none
contentHash: c345cc0b7778ce93d747f7c04a1897447f26d4ec0a532b67cb7b02e985cac1ec
policyHash: ebd3763732abe97d
---

# Issue #144115 — runTest is not honoring the launchArg '--new-window'

[https://github.com/microsoft/vscode/issues/144115](https://github.com/microsoft/vscode/issues/144115)

## Summary

The reporter's scenario (sequential `runTests` calls sharing user-data) may be resolved by improvements to `--new-window` handling on current main (notably commit 2c3224ba1ff which fixed `--new-window` not opening an empty window). The issue is over 4 years old with no confirmed reproduction on a recent build, and the root cause may partially live in `@vscode/test-electron` (separate repo). Requesting confirmation on current VS Code.

## Change summary

Sequential `runTests` calls in `@vscode/test-electron` restore the previous folder despite passing `--new-window`, because the shared user-data directory retains window state.

## Best solution

Verify whether this still reproduces on a recent VS Code build. If it does, the best fix is likely in `@vscode/test-electron` — either isolating user-data directories per `runTests` call or passing `window.restoreWindows: none` as a default test setting. The core `--new-window` handling in `src/vs/code/electron-main/app.ts` now correctly sets `forceEmpty: true` when no folder paths are provided.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** papercut
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** extensions-development
- **Confidence:** 0.7
- **Proposed comment:**

  > Thanks for the report. Since this was filed, VS Code's `--new-window` handling has been improved — notably in 1.93 (https://github.com/microsoft/vscode/issues/226392) where `--new-window` now reliably opens an empty window without restoring previous session state.
  > 
  > Could you check whether this still reproduces on a recent VS Code build (1.100+)? If it does, the cleanest fix for sequential `runTests` calls may be to use a unique `--user-data-dir` per invocation or to set `window.restoreWindows` to `none` in the test profile.
  > 
  > Note: `info-needed` issues auto-close after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Old issue likely addressed by subsequent improvements; unclear if still reproduces on current main. Root cause may be in @vscode/test-electron, not core.

## Evidence

- **current code path:** In src/vs/code/electron-main/app.ts line ~1474, when `--new-window` is passed without CLI file/folder args, `forceEmpty: true` is set, which skips session restoration entirely in windowsMainService.ts line ~838.
- **related fix:** Commit 2c3224ba1ffc691af706719dc66b047541e5c3a4 (2024-08-23) fixed `--new-window` not opening an empty window when `window.restoreWindows: preserve` — https://github.com/microsoft/vscode/issues/226392
- **member engagement:** connor4312 (MEMBER) commented that this is about VS Code's window restoration behavior, not specific to vscode-test, and noted the test user-data dir is separate from the standard profile.
- **workaround exists:** Reporter implemented clearUserWorkspaceData() to delete workspace state between runs.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
