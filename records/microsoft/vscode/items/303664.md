---
number: 303664
url: "https://github.com/microsoft/vscode/issues/303664"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-22T00:34:54.367Z"
itemUpdatedAt: "2026-03-22T20:14:27Z"
itemCreatedAt: "2026-03-21T01:51:07Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: none
contentHash: 996729d1c3a0608120af3142b1d9454f022db26e1ed71bd49eb11b81e451b60f
policyHash: ebd3763732abe97d
---

# Issue #303664 — Can't commit when git config is `~/.config/git/config`

[https://github.com/microsoft/vscode/issues/303664](https://github.com/microsoft/vscode/issues/303664)

## Summary

The report describes git config at XDG path `~/.config/git/config` not being read by VS Code's git integration, but VS Code simply shells out to `git config --get-all user.name/email` which should natively respect XDG paths. The reproduction details are too thin to confirm — no git output logs, no info on how VS Code is launched, and no independent confirmation.

## Change summary

VS Code's Source Control fails to commit when git config lives at the XDG path `~/.config/git/config` instead of `~/.gitconfig`, despite git CLI working fine.

## Best solution

Request the reporter to provide VS Code's Git Output channel logs (`Git: Show Git Output`) showing what `git config --get-all user.name` returns, and clarify how VS Code is launched (desktop entry, terminal, snap/flatpak). The most likely root cause is an environment difference (e.g. `HOME` or `XDG_CONFIG_HOME` not matching the user's shell). If confirmed, the fix would be in `extensions/git/src/git.ts` spawn environment handling.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the report. VS Code's git integration delegates to the `git` CLI for config lookups (`git config --get-all user.name`), which should natively respect `~/.config/git/config` per the XDG spec — so something in the environment is likely different when VS Code runs git versus your terminal.
  > 
  > Could you help narrow this down?
  > 1. Open the **Git Output** channel (Command Palette → `Git: Show Git Output`) and share the relevant output when a commit fails.
  > 2. How do you launch VS Code — from a terminal, a desktop entry, via Snap/Flatpak, etc.?
  > 3. Does `echo $HOME` and `echo $XDG_CONFIG_HOME` in VS Code's integrated terminal match your regular terminal?
  > 
  > This will help us determine whether the issue is an environment difference (e.g. Snap/Flatpak sandboxing) or something VS Code is doing.
  > 
  > Note: issues labeled `info-needed` are auto-closed after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Report too thin to confirm the defect — the code path correctly delegates to git CLI which should handle XDG paths; root cause likely in environment, not VS Code code.

## Evidence

- **code path:** extensions/git/src/git.ts lines 2157-2168: on commit failure, VS Code runs `git config --get-all user.name` and `git config --get-all user.email` without scope restriction — these should respect git's XDG config search order.
- **env passthrough:** extensions/git/src/git.ts line 689: spawn merges `process.env` into the child environment, so HOME and XDG_CONFIG_HOME should be inherited.
- **no GIT_CONFIG override:** No code in the git extension sets GIT_CONFIG or XDG_CONFIG_HOME environment variables that would interfere with config file discovery.
- **thin reproduction:** No Git Output channel logs provided, no info on VS Code launch method (snap/flatpak can sandbox HOME), no independent confirmation, 0 upvotes.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
