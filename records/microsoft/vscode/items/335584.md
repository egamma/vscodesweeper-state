---
number: 335584
url: "https://github.com/microsoft/vscode/issues/335584"
engine: copilot
owner: vritant24
labels: mcp, ai-customizations
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.55
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: plan
reviewedAt: "2026-09-19T12:53:38.342Z"
itemUpdatedAt: "2026-09-17T22:21:07Z"
itemCreatedAt: "2026-09-10T22:47:19Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 241ec5ffd98850d0c3921494b4c0b41de8d572227a18fe2a693e64de184a616b
policyHash: fd90e4d8a068f279
---

# Issue #335584 — Move failed MCP servers to the top of the MCP server list

[https://github.com/microsoft/vscode/issues/335584](https://github.com/microsoft/vscode/issues/335584)

## Summary

Real, unimplemented gap: the MCP server list only sorts by enabled state today, not by error/failure status, so a valid request — already correctly labeled `mcp`/`ai-customizations` — should stay open for a maintainer session rather than routing or closing.

## Change summary

Sort MCP servers that failed to start to the top of the MCP server list instead of leaving them mixed in below the fold.

## Best solution

Extend the existing sort in mcpListWidget.ts's `updateFilteredServers`/`renderFilteredServers` path to rank entries with an error connection state first, reusing the already-present `getMcpStatusKind`/`connectionState.get().state` helpers that surface `McpConnectionState.Kind.Error` per entry type.

## Proposed action

- **Triage action:** keep-open
- **Item type:** polish
- **Reproduction:** not_applicable
- **Evidence:** not applicable
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** plan
- **Rationale:** Bounded, well-scoped polish with the needed error-state data already computed elsewhere in the file, but no unit test harness exists for this DI-heavy UI widget, so a maintainer session should first decide how to validate (manual check vs. new test scaffolding) before implementing.

## Plan brief

- **Likely files:** `src/vs/workbench/contrib/chat/browser/aiCustomization/mcpListWidget.ts`
- **Validation:** Manually verify: with a workspace containing an MCP server configured to fail to start alongside healthy servers, the failed server appears above healthy/enabled servers in the rendered MCP server list.
- **Behavior:**

  > In the MCP server list (AI Customizations MCP view), a server whose connection is in an error state (McpConnectionState.Kind.Error) should be ordered above servers that are healthy/enabled, within its existing group (workspace vs. user, or built-in/plugin/extension). Enabled/healthy ordering among non-error servers is unchanged. Acceptance: reproduce a server failing to start and confirm it renders at or near the top of its section instead of being sorted purely by enabled state.
- **Trace:**

  > `updateFilteredServers()` in mcpListWidget.ts builds `installedEntries` (local, plugin, extension, built-in, and active-session groups) and sorts them at line 2510 solely by an enabled/disabled comparator; error/failure state is never consulted for ordering, even though the file already computes it per entry via `getMcpStatusKind()` (line 872) and `entry.localServer?.connectionState.get().state` (line 883), and uses `McpConnectionState.Kind.Error` / `McpServerStatus.Error` elsewhere for rendering (e.g. lines 407, 425, 443). The boundary: keep the existing group structure (workspace/user/plugin/extension/built-in sections) and enabled-first tiebreak intact — only add an error-first precedence within/across those groups; do not restructure how groups are built or how status badges render. Related: https://github.com/microsoft/vscode/issues/335372 referenced in the issue body as related context.

## Evidence

- **current sort:** src/vs/workbench/contrib/chat/browser/aiCustomization/mcpListWidget.ts:2510 sorts `installedEntries` only by enabled state (`getActiveSessionServer(...)?.enabled ?? this.isInstalledEntryEnabled(entry)`), with no error/failure precedence.
- **error state already tracked:** The same file already resolves per-entry error state via `getMcpStatusKind()` (line 872) and `entry.localServer?.connectionState.get().state === McpConnectionState.Kind.Error` (line 883), so the data needed for the requested sort already exists but isn't used for ordering.
- **no test harness:** No test directory exists under src/vs/workbench/contrib/chat/browser/aiCustomization for this widget, so no automated check can be named without first building one.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
