---
number: 334564
url: "https://github.com/microsoft/vscode/issues/334564"
engine: copilot
owner: vijayupadya
labels: triage-needed, stale
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: crash
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-12T12:27:02.002Z"
itemUpdatedAt: "2026-09-11T20:27:02Z"
itemCreatedAt: "2026-09-04T17:44:10Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: 04abdb499f69a438ac6989a74f4396d0b28c2d7b4da5646396d259a095360db2
policyHash: 5fa429198b8c8ed6
---

# Issue #334564 — VSCode 1.136 Debug Configuration Provider crashes Extension Host when extra parameters are passed for vscode.DebugConfiguration type

[https://github.com/microsoft/vscode/issues/334564](https://github.com/microsoft/vscode/issues/334564)

## Summary

Plausible extension-host crash report, but the reproduction is too thin to verify: no minimal debug configuration payload or attach/crash-reporter logs were shared, and the pasted extension-host log only shows termination side-effects (canceled proxy resolver, canceled chat provider calls), not a root-cause exception — so this needs more info rather than a keep-open park.

## Change summary

Calling vscode.debug.startDebugging with a DebugConfiguration that has extra/non-standard properties reportedly crashes the extension host on VS Code 1.136, whereas a minimal 3-property config works.

## Best solution

Ask the reporter for the exact DebugConfiguration object (or a minimal repro extension) that triggers the crash, plus the main-process/crash-reporter output around the termination — once a concrete config shape is available, trace it through vscode.debug.startDebugging -> MainThreadDebugService.$startDebugging -> DebugService.startDebugging (src/vs/workbench/contrib/debug/browser/debugService.ts) to see whether an extra field throws unhandled during resolveConfiguration.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** crash
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** debug
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the detailed report and video. To dig into this we need a bit more to go on:
  > 
  > - The full contents of the debug configuration object you're passing (e.g. `DEFAULT_IDL_DEBUG_CONFIGURATION`) — specifically which extra property(ies) beyond `type`/`name`/`request` trigger the crash.
  > - Any output from the main process log or crash reporter around the time the extension host terminates (the snippet posted only shows the host already shutting down, not the root cause).
  > - Whether this reproduces with a minimal test extension calling `vscode.debug.startDebugging` with the same extra fields, so we can rule out interaction with the rest of your extension.
  > 
  > This issue will auto-close in 7 days if there's no update, so please reply with the details above when you get a chance.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Reproduction status is unclear — no confirmed minimal repro or concrete config payload was provided, so there's nothing specific to trace or fix yet.

## Evidence

- **checkout limits:** The repository checkout is a shallow clone (git log shows a single commit, a8f49160), so no commit history is available to trace a 1.135->1.136 regression; verification is limited to reading current source.
- **source read:** src/vs/workbench/contrib/debug/browser/debugService.ts `startDebugging()` and src/vs/workbench/api/browser/mainThreadDebugService.ts `$startDebugging()` show no obvious strict-schema validation that would throw on extra DebugConfiguration properties; no failing code path was identified from source alone.
- **log content:** The pasted extension-host log only captures 'terminating: received terminate message from renderer' plus subsequently canceled in-flight calls (chat session provider, proxy resolver) — these are consequences of the host already shutting down, not the crash's root cause.
- **reporter data gap:** The actual contents of DEFAULT_IDL_DEBUG_CONFIGURATION (the 'extra parameters') were never posted, so the specific field/shape that supposedly triggers the crash can't be identified or traced.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
