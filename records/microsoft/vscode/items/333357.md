---
number: 333357
url: "https://github.com/microsoft/vscode/issues/333357"
engine: copilot
owner: vritant24
labels: new release
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.45
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-30T13:43:39.670Z"
itemUpdatedAt: "2026-08-29T22:40:43Z"
itemCreatedAt: "2026-08-29T22:31:52Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 62e17ed121e38525fbb778ca018abef7788f0abe18f7e0ca9eb3ed11fae79979
policyHash: 3d6943c3a15c721c
---

# Issue #333357 — customendpoint BYOK provider returns "Invalid API Key" against local server despite correct, verified API key

[https://github.com/microsoft/vscode/issues/333357](https://github.com/microsoft/vscode/issues/333357)

## Summary

Fresh, single-reporter issue (0 comments, filed hours ago) claiming the `customendpoint` BYOK provider sends a bad Authorization header against a local OpenAI-compatible server; I traced the current auth-header code and it looks correct (`Authorization: Bearer <key>` is sent by default for non-Azure, non-Messages-API endpoints in `CustomEndpointOAIEndpoint.getExtraHeaders`), so I can't confirm the defect from the report alone.

## Change summary

customendpoint BYOK provider (apiType chat-completions) returns 'Invalid API Key' against a local server even though the same key/endpoint works when called directly.

## Best solution

Ask the reporter for the exact outgoing request (e.g. via a local proxy/packet capture, or Copilot Chat's request logging) and their full `chatLanguageModels.json` including any `requestHeaders`, since the default Authorization-header logic in customEndpointProvider.ts looks correct on main; if a header-formation bug is confirmed it belongs in extensions/copilot/src/extension/byok/vscode-node/customEndpointProvider.ts or openAIEndpoint.ts.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** chat
- **Confidence:** 0.45
- **Proposed comment:**

  > Thanks for the detailed report. I looked at the BYOK custom-endpoint auth logic (extensions/copilot/src/extension/byok/vscode-node/customEndpointProvider.ts) and the default `Authorization: Bearer <key>` header path looks correct for a `chat-completions` endpoint like yours, so I can't pin down the defect from the description alone.
  > 
  > Could you help narrow this down:
  > - Do you have any `requestHeaders` set on the model config (even indirectly, e.g. through a shared snippet)? If a well-known auth header key is present there, the default Authorization header is suppressed.
  > - Can you capture the exact request VS Code sends (e.g. via a simple logging proxy in front of your LM Studio wrapper) and compare it to your working PowerShell call — specifically the `Authorization` header value and casing?
  > - Does the full, unredacted `chatLanguageModels.json` entry match what you pasted (no extra fields)?
  > 
  > Note this issue will auto-close in 7 days if there's no reply, so please add the details when you can.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Could not confirm a defect in current source — the default auth-header logic appears correct — so there's nothing concrete to auto-fix yet; needs reporter diagnostics first.

## Evidence

- **auth header code:** extensions/copilot/src/extension/byok/vscode-node/customEndpointProvider.ts, CustomEndpointOAIEndpoint.getExtraHeaders() (lines ~274-296): when apiType is chat-completions and the URL isn't 'openai.azure' and no user-supplied auth header is present, it sets headers['Authorization'] = `Bearer ${this._apiKey}`, matching the reporter's working PowerShell call.
- **no matching error string:** Searched extensions/copilot/src for 'Invalid API Key' / '401' — no BYOK-specific literal found, so the message likely originates from the reporter's own local server's response body being surfaced verbatim, which the report doesn't show.
- **no corroboration:** Issue has 0 comments, 0 reactions, and no linked related issues; single-reporter report against a custom local wrapper (LM Studio), making it hard to distinguish a VS Code bug from a local-server auth quirk (e.g. case sensitivity, extra whitespace, or a header the wrapper doesn't like) without more detail.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
