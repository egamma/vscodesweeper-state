---
number: 333357
url: "https://github.com/microsoft/vscode/issues/333357"
engine: copilot
owner: vritant24
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.4
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-31T16:18:38.111Z"
itemUpdatedAt: "2026-08-31T14:44:23Z"
itemCreatedAt: "2026-08-29T22:31:52Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 539e66653faa27f5b237bf0593a9a88932620247a02c6a433d805df0b6bb73ec
policyHash: 3d6943c3a15c721c
---

# Issue #333357 — customendpoint BYOK provider returns "Invalid API Key" against local server despite correct, verified API key

[https://github.com/microsoft/vscode/issues/333357](https://github.com/microsoft/vscode/issues/333357)

## Summary

Plausible bug report but too thin to verify: I traced the `customendpoint` BYOK auth-header logic in CustomEndpointOAIEndpoint.getExtraHeaders() and, for a non-Azure, non-Messages-API chat-completions endpoint with no custom requestHeaders, it sends exactly `Authorization: Bearer <apiKey>` — matching what the reporter says works via PowerShell — so no obvious defect surfaces from reading the source, and this behavior is already covered by existing unit tests in customEndpointProvider.spec.ts. Needs the reporter's actual request headers/network trace to pin down the real cause.

## Change summary

A `customendpoint` BYOK provider (chat-completions) against a local OpenAI-compatible server returns "Invalid API Key" even though the same key/endpoint succeeds when called directly.

## Best solution

Ask the reporter for a captured request (e.g. via a local proxy or the server's own request log) showing the exact Authorization header VS Code sent, plus their full `chatLanguageModels.json` entry (redacted key) — if the header differs from a plain `Bearer <key>`, that points to a concrete bug in extensions/copilot/src/extension/byok/vscode-node/customEndpointProvider.ts; if it doesn't, the issue likely isn't in Copilot Chat's request construction at all.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.4
- **Proposed comment:**

  > Thanks for the detailed report. I traced the request-building code for the `customendpoint` provider (CustomEndpointOAIEndpoint.getExtraHeaders in customEndpointProvider.ts) and for a plain chat-completions URL with no custom `requestHeaders`, it sends `Authorization: Bearer <apiKey>` — the same scheme you used successfully via PowerShell — so nothing jumps out from the source alone.
  > 
  > To dig further, could you capture the actual outgoing request from VS Code (e.g. point your server's URL at a local proxy like mitmproxy, or check your server's own access log) and share exactly what Authorization header/body it received, along with your full `chatLanguageModels.json` entry (with the key redacted)? That will tell us whether this is a header-construction bug on our side or something else in the request path.
  > 
  > Note: this issue will auto-close in 7 days if there's no reply with that information.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** No confirmed defect from source tracing — the relevant code path matches the reporter's own working manual request, and reproducing requires a live local server I don't have access to; needs more diagnostic info before any fix can be scoped.

## Evidence

- **header construction path:** extensions/copilot/src/extension/byok/vscode-node/customEndpointProvider.ts, CustomEndpointOAIEndpoint.getExtraHeaders(): for non-Messages-API, non-'openai.azure' URLs with no user-supplied auth header, it sets `headers['Authorization'] = `Bearer ${this._apiKey}`` — the same scheme the reporter used manually and got a valid response with.
- **existing test coverage:** extensions/copilot/src/extension/byok/vscode-node/test/customEndpointProvider.spec.ts has a passing test 'sends Authorization: Bearer <key> for Chat Completions endpoints', so this exact path is already asserted on main.
- **no confirmed repro:** No comments, no reactions, only 1 day old; I cannot run a local LM Studio-compatible server from this read-only checkout to confirm or rule out the failure, and nothing in the request-building code shows an obvious defect.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
