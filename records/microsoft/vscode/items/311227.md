---
number: 311227
url: "https://github.com/microsoft/vscode/issues/311227"
engine: copilot
owner: chrmarti
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: crash
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-21T13:54:32.628Z"
itemUpdatedAt: "2026-04-19T12:57:49Z"
itemCreatedAt: "2026-04-19T12:50:41Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 1
milestone: none
contentHash: 9503c4c20ee6bce77965b1a5961a5f7f6b29d448a5c9f9d96a2d1d25d6a5ebe6
policyHash: ebd3763732abe97d
---

# Issue #311227 — Remote window crashes with code 4 on reopen; PTY restore fails and Copilot Chat throws `PendingMigrationError`

[https://github.com/microsoft/vscode/issues/311227](https://github.com/microsoft/vscode/issues/311227)

## Summary

The PendingMigrationError is a known, handled deprecation notification in VS Code core (not crash-inducing) triggered by extensions accessing `navigator` in Node.js 22+. The actual renderer crash (code 4) has no clear attribution — the user hasn't tested with extensions disabled, so it's unclear whether this is a core crash or extension-triggered.

## Change summary

Remote VS Code window crashes with exit code 4 during reopen when terminal PTY restore fails and Copilot Chat extension triggers PendingMigrationError.

## Best solution

The PendingMigrationError is an extension-side issue (Copilot Chat accessing `navigator` global); VS Code already handles it gracefully via the deprecation service. The renderer crash needs isolation: the user should reproduce with all extensions disabled to determine whether the crash is in VS Code core or extension-triggered. If it's extension-only, this belongs on the Copilot Chat extension tracker.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** crash
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** terminal
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the detailed report and logs. A few notes on what's happening here:
  > 
  > - The `PendingMigrationError: navigator is now a global in nodejs` is a **known, handled deprecation notice** that VS Code deliberately emits when an extension accesses the `navigator` global in Node.js 22+. It is not a crash — VS Code catches it and reports it through the extension deprecation service. The Copilot Chat extension team is aware of this (tracked separately).
  > - The PTY restore errors (`Could not find pty X`) are expected graceful failures when reconnecting to a remote host where the PTY host has restarted — they shouldn't cause a renderer crash either.
  > - The actual renderer crash (code 4) is the concerning part, but it's not clear what caused it.
  > 
  > Could you help narrow this down?
  > 1. Can you reproduce the crash with **all extensions disabled** (`--disable-extensions`)?
  > 2. If it only crashes with extensions enabled, can you try disabling just Copilot Chat to see if that's the trigger?
  > 
  > This will help determine whether this is a VS Code core issue or an extension-triggered crash. Issues labeled `info-needed` will be closed automatically after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Crash cause is unattributed — cannot identify a localized fix without knowing if the crash is in core or extension-triggered.

## Evidence

- **PendingMigrationError is handled by design:** src/vs/workbench/api/node/extensionHostProcess.ts:142-148 defines the navigator getter that emits PendingMigrationError; src/vs/workbench/api/common/extensionHostMain.ts:123 suppresses it from error logging and routes it to apiDeprecationService instead. This is intentional and non-crashing.
- **Telemetry suppression shipped in 1.106:** Commit 79820a886d5 (2025-10-14) suppressed PendingMigrationError from error telemetry, shipped in 1.106.0. The user is on 1.116.0 which includes this.
- **Related codespaces issue:** https://github.com/github/codespaces/issues/23850 tracks the same PendingMigrationError as an extension-side issue with Node.js 22+.
- **No extension-disabled test:** The user has not verified whether the renderer crash reproduces with all extensions disabled, which is critical to attribute the crash to core vs extension.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
