---
number: 311227
url: "https://github.com/microsoft/vscode/issues/311227"
engine: copilot
owner: chrmarti
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.72
severity: crash
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-25T15:14:00.772Z"
itemUpdatedAt: "2026-04-19T12:57:49Z"
itemCreatedAt: "2026-04-19T12:50:41Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 1
milestone: none
contentHash: 9503c4c20ee6bce77965b1a5961a5f7f6b29d448a5c9f9d96a2d1d25d6a5ebe6
policyHash: ef2efec6827348f6
---

# Issue #311227 — Remote window crashes with code 4 on reopen; PTY restore fails and Copilot Chat throws `PendingMigrationError`

[https://github.com/microsoft/vscode/issues/311227](https://github.com/microsoft/vscode/issues/311227)

## Summary

The report describes a renderer crash (code 4) during remote window reopen, but the causal chain is unclear — the reporter themselves notes they cannot prove whether PTY restore or Copilot Chat is the trigger. The `PendingMigrationError` is VS Code's intentional `navigator` shim for Node.js 22+ and comes from an old marketplace Copilot Chat extension (0.44.1), not from core. Without a reproducible crash path or evidence linking the logged errors to the renderer termination, more information is needed.

## Change summary

Remote VS Code window crashes with exit code 4 on reopen while PTY restore fails and old Copilot Chat extension throws `PendingMigrationError`.

## Best solution

The reporter should uninstall the old marketplace `github.copilot-chat` extension (0.44.1) since Copilot Chat is now built into VS Code, then attempt to reproduce the crash. If the crash persists, a process monitor or `--verbose` log from the local side is needed to identify the actual renderer crash cause. The PTY restore errors are expected when reconnecting after PTY processes have exited.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** crash
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** terminal, remote
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for the detailed report and logs. A few observations:
  > 
  > - The `PendingMigrationError: navigator is now a global in nodejs` is an **intentional compatibility warning** that VS Code emits when an extension accesses `navigator` in the Node.js extension host. It comes from the old marketplace Copilot Chat extension (`0.44.1`) — since Copilot Chat is now **built into VS Code**, you should be able to uninstall that marketplace extension entirely, which should eliminate those errors.
  > - The `Could not find pty` messages are expected when reconnecting to a remote after the PTY host has recycled the old terminal processes — they shouldn't cause a renderer crash.
  > - The renderer crash (code 4) itself needs more investigation. Could you:
  >   1. **Uninstall the old marketplace `github.copilot-chat` extension** and see if the crash still occurs?
  >   2. If it does, try launching with `--verbose` on the local side and share the output?
  >   3. Check if disabling extensions (`--disable-extensions`) prevents the crash?
  > 
  > This will help narrow down whether the crash is caused by the old extension or something else. Note that `info-needed` issues auto-close after 7 days without a reply.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Unclear reproduction — the crash cause is not identified; the logged errors are either by-design or from an old third-party extension.

## Evidence

- **PendingMigrationError is by design:** src/vs/workbench/api/node/extensionHostProcess.ts line 145: VS Code intentionally intercepts `navigator` access in the extension host for Node.js 22+ and emits a PendingMigrationError to warn extensions. This is not a crash-causing bug.
- **Old marketplace extension:** The stack traces point to `/root/.vscode-server/extensions/github.copilot-chat-0.44.1/dist/extension.js` — the old marketplace Copilot Chat extension, not the current built-in Copilot Chat. This extension may have Node.js 22+ incompatibilities.
- **PTY errors are expected:** src/vs/platform/terminal/node/ptyService.ts line 672: `Could not find pty N on pty host` is thrown when a persistent terminal process no longer exists — expected when reconnecting after the PTY host has recycled.
- **No causal link established:** The reporter states: 'I cannot prove from remote logs alone whether terminal restore or Copilot Chat is the primary trigger for the local renderer crash.' The crash code 4 (renderer process crash) has no demonstrated connection to the logged extension errors.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
