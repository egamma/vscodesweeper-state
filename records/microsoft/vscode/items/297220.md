---
number: 297220
url: "https://github.com/microsoft/vscode/issues/297220"
engine: copilot
owner: lszomoru
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.75
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-21T21:06:15.763Z"
itemUpdatedAt: "2026-02-24T08:24:15Z"
itemCreatedAt: "2026-02-24T05:25:37Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: 77eea1867c1ea3059eb05eb7964a4d603e6920749049775e8f165ed5b3a03225
policyHash: ebd3763732abe97d
---

# Issue #297220 — Merge conflict markup not showing in marked file when attempting to merge branches using Git

[https://github.com/microsoft/vscode/issues/297220](https://github.com/microsoft/vscode/issues/297220)

## Summary

The report lacks key diagnostic information to determine whether this is a VS Code bug or an environment issue — we can't tell whether Git actually wrote conflict markers to the file on disk, or whether the built-in merge-conflict extension is active and detecting them.

## Change summary

Merge conflict inline decorations (<<<, ===, >>>) not appearing in the editor when a Git merge produces conflicts.

## Best solution

Request the reporter to verify: (1) whether the file on disk contains the standard Git conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`), (2) whether the built-in `merge-conflict` extension is enabled, and (3) the value of the `git.mergeEditor` setting. If the file on disk has markers but the extension isn't decorating them, that's a bug in `extensions/merge-conflict`; if the file lacks markers, the issue is upstream in Git or the user's workflow.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** git
- **Confidence:** 0.75
- **Proposed comment:**

  > Thanks for the report. To help narrow this down, could you check a few things?
  > 
  > 1. After the merge, open the `README.md` file **outside** VS Code (e.g. in Notepad) — does it contain the standard Git conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`)?
  > 2. Is the built-in **Merge Conflict** extension enabled? (Search for `@builtin merge-conflict` in the Extensions view.)
  > 3. What is your `git.mergeEditor` setting set to?
  > 
  > If the file on disk doesn't have the conflict markers at all, the issue may be with the Git operation itself rather than VS Code's display.
  > 
  > Note: this issue will auto-close in 7 days if there's no reply — please follow up so we can investigate further.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Insufficient reproduction info — cannot confirm a defect in VS Code's code without knowing whether Git markers are present on disk.

## Evidence

- **merge-conflict extension:** The built-in `extensions/merge-conflict` extension parses `<<<<<<<`/`=======`/`>>>>>>>` markers from file content and applies inline decorators — it requires the markers to be present in the file on disk (`extensions/merge-conflict/src/mergeConflictParser.ts`).
- **git.mergeEditor default:** `git.mergeEditor` defaults to `false` in `extensions/git/package.json`, meaning the inline merge-conflict decorators (not the 3-way merge editor) should be the default experience.
- **no diagnostic info:** The report does not confirm whether the file on disk actually contains Git conflict markers, whether the merge-conflict extension is enabled, or what editor is opening when the conflicted file is clicked.
- **screenshot:** Screenshot shows the file marked with a conflict icon in SCM but the editor shows only the checked-out branch content — this could mean Git didn't write markers or VS Code didn't reload the file.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
