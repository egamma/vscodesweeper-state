---
number: 336916
url: "https://github.com/microsoft/vscode/issues/336916"
engine: copilot
owner: sandy081
labels: ""
proposedLabel: "*not-reproducible"
closeReason: not-reproducible
triageAction: propose-close
confidence: 0.68
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-21T15:13:37.262Z"
itemUpdatedAt: "2026-09-20T13:57:22Z"
itemCreatedAt: "2026-09-20T13:48:36Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 74bb5c752c69be19b82645314b67411d6b1ee5beeee1eefd72fb080c51ea88f9
policyHash: d13126680f5ee38a
---

# Issue #336916 — MCP App UI resource is not rendered when the tool declares annotations

[https://github.com/microsoft/vscode/issues/336916](https://github.com/microsoft/vscode/issues/336916)

## Summary

Traced the full MCP App discovery/rendering pipeline on main and found no code path where `annotations` affects UI-resource detection — the App gating is independent of tool annotations, so this specific interaction doesn't reproduce as described.

## Change summary

Reporter claims adding MCP tool `annotations` (readOnlyHint/idempotentHint/openWorldHint) stops the host from reading the tool's registered `ui://` App resource.

## Best solution

If the reporter can attach server logs or a minimal reproducible server, re-open; as traced, `_normalizeTool` in mcpServer.ts derives `uiResourceUri` solely from `_meta.ui.resourceUri`, and `mcpAppData` gating in mcpLanguageModelToolContribution.ts depends only on the `mcpAppsEnabledConfig` setting and `tool.uiResourceUri` — `annotations` never enters either computation.

## Proposed action

- **Triage action:** propose-close
- **Item type:** bug
- **Reproduction:** not_reproduced
- **Evidence:** 2/6 Unverified
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *not-reproducible
- **Area labels:** mcp
- **Confidence:** 0.68
- **Proposed comment:**

  > Thanks for the detailed report and repro steps. I traced the MCP App discovery/rendering path on current main (mcpServer.ts's `_normalizeTool`, and the `mcpAppData` gating in mcpLanguageModelToolContribution.ts) and couldn't find anywhere that `annotations` factors into whether the `ui://` resource is used — the App detection is driven only by `_meta.ui.resourceUri` and the `mcp.chat.app.enabled`-style setting. I wasn't able to reproduce the described interaction from source. If you can share the actual `tools/list` response and server logs from a failing run (or a minimal server we can point a dev build at), that would help pin down what's actually different when annotations are present.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** No confirmed defect in current main to fix — the reported mechanism (annotations interfering with App detection) isn't present in the traced code paths.

## Evidence

- **tool normalization:** src/vs/workbench/contrib/mcp/common/mcpServer.ts:1056-1079 `_normalizeTool` computes `visibility` and `uiResourceUri` only from `originalTool._meta?.ui`; `originalTool.annotations` is never read.
- **app-data gating:** src/vs/workbench/contrib/mcp/common/mcpLanguageModelToolContribution.ts:245-260 gates `mcpAppData` on `mcpUiEnabled` (a config value) and `tool.uiResourceUri` only.
- **no outputSchema/annotations coupling:** `grep -rn outputSchema src/vs/workbench/contrib/mcp/` (excluding tests) returns no matches, and no reference to `.annotations` exists anywhere in the App-detection or rendering code (mcpServer.ts, mcpLanguageModelToolContribution.ts, chatToolInvocationPart.ts, chatListRenderer.ts).
- **schema validation scope:** The only schema validation in tool normalization (mcpServer.ts:1100-1121) validates `tool.inputSchema` against JSON Schema draft-07; it does not touch `annotations` or reject the whole tool object.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
