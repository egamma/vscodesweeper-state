---
number: 336219
url: "https://github.com/microsoft/vscode/issues/336219"
engine: copilot
owner: connor4312, vritant24
labels: bug, mcp, ai-customizations
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.72
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: none
reviewedAt: "2026-09-23T13:50:00.963Z"
itemUpdatedAt: "2026-09-22T22:36:20Z"
itemCreatedAt: "2026-09-14T23:59:59Z"
upvotes: 1
reactionsTotal: 1
commentCount: 0
relatedOpenIssues: 1
milestone: none
contentHash: 0714264503211a12f3cedcbd9cf149bbe35d4b0a1d2ce904b9b627f6cdedaa3e
policyHash: d13126680f5ee38a
---

# Issue #336219 — Agent Host: MCP tool invocation fails with "MCP tool catalog changed before tool … could be invoked"

[https://github.com/microsoft/vscode/issues/336219](https://github.com/microsoft/vscode/issues/336219)

## Summary

Genuine one-off report of an MCP tool-invocation race, but the reporter explicitly says it's not a deterministic reproduction and the trigger is unknown, and nobody has confirmed it since — this needs more detail (or a repro) before it can be actioned, so it goes to info-needed rather than sitting open indefinitely.

## Change summary

An MCP tool invocation in an Agent Host session fails with "MCP tool catalog changed before tool … could be invoked" instead of succeeding or recovering.

## Best solution

Ask the reporter (or wait for another reporter) to reproduce with the tool catalog-change/invocation race captured in Agent Host debug logs (see the `agent-host-logs` skill), since the failure text does not appear anywhere in this repo's TypeScript sources — it is raised inside the closed-source `@github/copilot-sdk` package (`package.json` dependency `@github/copilot-sdk`) that the Agent Host integrates, so root-causing it needs either SDK-side logs or an SDK issue.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.72
- **Proposed comment:**

  > Thanks for filing this — the exact error text ("MCP tool catalog changed before tool … could be invoked") doesn't appear anywhere in the VS Code or Copilot extension sources, so it looks like it's raised inside the Copilot SDK layer the Agent Host uses, which makes it hard to pin down from a single occurrence.
  > 
  > Since this wasn't a deterministic repro, could you help narrow it down further:
  > - Does it happen again, and if so, is it tied to a specific MCP server (which one advertises `exp-report_usage_telemetry`) reconnecting, restarting, or updating its tool list mid-session?
  > - If you still have logs from that session (Export Agent Host Debug Logs), attaching them would help a lot.
  > 
  > This issue will auto-close in 7 days if there's no reply, but feel free to reopen or re-file with more detail whenever it recurs.

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** none
- **Rationale:** The failing code path lives in a closed-source SDK dependency, not in this repo, and the report has no confirmed reproduction — nothing here for a checkout-based agent session to trace or fix.

## Evidence

- **no source match:** grep for 'MCP tool catalog changed', 'catalog changed before', and 'could be invoked' across the full src/vs and extensions/copilot trees returns no matches — the error string is not emitted by any code in this repository.
- **SDK dependency:** package.json declares "@github/copilot-sdk": "1.0.15-unstable.35663726336.gcb2a8cc" as a dependency; this is the private/closed-source SDK the Agent Host uses to drive Copilot sessions, and is the most likely source of the raised error.
- **self-reported uncertainty:** Issue body states: 'This is a report of an observed failure, not a deterministic reproduction. The trigger for the catalog change, frequency, and whether retrying resolves it are not yet known.'
- **related but distinct:** https://github.com/microsoft/vscode/issues/330899 (client-tool dispatch ordering) and https://github.com/microsoft/vscode/issues/336644 (session unusable after MCP server removal) are adjacent Agent Host/MCP timing issues but the reporter's own duplicate search found no exact match, and neither clearly owns this specific catalog-versioning failure.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
