---
number: 336386
url: "https://github.com/microsoft/vscode/issues/336386"
engine: copilot
owner: benibenj
labels: ""
proposedLabel: none
closeReason: none
triageAction: keep-open
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
agentReadiness: plan
reviewedAt: "2026-09-24T13:48:41.059Z"
itemUpdatedAt: "2026-09-23T15:58:47Z"
itemCreatedAt: "2026-09-16T09:11:42Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: cda30102b0a3c4818bb0f4796b4769987264eb3f721b002f33a8d401ad841523
policyHash: d13126680f5ee38a
---

# Issue #336386 — Agent Merge: Pull request gets merged directly without following configured monitoring/checks first

[https://github.com/microsoft/vscode/issues/336386](https://github.com/microsoft/vscode/issues/336386)

## Summary

Active, credible bug report on Agent Merge: a maintainer (benibenj) is already engaged and requesting evidence, and the reporter's second comment supplies a concrete contradiction — Agent Merge was configured with 'It will not merge the pull request automatically' yet merged the PR anyway, with Agent Host logs attached. This is a live investigation, not something to close.

## Change summary

Agent Merge merges a pull request even though it is configured to only monitor (not auto-merge), and/or before its configured review/CI gating conditions are satisfied.

## Best solution

Maintainers should continue the investigation with benibenj: reconcile why a PR merged while `mergePullRequest` was 'never' (or before review/CI gates cleared), likely in the folder-scoped Agent Merge configuration/reconciliation path (`AgentMergeController`, `evaluateAgentMerge` in src/vs/platform/agentHost/common/agentMerge.ts) or a race in session-config reconciliation logged as 'Reconciled autonomous session configuration'.

## Proposed action

- **Triage action:** keep-open
- **Item type:** bug
- **Reproduction:** reproduced
- **Evidence:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** no
- **Agent readiness:** plan
- **Rationale:** The gating function on main (evaluateAgentMerge) appears to correctly respect the 'never' merge setting and readiness checks, so the actual defect (a reconciliation race, folder-scoped config mismatch, or a code path outside evaluateAgentMerge) is not yet pinned down — needs maintainer-guided diagnosis with the full debug logs before a bounded fix can be scoped.

## Plan brief

- **Likely files:** `src/vs/platform/agentHost/node/agentMergeController.ts`, `src/vs/platform/agentHost/common/agentMerge.ts`
- **Validation:** A regression test in agentMergeController.test.ts / agentMerge.test.ts reproducing the reported sequence (folder configured with mergePullRequest: 'never' or pending review/CI) and asserting no merge mutation is issued.
- **Trace:**

  > evaluateAgentMerge (src/vs/platform/agentHost/common/agentMerge.ts:950-1034) gates the 'merge' outcome on `configuration.mergePullRequest !== 'never'` plus a full readiness check (draft, reviews, checks, mergeability). The reporter's second timeline shows a PR merged instantly while configured 'never' and while Copilot's review was still outstanding, contradicting this code path. Full root cause is unconfirmed — needs the requested Agent Host debug log export to trace whether the applied configuration at merge time actually reflected 'never', or whether a different mutation path (native GitHub auto-merge, a stale cached configuration, or the 'reconciled autonomous session configuration' step in AgentMergeController) bypassed the gate.
- **Open decisions:**
  - Was the merge caused by a stale/mis-scoped configuration read (folder vs session) at the moment of merge, or by a separate code path bypassing evaluateAgentMerge entirely?
  - Does GitHub's own auto-merge/branch-protection state factor into this, or is it purely an Agent Host-side decision as the reporter describes?

## Evidence

- **gate logic on main looks correct:** src/vs/platform/agentHost/common/agentMerge.ts:1030 — evaluateAgentMerge only returns kind:'merge' when `configuration.mergePullRequest !== 'never'` and mergeReady; on its face this should have blocked the reported merge, so the defect (if real) is elsewhere: config reconciliation, folder-scoped state, or a race between 'PR-creation' and monitor start.
- **maintainer engaged:** benibenj (CONTRIBUTOR) responded within 2 days asking for the affected PR link, whether checks were 'Required', whether Copilot review was still pending, and Agent Host debug logs.
- **second report corroborates with logs:** fraenkDUS's second comment (2026-09-23) shows the exact 'will not merge automatically' notice text (matches src/vs/platform/agentHost/common/agentMerge.ts:445) alongside a GitHub timeline where the PR was merged before Copilot's review even started, plus a partial Agent Host log excerpt.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
