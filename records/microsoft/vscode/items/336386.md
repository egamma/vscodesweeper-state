---
number: 336386
url: "https://github.com/microsoft/vscode/issues/336386"
engine: copilot
owner: benvillalobos
labels: ""
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-16T13:19:52.782Z"
itemUpdatedAt: "2026-09-16T09:23:05Z"
itemCreatedAt: "2026-09-16T09:11:42Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 1f6f02d348c5f3a21c6390342c36b1dcc26ef2fc6b3388bce23bb2cf913b1961
policyHash: 5fa429198b8c8ed6
---

# Issue #336386 — Agent Merge: Pull request gets merged directly without following configured monitoring/checks first

[https://github.com/microsoft/vscode/issues/336386](https://github.com/microsoft/vscode/issues/336386)

## Summary

Thanks for the report — this is a fresh (same-day), single-occurrence report with only a short transcript excerpt and no confirmed reproduction, so it needs more diagnostic detail before it can be triaged as a confirmed defect; the gating logic in `evaluateAgentMerge` on main looks intentionally strict, so it isn't obvious from the code alone how it was bypassed.

## Change summary

Agent Merge merged a pull request immediately instead of first waiting on configured CI checks and review monitoring.

## Best solution

Ask the reporter for the full Agent Merge session transcript/logs (including the evaluation right before the merge action), the exact Agent Merge configuration (which of addressReviews/fixCI/resolveConflicts/mergePullRequest were enabled), and whether branch protection/required checks were configured on that repo, then trace `evaluateAgentMerge` (src/vs/platform/agentHost/common/agentMerge.ts) against that specific PR's snapshot to see which gate condition was mis-evaluated.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the report. The merge gate in `evaluateAgentMerge` (src/vs/platform/agentHost/common/agentMerge.ts) is coded to require completed checks, no pending/failing CI, no unresolved reviews/comments, and a mergeable state before it will merge — so this would help to pin down as a possible edge case rather than a general bypass.
  > 
  > Could you share the full Agent Merge session log for PR #1898 (not just the final merge line), and confirm exactly which Agent Merge options were enabled (address reviews / fix CI / resolve conflicts / auto-merge) plus whether the repo had branch protection or required status checks configured? That will help trace which specific condition was satisfied unexpectedly.
  > 
  > This issue will auto-close in 7 days if there's no reply with more detail.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** The reported defect is unconfirmed against current source — the coded gating logic looks correct on inspection, so without a traced failing evaluation or a concrete reproduction this needs maintainer/reporter diagnosis, not an automatic fix.

## Evidence

- **gating logic on main:** src/vs/platform/agentHost/common/agentMerge.ts `evaluateAgentMerge()` (line ~636) only returns `{ kind: 'merge' }` when core/conversation/head fragments are all complete, `checks.failed.length === 0`, `!checks.pending`, `!behind`, `!conflicting`, `mergeability.mergeable === 'MERGEABLE'`, `mergeability.viewerCanMerge`, and the merge state status is in `mergeableStates` — i.e. the described monitoring conditions are already coded as hard preconditions for a merge action.
- **no repro confirmation:** Issue is 0 days old with no comments; the log excerpt only shows the Agent Merge intro message and a final 'merged pull request #1898' line — no intermediate evaluation/check-state logs are included that would show which gate condition returned true prematurely.
- **thin report:** The exact Agent Merge configuration (which review/CI/conflict options were enabled) and the target repo's branch-protection/required-checks setup are not specified, both of which materially affect `evaluateAgentMerge`'s outcome.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
