---
number: 312394
url: "https://github.com/microsoft/vscode/issues/312394"
engine: copilot
owner: chrmarti
labels: triage-needed, stale
proposedLabel: "*question"
triageAction: propose-close
confidence: 0.82
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-21T14:37:07.847Z"
itemUpdatedAt: "2026-05-04T16:34:19Z"
itemCreatedAt: "2026-04-24T16:21:36Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: beb56a059891e46e9c21dc17f1f819a73524a1ca3b79cf97cfd999b1af4e1c88
policyHash: ebd3763732abe97d
---

# Issue #312394 — Extensions are not installed on Debian 12 (Myvestacp)

[https://github.com/microsoft/vscode/issues/312394](https://github.com/microsoft/vscode/issues/312394)

## Summary

Environment-specific support question: the 'Error: aborted' on the download stream points to something in the user's hosting panel (Myvestacp/Hestiacp) terminating the download — not a VS Code bug. The code path at `extensionGalleryService.ts:1707` simply writes the HTTP response stream to disk; 'aborted' means the stream was cut off externally. The user confirms it works on a plain Linux server without the panel software.

## Change summary

Extension VSIX downloads fail with 'Error: aborted' on Debian 12 servers running Myvestacp/Hestiacp hosting panel software.

## Best solution

This is an environment issue — the hosting panel software (Myvestacp/Hestiacp) is likely imposing resource limits, process timeouts, or network restrictions that abort the download stream. The user should check their panel's process/resource limits, any mod_security or firewall rules, and try downloading extensions via the CLI (`code --install-extension <id>`) or manually transferring VSIX files. Not actionable as a VS Code change.

## Proposed action

- **Triage action:** propose-close
- **Item type:** question
- **Reproduction:** not_applicable
- **Rating:** not applicable
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *question
- **Area labels:** extensions
- **Confidence:** 0.82
- **Proposed comment:**

  > Thanks for the detailed logs. The `Error: aborted` on the download stream indicates something in your server environment is terminating the HTTP download before VS Code can finish writing the file — VS Code itself doesn't abort its own download stream in this path.
  > 
  > Since it works on a plain Linux server but fails on both Myvestacp and Hestiacp, the hosting panel software is very likely the culprit. A few things to check:
  > 
  > - **Process resource limits** — hosting panels often set CPU/memory/process-time limits per user via `ulimit`, cgroups, or similar. A large VSIX download may exceed those.
  > - **Network/firewall rules** — check if the panel's firewall or mod_security rules are blocking or throttling downloads from `az764295.vo.msecnd.net` (the VS Code marketplace CDN).
  > - **Proxy settings** — if the panel routes traffic through a proxy, it may be timing out.
  > 
  > You can also try installing extensions manually: download the `.vsix` file on your local machine and transfer it to the server, then run `code --install-extension /path/to/extension.vsix`.
  > 
  > Since this is specific to the hosting panel's process management rather than a VS Code issue, I'm closing this as a support question. If you find evidence that VS Code is doing something wrong on its end, please open a new issue with those details.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Environment-specific support question, not a VS Code code defect.

## Evidence

- **code path:** The error originates at `src/vs/platform/extensionManagement/common/extensionGalleryService.ts:1707-1715` — `fileService.writeFile(location, context.stream)` fails with 'aborted', meaning the HTTP response stream was terminated before the write completed. VS Code wraps this as `DownloadFailedWriting`.
- **environment correlation:** Reporter confirms the same setup works on another Linux server without Myvestacp, and fails identically on Hestiacp — both are hosting control panels that impose process/resource management on the server.
- **not a permissions issue:** Reporter applied `chmod -R 775` and the temporary file is created (appears in CachedExtensionVSIXs) before being aborted mid-write, ruling out simple filesystem permissions.
- **stream abort:** The 'Error: aborted' on the stream is a Node.js stream abort signal, typically caused by the connection being dropped, a timeout, or the process being killed externally — not by VS Code logic.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
