---
number: 305983
url: "https://github.com/microsoft/vscode/issues/305983"
engine: copilot
owner: justschen
labels: bug, chat-vision
proposedLabel: "*duplicate"
triageAction: propose-close
confidence: 0.85
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-19T11:31:26.295Z"
itemUpdatedAt: "2026-03-30T21:00:11Z"
itemCreatedAt: "2026-03-28T15:19:27Z"
upvotes: 1
reactionsTotal: 1
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: 21d7fa27906fb2a7ec1816f38750e2a906637ae9125fa571a6391b41dd49e97d
policyHash: ebd3763732abe97d
---

# Issue #305983 — Major Issue Killing Entire Conversation

[https://github.com/microsoft/vscode/issues/305983](https://github.com/microsoft/vscode/issues/305983)

## Summary

Verified duplicate: the root cause — MIME type/actual-data mismatch in images sent to the API — is the same problem comprehensively tracked by #311732, which covers both the WebP-resize path and the broader MCP-tool case and already has a suggested fix (magic-bytes sniffing). Proposing a duplicate close to consolidate.

## Change summary

When a WebP image is resized via the `_chat.resizeImage` command, the binary data is re-encoded as PNG but the MIME type stays `image/webp`, causing the API to reject it with a 400 error.

## Best solution

Consolidate on https://github.com/microsoft/vscode/issues/311732 — the fix (magic-bytes sniffing to detect actual image format before sending to the API) addresses both this WebP-resize path and the broader MCP-tool MIME mismatch. The specific code gap in this case is `extensions/copilot/src/platform/image/vscode-node/imageServiceImpl.ts` line 23, which returns the original `mimeType` unchanged after resizing even though `_chat.resizeImage` may have re-encoded to PNG.

## Proposed action

- **Triage action:** propose-close
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *duplicate
- **Area labels:** chat-vision
- **Confidence:** 0.85
- **Proposed comment:**

  > Thanks for the report and the clear reproduction steps. The root cause here is that when a WebP image is resized, the binary data gets re-encoded as PNG (via `canvas.toBlob`) but the MIME type stays `image/webp` — which the API rightly rejects.
  > 
  > This is the same underlying problem tracked in https://github.com/microsoft/vscode/issues/311732, which covers the broader case of MIME-type/actual-data mismatches in images sent to the API. The fix (magic-bytes detection to correct the declared MIME type) would resolve both.
  > 
  > /duplicate of #311732

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Duplicate — the fix should land via the canonical issue #311732.

## Evidence

- **resize converts WebP to PNG:** In `src/vs/workbench/contrib/chat/browser/chatImageUtils.ts` lines 62-63, `resizeImage()` outputs as `image/png` for any non-JPEG input (including WebP). The `_chat.resizeImage` command (line 267) returns only the resized bytes, not the new MIME type.
- **Copilot extension preserves stale MIME type:** In `extensions/copilot/src/platform/image/vscode-node/imageServiceImpl.ts` line 23, `resizeImage()` returns `{ data: result, mimeType }` where `mimeType` is the original unchanged `image/webp`, even though the bytes are now PNG.
- **API rejects the mismatch:** The Anthropic API validates that declared `media_type` matches the actual image magic bytes (constructed in `extensions/copilot/src/platform/endpoint/node/messagesApi.ts` line 423), producing the 400 error the reporter sees.
- **canonical issue:** https://github.com/microsoft/vscode/issues/311732 is open and tracks the same MIME-type mismatch problem more broadly, including the conversation-poisoning aspect and a suggested magic-bytes fix. Multiple users confirmed the pattern.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
