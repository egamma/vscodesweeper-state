---
number: 325872
url: "https://github.com/microsoft/vscode/issues/325872"
engine: copilot
owner: joshspicer
labels: ""
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.55
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-07T13:45:42.908Z"
itemUpdatedAt: "2026-07-28T15:52:17Z"
itemCreatedAt: "2026-07-14T22:29:02Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: ff8c6f3995ce3c4cadfa4d0ae50e88597dd0af43343d29c1f3515fea901e61f5
policyHash: 70e952cc2fb28823
---

# Issue #325872 — VS Code Remote Server Update: Incomplete node_modules Installation

[https://github.com/microsoft/vscode/issues/325872](https://github.com/microsoft/vscode/issues/325872)

## Summary

Plausible but unconfirmed single report of a broken VS Code Server update; the current cli/src code already extracts the whole server archive into a temp dir and does one atomic std::fs::rename into the live path (not an incremental staging-to-live copy as described), so the exact mechanism described doesn't match main — asking for more detail before deciding.

## Change summary

Remote-SSH server update left the live server directory with only 73 of 93 node_modules packages, causing extension host crashes and a reconnect loop, allegedly due to an incomplete staging-to-live copy.

## Best solution

Ask the reporter to confirm the filesystem type for ~/.vscode-server (network/overlay FS vs local disk) and whether this reproduces on a fresh install of the latest release; if confirmed, the real gap is the lack of a post-extraction integrity check in DownloadCache::create (cli/src/download_cache.rs) before the server is marked ready, not a copy step (the code already uses a single atomic rename, per RENAME_ATTEMPTS retry loop).

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** remote, server
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the very thorough write-up — the module counts and workaround are helpful. One thing worth checking before we can act on this: the current server install code (cli/src/tunnels/code_server.rs + cli/src/download_cache.rs) downloads the whole server archive into a temp directory and then does a single atomic `rename()` into the live path — it doesn't do an incremental copy from staging to live the way the report describes, so I'd like to understand more about what happened here. Could you confirm what filesystem `~/.vscode-server` lives on (local disk, NFS/EFS, or another network/overlay filesystem), and whether you can reproduce this on a fresh `rm -rf ~/.vscode-server` reconnect with the latest release? That said, you're right that there's no integrity check (module count/checksum) before the server is marked ready to accept connections — that part of your suggestion looks like a real, still-open gap worth tracking regardless.
  > 
  > This will auto-close in 7 days without a reply, so please follow up when you get a chance.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Single unconfirmed report whose specific root-cause diagnosis (incremental staging copy) doesn't match the current atomic-rename code path; needs reporter confirmation before any fix can be scoped or validated.

## Evidence

- **extraction flow:** cli/src/tunnels/code_server.rs ServerBuilder::setup() downloads the archive, calls unzip_downloaded_release() (cli/src/update_service.rs) into a temp target_dir, then DownloadCache::create() (cli/src/download_cache.rs) does a single std::fs::rename(temp_dir, target_dir) with retries — a whole-tarball atomic move, not an incremental node_modules copy.
- **no integrity check:** download_cache.rs create(): once target_dir.exists() or rename succeeds, the directory is used as-is; there is no module-count or checksum verification before the server is considered installed, which matches the reporter's suggestion #2 as a genuine, still-open gap.
- **single unconfirmed report:** Issue has 0 reactions, no comments, and no other cross-referenced report of this exact symptom; reporter's environment (Amazon Linux devbox, internal 'vscode-brazil' extension, remote host over what may be a networked home directory) suggests an unusual setup that could affect filesystem rename semantics.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
