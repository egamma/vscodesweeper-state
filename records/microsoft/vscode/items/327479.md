---
number: 327479
url: "https://github.com/microsoft/vscode/issues/327479"
engine: copilot
owner: deepak1556
labels: bug
proposedLabel: info-needed
triageAction: needs-info
confidence: 0.55
severity: freeze
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-08-08T09:00:08.903Z"
itemUpdatedAt: "2026-08-07T17:08:56Z"
itemCreatedAt: "2026-07-25T18:43:49Z"
upvotes: 0
reactionsTotal: 0
commentCount: 2
relatedOpenIssues: 0
milestone: none
contentHash: 26a349dd118ac56767fcb3dc9c0f6b79ccede2d43dae7d20bdf7ac4d0f0866eb
policyHash: 01897759af890e7f
---

# Issue #327479 — Unresponsive for 30–90s intermittently

[https://github.com/microsoft/vscode/issues/327479](https://github.com/microsoft/vscode/issues/327479)

## Summary

Thanks for the very thorough diagnostics — this looks like a real intermittent freeze, but the causal chain from the extension-registry lock warning to the DefaultAccount/managed_settings network fetch is still circumstantial, not proven. Given the substantial but unconfirmed evidence, I'd ask for a couple more targeted captures before we can localize a fix.

## Change summary

VS Code window becomes fully unresponsive for 30–90s intermittently, correlated with extensionDescriptionRegistry lock warnings and long silent gaps in the extension host log around DefaultAccount/managed_settings initialization.

## Best solution

If confirmed, the fix likely belongs in src/vs/workbench/services/accounts/browser/defaultAccount.ts — three of its four account/network requests (tokenEntitlements, entitlements, mcpRegistryProvider) call `this.request(...)` without a `requestTimeoutMs`, unlike `managedSettings` which passes a 5s timeout (MANAGED_SETTINGS_REQUEST_TIMEOUT_MS at line 70) — a hung/slow network call there could stall whatever awaits it. But the link to the `_initialize` extension-registry lock (abstractExtensionService.ts:471) holding for 30s+ needs a concrete trace (e.g. a full Issue Reporter perf capture, or `--verbose --log trace` spanning a freeze) before committing to that as the root cause.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Rating:** 3/6 Plausible
- **Severity:** freeze
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** extensions
- **Confidence:** 0.55
- **Proposed comment:**

  > Thanks for the very detailed investigation here — this is more diagnostic work than we usually get, and it's genuinely useful. That said, I can't yet connect the dots definitively between the extension-registry lock warning and the DefaultAccount/managed_settings fetch gap from the source alone (a few of the account-related network calls in defaultAccount.ts don't set an explicit timeout, unlike the managed_settings one, which is suggestive but not conclusive).
  > 
  > Could you help narrow it down further:
  > - A full capture from Help > Report Issue (or the Process Explorer) taken *during* one of these freezes, including the extension host log for that window.
  > - Whether the freeze still occurs with `--disable-extensions` if you haven't tried a long session that way (bisect ruling out a single extension doesn't rule out core paths, but a longer no-extension run would help isolate the extension-registry vs. network angle).
  > - Confirmation of whether your organization has an enterprise/managed-settings policy configured, since that's the endpoint showing 404s and repeated re-fetches in your logs.
  > 
  > Note this issue will auto-close in 7 days if there's no reply, since we need more to pin down before we can route it to a fix.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Real freeze with strong circumstantial evidence, but the causal chain (network fetch → registry lock) is unconfirmed from source alone; needs a live trace/repro before a localized code fix can be identified and validated.

## Evidence

- **lock warning source:** Warning text matches src/vs/workbench/services/extensions/common/extensionDescriptionRegistry.ts:371; the '_initialize' customer name comes from abstractExtensionService.ts:471 (`this._registry.acquireLock('_initialize')` inside `_initialize()`).
- **untimed account requests:** src/vs/workbench/services/accounts/browser/defaultAccount.ts: tokenEntitlements (line 753), entitlements (line 804), and mcpRegistryProvider (line 848) call `this.request(...)` with no `requestTimeoutMs`, while managedSettings (line 924) passes `MANAGED_SETTINGS_REQUEST_TIMEOUT_MS = 5000` (line 70) — an asymmetry that could explain longer-than-5s hangs on flaky networks, but I could not trace this to actually block the extension-registry lock or the renderer thread from source alone.
- **prior deadlock note:** defaultAccount.ts:369-372 has an existing comment about a related deadlock class being avoided ('waiting for whenInstalledExtensionsRegistered can deadlock initialization'), suggesting this startup path has a history of subtle blocking issues, but that specific fix predates this report and isn't the same code path.
- **reporter follow-up:** Reporter's own comments (2026-08-05, 2026-08-07) suggest network/wifi and enterprise-policy fetch as suspects, matching the diagnostic direction above, but no maintainer or second reporter has confirmed a reproducible trigger yet.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
