---
number: 204441
url: "https://github.com/microsoft/vscode/issues/204441"
engine: copilot
owner: roblourens
labels: bug, debug, debug-console
proposedLabel: "*caused-by-extension"
triageAction: propose-close
confidence: 0.82
severity: none
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-07-18T07:28:01.353Z"
itemUpdatedAt: "2024-02-08T20:21:11Z"
itemCreatedAt: "2024-02-06T03:19:40Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 7a4e102c1f2a3935f53ad498eb0904f606730598db57f09377e8b58dd42cbcbe
policyHash: ebd3763732abe97d
---

# Issue #204441 — internalConsole does not work for "attach" mode

[https://github.com/microsoft/vscode/issues/204441](https://github.com/microsoft/vscode/issues/204441)

## Summary

The `console` launch.json property (e.g. `integratedTerminal`, `internalConsole`) is controlled by the debug adapter extension, not VS Code core. Whether it is supported in attach mode is a debug adapter decision — this belongs in the debugger extension's repo (likely ms-python/debugpy).

## Change summary

Request for the `console` launch configuration option to work in debug attach mode so output with control characters renders correctly in a terminal instead of the debug console.

## Best solution

File against the specific debug adapter extension (likely ms-python/debugpy for Python). The debug console intentionally does not emulate terminal control sequences (confirmed as by-design in https://github.com/microsoft/vscode/issues/183971); the workaround is `"console": "integratedTerminal"`, but support for that in attach mode is the debug adapter's responsibility.

## Proposed action

- **Triage action:** propose-close
- **Item type:** feature-request
- **Reproduction:** not_applicable
- **Rating:** not applicable
- **Severity:** none
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** *caused-by-extension
- **Area labels:** debug
- **Confidence:** 0.82
- **Proposed comment:**

  > Thanks for the report. The `console` property in `launch.json` (e.g. `integratedTerminal`, `internalConsole`) is defined and controlled by the debug adapter extension you're using, not by VS Code core. VS Code's debug console intentionally does not emulate terminal control sequences (see https://github.com/microsoft/vscode/issues/183971).
  > 
  > For Python remote attach, this would need to be supported by the Python debugger extension (debugpy). I'd suggest filing this request in the [Python Debugger extension repo](https://github.com/microsoft/debugpy) so the right team can evaluate it.
  > 
  > Closing this as the behavior is owned by the debug adapter extension rather than VS Code core.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Not a VS Code core issue — the console property for attach mode is controlled by the debug adapter extension.

## Evidence

- **same author, prior issue:** https://github.com/microsoft/vscode/issues/183971 — same author reported carriage return rendering in the debug console. A VS Code maintainer (roblourens) closed it as by-design, explaining the debug console is not a terminal emulator. The author's last comment there confirms the real ask is `"console": "integratedTerminal"` support in Python remote attach mode.
- **console property is adapter-owned:** The `console` property in launch.json (internalConsole / integratedTerminal / externalTerminal) is defined and handled by individual debug adapter extensions. VS Code core only acts on the `runInTerminal` reverse request from adapters; it does not decide which console options are valid per request type.
- **code inspection:** src/vs/workbench/api/node/extHostDebugService.ts handles `runInTerminal` requests from debug adapters but has no logic governing which `console` values are valid for launch vs. attach — that schema and behavior is entirely the adapter's responsibility.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
