---
number: 330363
url: "https://github.com/microsoft/vscode/issues/330363"
engine: copilot
owner: anthonykim1
labels: vscode-build, agentic-workflows
proposedLabel: none
closeReason: none
triageAction: route-to-area
confidence: 0.82
severity: perf
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: true
reviewedAt: "2026-08-12T09:20:06.312Z"
itemUpdatedAt: "2026-08-11T23:42:07Z"
itemCreatedAt: "2026-08-11T23:32:07Z"
upvotes: 0
reactionsTotal: 0
commentCount: 0
relatedOpenIssues: 0
milestone: none
contentHash: 5a2ca1bb49270e04d2e7357a47f6fdda56eaa40cf9472010c9ef32829d7ff55e
policyHash: f13d7795d7aa28b9
---

# Issue #330363 — fix: raise mocha timeout for AgentSdkDownloader tests (build fix for vscode-engineering#3401)

[https://github.com/microsoft/vscode/issues/330363](https://github.com/microsoft/vscode/issues/330363)

## Summary

Legitimate build-fix: the `AgentSdkDownloader` integration suite in src/vs/platform/agentHost/test/node/agentSdkDownloader.test.ts has no suite-level timeout override and its real network/extract tests can exceed mocha's 2000ms default on Windows CI; the bot's PR creation failed on a fork-collab permission error, so the fix only exists as a branch/diff attached to this issue.

## Change summary

Windows CI `Run unit tests (node.js)` flakes with 2000ms mocha timeouts in the AgentSdkDownloader integration suite because it does real HTTP download + gzip extraction without a suite-level timeout override.

## Best solution

Land the proposed one-file change (convert `suite('AgentSdkDownloader', () => {...})` to a function expression and add `this.timeout(20_000)`), matching the pattern already used in copilotSdkImportSession.integrationTest.ts and the sibling cancellation test's own `this.timeout(15_000)`; a maintainer can cherry-pick from branch `fix/agent-sdk-downloader-test-timeout-21dc35bbc52b78db` or open the PR manually since the bot's automated PR creation failed with a `fork_collab` permission error.

## Proposed action

- **Triage action:** route-to-area
- **Item type:** bug
- **Reproduction:** source_reproducible
- **Rating:** 5/6 Source-confirmed
- **Severity:** perf
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** none
- **Area labels:** vscode-build
- **Confidence:** 0.82
- **Proposed comment:** none

## Auto-fix candidate

- **Auto-fixable:** yes
- **Rationale:** Single-file, one-line-of-substance test-only change (add suite-level `this.timeout(20_000)`) confirmed missing on main, matching an established pattern in sibling files; low risk, no production code touched.
- **Likely files:** `src/vs/platform/agentHost/test/node/agentSdkDownloader.test.ts`
- **Validation:** Run `scripts/test.sh --grep AgentSdkDownloader` (or the node.js unit test runner scoped to `vs/platform/agentHost/test/node/agentSdkDownloader.test.ts`) on a slow/loaded machine or Windows CI runner and confirm the suite no longer times out at 2000ms; before the fix, artificially slowing the loopback server/tar extraction should reproduce a `Timeout of 2000ms exceeded` failure that the fix resolves.
- **Fix prompt:**

  > The `AgentSdkDownloader` integration suite (src/vs/platform/agentHost/test/node/agentSdkDownloader.test.ts, `suite('AgentSdkDownloader', () => {...})` starting at line 182) has no suite-level mocha timeout, so its tests — which spin up a real loopback HTTP server, download a gzip tarball, and extract it via `tar` — routinely exceed mocha's default 2000ms on slower/Windows CI, producing flaky `Timeout of 2000ms exceeded` failures even with no product regression. Convert the suite's arrow-function callback to a `function` expression so `this` binds to the mocha suite context, and add `this.timeout(20_000);` as the first statement in the suite body — mirroring the existing pattern in copilotSdkImportSession.integrationTest.ts (`this.timeout(120_000)`) and the sibling test's own `this.timeout(15_000)` override at line 363, which must be left untouched. Do not change any other test logic, assertions, or production code in agentSdkDownloader.ts. Validate by confirming the suite runs cleanly under `scripts/test.sh` scoped to this file, and that the change is purely additive (a diff of only the suite declaration line and the new timeout call).

## Evidence

- **no timeout on main:** src/vs/platform/agentHost/test/node/agentSdkDownloader.test.ts:182 declares `suite('AgentSdkDownloader', () => {...})` with no suite-level `this.timeout`; only the cancellation test at line 363 sets `this.timeout(15_000)` locally, confirmed on current main (HEAD 802ccf1e430594da0836a36d8095d538361eb9dd).
- **established pattern:** src/vs/platform/agentHost/test/node/copilotSdkImportSession.integrationTest.ts:18 and mapSessionEvents.perf.test.ts:104 already use suite/test-level `this.timeout(...)` for long-running integration/perf tests in the same directory.
- **PR creation failed:** Body states automated PR creation hit `Validation Failed: fork_collab Fork collab can't be granted by someone without permission`, so the diff exists only on the bot's fork branch, not as an open PR against microsoft/vscode.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
