---
number: 248
url: "https://github.com/microsoft/vscode-livepreview/issues/248"
engine: copilot
owner: andreamah
labels: bug
proposedLabel: info-needed
closeReason: none
triageAction: needs-info
confidence: 0.84
severity: correctness
breadth: niche
priority: none
visionFit: not_applicable
autoFixable: false
reviewedAt: "2026-09-11T08:07:47.706Z"
itemUpdatedAt: "2022-12-10T00:03:03Z"
itemCreatedAt: "2022-08-01T02:36:05Z"
upvotes: 0
reactionsTotal: 0
commentCount: 1
relatedOpenIssues: 0
milestone: Backlog
contentHash: 7b1861841e6cc29262b299f9e22acb34b6ee6a57167a5ec6661ccfdc633ac61c
policyHash: 606d474a59c93361
---

# Issue #248 — PDF in iframe tag causes File Explorer pop-up on refresh

[https://github.com/microsoft/vscode-livepreview/issues/248](https://github.com/microsoft/vscode-livepreview/issues/248)

## Summary

The original report provides concrete steps for the bundled webview browser, but current Live Preview uses VS Code's Integrated Browser by default and no current-version retest establishes whether either browser still opens File Explorer. The bundled path remains available, so the default change is not itself a fix; ask for results with both values of `livePreview.useIntegratedBrowser`.

## Change summary

Refreshing an HTML page containing a local PDF iframe repeatedly opens operating-system File Explorer windows and steals focus.

## Best solution

Retest the supplied project on current VS Code and Live Preview with `livePreview.useIntegratedBrowser` both `true` and `false`. If only the bundled browser fails, determine whether that legacy `src/editorPreview/` path remains supported or whether the behavior originates in VS Code's webview/PDF handling; if the Integrated Browser also fails, route the rendering behavior to `microsoft/vscode` under `browser-integration` while confirming the server returns the PDF normally.

## Proposed action

- **Triage action:** needs-info
- **Item type:** bug
- **Reproduction:** unclear
- **Evidence:** 3/6 Plausible
- **Severity:** correctness
- **Breadth:** niche
- **Priority:** none
- **Proposed label:** info-needed
- **Area labels:** none
- **Confidence:** 0.84
- **Proposed comment:**

  > Thanks for the detailed reproduction. Live Preview now uses VS Code's Integrated Browser by default, while the bundled browser involved when this was reported is still available behind `livePreview.useIntegratedBrowser: false`. Could you retest the project with current VS Code and Live Preview and let us know what happens with that setting first at `true` and then at `false`? Please also include both version numbers and note whether Windows File Explorer opens in either case. That will show whether this remains specific to the bundled browser or also affects the current default preview.

## Auto-fix candidate

- **Auto-fixable:** no
- **Rationale:** Current behavior is unconfirmed and the operating-system popup may originate in VS Code's browser/webview handling rather than a small, localized extension defect.

## Evidence

- **original reproduction:** The issue supplies specific markup (`<iframe src="resume.pdf">`), repeated-refresh steps, a screenshot, and a public reproduction project; the reporter also describes repeated Explorer windows and focus loss during auto-save.
- **current default changed:** Commit https://github.com/microsoft/vscode-livepreview/commit/9f09bcde70e815f80409b017a86833ab83fb618f changed only `livePreview.useIntegratedBrowser` from false to true. Current `package.json` still defaults that setting to true, so embedded previews normally bypass the bundled webview implicated by this 2022 report.
- **legacy path remains:** The bundled implementation still exists: `src/editorPreview/webviewComm.ts` creates a full-page `hostedContent` iframe, while the served page can contain the reporter's nested PDF iframe. The source does not conclusively establish whether current VS Code will invoke File Explorer for that PDF, so this cannot be marked reproduced or fixed from source alone.
- **no canonical found:** Searches for PDF, iframe, and File Explorer reports in `microsoft/vscode-livepreview` found no separate open issue that clearly owns this defect.

---
Reviewed by vscodesweeper (copilot) — proposal only, not applied.
