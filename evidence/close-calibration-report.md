> Preserved snapshot (2026-08-18) of `out/close-calibration/report.md` from the
> 2026-08-13 calibration run — the evidence behind the blog post's adversarial-
> refutation paragraph and auto-close-plan.md Phase 0. `out/` is gitignored and
> regenerated by `npm run calibrate-close`; this copy is the durable record.
> The refuter in this run was the codex engine, retired 2026-08-21
> (`plans/copilot-only-plan.md`); later runs refute on a different *model* of the
> Copilot CLI, so the "refuter: codex" lines below are historical.
>
> **Adjudicated 2026-08-18** (see `close-calibration-adjudication.md`): of the
> 28 refuted/uncertain flags, 20 confirmed as genuine false positives, 8 were
> refuter overreach. Adjudicated precision: (64 + 8) / 92 = **78%** — the
> machine-pass survival rates below were, as stated, a lower bound.

# Close-proposal calibration — adversarial refutation (auto-close-plan.md Phase 0)

Generated 2026-08-13T13:06:09.381Z · pool: 805 open propose-close at confidence ≥ 0.9
(implemented on main 306 · *duplicate 276 · *caused-by-extension 67 · *as-designed 44 · *question 39 · *off-topic 25 · other 17 · *out-of-scope 14 · *extension-candidate 10 · *not-reproducible 5 · duplicate 1 · out-of-scope 1) · sample: 1 (seed 42)

**Survival = stands / engine-judged.** `refuted`/`uncertain` rows are the human
adjudication queue below — survival is a LOWER bound on precision until adjudicated,
and a refuter verdict is itself a model opinion, not ground truth.

## By confidence bucket

| bucket | judged | stands | refuted | uncertain | survival |
| --- | --- | --- | --- | --- | --- |
| 0.90 | 74 | 51 | 21 | 2 | 69% |
| 0.95 | 18 | 13 | 5 | 0 | 72% |

## By close reason

| reason | judged | stands | refuted | uncertain | survival |
| --- | --- | --- | --- | --- | --- |
| *as-designed | 3 | 2 | 0 | 1 | 67% |
| *caused-by-extension | 8 | 5 | 2 | 1 | 63% |
| *duplicate | 32 | 25 | 7 | 0 | 78% |
| *extension-candidate | 2 | 0 | 2 | 0 | 0% |
| *not-reproducible | 2 | 0 | 2 | 0 | 0% |
| *off-topic | 4 | 2 | 2 | 0 | 50% |
| *out-of-scope | 2 | 2 | 0 | 0 | 100% |
| *question | 4 | 4 | 0 | 0 | 100% |
| duplicate | 2 | 2 | 0 | 0 | 100% |
| implemented on main | 31 | 20 | 11 | 0 | 65% |
| other | 2 | 2 | 0 | 0 | 100% |

## Not judged

- Closed since review (no engine run — acted on or overtaken): 1 — #329745
- Engine/parse failures (delete `results/<n>.json` and re-run to retry): 0

## Adjudication queue (refuted first, then uncertain)

### [#104292](https://github.com/microsoft/vscode/issues/104292) — REFUTED
- Review: *duplicate at 0.97 (copilot) · refuter: codex at 0.72
- Refuter reasoning: The proposed duplicate comment relies on the stronger claim that #47770 already covers Remote Desktop, Surface, iPad, and external-keyboard scenarios. The canonical issue page I could inspect only describes direct touch-screen editor selection on Windows and empty-space touch scrolling; it does not mention Remote Desktop, iPad, stylus/pen, or external-keyboard behavior, while #104292 specifically centers Remote Desktop/tablet/stylus use and its later comment adds iPad keyboard selection failures. Local editor code also treats touch and pen differently in pointer handling, so the remote/stylus angle is not obviously the same input path as generic touch-drag selection.
- Evidence: GitHub issue #47770 page: title/body/labels/metadata lines 131-171, 149-155, 199-204; body only says touch screen drag on text should select and empty space should scroll, status Open/Backlog.
GitHub issue #104292 page: title/body/labels/metadata lines 131-170, 181-199; body names Remote Desktop/Azure Windows, Surface Pro X, iPad Pro, pen/stylus, and asks to link touch events to text selection.
Prompt-provided #104292 recent comment from 2021-07-26: Remote Desktop on iOS, Magic Keyboard Shift+arrow and double-tap selection failures.
Local checkout: src/vs/editor/browser/controller/pointerHandler.ts lines 23-104 show pointerType-specific handling, with touch scrolling and pen dispatching selection in PointerEventHandler; TouchHandler falls back to scroll-only change handling.
Local checkout: git log --all --grep for 47770/104292/touch-selection returned no matching commits; gh issue view for both issues failed due network connection.

### [#163778](https://github.com/microsoft/vscode/issues/163778) — REFUTED
- Review: *duplicate at 0.96 (copilot) · refuter: codex at 0.74
- Refuter reasoning: The canonical issue I could inspect does not cleanly support the close proposal's decisive claim. #149153's repro writes `vscode.Uri.file(...)` values, says both `file:///...` entries are already clickable, and reports that the outside-workspace one fails to open, especially in remote contexts; #163778 is about Python/plain filesystem paths in Output not being link-detected at all when outside the workspace, plus a later `..` normalization case. The shared workspace-root involvement makes them related, but the canonical's concrete symptom is an already-detected file URI opening as not found, not the same plain-path link-detection request described in #163778 and in the proposed close comment.
- Evidence: #163778 issue text and comments supplied in prompt: Python error output; maintainer says output link detection only detects paths inside workspace; later comment asks about `/path/to/others/../workspace/file` normalization.
#149153 GitHub page opened via web: title `Support to detect file links in output`, open/backlog; repro appends `vscode.Uri.file(...)` values producing `file:///home/...` output; says both file links are clickable but only the workspace-contained one opens, outside-workspace fails with `File not found`, and remote labels are present.
Local checkout `src/vs/workbench/contrib/output/common/outputLinkComputer.ts`: `computePatterns` builds regexes from workspace folder URI/path only; links are constructed by joining the workspace folder with matched relative suffix.
Local `git log --grep "149153\|output.*link\|workspace.*output"` found no commit evidence tying #149153 to an implementation or clarifying scope.

### [#204398](https://github.com/microsoft/vscode/issues/204398) — REFUTED
- Review: implemented on main at 0.95 (copilot) · refuter: codex at 0.92
- Refuter reasoning: The cited commit exists and did migrate the root and several test/build scripts to `npm-run-all2`, but the decisive “across the board” claim does not hold on current `main`. Tracked active package manifests under `extensions/copilot` still declare `npm-run-all@^4.1.5` and use `npm-run-all` in dev scripts (`watch`, `test`, and `chat-lib` build), so the reported repository-wide migration is incomplete or has regressed since the fix.
- Evidence: Issue text requested moving from `npm-run-all` to `npm-run-all2` for dev scripts.
`git show dd65f0c5ef8782267e37abfac88c5931c63c9ba2 --name-only` showed PR #284087 migrated root/test/build workflow files.
Current checkout: `git branch --show-current` = `main`, `git rev-parse HEAD` = `b966285933b27aaa9be0904c1c6f2bbe666aefa9`.
`rg -n '"npm-run-all"|npm-run-all ' package.json extensions test build .github --glob 'package.json' --glob '!**/node_modules/**' --glob '!**/fixtures/**'` found tracked active references in `extensions/copilot/package.json` and `extensions/copilot/chat-lib/package.json`.
`git ls-files` confirmed `extensions/copilot/package.json`, `extensions/copilot/package-lock.json`, `extensions/copilot/chat-lib/package.json`, and `extensions/copilot/chat-lib/package-lock.json` are tracked.
Inspected `extensions/copilot/package.json`: scripts `watch` and `test` invoke `npm-run-all`, and devDependencies include `"npm-run-all": "^4.1.5"`.
Inspected `extensions/copilot/chat-lib/package.json`: script `build` invokes `npm-run-all`, and devDependencies include `"npm-run-all": "^4.1.5"`.

### [#230838](https://github.com/microsoft/vscode/issues/230838) — REFUTED
- Review: implemented on main at 0.95 (copilot) · refuter: codex at 0.88
- Refuter reasoning: The cited commit exists on main and does reduce `.open-editors .monaco-list .monaco-list-row` left padding from `22px` to `8px`, but it is only a one-line CSS change. The issue as reported explicitly asked both to remove extra padding and to move the close `x` to appear on hover on the right-hand side; current `openEditorsView.ts` still creates the `ActionBar` before the label and current CSS uses normal flex row layout, so the close/dirty control remains on the left. The later 2026-05-09 issue comment also continues to request the right-side close-button behavior, so treating that as fully implemented-on-main is a partial-fix close, not a complete implementation of the reported request.
- Evidence: Issue text for #230838: asks whether it makes sense to remove extra padding and change the `x` button to only appear on hover on the right-hand side.
Recent issue comments: bpasero suggested moving the `X` button to the right on 2024-12-20; bpasero later said only that he was reducing padding from `22px` to `8px` on 2025-11-10; JohnVeness reiterated the right-side close-button request on 2026-05-09.
Commit inspected: `git show f8d5db5415db908a93712f57239a58a682fdd773` shows only `src/vs/workbench/contrib/files/browser/views/media/openeditors.css` changing `padding-left: 22px` to `padding-left: 8px`.
Current code inspected: `src/vs/workbench/contrib/files/browser/views/openEditorsView.ts` `renderTemplate` appends `new ActionBar(container, ...)` before `this.labels.create(container)`, and current `openeditors.css` keeps `.monaco-list-row { padding-left: 8px; display: flex; }` with the action bar hidden/visible on the left.

### [#282454](https://github.com/microsoft/vscode/issues/282454) — REFUTED
- Review: implemented on main at 0.95 (copilot) · refuter: codex at 0.78
- Refuter reasoning: Current main does define `git.similarityThreshold` and wires it into status and some diff-stat paths, but it is not fully functional as claimed: the schema allows `0`, while every current command path checks the setting with a truthiness guard, so `0` is silently not passed as `--find-renames=0%`. I also found that the cited commit itself only wired the setting into `git status`, not `git diff` or `git diff-tree`; those were only present in later/current code paths. Since the issue asks for a custom similarity threshold and the close comment claims the setting is fully functional and applied broadly, the implementation has a concrete allowed-value gap and the cited fix overstates what that commit did.
- Evidence: Issue #282454 text: requests support for `git.similarityThreshold` to detect renamed files with custom similarity threshold; no comments.
Commit b0252732a75557a0a42204085b0b51188aecae70: adds schema and passes `similarityThreshold` only into `Repository.getStatus`.
extensions/git/package.json:4067: `git.similarityThreshold` is a number with default 50, minimum 0, maximum 100.
extensions/git/src/git.ts:1843, 1867, 1914, 2759: current main uses truthiness checks before pushing `--find-renames=${threshold}%`, so configured `0` is ignored.
extensions/git/src/repository.ts:1238-1256 and 2965-2968: current main reads the setting for diff-stat/diff-tree/status paths.

### [#211904](https://github.com/microsoft/vscode/issues/211904) — REFUTED
- Review: implemented on main at 0.94 (copilot) · refuter: codex at 0.78
- Refuter reasoning: The cited implementation covers Copilot Chat language-model providers, including Ollama via BYOK, but I found evidence that Copilot inline code suggestions/completions are still a separate provider path that requires a Copilot token and calls the Copilot completions endpoint. The issue text specifically frames the request around Copilot relying on GPT models for generating code suggestions, so closing as fully implemented based only on Chat/BYOK support appears overbroad. The later comments do ask for Ollama integration or a custom LLM registration API, and that part is implemented, but the original reported code-suggestion scenario is not shown to be covered.
- Evidence: Issue #211904 text: asks to use alternative local models because Copilot relies on GPT models for generating code suggestions.
Commit 333d9a4053e71ed5249450e112b6a9decd3997b4: exists, subject 'Hello Copilot', adds extensions/copilot BYOK files including ollamaProvider.ts/byokContribution.ts.
extensions/copilot/src/extension/byok/vscode-node/byokContribution.ts: registers Ollama and other BYOK providers only through lm.registerLanguageModelChatProvider.
extensions/copilot/src/extension/byok/vscode-node/ollamaProvider.ts: Ollama provider creates an OpenAI-compatible /v1/chat/completions endpoint, i.e. chat-model plumbing.
src/vscode-dts/vscode.d.ts: registerLanguageModelChatProvider is documented for models used through the chat view or LanguageModelChat extension API.
extensions/copilot/src/extension/completions/vscode-node/completionsCoreContribution.ts: comment says completions require a Copilot token to call the completions endpoint, and registration is gated on copilotToken.
extensions/copilot/src/extension/completions-core/vscode-node/extension/src/vscodeInlineCompletionItemProvider.ts: inline completion implementation is CopilotInlineCompletionItemProvider, separate from LanguageModelChatProvider/BYOK.

### [#324835](https://github.com/microsoft/vscode/issues/324835) — REFUTED
- Review: *extension-candidate at 0.94 (copilot) · refuter: codex at 0.74
- Refuter reasoning: The prior issue #132689 is a same-family request, but its own body defines the feature as word/name-based coloring, including giving `.fun` the same color wherever it appears. Issue #324835 explicitly asks about a semantically smarter case where the same spelling should get different colors for different symbols, and the cited extension’s public description/settings only support semantic-token filtering plus sequential or name-hash coloring. That undercuts both decisive claims in the close proposal: the prior issue is not identical in scope, and the extension is not shown to already provide the full requested behavior.
- Evidence: GitHub issue #132689 page: title/body/closed state/extension-candidate label; body lines describing word-based coloring by variable name and same color wherever it appears.
Issue #324835 text supplied in prompt: Rust example asking function `a_name` and variable `a_name` to have distinct colors despite identical spelling.
Visual Studio Marketplace / mnespor README for `vscode-color-identifiers-mode`: says it uses semantic highlighting to choose words to highlight and offers `sequential` or `hash` where hash uses the variable name.
Local checkout searches: `rg` for Color Identifiers / identifier-based coloring found no native implementation; semantic-token code present only standard token provider/styling paths.

### [#1833](https://github.com/microsoft/vscode/issues/1833) — REFUTED
- Review: implemented on main at 0.93 (copilot) · refuter: codex at 0.82
- Refuter reasoning: The cited implementation does not cover the issue as reported: #1833 asks for an API to add and remove UI elements and frames the broader goal as workbench/UI customization, while the shipped APIs only let extensions contribute their own static view containers/views and webview content. Commit f99ff58d30e518daa111e3ac595d1909d0afd131 is especially narrow: it merely removes the proposed-API guard for webview views, not a general GUI customization or UI removal API. Current main confirms containers can be contributed to Activity Bar, Panel, and Secondary Side Bar, but removal code only deregisters extension-owned contributions when the extension contribution is removed; it does not let extensions remove existing VS Code UI elements or customize every workbench element/theme surface.
- Evidence: Issue #1833 text: requested “adding the ability to add and remove ui elements” and described UI customization/theming beyond editor content.
git show f99ff58d30e518daa111e3ac595d1909d0afd131: only deleted the enableProposedApi check for ViewType.Webview in viewsExtensionPoint.ts.
git show 4fd4bef1355ef62ee734d5e6b0c03dbe808b7443: introduced viewsContainers for Activity Bar custom viewlets, not broad GUI customization.
src/vs/workbench/api/browser/viewsExtensionPoint.ts lines 60-80: current schema allows contributing containers only under activitybar, panel, secondarySidebar.
src/vs/workbench/api/browser/viewsExtensionPoint.ts lines 331-340 and 568-579: removal paths deregister/move views belonging to removed extensions, not arbitrary existing UI elements.
src/vscode-dts/vscode.d.ts lines 11741-11749: registerWebviewViewProvider registers a provider for a view id from package.json, not a general workbench UI customization/removal API.

### [#298330](https://github.com/microsoft/vscode/issues/298330) — REFUTED
- Review: *duplicate at 0.93 (copilot) · refuter: codex at 0.86
- Refuter reasoning: The duplicate claim does not survive a root-cause check. The cited canonical #257888 is an open feature request for accepting sovereign-cloud Azure OpenAI deployment URLs and reports the “Unrecognized Azure deployment URL” path, but #298330 supplies a full `.openai.azure.us/.../chat/completions` URL that the current code treats as an explicit API path and returns unchanged. The remaining failure in #298330 is the Entra authentication path hardcoding the commercial `https://cognitiveservices.azure.com/.default` scope, and I found no canonical issue text/comments tying #257888 to that auth-scope defect.
- Evidence: Issue #298330 text provided in prompt: reports hardcoded commercial-cloud authentication for a full Fairfax/MAG `.openai.azure.us/openai/deployments/.../chat/completions` URL.
GitHub issue #257888 opened via https://github.com/microsoft/vscode/issues/257888: title/body say “Allow sovereign cloud Azure Openai deployments as custom models” and “Unrecognized Azure Deployment URL”; open Backlog feature-request; page shows no relationships/development.
extensions/copilot/src/extension/byok/vscode-node/customOAIProvider.ts lines 44-46: `hasExplicitApiPath` returns true for URLs containing `/chat/completions`.
extensions/copilot/src/extension/byok/vscode-node/azureProvider.ts lines 21-24: explicit API paths are returned unchanged, so #298330’s supplied URL bypasses the unrecognized-URL branch tracked by #257888.
extensions/copilot/src/extension/byok/vscode-node/azureProvider.ts lines 120-122 and extensions/copilot/src/platform/configuration/common/configurationService.ts lines 564-568: Entra auth always requests `https://cognitiveservices.azure.com/.default`.

### [#327242](https://github.com/microsoft/vscode/issues/327242) — REFUTED
- Review: *extension-candidate at 0.93 (copilot) · refuter: codex at 0.82
- Refuter reasoning: The close proposal relies on manual shortcuts as covering the reported workflow, but the issue is specifically asking for an extension-observable built-in Explorer expand/collapse API so an extension can make recursive collapse the default behavior. The checkout shows TreeView expansion events exist only on the public `TreeView<T>` API, while the Explorer's collapse-state event is consumed internally in `ExplorerView` and is not exposed through `IExplorerView`/`IExplorerService` or vscode.d.ts. The cited prior issue is similar, but the accessible issue page only verifies it was closed and labeled `*extension-candidate`; I could not verify the claimed maintainer reasoning from comments, and the current issue has a live backlog-candidate bot comment rather than a maintainer duplicate/decline decision.
- Evidence: Issue #327242 text and comments: requester explicitly distinguishes extension TreeView events from built-in File Explorer and rejects manual shortcuts as sufficient.
GitHub page for #142041: title/body match the Explorer expand/collapse event API request; page shows Closed and labels `*extension-candidate`/`info-needed`, but comments were not available via gh/web.
src/vscode-dts/vscode.d.ts:12146-12151 exposes `TreeView<T>.onDidExpandElement` and `onDidCollapseElement`, with no Explorer-specific events found by rg.
src/vs/workbench/contrib/files/browser/views/explorerView.ts:565 consumes `tree.onDidChangeCollapseState` internally only; src/vs/workbench/contrib/files/browser/files.ts:59-72 exposes `IExplorerView` methods including `isItemCollapsed` but no collapse/expand event.
src/vs/base/browser/ui/tree/abstractTree.ts:2426-2428 implements Alt-click recursive toggle; src/vs/workbench/browser/actions/listCommands.ts:394-406 implements Ctrl/Cmd+Left `list.collapseAll`, confirming these are manual tree interactions rather than extension event hooks.

### [#211394](https://github.com/microsoft/vscode/issues/211394) — REFUTED
- Review: implemented on main at 0.92 (copilot) · refuter: codex at 0.88
- Refuter reasoning: The cited implementation fixes explicit `CSI 0 SP q`, but it does not cover the issue as reported because the repro also says empty `CSI SP q` should have the same reset behavior. Current xterm code treats `params.length === 0` as parameter `1`, and the documented supported styles in the same block say `empty, 1: blinking block`, so `printf "\033[ q"` still resolves to block rather than the user-configured VS Code cursor style. That makes the close proposal a partial fix, not a complete implemented-on-main resolution of #211394.
- Evidence: Issue #211394 body: repro includes both `printf "\033[0 q"` and `printf "\033[ q"`, with the comment that empty has the same effect as `0` and should restore the user-defined preference.
`node_modules/@xterm/xterm/src/common/InputHandler.ts` lines around `setCursorStyle`: `const param = params.length === 0 ? 1 : params.params[0]`; only `param === 0` clears `decPrivateModes.cursorStyle`/`cursorBlink`, while `1`/`2` set block.
`node_modules/@xterm/xterm/src/common/InputHandler.ts` DECSCUSR docs in the checkout: `0: reset to option`, but `empty, 1: blinking block`.
`node_modules/@xterm/xterm/src/browser/renderer/dom/DomRenderer.ts` and `node_modules/@xterm/addon-webgl/src/WebglRenderer.ts`: renderers fall back to terminal options only when `decPrivateModes.cursorStyle` is `undefined`, which the empty sequence does not cause.
VS Code commit `292d0bd51af8d7e107fa34b00955aceb7d9c03d5`: inspected as a dependency bump to `@xterm/xterm@5.6.0-beta.74`.
Upstream PR https://github.com/xtermjs/xterm.js/pull/5265: inspected title/status and commits, including `Revert to cursor options after DECSCUSR 0` and `Fix default param DECSCUSR handling`.

### [#226720](https://github.com/microsoft/vscode/issues/226720) — REFUTED
- Review: implemented on main at 0.92 (copilot) · refuter: codex at 0.9
- Refuter reasoning: The cited commit did add `SessionLifecycle`, but it did not make `FileSearchProviderOptions.session` unconditional. On current main, `FileSearchManager.getSessionTokenSource` still returns `undefined` when `config.cacheKey` is absent, and `FileSearchEngine.doSearch` assigns `options.session` via optional chaining from that possibly undefined lifecycle. I also found current file-search call sites that build `IFileQuery` without `cacheKey`, so extension file search providers can still receive `session: undefined`, contradicting the proposed closing claim that the session is always a proper object.
- Evidence: Issue text for #226720: reporter says `FileSearchProviderOptions#session` is sometimes `undefined` and should always have some value.
Commit 0e62f015f7b1a2bc9f8b953c47400b6329ae3c99: inspected diff; it adds `SessionLifecycle` but keeps `getSessionTokenSource(cacheKey)` returning undefined when no cache key exists.
src/vs/workbench/services/search/common/fileSearchManager.ts:122-127,340-342,372-381: current main constructs `session` from `this.sessionLifecycle?.obj` and returns undefined for missing `cacheKey`.
src/vs/workbench/services/search/common/search.ts:111-126: `IFileQuery.cacheKey` remains optional.
src/vs/workbench/contrib/mcp/browser/mcpPromptArgumentPick.ts:257-262, src/vs/workbench/contrib/terminalContrib/links/browser/terminalLinkOpeners.ts:220-224 and 237-241, src/vs/sessions/contrib/sessions/browser/sessionsTelemetry.contribution.ts:630-635: current callers issue file searches without a cache key.

### [#253424](https://github.com/microsoft/vscode/issues/253424) — REFUTED
- Review: implemented on main at 0.92 (copilot) · refuter: codex at 0.82
- Refuter reasoning: The cited commit adds a visual status badge inside the Agent title-bar/command-center widget, but it does not change the actual window title or add anything to the window quick pick requested in the issue. More importantly, the needs-input list explicitly excludes any session that already has a widget open in the current window, so the window containing the pending confirmation may not show this new needs-input badge at all. That makes the proposed close too broad for the reported “which of my windows is pending confirmation” scenario.
- Evidence: Issue text: requested a clearer way to tell which window is pending confirmation, specifically by changing window title or adding a badge in window quick pick.
Commit bb25992d5c55d8eb6ca74d6f00df0fbc86b05c84: exists and modifies only agentTitleBarStatusWidget.ts and its CSS.
src/vs/workbench/contrib/chat/browser/agentSessions/experiments/agentTitleBarStatusWidget.ts:481 computes attentionNeededSessions as NeedsInput and !chatWidgetService.getWidgetBySessionResource(s.resource).
src/vs/workbench/contrib/chat/browser/agentSessions/experiments/agentTitleBarStatusWidget.ts:985-1017 renders a needs-input badge/count in the widget and tooltip.
src/vs/workbench/contrib/chat/browser/agentSessions/experiments/agentTitleBarStatusWidget.ts:1191-1241 applies a NeedsInput filter and opens the agent sessions view.
src/vs/workbench/browser/parts/titlebar/windowTitle.ts:189-214 and 386-407 show actual document/window title generation; the cited commit does not touch this path.

### [#291301](https://github.com/microsoft/vscode/issues/291301) — REFUTED
- Review: *not-reproducible at 0.92 (copilot) · refuter: codex at 0.74
- Refuter reasoning: The reporter's follow-up supports closure, but it does not cleanly support the review's decisive claim that this was only hallucinated nonexistent paths: the original error `Unable to resolve resource c:%5Cgit%5CFoo.cs` matches a Windows absolute markdown target being treated as a `c:` URI. I found a later main-branch commit explicitly titled `Fix broken file links in chat when model emits bare line anchors or Windows absolute paths`, with tests adding coverage for de-linkifying Windows drive-letter links. That is concrete evidence this symptom was a real chat linkification/opening problem class, so closing specifically as not-reproducible because it was merely hallucination is not well supported.
- Evidence: Issue text: Windows report says hovering shows the correct path but clicking opens error `Unable to resolve resource c:%5Cgit%5CFoo.cs`; follow-up comment says `I think this can be closed... erroneous links. Perhaps due to model haliucination.`
Commit inspected: `0911342795e899fe89980e5b5be8e1b7cb4340fc` dated 2026-02-20, title `Fix broken file links in chat when model emits bare line anchors or Windows absolute paths (#3847)`.
Files inspected: `extensions/copilot/src/extension/linkify/common/modelFilePathLinkifier.ts`, `extensions/copilot/src/extension/linkify/test/node/modelFilePathLinkifier.spec.ts`, `extensions/copilot/src/extension/linkify/test/node/linkifier.spec.ts`.
Specific test inspected: `Should de-linkify Windows absolute paths with drive letters` for `[text](c:\src\file.ts)`, matching the malformed `c:%5C...` failure mode.

### [#303905](https://github.com/microsoft/vscode/issues/303905) — REFUTED
- Review: *off-topic at 0.92 (copilot) · refuter: codex at 0.74
- Refuter reasoning: The close proposal’s decisive premise is too broad: it says agent execution/fabricated tool evidence is outside VS Code, but the checkout shows this repo contains first-party agent host and bundled Copilot chat/agent execution surfaces. The issue text does not identify a third-party extension and complains specifically about execution-phase terminal/runtime claims, which overlaps with repo-owned code paths exposing execution subagents, terminal-command tool logging, and agent-host process/tool execution. This does not prove the reported bug is valid, but it is concrete evidence against closing solely because agent behavior is categorically not VS Code code/functionality.
- Evidence: Issue #303905 text provided in prompt: reports an agent accepting a no-terminal/no-runtime/no-file-change contract, then presenting PID/process/port/health-check runtime claims and admitting commands ran/hallucinated evidence; no comments and no extension identification.
extensions/copilot/package.json: GitHub Copilot is present in this checkout, has bugs.url set to https://github.com/microsoft/vscode/issues, and contributes an execution_subagent tool whose modelDescription says it launches an execution-focused subagent, can run commands, and returns commands/output excerpts.
src/vscode-dts/vscode.proposed.chatDebug.d.ts: ChatDebugToolCallEvent explicitly covers tool invocations including terminal commands and code edits, with serialized input/output/result fields.
src/vs/platform/agentHost/electron-main/electronAgentHostStarter.ts: VS Code starts an agent host process, resolves shell environment for spawned tools/terminals, forwards Codex/Copilot agent settings, and kills the utility process on exit.
src/vs/workbench/contrib/chat/electron-browser/chat.contribution.ts and src/vs/workbench/contrib/chat/test/browser/agentSessions/stateToProgressAdapter.test.ts: agent-host chat sessions and terminal tool-call state/output/exit-code presentation are part of VS Code workbench tests/code.

### [#307474](https://github.com/microsoft/vscode/issues/307474) — REFUTED
- Review: *caused-by-extension at 0.92 (copilot) · refuter: codex at 0.86
- Refuter reasoning: The proposed close leans on the attached Process Info as confirming that the AL language server is the heavy memory consumer, but that diagnostic output is not reliable evidence of actual memory use. In VS Code 1.113.0 and current main, `listProcesses` already converts macOS `ps %mem` into bytes, while `diagnosticsService.formatProcessItem` converts the same value again as if it were still a percent; that exactly explains the impossible multi-billion MB values shown for many VS Code processes. The attached tree also shows only one `Microsoft.Dynamics.Nav.EditorServices.Host` row, with `Mem MB` reported as `0`, so the review's decisive factual claim that the reporter's own process list confirms heavy AL child processes is false.
- Evidence: Issue #307474 text and Process Info: many impossible Mem MB values for Code processes; only one `Microsoft.Dynamics.Nav.EditorServices.Host` entry, under extension-host, with Mem MB `0`.
`git show 1.113.0:src/vs/base/node/ps.ts`: macOS/Linux `addToTree` stores `mem: totalMemory * (mem / 100)`.
`git show 1.113.0:src/vs/platform/diagnostics/node/diagnosticsService.ts`: `formatProcessItem` again computes `osLib.totalmem() * (item.mem / 100)` before printing Mem MB.
Current checkout `src/vs/base/node/ps.ts` and `src/vs/platform/diagnostics/node/diagnosticsService.ts`: the same double-conversion remains on main.

### [#325734](https://github.com/microsoft/vscode/issues/325734) — REFUTED
- Review: *off-topic at 0.92 (copilot) · refuter: codex at 0.98
- Refuter reasoning: The close proposal’s core claim is false: the checkout contains a built-in VS Code aquarium feature. `src/vs/sessions/contrib/aquarium` defines the aquarium service, fish rendering, CSS animation, RAF update loop, commands, and tests, so an issue saying “The fish in the aquarium do not move” is about VS Code product code, not automatically off-topic.
- Evidence: Issue text in prompt: #325734 says “The fish in the aquarium do not move”, includes VS Code 1.127.0, and has no comments.
Checkout search: `rg -i "aquarium|fish"` found `src/vs/sessions/contrib/aquarium/...` and `build/lib/i18n.resources.json` entry `vs/sessions/contrib/aquarium`.
`src/vs/sessions/contrib/aquarium/browser/aquariumOverlay.ts`: defines `AquariumService`, `FISH_COUNT = 50`, `createActiveAquarium`, fish creation, `scheduleAtNextAnimationFrame(... tick)`, and `updateFish(dt)`.
`src/vs/sessions/contrib/aquarium/browser/fish.ts`: defines the swimming fish DOM/SVG implementation.
`src/vs/sessions/contrib/aquarium/browser/media/aquarium.css`: contains `.agents-aquarium-fish` and fish body-wave animation styles.
`src/vs/sessions/contrib/aquarium/test/browser/aquariumOverlay.test.ts` and `fishFeedingStreak.test.ts`: tests for the aquarium feature.

### [#326939](https://github.com/microsoft/vscode/issues/326939) — REFUTED
- Review: *caused-by-extension at 0.92 (copilot) · refuter: codex at 0.82
- Refuter reasoning: The close proposal’s strongest claim is too broad: `cppdbg` itself is not in the VS Code repo, but the integrated-terminal handoff for debug adapters is implemented in VS Code core via the DAP `runInTerminal` request. The reported log message, `Exception occurred during xterm disposal`, is also emitted by core terminal code, not cpptools. That does not prove cpptools is innocent, but it concretely refutes the proposed rationale that the launch handoff and observed failure surface are wholly owned by the C/C++ extension and should be closed as caused-by-extension on that basis.
- Evidence: Issue text: repro uses `cppdbg`/`externalConsole: false`, failure is specifically integrated-terminal launch handoff, and includes repeated `Exception occurred during xterm disposal`; issue has no comments.
extensions/cpp/package.json: built-in C/C++ extension only contributes languages, grammars, problem patterns/matchers, and snippets; no `cppdbg` or `cppbuild` debugger/task contribution.
Repo search: `rg 'cppdbg|cppbuild|ms-vscode.cpptools' --glob '!node_modules/**' --glob '!*.perf-data.ts' --glob '!extensions/copilot/**' .` found no `cppdbg`/`cppbuild` implementation in VS Code core.
src/vs/workbench/contrib/debug/browser/rawDebugSession.ts:685-693: core handles debug adapter `runInTerminal` requests and calls `this.dbgr.runInTerminal(...)`.
src/vs/workbench/api/node/extHostDebugService.ts:76-212: core implements integrated-terminal debug launch logic for `args.kind === 'integrated'`, creates/reuses terminals, waits for shell init, executes the prepared command via shell integration/sendText, and tracks terminal reuse.
src/vs/workbench/contrib/terminal/browser/terminalInstance.ts:1322-1325: core catches and logs `Exception occurred during xterm disposal`.

### [#32267](https://github.com/microsoft/vscode/issues/32267) — REFUTED
- Review: implemented on main at 0.9 (copilot) · refuter: codex at 0.88
- Refuter reasoning: The cited commit exists and added dedicated drag-selection scrolling for #40890, but it does not implement the issue’s requested setting/control. On current main, selection drag scrolling computes speed from hard-coded thresholds plus viewport/line/character metrics, while `mouseWheelScrollSensitivity` is still documented as applying to mouse-wheel event deltas and is wired into the scrollbar path, not drag-selection scrolling. That makes the proposed close overclaim: the fast-scroll behavior may have changed, but the reported request to control selection-scroll sensitivity remains unimplemented.
- Evidence: Issue #32267 text supplied in prompt: asks `please make a setting to control the sensitivity of scrolling while selecting text.`
Commit dfc8776b46579a578c120f4135217119d5560cc2: message is `First scroll and then update the selection when dragging (#40890)` and adds `TopBottomDragScrolling` with hard-coded speed thresholds.
src/vs/editor/browser/controller/dragScrolling.ts:111-132 and 175-192: current main computes drag-scroll speed from lineHeight/viewport/outsideDistance with `Math.max(30/60/200, ...)`, no `mouseWheelScrollSensitivity`.
src/vs/editor/common/config/editorOptions.ts:6433-6436: `mouseWheelScrollSensitivity` remains described as a multiplier for `deltaX`/`deltaY` of mouse wheel events.
src/vs/editor/browser/viewParts/editorScrollbar/editorScrollbar.ts:37-62: `mouseWheelScrollSensitivity` is read and passed to scrollbar options for wheel scrolling.

### [#123187](https://github.com/microsoft/vscode/issues/123187) — REFUTED
- Review: *duplicate at 0.9 (copilot) · refuter: codex at 0.86
- Refuter reasoning: The proposed canonical #97399 is not the same request: its issue body asks for extension-development tooling to keep package.json contribution identifiers aligned with implementation, either by lint rules or by generating package.json from TypeScript. #123187 asks VS Code/vsce to support composing the manifest from external JSON contribution files, with ordering and collision semantics. I found no explicit external-file contribution design in #97399’s visible issue text, and local manifest scanning still reads and parses only package.json, which supports the distinction rather than the duplicate claim.
- Evidence: GitHub issue #97399 page: title/body/labels/status; body lines 205-255 discuss matching contribution keys, lint rules, and TypeScript-generated package.json, not external contribution include files.
GitHub issue #123187 page and provided issue text: body lines 164-200 propose contributes.include external JSON files plus composition order and collision handling.
Local checkout: src/vs/platform/extensionManagement/common/extensionsScannerService.ts lines 768-790 reads joinPath(extensionLocation, 'package.json') and JSON.parse(content).
Local checkout: src/vs/workbench/services/extensionManagement/browser/webExtensionsScannerService.ts lines 820-823 reads only package.json for web extension manifests.
Local checkout: src/vs/workbench/services/extensions/common/extensionsRegistry.ts lines 226-234 defines contributes as an object populated by extension points, with no include property visible there.
Local checkout searches: rg for 97399/123187/external contribution/include-contributes found no relevant implementation or issue-reference hits; git log --grep for those terms found no matching commits.
Attempted gh issue view for #97399/#123187, but local network access to api.github.com failed.

### [#159664](https://github.com/microsoft/vscode/issues/159664) — REFUTED
- Review: implemented on main at 0.9 (copilot) · refuter: codex at 0.86
- Refuter reasoning: The issue behavior is implemented on current main, but the cited fix commit is not the implementation commit. `2c12afef598a63b40df1b9654b74c71d2aed0b37` is an ESM conversion that deletes `nls.js` and adds `nls.ts` while carrying forward an already-existing `userLocale.startsWith('en')` condition; the actual behavioral change appears in `66f7365b4d9e802be8e85e71adc6ae23759c942e`, which changed `userLocale === 'en' || userLocale === 'en-us'` to `userLocale.startsWith('en')`. Callers in `src/main.ts` lower-case user and Electron locales before invoking `resolveNLSConfiguration`, so the implemented behavior itself covers `en-GB`, but the close proposal's cited commit and shipped-version claim are not supported by the inspected commit history.
- Evidence: Issue text supplied in prompt: requested treating `en-GB`/`en-*` as default to avoid language-pack lookup.
`git show --stat --oneline 2c12afef598a63b40df1b9654b74c71d2aed0b37`: commit is titled `esm - convert nls.js to nls.ts` and changes only conversion-related files.
`git show --unified=120 2c12afef598a63b40df1b9654b74c71d2aed0b37 -- src/vs/base/node/nls.js src/vs/base/node/nls.ts`: both deleted JS and added TS already contain `userLocale.startsWith('en')`, so this commit did not introduce the optimization.
`git show --unified=80 66f7365b4d9e802be8e85e71adc6ae23759c942e -- src/vs/base/node/nls.js`: this commit changes `userLocale === 'en' || userLocale === 'en-us'` to `userLocale.startsWith('en')`.
Current `src/vs/base/node/nls.ts:42-56`: `resolveNLSConfiguration` returns default NLS before `getLanguagePackConfigurations` when `userLocale.startsWith('en')`.
Current `src/main.ts` search results: user-specified locale and Electron app locale are lower-cased before `resolveNLSConfiguration` is called.

### [#180234](https://github.com/microsoft/vscode/issues/180234) — REFUTED
- Review: *duplicate at 0.9 (copilot) · refuter: codex at 0.82
- Refuter reasoning: The duplicate claim overreaches on the context-menu part. The canonical #129178 page I inspected is framed around Cmd+A/C/V keyboard shortcuts in an iframe inside a webview on macOS, while #180234 separately reports right-click/context-menu loss and the reporter explicitly says they do not see that part addressed by #129178. The checkout supports that distinction: current webview preload code forwards `contextmenu` from the webview frame window, but nested iframe events are a separate frame boundary, and the documented iframe workaround in `webview.ts` only discusses key events, not context menus.
- Evidence: GitHub #129178 page: title/body describe Cmd+A/C/V keyboard shortcuts in iframe inside webview on macOS, opened July 22, 2021: https://github.com/microsoft/vscode/issues/129178
GitHub #180234 page: repro includes right-click on iframe content causing no context menu, plus Cmd+C failure: https://github.com/microsoft/vscode/issues/180234
Provided #180234 comment by scztt on 2023-04-18: keyboard probably duplicate of #129178, but missing context menu not seen as addressed there.
Local checkout `src/vs/workbench/contrib/webview/browser/webview.ts:126-136`: iframe note only covers key events needing forwarding.
Local checkout `src/vs/workbench/contrib/webview/browser/pre/index.html:1174-1217`: VS Code forwards `contextmenu` from the webview frame window via `did-context-menu`, not from arbitrary embedded subframes.
Tried `gh issue view 129178/180234 --comments`; shell network access failed, so comments beyond supplied live-state text were not independently fetched via `gh`.

### [#189407](https://github.com/microsoft/vscode/issues/189407) — REFUTED
- Review: *not-reproducible at 0.9 (copilot) · refuter: codex at 0.78
- Refuter reasoning: I found a concrete code-level contradiction to the close proposal’s decisive rationale. On current main, the `git.stage` command reaches `Repository.add`, and `Repository.add` passes `Operation.Add(!this.optimisticUpdateEnabled())`, so with the default `git.optimisticUpdate: true` the operation is marked `showProgress: false`; `ProgressManager` only shows Source Control progress when a running operation has `showProgress`. The optimistic model update is invoked only after `retryRun` awaits the underlying `git add`, so a slow `git add` still creates the reported window of no progress/indicator rather than an immediately updated UI.
- Evidence: Issue comments: reporter showed 6-9s stage/unstage delay and git log entries with `git add` around 2099ms; maintainer said they would look at improving progress indicators for optimistic commands; reporter later said on 2026-01-19 that they do not have the issue anymore and it can be closed if nothing remains to track.
extensions/git/src/commands.ts:1514-1572: `git.stage` collects SCM resources and calls `repository.add(resources)`.
extensions/git/src/repository.ts:1267-1272: `add` starts `Operation.Add(!this.optimisticUpdateEnabled())` and awaits `this.repository.add(...)`.
extensions/git/src/repository.ts:2707-2727: `run` awaits `retryRun(operation, runOperation)` before `updateModelState(...getOptimisticResourceGroups...)`.
extensions/git/src/repository.ts:379-401 and extensions/git/src/operation.ts:277-288: Source Control progress appears only when `git.showProgress` is enabled and `operations.shouldShowProgress()` finds a running operation with `showProgress` true.
extensions/git/package.json:3799-3803 and 4042-4045: `git.showProgress` and `git.optimisticUpdate` both default to true.
git log/show: commit b4fbff3218b introduced “Git - Don't show progress for commands that support optimistic UI updates (#166124)”; no commit mentioning #189407 was found.

### [#195449](https://github.com/microsoft/vscode/issues/195449) — REFUTED
- Review: *duplicate at 0.9 (copilot) · refuter: codex at 0.78
- Refuter reasoning: The cited canonical #123517 is about macOS Mission Control Desktops/Spaces: restored VS Code windows all return to the current desktop instead of their prior desktop. #195449 reports a different scenario: one fullscreen window on a secondary physical display reopens fullscreen on the main display. The local code also treats fullscreen display restoration as its own Electron display-id path, saving `display` while fullscreen and later matching `state.display` to display bounds, which is distinct from restoring macOS desktop-manager/Space assignment.
- Evidence: GitHub issue #123517 page: title/body describe macOS Desktop/Mission Control restoration and all windows appearing on current desktop; labels include upstream/electron/backlog.
Issue #195449 text supplied in prompt: dual-monitor macOS, fullscreen on secondary monitor, relaunch opens on main display; `window.restoreFullscreen: true`; maintainer comment only says "Probably a duplicate".
src/vs/platform/window/electron-main/window.ts:131-138 defines `IWindowState.display` separately from x/y/mode.
src/vs/platform/windows/electron-main/windowImpl.ts:1371-1390 serializes fullscreen state with `display: electron.screen.getDisplayMatching(this.getBounds()).id`.
src/vs/platform/windows/electron-main/windows.ts:374-384 has explicit "Multi Monitor (fullscreen)" logic restoring fullscreen to the previous display by matching `state.display` and using that display's bounds.

### [#317061](https://github.com/microsoft/vscode/issues/317061) — REFUTED
- Review: *duplicate at 0.9 (copilot) · refuter: codex at 0.86
- Refuter reasoning: The cited canonical issue is not the same report: #283514’s body asks to restore the ability to rename local chat sessions and add optional metadata, while #317061 reports an existing Copilot Chat rename reverting/being ignored in GitHub Copilot Chat 0.48.1. The cited PR #323323 scopes its fix to local chat session rename persistence and changes `ChatSessionStore` plus `LocalChatSessionsProvider`; the checkout shows the Copilot Chat provider uses extension commands instead of the local provider path. That means the proposed duplicate claim overstates the match: it may share a surface symptom, but the canonical/fix path does not demonstrably cover the Copilot Chat extension path described by #317061.
- Evidence: Issue #317061 text: GitHub Copilot Chat extension 0.48.1, `GitHub.copilot-chat` transcript/debug-log paths, title reverts or UI ignores rename.
GitHub issue #283514 opened Dec 15, 2025: title `Allow to rename local chat sessions`; body says renaming was possible pre-v0.35.0 and requests rename/tags/branch linking.
GitHub PR #323323 opened Jun 28, 2026: title `Fix: Session title rename not persisting after reload`; body says it closes #283514/#318442/#322566 and changes `ChatSessionStore` and `LocalChatSessionsProvider` for local chat sessions.
`src/vs/workbench/contrib/chat/common/model/chatSessionStore.ts:510`-`516`: `setSessionTitle()` mutates only the index entry title and does not call `flushIndex()`.
`src/vs/sessions/contrib/providers/localChatSessions/browser/localChatSessionsProvider.ts:915`-`921`: local provider rename calls `chatService.setSessionTitle()` then updates its own session state.
`src/vs/sessions/contrib/providers/copilotChatSessions/browser/copilotChatSessionsProvider.ts:2051`-`2061`: Copilot provider rename delegates to Copilot CLI/Claude extension commands, not the local `ChatSessionStore`/`LocalChatSessionsProvider` path.

### [#322408](https://github.com/microsoft/vscode/issues/322408) — REFUTED
- Review: implemented on main at 0.9 (copilot) · refuter: codex at 0.88
- Refuter reasoning: The cited commit fixes only the no-existing-settings fallback case: it still first selects `allGroups.find(g => g.vendor === metadata.vendor && g.settings?.[metadata.id] !== undefined)`, so the model→group map is skipped whenever any same-vendor group already has that bare model id. The issue explicitly reports that first-step lookup as part of the bug when multiple same-vendor groups have the same `metadata.id`, so fa3b663 is a partial fix, not coverage for the issue as reported. A later non-main branch commit, `26b85157153`, changes the ordering to consult `_modelsGroups` first and adds a same-model-id-across-groups test, which further confirms the cited main commit left this gap.
- Evidence: Issue #322408 text: explicitly calls out both the fallback vendor-only lookup and the first-step `vendor + metadata.id` lookup as flawed when same-vendor groups share model ids.
Commit fa3b663f7234b8a6965d921bbe2f855c13e54a17: added `_modelsGroups` lookup only after the existing settings lookup, and added a test where groups use different model ids (`deepseek-v4-pro` vs `gpt-5.5`).
`origin/main:src/vs/workbench/contrib/chat/common/languageModels.ts` around `setModelConfiguration`: still performs `group = allGroups.find(g => g.vendor === metadata.vendor && g.settings?.[metadata.id] !== undefined)` before consulting `_modelsGroups`.
`origin/main:src/vs/workbench/contrib/chat/test/common/languageModels.test.ts`: only tests same vendor with different model ids for `setModelConfiguration`; no duplicate bare `metadata.id` settings-write test.
`git merge-base --is-ancestor 26b851571537c4bc002e0f8e2b59dc3380818f7f origin/main` returned 1, while that branch commit explicitly reorders resolution to `_modelsGroups` first and adds a same model id contamination test.

### [#305249](https://github.com/microsoft/vscode/issues/305249) — UNCERTAIN
- Review: *caused-by-extension at 0.94 (copilot) · refuter: codex at 0.68
- Refuter reasoning: I could verify that the duplicate interpreter selector is plausibly extension-owned: vscode-python-environments#1284 explicitly says installing vscode-python-environments 1.20.1 shows duplicate interpreter selectors and downgrading removes them. I could not verify the stronger close-proposal claim that the reporter later confirmed uninstalling Python Environments stopped the first-run failure: #1425’s visible body only says rollback to VS Code 1.108.2 avoids the problem, and GitHub API access from the checkout failed. Local VS Code search did not reveal a core Python environment selector implementation, but that does not prove the #1425 training failure was caused by the extension.
- Evidence: Issue text for microsoft/vscode#305249: open, no comments visible, reports duplicate Python virtual environment selector and training/re-run failures.
GitHub page for microsoft/vscode-python-environments#1284: duplicate selector appears with vscode-python-environments 1.20.1 and not after downgrading to 1.16.0; closed as not planned.
GitHub page for microsoft/vscode-python-environments#1425: same reporter, first ultralytics run fails on VS Code 1.114.0 and works after rollback to 1.108.2; no visible uninstall confirmation in fetched content.
Local checkout searches: rg over src/extensions/.github for Python Environments/interpreter/venv found extension references and tests, but no VS Code core Python interpreter selector implementation.
gh issue view attempts for microsoft/vscode#305249 and vscode-python-environments#1284/#1425 failed due api.github.com connectivity.

### [#323080](https://github.com/microsoft/vscode/issues/323080) — UNCERTAIN
- Review: *as-designed at 0.94 (copilot) · refuter: codex at 0.72
- Refuter reasoning: The local code supports the mechanism asserted by the close proposal: VS Code’s default Mermaid config resolves the `vscode` theme to Mermaid `base` plus VS Code-derived `themeVariables`, and Mermaid’s preprocessing merges diagram-local init/frontmatter config over the site config before rendering. However, I could not independently inspect the attached `test.zip`/`test.md` because shell network access failed and the web fetch did not expose the binary contents, so I cannot verify the decisive factual claim that this particular repro hardcodes `theme: base` and light colors. The WSL comment says the same problem occurs for “all MD files” only in WSL, but it does not include source showing a minimal no-init Mermaid diagram, so it is suspicious but not concrete enough by itself to refute the as-designed close.
- Evidence: GitHub issue page/body lines 131-169, plus provided recent comment text.
Attempted attachment fetch: `curl -L https://github.com/user-attachments/files/29375116/test.zip | bsdtar -tf -` failed with DNS resolution error; web open/click did not expose zip contents.
Read `extensions/mermaid-markdown-features/preview-src/shared/vsCodeTheme.ts`: `resolveMermaidTheme` maps `vscode` to Mermaid `base` with VS Code-derived `themeVariables`.
Read `extensions/mermaid-markdown-features/preview-src/shared/index.ts` and `preview-src/markdown/index.ts`: markdown preview initializes Mermaid with that config before rendering blocks.
Read Mermaid bundled code in `node_modules/mermaid/dist/mermaid.core.mjs` and chunks: `preprocessDiagram` uses `cleanAndMerge(frontmatter, directive)`, and config code merges directives over site config.
Checked git history for `#323080` / Mermaid dark-theme fixes; no direct issue-specific commit found.
